---
title: "02 — MVP Faz Planı"
description: "MVP fazları, faz çıktıları ve mevcut durum özeti."
aliases:
  - "MVP Faz Planı"
  - "Phase Plan"
tags:
  - "pusula"
  - "process/phase-plan"
type: "plan"
axis: "process"
status: "active"
parent: "[[docs/process/README|Süreç]]"
updated: 2026-05-13
---
# 02 — MVP Faz Planı

> Eksen: **süreç**. Statü değiştiğinde bu dosyayı güncelle (kaynak: bu dosya). Linear tarafında her faz
> Pusula projesinde bir **milestone**; Faz 2–8 ayrıca birer faz-seviyesi **epic** issue taşır. Önceden bölünmüş ama
> henüz başlamamış "bir sonraki faz"ın alt işleri Linear'da `Sonraki Faz` durumundadır (Unstarted kategorisinde, `Backlog` ile
> `Todo` arasında ara katman: `Backlog` = uzak, `Sonraki Faz` = bir sonraki faz/planlı, `Todo` = mevcut fazın bekleyen işleri, `In Progress` = aktif, `Review`/`Done`); faz başlayınca alt işler `Todo`'ya alınır.

| Faz | İçerik | Çıktı | Durum | Linear |
| --- | --- | --- | --- | --- |
| **0 — Temel altyapı** | Monorepo (pnpm + Turborepo), TypeScript strict, `apps/` + `packages/` iskeleti, Drizzle migration sistemi, Better Auth bağlantısı, yerel Docker Compose (Postgres/Redis/MinIO) | Boş ama çalışan web/api/worker uygulamaları; Drizzle şema iskeleti; docker-compose | ✅ Tamam | _Faz 0_ milestone · [DEM-18](https://linear.app/demirkol/issue/DEM-18) |
| **1 — Auth & Workspace** | Better Auth sign-in/up/out akışları, session yönetimi, Workspace CRUD, workspace member modeli, permission helper'ları procedure'lere bağlı | Kullanıcı login olup workspace görebilir | ✅ Tamam (ana + ek işler `Done` — DEM-68 şifre sıfırlama dahil) | _Faz 1_ milestone · [DEM-20](https://linear.app/demirkol/issue/DEM-20) (Done), [DEM-22](https://linear.app/demirkol/issue/DEM-22) (Done), [DEM-23](https://linear.app/demirkol/issue/DEM-23) (Done), [DEM-24](https://linear.app/demirkol/issue/DEM-24) (Done), [DEM-39](https://linear.app/demirkol/issue/DEM-39) (Done), [DEM-40](https://linear.app/demirkol/issue/DEM-40) (Done), [DEM-46](https://linear.app/demirkol/issue/DEM-46) (Done), [DEM-47](https://linear.app/demirkol/issue/DEM-47) (Done), [DEM-55](https://linear.app/demirkol/issue/DEM-55) (Done), [DEM-68](https://linear.app/demirkol/issue/DEM-68) (Done — şifre sıfırlama, `bosluk-tara` 2. tur) |
| **2 — Board/List/Card CRUD** | Board/List/Card CRUD (tRPC + Drizzle), temel board ekranı, transaction yapısı | Kullanıcı pano/liste/kart oluşturabilir | ✅ Tamam (2.0/2A/2B/2C/2D `Done` — backend + temel web board ekranı) | _Faz 2_ milestone · [DEM-25](https://linear.app/demirkol/issue/DEM-25) (epic) → [DEM-33](https://linear.app/demirkol/issue/DEM-33) (Done) · [DEM-34](https://linear.app/demirkol/issue/DEM-34) (Done) · [DEM-35](https://linear.app/demirkol/issue/DEM-35) (Done) · [DEM-36](https://linear.app/demirkol/issue/DEM-36) (Done) · [DEM-37](https://linear.app/demirkol/issue/DEM-37) (Done) |
| **2.5 — Kart detayı + board işbirliği** | Kart detay görünümü (açıklama/due-date/üyeler/etiketler/checklist/yorum/activity) + board üye yönetimi (admin/member/viewer) + board davetleri + etiketler | Kullanıcı kart detayını açıp düzenler, board'a üye davet eder | ✅ Tamam (DEM-49–54 hepsi `Done` — backend + web; kart detay modali + board üye/etiket yönetimi + board davetleri + etiket filtresi) | _Faz 2.5_ milestone · [DEM-48](https://linear.app/demirkol/issue/DEM-48) (epic, Done) → [DEM-49](https://linear.app/demirkol/issue/DEM-49) (Done) · [DEM-50](https://linear.app/demirkol/issue/DEM-50) (Done) · [DEM-51](https://linear.app/demirkol/issue/DEM-51) (Done) · [DEM-52](https://linear.app/demirkol/issue/DEM-52) (Done) · [DEM-53](https://linear.app/demirkol/issue/DEM-53) (Done) · [DEM-54](https://linear.app/demirkol/issue/DEM-54) (Done) |
| **2.7 — UI tasarım dili & board/kart cilalama** | Yeni renk paleti + design token sistemi (`docs/architecture/13-ui-tasarim-dili.md`); board/kolon/kart anatomisi + metadata satırı; kart detay modalı yeniden yapı (iki-kolon + sağ panel); Tiptap rich text editör; ortak bileşenler (Avatar/SectionHeader/Progress/EmptyState/MetaChip/LabelChip/CardCompleteToggle + shadcn Tooltip/DropdownMenu/Checkbox/Tabs) | UI "olması gerektiği gibi" — fonksiyon değişmedi, görsel/yapısal cilalama | ✅ Tamam — 2.7.0 önce-belge + 2.7A (tema+token+`packages/ui`) + 2.7B (board ekranı + `board.get` additive) + 2.7C (kart detay modalı + Tiptap) + 2.7D (workspace/app-shell + a11y) hepsi `Done` (`FE-2026-05-12-011..015` + DEM-66/67 `FE-2026-05-12-014` aynı süreçte). Palet=Trello-vari mavi / yoğunluk=compact / font=Inter / rich text=Tiptap JSON. `bosluk-tara` benzeri araya-faz (referans: eski Pusula `D:\projects\pusula` UI baz + Trello). Kapanış-sonrası tasarım-uyum follow-up'ı **[DEM-74](https://linear.app/demirkol/issue/DEM-74) (2.7C-2 — kart detay modalı §13.3 tasarım uyumu + DEM-66/67 wire) `Done`** (`FE-2026-05-13-001`, commit `e9849d2`, 2026-05-13) — modal genişlik (`w-[min(960px,92vw)]` + `sm:max-w-none` — kök sebep shadcn `DialogContent` base'inde `sm:max-w-lg`) + iki-kolon grid (`min-w-0` ile sol kolon overflow fix) + kapak-renkli başlık çubuğu + 12-renk kapak rengi picker (`card.update({coverColor})`) + `CardCompleteToggle` → `card.complete`/`uncomplete` wire (modal + kart; tamamlanmış → `bg-success` tik + başlık `line-through`) + "İşlemler"→"Aktivite" sekmesi + `SectionHeader` aksiyon-slot deseni (metin taşması bitti) + Tiptap `<pre>` overflow fix; backend dokunulmadı (DEM-64/66/67 zaten ekledi); 440 test yeşil; **bununla modal tamamen §13.3'te**. Açık follow-up'lar (Faz 8 / ayrı iş): `RichTextContent` per-comment editör perf'i, board-başına özelleştirilebilir zemin + favoriler ([DEM-57](https://linear.app/demirkol/issue/DEM-57)), "Liste"/"Etiketler" board görünümleri | _Faz 2.7_ milestone · [DEM-58](https://linear.app/demirkol/issue/DEM-58) (epic, Done) → [DEM-61](https://linear.app/demirkol/issue/DEM-61) (Done) · [DEM-62](https://linear.app/demirkol/issue/DEM-62) (Done) · [DEM-63](https://linear.app/demirkol/issue/DEM-63) (Done) · [DEM-64](https://linear.app/demirkol/issue/DEM-64) (Done) · [DEM-65](https://linear.app/demirkol/issue/DEM-65) (Done) + [DEM-66](https://linear.app/demirkol/issue/DEM-66)/[DEM-67](https://linear.app/demirkol/issue/DEM-67) (kart tamamlama/kapak rengi backend, Done) + [DEM-74](https://linear.app/demirkol/issue/DEM-74) (2.7C-2 tasarım uyumu, Done) |
| **3 — Drag-Drop** | Atlassian Pragmatic Drag and Drop ile web board drag-drop; liste reorder, kart reorder, listeler arası taşıma; position/ranking algoritması; Playwright drag-drop testleri | Drag-drop akıcı ve backend ile tutarlı | 🚧 **Devam ediyor** — geliştirme 2026-05-13 başladı (`faz-baslat 3`). 3.0 önce-belge ([DEM-41](https://linear.app/demirkol/issue/DEM-41)) `Review` (kontrol odası tab'ı — **önce-belge tamam:** `05-board-mekanigi §5.1` + `03-siralama-kurallari` + `08-web §8.1.8` "Board ekranı drag-drop" + `@pusula/domain` move-input şekilleri). DEM-74 (Faz 2.7C-2 kart detay modalı) `Done` (`e9849d2`) — bloklayıcı değil. 3A backend ([DEM-42](https://linear.app/demirkol/issue/DEM-42)) `Todo` — başlatmaya hazır. 3B/3C/3D/3E `Todo` (sıra: 3A → 3B∥3C → 3D; 3E ⊇ 3A) | _Faz 3_ milestone · [DEM-26](https://linear.app/demirkol/issue/DEM-26) (epic, 🚧 In Progress) → [DEM-41](https://linear.app/demirkol/issue/DEM-41) (Review) · [DEM-42](https://linear.app/demirkol/issue/DEM-42) (Todo) · [DEM-43](https://linear.app/demirkol/issue/DEM-43) (Todo) · [DEM-44](https://linear.app/demirkol/issue/DEM-44) (Todo) · [DEM-45](https://linear.app/demirkol/issue/DEM-45) (Todo) · [DEM-69](https://linear.app/demirkol/issue/DEM-69) (Faz 3E `card.moveToList`/`copy`, `bosluk-tara`, Todo) |
| **4 — Optimistic UI** | TanStack Query cache modeli, `clientMutationId`, optimistic move/create/update, rollback, mutation failure testleri | UI network beklemeden tepki verir | — | _Faz 4_ milestone · [DEM-27](https://linear.app/demirkol/issue/DEM-27) (epic) |
| **5 — Realtime** | Socket.IO server, board room, user room, Redis adapter, event envelope, client reconciliation | İki kullanıcı aynı board'da değişiklikleri canlı görür | — | _Faz 5_ milestone · [DEM-28](https://linear.app/demirkol/issue/DEM-28) (epic) |
| **6 — Bildirim** | Activity event, notification outbox, worker processor, in-app notification center, badge, Expo push token modeli, push gönderimi | Atama/mention/yorum bildirimleri çalışır | — | _Faz 6_ milestone · [DEM-29](https://linear.app/demirkol/issue/DEM-29) (epic) + [DEM-70](https://linear.app/demirkol/issue/DEM-70) (board activity feed, `bosluk-tara`) |
| **6.5 — Arama** | PostgreSQL full-text search — board/list/card/comment/label içeriğinde arama (üst bar global + board içi); `search_documents` + worker index | Kullanıcı içerikte arama yapabilir | — (Faz 8'den öne çekildi, `bosluk-tara` G4) | _Faz 6.5_ milestone · [DEM-56](https://linear.app/demirkol/issue/DEM-56) (epic) |
| **7 — Mobil** | Expo app, auth, board listesi, board görüntüleme, card detail, notification center, push deep link | Mobil temel görev yönetimi + bildirim | — | _Faz 7_ milestone · [DEM-30](https://linear.app/demirkol/issue/DEM-30) (epic) |
| **8 — Sertleştirme** | E2E testler, load test, error tracking, audit log, MinIO attachment, permission edge case'leri, board favorileri/son görülenler (UX) | Beta yayına hazır ürün | — | _Faz 8_ milestone · [DEM-31](https://linear.app/demirkol/issue/DEM-31) (epic) + [DEM-57](https://linear.app/demirkol/issue/DEM-57) (board favorileri) + [DEM-71](https://linear.app/demirkol/issue/DEM-71) (arşivli kartlar görünümü) + [DEM-72](https://linear.app/demirkol/issue/DEM-72) (signup e-posta doğrulama) + [DEM-73](https://linear.app/demirkol/issue/DEM-73) (klavye kısayolları) — son dördü `bosluk-tara` |

> Faz 2 ([DEM-25](https://linear.app/demirkol/issue/DEM-25)) tamam (alt işler aşağıda). **Faz 2.5 epic'i ([DEM-48](https://linear.app/demirkol/issue/DEM-48)) `Done`** — `bosluk-tara` (G1) ile Faz 2 ↔ Faz 3 arasına eklendi; DEM-49–54 hepsi `Done` (2026-05-12). **Faz 2.7 epic'i ([DEM-58](https://linear.app/demirkol/issue/DEM-58)) `Todo`** — UI cilalama, sıradaki faz; 2026-05-12'de Faz 2.5 ↔ Faz 3 arasına eklendi, geliştirmesi başlıyor (mevcut web UI fonksiyonel ama görsel olarak ham; tasarım dili + board/kart anatomisi + kart detay modalı yeniden yapı + Tiptap; referans = eski Pusula UI baz + Trello); ilk somut adım **2.7.0 önce-belge** (`docs/architecture/13-ui-tasarim-dili.md` + palet/yoğunluk/font seçenekleri — kontrol odasında). Alt işler `faz-bol 2.7`'de. **Faz 3 epic'i ([DEM-26](https://linear.app/demirkol/issue/DEM-26)) `🚧 In Progress`** — geliştirme 2026-05-13 başladı (`faz-baslat 3`); 3.0 önce-belge ([DEM-41](https://linear.app/demirkol/issue/DEM-41)) `Review` (kontrol odası — `05-board-mekanigi §5.1` + `03-siralama-kurallari` + `08-web §8.1.8` "Board ekranı drag-drop" + `@pusula/domain` move-input şekilleri tamam), 3A/3B/3C/3D/3E (DEM-42–45 + DEM-69) `Todo` (alt işler aşağıda; DEM-74 land etti — `e9849d2`, bloklayıcı değil; sıra: 3A → 3B∥3C → 3D, 3E⊇3A). Faz 4–6, Faz 6.5 ([DEM-56](https://linear.app/demirkol/issue/DEM-56) — `bosluk-tara` G4 ile Faz 8'den ayrılan arama epic'i, Faz 6 ↔ Faz 7 arası) ve Faz 7–8 epic'leri
> Linear'da `Backlog`'tadır; ilgili fazın geliştirmesi başlayınca epic alt issue'lara bölünür ve her alt iş
> [`05-is-kayit-defteri.md`](05-is-kayit-defteri.md)'ye satır olarak eklenir.

## Faz 1 alt işleri

Faz 1, alt işlere bölündü (sıra: 1 → 2 → 3; `workspace.delete` ek iş). Her biri iş kayıt defterinde
ayrı satır ve Pusula projesi `Faz 1 — Auth & Workspace` milestone'unda bir Linear issue taşır
([`05-is-kayit-defteri.md`](05-is-kayit-defteri.md)).

1. **Workspace alanı (backend)** — `workspaceProcedure` middleware (bkz. [`../architecture/03-backend.md`](../architecture/03-backend.md)), Workspace CRUD (create / list / get / update / archive), workspace member listesi + rol yönetimi, permission enforcement (`@pusula/domain/permissions`), ilgili `activity_events`. Procedure → rol haritası: [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md). — `Done` (`API-2026-05-12-001` / [DEM-20](https://linear.app/demirkol/issue/DEM-20); QA + verifier PASS, commit `97cd9b8`, kullanıcı onayladı).
2. **Auth web UI & session** — sign-up / sign-in / sign-out ekranları, session-aware (client-side) layout, korumalı route group, workspace listesi/oluşturma ekranı; gerekli shadcn bileşenleri (`@pusula/ui`). Bkz. [`../architecture/08-web-ve-mobil.md`](../architecture/08-web-ve-mobil.md) §8.1.1. — `Done` (`FE-2026-05-12-001` / [DEM-22](https://linear.app/demirkol/issue/DEM-22); QA + verifier PASS, commit `cefd148`, kullanıcı onayladı).
3. **Workspace davet akışı** — `workspace_invitations` tablosu (süreli, tek kullanımlık token), `members.invite` + `invitations.{list,revoke,mine,accept,decline}` procedure'leri, davet bildirimi (`notification_outbox`); web: davet et dialog'u + bekleyen davetler bölümü ([`../architecture/08-web-ve-mobil.md`](../architecture/08-web-ve-mobil.md) §8.1.1); davet akışı kuralları [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Workspace davet akışı). — `Done` (`API-2026-05-12-002` / [DEM-23](https://linear.app/demirkol/issue/DEM-23); backend + web UI tamam — db + domain + api + Vitest (46 test) + `apps/web` davet UI + RTL, migration `0002`+`0003`, QA + verifier PASS, commit `bca834a` (backend) + `a3fba50` (web); kullanıcı onayladı. Ertelenen: davet rol seçimi + gönderilmiş davet yönetimi UI'ı).
4. **`workspace.delete`** (ek iş) — owner-only **kalıcı silme** (input'ta workspace adı `confirmName` birebir eşleşmeli; `DELETE FROM workspaces` → üye/davet/board… cascade; `archive`'dan ayrı, geri dönüşsüz; cascade nedeniyle DB içi iz tutulmaz). Bkz. [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md). — `Done` (`API-2026-05-12-003` / [DEM-24](https://linear.app/demirkol/issue/DEM-24); hard delete; backend + 4 test, QA + verifier PASS, commit `7c91527`; kullanıcı onayladı; Linear DEM-24 → Done + kapanış yorumu).
5. **Workspace yönetim ekranı (web)** (ek iş) — backend'de var olan `workspace.update`/`archive` + `workspace.members.{list,updateRole,remove}` + `workspace.invitations.{list,revoke}` procedure'lerini UI'ye bağlar: route `(app)/workspaces/[id]` — rename/slug + arşivle + üye listesi/rol/çıkar/ayrıl + gönderilmiş davetler + davet et (rol-bazlı görünürlük). Bkz. [`../architecture/08-web-ve-mobil.md`](../architecture/08-web-ve-mobil.md) §8.1.2. — `Done` (`FE-2026-05-12-003` / [DEM-39](https://linear.app/demirkol/issue/DEM-39); page + alt component'ler + `@pusula/ui` `Select`; QA round-1/2 + verifier PASS, commit `7c91527` + `f715735`; kullanıcı onayladı; Linear DEM-39 → Done + kapanış yorumu).
6. **`workspace.delete` UI** (ek iş — madde 5'in tamamlayıcısı) — workspace yönetim ekranının tehlikeli bölgesine owner-only "Kalıcı sil" dialog'u: workspace adını birebir yazma onayı (`confirmName`), `trpc.workspace.delete` → `/`'a dön + `workspace.list` invalidate. Bkz. [`../architecture/08-web-ve-mobil.md`](../architecture/08-web-ve-mobil.md) §8.1.2. — `Done` (`FE-2026-05-12-004` / [DEM-40](https://linear.app/demirkol/issue/DEM-40); `DeleteWorkspaceDialog` + 8 RTL test, QA + verifier PASS, commit `77c2627`; kullanıcı talebiyle açıldı + tamamlandı; Linear DEM-40 → Done + kapanış yorumu).

7. **Yeni kullanıcı onboarding'i** (ek iş) — signup'ta otomatik default workspace + boş "İlk Pano" + login sonrası yönlendirme (0 workspace → onboarding, 1 → `/workspaces/[id]`, 2+ → liste) + onboarding empty state. Pre-dev (tamam): `docs/domain/01-urun-modeli.md` invariant 11 (best-effort default workspace + boş "İlk Pano"; `workspace.list` boş olabilir → UI onboarding; şablon listeler/kartlar kapsam dışı) + [`../architecture/08-web-ve-mobil.md`](../architecture/08-web-ve-mobil.md) §8.1.3 (signup bootstrap akışı + `(app)/page.tsx` yönlendirme) + [`../architecture/07-auth.md`](../architecture/07-auth.md) (Better Auth `databaseHooks.user.create.after` bootstrap notu). UX tartışmasıyla açıldı. — `Done` (`FE-2026-05-12-005` / [DEM-46](https://linear.app/demirkol/issue/DEM-46); `@pusula/domain` 2 sabit + `apps/api/src/bootstrap.ts` (signup `databaseHooks.user.create.after`, best-effort) + `apps/web` `(app)/page.tsx` 0/1/2+ yönlendirme + `_components/onboarding-empty-state.tsx` + `create-workspace-dialog.tsx` `triggerLabel` prop + RTL; QA code-reviewer + security-reviewer = APPROVE + verifier PASS (typecheck/lint/test 121/build yeşil); kullanıcı onayladı; Linear DEM-46 → `Done` + kapanış yorumu; `blockedBy` DEM-20 + DEM-34 [Done]; `blocks` DEM-47).
8. **Pano şablonu** (ek iş — madde 7'nin tamamlayıcısı) — onboarding "İlk Pano"sına varsayılan listeler ("Yapılacak" / "Devam Eden" / "Bitti") + `Yapılacak`'ta birkaç welcome/örnek kart (Trello "Welcome board" benzeri); hepsi signup bootstrap transaction'ında, `list.created`/`card.created` activity'leriyle. Pre-dev (tamam): `docs/domain/01-urun-modeli.md` invariant 11 (default workspace → pano şablonu da kapsamda) + [`../architecture/08-web-ve-mobil.md`](../architecture/08-web-ve-mobil.md) §8.1.3 (bootstrap → varsayılan listeler + welcome kartlar). — `Done` (`FE-2026-05-12-006` / [DEM-47](https://linear.app/demirkol/issue/DEM-47); `@pusula/domain` `ONBOARDING_LIST_TITLES` + `ONBOARDING_WELCOME_CARDS` sabitleri + `apps/api/src/bootstrap.ts` "İlk Pano"yu 3 liste (`positionBetween` cursor'u ile sıralı) + `Yapılacak`'ta welcome kartlarla seed eder, aynı tx'te `list.created`/`card.created` activity (`list.create`/`card.create` payload şekliyle); `boards.version` seed'de bump edilmez; QA code-reviewer = APPROVE (L1 payload şekli + L2 stale comment düzeltildi) + verifier PASS (typecheck/lint/test 121/build yeşil); kullanıcı onayladı; Linear DEM-47 → `Done` + kapanış yorumu; `blockedBy` DEM-46 + DEM-35 + DEM-36 [Done]).

9. **Kullanıcı profili / hesap ayarları ekranı** (ek iş) — `(app)/account` ekranı: ad + avatar (**basit URL** — yükleme yok, karar 2026-05-12) değiştir, parola değiştir, hesabı sil (parola onaylı; kullanıcı bir workspace `owner`'ıysa **engellenir** — `@pusula/domain` `canDeleteOwnAccount`); app-shell'e "Hesap" linki. **Yeni tRPC `user.*` router yok** — doğrudan Better Auth client (`updateUser`/`changePassword`/`deleteUser`); sunucu tarafı yalnızca Better Auth `user.deleteUser.enabled` + `beforeDelete` hook (owner kontrolü). `bosluk-tara` (G5) ile açıldı. Pre-dev (tamam): [`../architecture/07-auth.md`](../architecture/07-auth.md) (Profil & hesap yönetimi) + [`../architecture/08-web-ve-mobil.md`](../architecture/08-web-ve-mobil.md) §8.1.7 + [`../architecture/03-backend.md`](../architecture/03-backend.md) (auth/non-tRPC notu) + [`../architecture/02-teknoloji-kararlari.md`](../architecture/02-teknoloji-kararlari.md) (karar kaydı 2026-05-12) + [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) (invariant 14) + [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Hesap (User) — öz-yönetim). — `Done` (`FE-2026-05-12-007` / [DEM-55](https://linear.app/demirkol/issue/DEM-55); kod + testler + QA (code-reviewer APPROVE, security-reviewer LOW risk) + verifier (typecheck/lint/build/test yeşil) tamam; kullanıcı onayladı → commit `3d8c7c3`; `blockedBy` DEM-22 + DEM-39 [ikisi de Done]).

10. **Şifre sıfırlama akışı** (ek iş) — `(auth)/forgot-password` (e-posta → `authClient.requestPasswordReset({ email, redirectTo:'/reset-password' })` → "gönderildi" — e-posta var/yok ayırt edilmez) + `(auth)/reset-password` (URL `?token=` → yeni parola → `authClient.resetPassword({ newPassword, token })` → sign-in'e); sign-in'e "Şifremi unuttum" linki. **Yeni tRPC `user.*` router yok** — Better Auth client uçları + `apps/api/src/auth.ts` `emailAndPassword.sendResetPassword` callback'i → **Resend** transactional e-postası (request-path; Faz 6 bildirim outbox'undan AYRI); `RESEND_API_KEY`/`EMAIL_FROM` `apps/api/src/env.ts`'e, `resend` paketi `apps/api` deps'e; `RESEND_API_KEY` yoksa best-effort. `@pusula/domain` `forgotPasswordInput`/`resetPasswordInput` (`passwordSchema` yeniden kullanılır). `bosluk-tara` 2. tur ile açıldı. Pre-dev (tamam): [`../architecture/07-auth.md`](../architecture/07-auth.md) (Şifre sıfırlama akışı) + [`../architecture/08-web-ve-mobil.md`](../architecture/08-web-ve-mobil.md) §8.1.1 + [`../architecture/02-teknoloji-kararlari.md`](../architecture/02-teknoloji-kararlari.md) (karar kaydı 2026-05-12 — Resend auth e-postaları request-path, bildirim outbox'tan ayrı) + bu dosya. — `Done` (`FE-2026-05-12-016` / [DEM-68](https://linear.app/demirkol/issue/DEM-68); `RESEND_API_KEY` env sağlandı; backend [@backend-dev: `auth.ts` `sendResetPassword` → yeni `auth-emails.ts` Resend helper + `revokeSessionsOnPasswordReset`; `env.ts` `RESEND_API_KEY`/`EMAIL_FROM`; `resend@^6` dep; `@pusula/domain` `forgotPasswordInput`/`resetPasswordInput`] + web [@frontend-dev: `(auth)/forgot-password` + `(auth)/reset-password` + sign-in linki + `next.config.ts` `Referrer-Policy: no-referrer` + `strings.auth.*`]; QA 2 tur — code-reviewer 1.tur REQUEST CHANGES [H1 `redirectTo` relative→API'ye 404] + security APPROVE WITH CHANGES [M1 token log'a düşüyor] → düzeltildi → 2.tur code-reviewer/security-reviewer **APPROVE** + verifier PASS [408 test]; kullanıcı onayladı; commit `feat: DEM-68 …` + push; `blockedBy` DEM-22 [Done]). Follow-up (kapsam dışı): `/api/auth/forgot-password` rate-limit → Faz 8; `(auth)/layout` oturumlu kullanıcıyı reset linkinden de yönlendiriyor (UX edge); `EMAIL_FROM` gerçek gönderim için doğrulanmış domain gerektirir (deployment). (İleride: signup e-posta doğrulama — [DEM-72](https://linear.app/demirkol/issue/DEM-72), Faz 8, aynı Resend kurulumu.) **Bununla Faz 1 (ana + ek işler) tamamen `Done`.**

## Faz 2 alt işleri

Faz 2 ([DEM-25](https://linear.app/demirkol/issue/DEM-25) — epic) beş alt işe bölündü (sıra: 2.0 → 2A → (2B ∥ 2C) → 2D).
Linear'da hepsi DEM-25 altında, `Faz 2 — Board/List/Card CRUD` milestone'unda; **DEM-33–37 hepsi `Done` — Faz 2 tamamlandı.** Aktif faz **Faz 2.5** ([DEM-48](https://linear.app/demirkol/issue/DEM-48), `In Progress`), sonra Faz 3 ([DEM-26](https://linear.app/demirkol/issue/DEM-26), `Backlog`). `Sonraki Faz` = `Backlog` ile `Todo` arasında ara katman ("bir sonraki faz, planlı"); faz geliştirmesi başlayınca o iş `Todo`'ya
alınır. İş kayıt defteri satırları eklendi (DOC-2026-05-12-005 + API-2026-05-12-004/005/006 + FE-2026-05-12-002 — bkz. [`05-is-kayit-defteri.md`](05-is-kayit-defteri.md)).
Karar: `move`/reorder mutation'ları (list/kart taşıma) Faz 2 değil **Faz 3** kapsamı ([DEM-26](https://linear.app/demirkol/issue/DEM-26)) — Faz 2 = statik CRUD (create sona ekler, alan güncelle, arşivle).

1. **Faz 2.0 — Önce belge** — `docs/architecture/03-backend.md` (`boardProcedure`/`cardProcedure` + `board.*`/`list.*`/`card.*` procedure listesi), [`../architecture/04-veri-katmani.md`](../architecture/04-veri-katmani.md) (boards/lists/cards şema + `position` kolonları), [`../architecture/05-board-mekanigi.md`](../architecture/05-board-mekanigi.md) (CRUD veri akışı), [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) (board/list/card invariant'ları), [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (procedure → rol haritası), bu dosyanın "Faz 2 alt işleri" bölümü + `05-is-kayit-defteri.md` satırları. **Kontrol odası tab'ında** yapılır ([`../kontrol-odasi/README.md`](../kontrol-odasi/README.md)). — `Done` ([DEM-33](https://linear.app/demirkol/issue/DEM-33); `03-backend.md` procedure tablosu + `02-yetkilendirme-kurallari.md` rol haritası + `04-veri-katmani.md` Faz 2 kapsamı notu + `05-board-mekanigi.md` §5.0 CRUD veri akışı + `01-urun-modeli.md` arşivli board notu; `05-aktivite-kurallari.md` taksonomisi board/list/card'ı zaten kapsıyor; Linear DEM-33 → Done + kapanış yorumu, 2026-05-12). Bağımlılık: yok; 2A öncesi tamamlandı.
2. **Faz 2A — Board CRUD (backend)** — `boardProcedure` middleware (board resolve + `effectiveBoardRole`, `ctx.board`), `board.{list,create,get,update,archive}`, server-side permission enforcement (`@pusula/domain/permissions` — `canViewBoard`/`canEditBoardContent`/`canManageBoard`), `activity_events` (`board.created`/`board.renamed`/`board.archived`) aynı transaction. Şema mevcut (`packages/db/src/schema/boards.ts`, Faz 0); `ACTIVITY_EVENT_TYPES`'a Faz 2 board/list/card tipleri (`board.renamed`/`list.renamed`/`card.renamed`/`card.description_changed`) append + migration `drizzle/0004_graceful_archangel.sql`. — `Done` ([DEM-34](https://linear.app/demirkol/issue/DEM-34); `API-2026-05-12-004`; `middleware/board.ts` + `routers/board.ts` + `root.ts` mount + 9 tRPC integration testi; QA code-reviewer + verifier PASS — W1 `version` return-shape düzeltildi; commit `feat: DEM-34 …`, kullanıcı onayladı; Linear DEM-34 → Done + kapanış yorumu, 2026-05-12). Bağımlılık: Faz 1A (DEM-20, Done) + Faz 2.0 (DEM-33, Done).
3. **Faz 2B — List CRUD (backend)** — `list.{create,update,archive}` (`boardProcedure` üzerinde), create = board sonuna fractional `position` (`@pusula/domain/position`), board-edit permission (`canEditBoardContent`), `activity_events` (`list.created/renamed/archived`), arşivli board'a liste eklenemez, `boards.version` artar. — `Done` (`API-2026-05-12-005` / [DEM-35](https://linear.app/demirkol/issue/DEM-35); `routers/list.ts` + paylaşılan `middleware/board-access.ts` (`resolveBoardAccess`) + `root.ts` mount + 6 tRPC integration testi; DEM-36 ile aynı turda; QA code-reviewer + verifier PASS; commit `feat: DEM-35 + DEM-36 …`, kullanıcı onayladı; Linear DEM-35 → Done + kapanış yorumu, 2026-05-12). Bağımlılık: Faz 2A (DEM-34, Done); 2C ile paralel — birlikte yapıldı.
4. **Faz 2C — Card CRUD (backend)** — `cardProcedure` context (`middleware/card.ts`), `card.{create,get,update,archive}`, `card.create` `protectedProcedure` (listenin board'unu `resolveBoardAccess` ile çözer; `createCardInput` yalnızca `listId` taşır) + liste sonuna fractional `position`, **kart ⊆ liste.board invariant'ı** (`card.board_id` = listenin board'u) + arşivli board/liste yeni kart almaz, board-edit permission, `activity_events` (`card.created/renamed/description_changed/due_set/due_cleared/archived`), `boards.version` artar. — `Done` (`API-2026-05-12-006` / [DEM-36](https://linear.app/demirkol/issue/DEM-36); `middleware/card.ts` + `routers/card.ts` + `root.ts` mount + 7 tRPC integration testi; DEM-35 ile aynı turda; QA PASS; commit `feat: DEM-35 + DEM-36 …`, kullanıcı onayladı; Linear DEM-36 → Done + kapanış yorumu, 2026-05-12). Bağımlılık: Faz 2A (DEM-34, Done); 2B ile paralel — birlikte yapıldı. `card.move`/`moveCard` kapsam dışı → Faz 3 (DEM-26).
5. **Faz 2D — Temel board ekranı (web)** — workspace içi board listesi (`workspaces/[id]/page.tsx` üst bölümünde) + board detay sayfası (`(app)/workspaces/[id]/boards/[boardId]/page.tsx`): kolon + kart render, board/liste/kart oluştur/yeniden adlandır/arşivle akışları (inline form / onaylı dialog, **drag-drop yok**), board rolüne göre aksiyon gizleme, arşivli board/liste salt-okunur, shadcn (`@pusula/ui` + yeni `Textarea`), Türkçe metinler `strings.board`, RTL testleri (19 yeni web testi). Optimistic UI yok (Faz 4 — DEM-27); mutation → `await` → `board.get`/`board.list` invalidate → refetch. — `Done` (`FE-2026-05-12-002` / [DEM-37](https://linear.app/demirkol/issue/DEM-37); `BoardListSection` + `boards/[boardId]/page.tsx` + `_components/*` (`BoardColumns`/`ListColumn`/`CardItem`/`EditCardDialog`/`AddListForm`/`AddCardForm`/`RenameBoardForm`/`ArchiveBoardDialog`/`CreateBoardDialog`) + `lib/format.ts` + `@pusula/ui` `Textarea`; QA code-reviewer + verifier PASS (5 LOW ertelendi); commit `feat: DEM-37 …`, kullanıcı onayladı; Linear DEM-37 → Done + kapanış yorumu, 2026-05-12). Bağımlılık: Faz 2A + 2B + 2C (DEM-34/35/36, Done). **Bununla Faz 2 tamam.**

## Faz 2.5 alt işleri

Faz 2.5 ([DEM-48](https://linear.app/demirkol/issue/DEM-48) — epic, Faz 2 ↔ Faz 3 arası; `bosluk-tara` G1) altı alt işe bölündü: 2.5.0 (önce belge) → 2.5A ∥ 2.5B ∥ 2.5C → 2.5D → 2.5E. Linear'da hepsi DEM-48 altında, `Faz 2.5 — Kart detayı + board işbirliği` milestone'unda. **Faz 2.5 tamam (2026-05-12) — DEM-48 epic `Done`:** DEM-49 (önce belge) `Done`, DEM-50 (comment + checklist backend) `Done`, DEM-51 (card members + label backend) `Done`, DEM-52 (board üye + davet backend) `Done`, DEM-53 (kart detay görünümü web) `Done`, DEM-54 (board üye/etiket UI web + etiket filtresi) `Done`. "Trello'nun geri kalan çekirdeği" — domain modelinde tanımlı ama hiçbir fazda olmayan kart detayı / board işbirliği. **Önce-belge kararları (DEM-49, kullanıcı onayıyla):** (a) kart detay görünümü = **modal + `?card=<id>` query param** (shadcn `Dialog`, board arkada; intercepting route yok); (b) karta üye adayı o board'a effective erişimi olan kullanıcı olmalı (`effectiveBoardRole !== null`); (c) `board.members.add` workspace-dışı kişiyi de davet edebilir → yeni `board_invitations` tablosu (`workspace_invitations` ile aynı disiplin), e-posta + outbox; kabulde davetli workspace `guest`'i + board üyesi olur. Detay → [`../architecture/02-teknoloji-kararlari.md`](../architecture/02-teknoloji-kararlari.md) (Karar kaydı, 2026-05-12), `03-backend.md` (Faz 2.5 procedure'leri), `04-veri-katmani.md` (Faz 2.5 kapsamı + `board_invitations`), `08-web-ve-mobil.md` §8.1.5–8.1.6, `../domain/01-urun-modeli.md` (invariant 12–13), `../domain/02-yetkilendirme-kurallari.md` (Board / Card içerik procedure haritası — Faz 2.5).

1. **Faz 2.5.0 — Önce belge** — `03-backend.md` (`comment.*`/`checklist.*`/`checklist.item.*`/`card.members.*`/`card.labels.*`/`label.*`/`board.members.*`/`board.invitations.*` procedure haritası), [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (board rolleri matrisi + Faz 2.5 procedure haritası), [`../domain/05-aktivite-kurallari.md`](../domain/05-aktivite-kurallari.md) (adlandırma notları + yeni board davet tipleri), [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) (Board invitation entity + invariant 12–13), [`../architecture/08-web-ve-mobil.md`](../architecture/08-web-ve-mobil.md) §8.1.5–8.1.6 (kart detay modali + board üye/etiket UI), [`../architecture/04-veri-katmani.md`](../architecture/04-veri-katmani.md) (Faz 2.5 kapsamı + `board_invitations`), [`../architecture/02-teknoloji-kararlari.md`](../architecture/02-teknoloji-kararlari.md) (Karar kaydı), bu dosya + `05-is-kayit-defteri.md`. **Kod-yazma tab'ında pre-dev olarak yapıldı** (kontrol odası tab'ında değil — bu turda öyle yürütüldü). — `Done` ([DEM-49](https://linear.app/demirkol/issue/DEM-49)). Bağımlılık: yok.
2. **Faz 2.5A — Comment + Checklist CRUD (backend)** — `comment.{list,create,update,delete}` + `checklist.{create,update,delete}`/`checklist.item.{create,toggle,update,delete,reorder}` (`cardProcedure`); item `reorder` fractional `position`; activity (`comment.created/updated/deleted`, `checklist.created`, `checklist.item_added/checked/unchecked/removed`); etiket-dışı düşük sinyaller activity üretmez ama `boards.version` artar. — `Done` (`API-2026-05-12-007` / [DEM-50](https://linear.app/demirkol/issue/DEM-50); `cardProcedure` comment/checklist/checklist.item; yorum soft-delete (`deletedAt` + `body=''`, `list` silinmişleri de döner); item `reorder` fractional `position` + `positionBetween` try/catch; `ACTIVITY_EVENT_TYPES` +4 değer + migration `0005`; `comment.test.ts` 8 + `checklist.test.ts` 13; QA code-reviewer + security-reviewer PASS + verifier PASS [test 142]; commit `caa880d`; Linear → `Done` + kapanış yorumu). Bağımlılık: DEM-49 + DEM-36 (ikisi de Done).
3. **Faz 2.5B — Card members + Label CRUD + karta etiket atama (backend)** — `card.members.{add,remove}` (assignee/watcher; aday `effectiveBoardRole !== null` olmalı; `viewer` yalnız kendini watcher) + `label.{create,update,delete}` (board scope, `member+`, `(board,renk,ad)` benzersiz, activity yok) + `card.labels.{add,remove}` (etiket kartın board'una ait olmalı); activity (`card.member_added/removed`, `card.label_added/removed`). — `Done` (`API-2026-05-12-008` / [DEM-51](https://linear.app/demirkol/issue/DEM-51); `card.members.*` [aday `effectiveBoardRole !== null` yoksa `BAD_REQUEST`; `list` yalnız `{userId,role,name}` — e-posta dönmez, privacy] + `label.*` [board scope, `(board,renk,ad)` benzersiz → `CONFLICT`, activity yok] + `card.labels.*`; `@pusula/domain` `LABEL_COLORS` (10 renk) + `schemas/label.ts`; `card-members.test.ts` 10 + `card-labels.test.ts` 6 + `label.test.ts` 7; QA code-reviewer + security-reviewer PASS + verifier PASS [test 165]; commit `b3fc9a4`; Linear → `Done` + kapanış yorumu). Bağımlılık: DEM-49 + DEM-34 + DEM-36 (hepsi Done). 2.5A ile paralel yapıldı.
4. **Faz 2.5C — Board üye yönetimi + board davetleri (backend)** — `board.members.{list,add,updateRole,remove}` (`boardProcedure`; `add` e-postayla — workspace üyesini doğrudan / hesabı olan non-member'ı `guest` yapıp / hesabı yoksa `board_invitations` daveti) + `board.invitations.{list,revoke,mine,accept,decline}`; yeni `board_invitations` tablosu; activity (`board.member_added/removed/role_changed`, `board.member_invited`, `board.invitation_revoked`); `effectiveBoardRole` zaten explicit board üyeliğini kapsıyor — bu iş onu yönetir; son board `admin` korunur. — `Done` (`API-2026-05-12-009` / [DEM-52](https://linear.app/demirkol/issue/DEM-52); `board.members.*` [`list` e-posta dönmez + `inherited` flag; `add` üç yol: ws üyesi direkt / ws-dışı hesap → workspace `guest` + board üye / hesapsız → `board_invitations`; son explicit admin `.for('update')` ile korunur] + `board.invitations.{list,revoke,mine,accept,decline}` [`workspace_invitations` disiplini — gizli token, süreli, tek kullanımlık, partial unique; kabulde davetli ws `guest` + board üye]; yeni `board_invitations` tablosu + migration `0006` (enum `ADD VALUE` ×3 + `CREATE TABLE`); `board-members.test.ts` 8 + `board-invitations.test.ts` 7; QA round-2 PASS (round-1 FAIL — H1 docs-drift + M1 accept arşiv kontrolü + M2/M3 race/TOCTOU düzeltildi) + verifier PASS; commit `c321154`; Linear → `Done` + kapanış yorumu). Bağımlılık: DEM-49 + DEM-34 (ikisi de Done). 2.5A/2.5B ile paralel yapıldı.
5. **Faz 2.5D — Kart detay görünümü (web)** — karta tıklanınca açılan **modal** (`?card=<id>` query param): başlık/açıklama/due-date (`card.update`), üyeler (`card.members.*`), etiketler (`card.labels.*`), checklist'ler (`checklist.*`/`checklist.item.*`), yorumlar (`comment.*`), kart activity feed (ayrı `card.activity.list` — `bosluk-tara` G2). Yalnız shadcn/ui; metinler `strings.card`; optimistic UI yok (mutation → invalidate → refetch); `clientMutationId` istemcide. — `Done` (`FE-2026-05-12-008` / [DEM-53](https://linear.app/demirkol/issue/DEM-53); `_components/card-detail/` modal (`card-detail-route/dialog` + title/description/due-date/labels/members/checklists(+item-row/block/add-forms)/comments/activity + `activity-summary.ts` + `label-colors.ts`) + `card-item` `?card` push, eski `edit-card-dialog` modale taşındı/silindi; backend +`checklist.list`+`card.activity.list`; QA code-reviewer PASS (round-2: dosya bölme + activity skeleton + ayrı mutation) + verifier PASS (test 223); commit `7881461`; Linear → `Done` + kapanış yorumu; `blockedBy` DEM-50 + DEM-51 + DEM-37 + DEM-49 — hepsi Done). Bağımlılık: DEM-49 + 2.5A + 2.5B + DEM-37.
6. **Faz 2.5E — Board üye + etiket yönetimi UI (web)** — board ayarlarına üye/rol yönetimi + board davet listesi/iptal (`board.members.*`/`board.invitations.*` — `bosluk-tara` G3) + etiket CRUD (`label.*`) + board ekranında etiket filtresi + arşivli liste toggle (`bosluk-tara` G7; arşivli kartlar ileri faz). Yalnız shadcn/ui; metinler `strings.board`; `clientMutationId` istemcide. — `Done` (`FE-2026-05-12-009` / [DEM-54](https://linear.app/demirkol/issue/DEM-54); `_components/board-settings/` (dialog + members-section/member-row + add-board-member-form + sent-invitations + labels-section/label-row + create-board-label-form) + `board-filter(-bar).ts(x)` + `board-columns`/`card-item` (etiket çipleri) + `pending-invitations` (board davetleri) + `label-colors.ts` `_components/`'a taşındı; backend `board.get` → `cards[].labels` (additive, tek sorgu); `vitest.setup.ts` Radix Select polyfill; QA code-reviewer PASS ("ship it"); verifier PASS (test 283); commit `ca9fe45`; Linear → `Done` + kapanış yorumu; `blockedBy` DEM-51 + DEM-52 + DEM-37 + DEM-49 — hepsi Done). Bağımlılık: DEM-49 + 2.5B + 2.5C + DEM-37. **Bununla Faz 2.5 (DEM-48 epic) tamam** → Faz 3 ([DEM-26](https://linear.app/demirkol/issue/DEM-26)) `Sonraki Faz`'ta (sıradaki faz).

> `bosluk-tara` notları (G1–G7): G1 → bu faz (DEM-48); G2 (activity feed UI) → DEM-53/54; G3 (board members) → DEM-52/54; G4 (arama) → Faz 6.5 ([DEM-56](https://linear.app/demirkol/issue/DEM-56)); G5 (profil ekranı) → Faz 1 ek iş madde 9 ([DEM-55](https://linear.app/demirkol/issue/DEM-55)); G6 (board favorileri/son görülenler) → Faz 8 ([DEM-57](https://linear.app/demirkol/issue/DEM-57), web katmanı olgunlaştıktan sonra); G7 (arşivli öğeler görünümü) → DEM-53/54 kapsamında.

## Faz 2.7 alt işleri

Faz 2.7 ([DEM-58](https://linear.app/demirkol/issue/DEM-58) — epic) beş alt iş + bir tasarım-uyum follow-up'ı (`faz-bol 2.7` + `bosluk-tara`/tarayıcı görseli, 2026-05-12/13; sıra: 2.7.0 → 2.7A ∥ 2.7B ∥ 2.7C → 2.7D → 2.7C-2). Linear'da hepsi DEM-58 altında, `Faz 2.7 — UI tasarım dili & board/kart cilalama` milestone'unda; **5'i `Done`** — 2.7.0 (`DOC-2026-05-12-007`), 2.7A (`FE-2026-05-12-011`), 2.7B (`FE-2026-05-12-012`), 2.7C (`FE-2026-05-12-013`), 2.7D (`FE-2026-05-12-015`) + DEM-66/67 kart tamamlama/kapak rengi backend (`FE-2026-05-12-014`, ayrı commit `d6dc102`) — ve **2.7C-2 follow-up'ı DEM-74 (modal §13.3 tasarım uyumu + DEM-66/67 wire) `Done`** (`FE-2026-05-13-001`, commit `e9849d2`, 2026-05-13). **DEM-58 epic `Done`** — Faz 2.7'nin tüm teslimleri (kapanış-sonrası DEM-74 cilalama follow-up'ı dahil) tamam; modal tamamen §13.3'te. Sıradaki **Faz 3** ([DEM-26](https://linear.app/demirkol/issue/DEM-26)) — `faz-baslat 3` ile başlatıldı (2026-05-13; aşağıda).

1. **2.7.0 — Önce belge / tasarım dili** ([DEM-61](https://linear.app/demirkol/issue/DEM-61), `Done`) (kontrol odası tab'ında, 2026-05-12) — yeni [`../architecture/13-ui-tasarim-dili.md`](../architecture/13-ui-tasarim-dili.md) yazıldı (design token sistemi + board/kolon/kart anatomisi + kart detay modalı yapısı [iki-kolon + kapak-renkli başlık + sağ ~360px panel] + section başlık deseni + `Avatar`/`SectionHeader`/`Progress`/`EmptyState`/`MetaChip`/`LabelChip`/`CardCompleteToggle` + shadcn `Tooltip`/`DropdownMenu`/`Checkbox`/`Tabs` spec'leri + Tiptap entegrasyonu); kullanıcı seçimi: **palet = Trello-vari** (parlak mavi primary + canlı çoklu accent + 12-renk etiket paleti; board zemini açık mavi-gri), **yoğunluk = compact-balanced** (kart `p-2`, kolon `w-72`, gap-3), **font = Inter**, rich text storage = **Tiptap JSON** (parse-time plain-text fallback, migration yok). [`../architecture/02-teknoloji-kararlari.md`](../architecture/02-teknoloji-kararlari.md) Karar kaydı (Faz 2.7 + Tiptap + 2.7.0 palet/yoğunluk/font kararları) + kök `CLAUDE.md` §2 #8 (rich text = Tiptap notu) + [`../architecture/08-web-ve-mobil.md`](../architecture/08-web-ve-mobil.md) pointer'ı + [`../architecture/README.md`](../architecture/README.md) indeksi güncellendi.
2. **2.7A — Tema + token + `packages/ui` rafinasyonu** ([DEM-62](https://linear.app/demirkol/issue/DEM-62), `Done` — `FE-2026-05-12-011`; `blockedBy` DEM-61) — _implement edildi + QA PASS + kullanıcı onayladı 2026-05-12:_ `theme.css` baştan yazıldı (Trello-vari OKLCH `:root`+`.dark` paleti, 12-renk `--palet-*` + `-foreground` eşleri, radius base `0.5rem` + `-sm/-md/-lg/-xl`, shadow scale `--shadow-card/-card-hover/-popover/-drag`, `--font-sans`=Inter, `@theme inline` `--color-*`+`--color-palet-*`+shadow+font map; eski `--chart-*`/`--sidebar*` kaldırıldı); `apps/web` Inter `next/font` self-host (`--font-inter`); `_components/label-colors.ts` ham Tailwind isimleri → `bg-palet-*` token + `PaletteName`/`LABEL_PALETTE` map (domain `LABEL_COLORS` değişmedi); yeni `packages/ui` bileşenleri `Avatar`/`SectionHeader`/`Progress`/`EmptyState`/`MetaChip`+`MetaRow`/`LabelChip`(+`LabelSwatch`)/`CardCompleteToggle` + shadcn `Tooltip`/`DropdownMenu`/`Checkbox`/`Tabs` (Radix tabanlı; deps eklendi); mevcut shadcn (`button/input/label/card/dialog/alert/badge/textarea/select`) tema rafinasyonu (`shadow-card`/`shadow-popover`, `ring-ring/60`, `--radius` 0.5rem); 10 yeni RTL test dosyası + `vitest.setup.ts` `ResizeObserver` shim'i; `pnpm -w typecheck`/`lint`/`test` (317 test) + `@pusula/web build` yeşil; QA = code-reviewer APPROVE WITH NITS (M1 `PaletteName` duplikesi `@pusula/ui`'den import + M2 `13-ui-tasarim-dili.md` path düzeltildi) + verifier PASS. Fonksiyon değişmedi; kullanıcı onayladı → commit `feat: DEM-62 — Faz 2.7A …` + push (main).
3. **2.7B — Board ekranı cilalama** ([DEM-63](https://linear.app/demirkol/issue/DEM-63), `Done` — `FE-2026-05-12-012`; `blockedBy` DEM-62 + DEM-37) — _implement edildi + QA PASS + kullanıcı onayladı 2026-05-12:_ `board.get` additive zenginleştirme (`cards[]` → `checklistTotal`/`checklistDone`/`commentCount`/`members[]`; toplu sorgular `Promise.all` paralel; "önce belge" `03-backend.md` + `05-board-mekanigi.md`); `page.tsx` + yeni `_components/board-top-bar.tsx` (`BoardIdentity` + `BoardViewSwitch` ["Pano" aktif, "Liste"/"Etiketler" disabled] + `BoardActions` [Davet/⋮ DropdownMenu]) + `board-skeleton.tsx` (`role=status`) + `card-meta-row.tsx` (due chip + "GECİKTİ" rozeti + ≤72h amber-soon-dot + açıklama-var + checklist progress + yorum sayısı + üye avatar yığını); `card-item.tsx` (`role=button` + Enter/Space → `?card=`, `LabelChip` etiket chip'leri, `CardMetaRow`, hover arşiv ikonu); `list-column.tsx` (kolon anatomisi + ⋮ DropdownMenu + arşivli `border-dashed` salt-okunur); `board-columns.tsx` (`gap-3` + boş board `EmptyState`); `board-filter-bar.tsx` (`LabelChip` + `Checkbox` arşiv toggle); `rename-board-form`/`archive-board-dialog`(+`useRestoreBoard`)/`board-settings-dialog` opsiyonel controlled props; `strings.board.*` genişletildi; 4 RTL + `board.test.ts` güncel; QA = code-reviewer APPROVE WITH NITS (M1 sorgu paralelleştirme + L2 etiket fallback düzeltildi) + verifier PASS (336 test, build yeşil). **`CardCompleteToggle` board kartında render edilmedi** (kart-completion özelliği yok — kullanıcı kararı); cover/ek chip'i yok (attachment Faz 8). Fonksiyon davranışı değişmedi; commit `feat: DEM-63 — Faz 2.7B …` + push (main).
4. **2.7C — Kart detay modalı yeniden yapı + Tiptap** ([DEM-64](https://linear.app/demirkol/issue/DEM-64), `Done` — `FE-2026-05-12-013`; `blockedBy` DEM-62 + DEM-53) — _implement edildi + QA PASS + kullanıcı onayladı 2026-05-12:_ `packages/ui` `RichTextEditor`/`RichTextContent` (Tiptap v3 StarterKit + Placeholder; Link `isAllowedUri`/`shouldAutoLink` ile `http`/`https`/`mailto` whitelist — `javascript:` href düşürülür; `parseRichTextValue` parse-time plain-text fallback; prose stilleri token'lı); `card-detail-dialog.tsx` baştan (iki-kolon `grid md:grid-cols-[1fr_360px]` — sol: sticky `CardDetailTitle` → `CardDetailLabels`/`Members`/`DueDate` → AÇIKLAMA Tiptap → KONTROL LİSTESİ `Checkbox`+`Progress`; sağ: `CardModalSidebar` `Tabs` [Yorumlar/Aktivite/Ekler/Tümü] + yorum composer); `CardModalHeader` (breadcrumb + ⋮ `DropdownMenu` arşivle/geri al + `LinkIcon` derin link + `X`); `card-detail-comments`/`description`/`activity`/`checklists` yeni stile; `strings.card.detail.*` genişletildi; storage = Tiptap JSON `text` kolonlarda (procedure imzaları aynı, **migration yok**); RTL testleri + `vitest.setup.ts` ProseMirror shim; QA = code-reviewer APPROVE WITH NITS (Tiptap Link XSS sertleştirme + yeni activity tipleri özet + yorum no-op normalize düzeltildi; `RichTextContent` per-comment editör perf'i follow-up) + verifier PASS (365 test, build yeşil). **`CardCompleteToggle` modal başlığında render edilmedi + kapak-rengi picker yapılmadı** (DEM-66/67 backend bu commit'te var ama 2.7C henüz kullanmıyor — follow-up: modal'a wire). **DEM-66/67 (kart tamamlama + kapak rengi backend) bu commit'te birlikte** (kullanıcı kararı — şema/domain/test iç içe; `FE-2026-05-12-014`). Fonksiyon davranışı (2.7C tarafında) değişmedi; commit `feat: DEM-64 (+ DEM-66/67) — …` + push (main). (DEM-53'ün [Done] modalı üzerine yapısal yeniden kurulum.)
5. **2.7D — Workspace / app-shell ekranları + a11y rötuş** ([DEM-65](https://linear.app/demirkol/issue/DEM-65), `Done` — `FE-2026-05-12-015`; `blockedBy` DEM-63 + DEM-64) — _implement edildi + QA PASS + kullanıcı onayladı 2026-05-12:_ `packages/ui` `DialogContent` `closeLabel?` prop (default `'Kapat'`); `apps/web` `strings.common.close` + tüm açık-uçlu `DialogContent`'lere `closeLabel`; `app-shell.tsx` header tema token'larına + `usePathname()` ile board route'unda full-width `<main>` (diğerlerinde `max-w-5xl`); `boards/[boardId]/page.tsx` full-bleed layout; `(app)/page.tsx`/`board-list-section` kart hover + link focus-ring; `onboarding-empty-state` → `@pusula/ui` `EmptyState`; `workspaces/[id]`/`account` link rötuşları; `board-skeleton` `motion-reduce:animate-none`; `board-settings-dialog` `<h3>` → `SectionHeader`; renk-picker/`×` butonlarına `focus-visible:ring`; yeni `dialog.test.tsx` + mevcut testler uyumlu; QA = code-reviewer APPROVE WITH NITS (sadece NIT/LOW — fix gerekmedi; `SectionHeader` heading semantiği notu follow-up) + verifier PASS (368 test, build yeşil). **Fonksiyon değişmedi**; commit `feat: DEM-65 — Faz 2.7D …` + push (main). **Bununla Faz 2.7 (DEM-58 epic) tamam — DEM-58 `Done`** → Faz 3 ([DEM-26](https://linear.app/demirkol/issue/DEM-26)) `Sonraki Faz` zaten (sıradaki — `faz-baslat 3`).
6. **2.7C-2 — Kart detay modalı tasarım uyumu (follow-up)** ([DEM-74](https://linear.app/demirkol/issue/DEM-74), `Done` — `FE-2026-05-13-001`, commit `e9849d2`, 2026-05-13; `relatedTo` DEM-53/64/66/67; DEM-58 altında) — _implement edildi + QA PASS + kullanıcı onayladı (tarayıcı doğrulaması dahil) 2026-05-13:_ DEM-64'ün modalı `13-ui-tasarim-dili.md` §13.3'e çekildi: `DialogContent` `w-[min(960px,92vw)]` + `sm:max-w-none` (**kök sebep:** shadcn `DialogContent` base'inde `sm:max-w-lg` var — `twMerge` farklı breakpoint variant'larını çakışan saymaz, modal ~512px'e kapanıyordu) + iki-kolon grid `md:grid-cols-[1fr_360px]` + sol kolon/sticky div'e `min-w-0` (yatay scrollbar'ın diğer kök sebebi: `1fr` track `min-width:auto`) + `PROSE_CLASS` `[&_pre]:overflow-x-auto`; kapak-renkli başlık çubuğu (`bg-palet-{ad}` / `bg-background border-b`) + 12-renk kapak rengi picker (`card-detail-cover-color.tsx` — `CARD_COVER_COLORS` swatch ızgarası) → `card.update({coverColor})` (clear=null) + kartta kapak şeridi (`-mx-2 -mt-2 mb-1.5 h-3 rounded-t-md bg-palet-{ad}`); `CardCompleteToggle` → `card.complete`/`card.uncomplete` wire (modal sticky başlık + `card-item.tsx`; tamamlanmış → `bg-success` tik + başlık `line-through text-muted-foreground`); sağ panel sekmesi "İşlemler"→"Aktivite" (`strings.card.detail.tabs.activity`); `SectionHeader` aksiyon-slotları ikon-only `ghost` + `Tooltip` (description/checklist/comment edit-delete + checklist add — metin taşması bitti); başlık inline-edit hizalandı (placeholder kaldırıldı, bleed yok); `LabelChip` kart etiket chip'leri; comment composer'a disabled `PaperclipIcon` placeholder; `card-complete-toggle.tsx` `aria-disabled` (QA); `strings.ts` `tabs.activity`/`activity.empty` güncellendi + cover/complete key'leri eklendi, `completeToggleSoon`/`coverColorSoon` placeholder key'leri silindi. **Backend dokunulmadı** (DEM-64/66/67 zaten ekledi — `card.get`/`card.update`/`card.complete`/`card.uncomplete`/`board.get` `completed`/`coverColor` döndürüyor). QA = code-reviewer APPROVE WITH NITS (CRITICAL/HIGH/MEDIUM 0; nit-fix: 7 ölü `strings` key silindi, `aria-disabled`, gereksiz cast kaldırıldı) + verifier PASS (440 test; build yeşil; güvenlik temiz; kapsam doğru — `packages/api/db/domain` dokunulmadı). Fonksiyon değişmedi; commit `feat: DEM-74 — Faz 2.7C-2 …` + push (main). **Bununla DEM-58 (Faz 2.7) epic'in tüm teslimleri tamam — modal tamamen §13.3'te.**

> **Kararlar** (kullanıcı onayıyla, 2026-05-12 — iki-ajan analizi [eski Pusula UI + mevcut `pusula_v2` web UI envanteri] + ekran görüntüleri + Trello karşılaştırması sonrası): referans = **karma** (eski Pusula `D:\projects\pusula` UI'ı layout/anatomi/token sistemi baz + Trello iyileştirmeleri; eski projenin kodu birebir kopyalanmaz — `@base-ui/react` getirmemek için shadcn/Radix üzerine yeniden kurulur); tema = **yeni palet** (2.7.0'da seçenekler hazırlanır → kullanıcı seçer); rich text = **Tiptap** eklenir (headless editör — component library değil; `02-teknoloji-kararlari.md` Karar kaydı + `CLAUDE.md` §2 #8 notu eklendi 2026-05-12); sıra = 2.7.0 önce-belge önce, uygulama Faz 2.5 web bittiğinden serbest. Ayrıntı → [DEM-58](https://linear.app/demirkol/issue/DEM-58) açıklaması.

## Faz 3 alt işleri

Faz 3 ([DEM-26](https://linear.app/demirkol/issue/DEM-26) — epic) altı alt işe bölündü (sıra: 3.0 → 3A → (3B ∥ 3C) → 3D; 3E ⊇ 3A, 3A sonrası).
Linear'da hepsi DEM-26 altında, `Faz 3 — Drag-Drop` milestone'unda. **Geliştirme 2026-05-13 başladı (`faz-baslat 3`):** epic DEM-26 `In Progress`; 3.0 ([DEM-41](https://linear.app/demirkol/issue/DEM-41)) `Review` (kontrol odası tab'ı — önce-belge tamam 2026-05-13: `05-board-mekanigi §5.1` + `03-siralama-kurallari` + `08-web §8.1.8` + `@pusula/domain` move-input şekilleri), 3A ([DEM-42](https://linear.app/demirkol/issue/DEM-42)) `Review` (implement + QA PASS 2026-05-13 — kullanıcı onayı bekliyor; koddan önce `03-backend.md` "Faz 3 — `list.move` / `card.move`" procedure tablosu + `02-yetkilendirme-kurallari.md` "Drag-drop / move procedure haritası (Faz 3)" eklendi), 3B–3E (DEM-43–45 + DEM-69) `Todo`.
`move`/reorder mutation'ları (Faz 2'de ertelenmişti) bu fazda; sürükle-bırak yalnızca Atlassian Pragmatic Drag and Drop (dnd-kit yok); tam optimistic cache Faz 4.

**Paralellik:** DEM-74 (Faz 2.7C-2, kart detay modalı, `apps/web`/`packages/ui`) **land etti** (`e9849d2`, 2026-05-13) — artık bloklayıcı değil. 3A backend ([DEM-42](https://linear.app/demirkol/issue/DEM-42)) `apps/web` dosyalarına dokunmaz; 3B drag-drop UI ([DEM-43](https://linear.app/demirkol/issue/DEM-43)) `apps/web` board dosyalarına (`board-columns`, `card-item.tsx`) dokunduğu için **yalnız 3A'ya bağımlı** (DEM-74 land etti); 3C worker bağımsız. (Tab haritası → [`../kontrol-odasi/README.md`](../kontrol-odasi/README.md) §5.)

1. **Faz 3.0 — Önce belge** — `docs/architecture/05-board-mekanigi.md` §5.1 (drag-drop mekanikleri netleştir), [`../domain/03-siralama-kurallari.md`](../domain/03-siralama-kurallari.md) (compaction tetiği + eşzamanlı move semantiği — eksikse tamamla), [`../architecture/08-web-ve-mobil.md`](../architecture/08-web-ve-mobil.md) §8.1.8 (board ekranına drag-drop), bu dosyanın "Faz 3 alt işleri" bölümü + `05-is-kayit-defteri.md`; `@pusula/domain` `moveCardInput`/`moveListInput` şekilleri. **Kontrol odası tab'ında** yapılır ([`../kontrol-odasi/README.md`](../kontrol-odasi/README.md)). — `Review` ([DEM-41](https://linear.app/demirkol/issue/DEM-41); `DOC-2026-05-13-001`) — **önce-belge tamam (2026-05-13):** `05-board-mekanigi §5.1` yeniden yazıldı (UI ilkeleri + `moveCard`/`moveList` şekilleri + server move akışı + faz kapsamı + test), `03-siralama-kurallari` tamamlandı (compaction tetiği `POSITION_COMPACTION_MAX_LEN≈50` + `CONFLICT` semantiği + cross-board `card.moveToList` notu), **`08-web §8.1.8` "Board ekranı drag-drop (Faz 3B)" eklendi** (Pragmatic DnD draggable/dropTarget/monitor + `onDrop` → tek `card.move`/`list.move` + optimistic cache/rollback + `CONFLICT` refetch + yetki/arşiv + erişilebilir alternatif "Taşı" menüsü + test ayrımı 3D Playwright'a), `@pusula/domain` move-input şemaları zaten mevcut & doğru (`schemas/card.ts` `moveCardInput` / `schemas/list.ts` `moveListInput`). Bağımlılık: yok. Kullanıcı `08-web §8.1.8`'i onaylayınca `Done`; 3A buna bağlı değil — paralel başlayabilir.
2. **Faz 3A — `list.move` + `card.move` (backend)** — reorder + listeler arası taşıma; `@pusula/domain/position` ile pozisyon hesabı/doğrulama; "kart hâlâ beklenen listede mi" → `CONFLICT` + arşivli hedef + kart ⊆ liste.board kontrolleri; transaction (move + `activity_events` `list.moved`/`card.moved` + `boards.version`); idempotent (`clientMutationId`). Middleware: `list.move` → `boardProcedure` (`moveListInput` `boardId` taşır); `card.move` → `cardProcedure` (`moveCardInput` `cardId`-anahtarlı; `05-board-mekanigi.md` §5.1'deki `boardProcedure` yazımı `card.move` için `cardProcedure` okunur). Procedure haritası → `03-backend.md` "Faz 3 — `list.move` / `card.move`" + `02-yetkilendirme-kurallari.md` "Drag-drop / move procedure haritası (Faz 3)"; server akışı → `05-board-mekanigi.md` §5.1. — `Review` ([DEM-42](https://linear.app/demirkol/issue/DEM-42); `API-2026-05-13-001`) — implement + QA PASS 2026-05-13 (code-reviewer APPROVE + verifier PASS [464 test, typecheck/lint/build temiz], kullanıcı onayı bekliyor; commit henüz yok); koddan önce `03-backend.md` + `02-yetkilendirme-kurallari.md` move procedure tabloları eklendi; `position.ts`/`schemas/card.ts` JSDoc'larındaki stale `docs/PUSULA_TEKNIK_MIMARI.md` atıfları güncel docs'a düzeltildi; yeni `packages/api/src/lib/position.ts` + `packages/domain/src/position.test.ts`. Bağımlılık: Faz 2A/2B/2C (CRUD backend, `Done`) + Faz 3.0 (`05` + `03` kısmı tamam).
3. **Faz 3B — Drag-drop UI (web)** — Atlassian Pragmatic Drag and Drop board ekranına (DEM-37): liste reorder, kart reorder, listeler arası taşıma; drag sırasında local state, `onDragEnd` → `list.move`/`card.move`, başarısızlıkta rollback; `clientMutationId`. UI ilkeleri → `05-board-mekanigi.md` §5.1 + `08-web-ve-mobil.md` §8.1.8. — `Todo` ([DEM-43](https://linear.app/demirkol/issue/DEM-43)). Bağımlılık: Faz 3A + Faz 2D (DEM-37) (DEM-74 land etti — `e9849d2`; `apps/web` board dosyaları artık serbest).
4. **Faz 3C — Position compaction worker** — fractional `position` aralığı tükenmeye yaklaşınca ilgili scope için background compaction (`apps/worker`, BullMQ); tetik kuralı `03-siralama-kurallari.md` "Compaction" (`POSITION_COMPACTION_MAX_LEN`, debounce). — `Todo` ([DEM-44](https://linear.app/demirkol/issue/DEM-44)). Bağımlılık: Faz 3A; 3B ile paralel.
5. **Faz 3D — Playwright drag-drop testleri** — Playwright kurulumu (yoksa) + e2e: liste reorder, kart reorder, cross-list move, başarısızlık rollback, (ops.) eşzamanlı kullanıcı / kart taşınmışken move. Geniş e2e suite Faz 8 ([DEM-31](https://linear.app/demirkol/issue/DEM-31)). — `Todo` ([DEM-45](https://linear.app/demirkol/issue/DEM-45)). Bağımlılık: Faz 3B.
6. **Faz 3E — `card.moveToList` (cross-board) + `card.copy`** — kartı **başka board'a** taşıma (kartın `board_id`'sini de değiştirir, hedef board'da yetki + arşiv kontrolü, checklist/yorum/etiket/üye bağları için kuralı netleştirir) + kart kopyalama. Trello standart kart menüsü ("Move", "Copy"); `bosluk-tara` 2. tur. — `Todo` ([DEM-69](https://linear.app/demirkol/issue/DEM-69)). Bağımlılık: Faz 3A (DEM-42).

Her fazın teknik ayrıntısı ilgili `docs/architecture/` dosyalarında; ilgili iş kuralları
`docs/domain/` dosyalarında. Faz sırası kullanıcı aksini istemedikçe bu şekilde ilerler.
