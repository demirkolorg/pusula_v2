---
title: "05 — İş Kayıt Defteri"
description: "Linear işlerinin repo içindeki takip aynası."
aliases:
  - "İş Kayıt Defteri"
  - "Work Register"
tags:
  - "pusula"
  - "process/work-register"
  - "linear"
type: "register"
axis: "process"
status: "active"
parent: "[[docs/process/README|Süreç]]"
updated: 2026-05-13
---
# 05 — İş Kayıt Defteri

> Eksen: **süreç**. Bu dosya Linear'ın repo içindeki takip aynasıdır. Her geliştirme,
> refactor, bug fix, dokümantasyon ve infra işi için tek satır tutulur.

Durumlar `Backlog`, `Sonraki Faz`, `Todo`, `In Progress`, `Review`, `Done`, `Canceled` setinden seçilir
(Linear karşılıkları ve "ayrı state olmayan" durumlar için kural → [`04-otomatik-is-akisi-protokolu.md`](04-otomatik-is-akisi-protokolu.md) "Durum sözlüğü";
Demirkol takımında `Review` için ayrı state yok → Linear'da `In Progress` + notta belirtilir; `Blocked` için ayrı state yok → bloke iş `Todo`/`Backlog`'ta kalır + `blockedBy` ilişkisi + notta belirtilir).
Linear MCP erişilemiyorsa `Linear` alanına `MCP bekliyor` yazılır ve ilk uygun oturumda Linear issue ile eşlenir.

| İş ID | Linear | Başlık | Faz | Durum | Sahip | Etkilenen belgeler | Etkilenen katmanlar | Son senkron | Not |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DOC-2026-05-12-001 | DEM-16 | Otomatik iş akışı protokolünü kur | Süreç | Done | Claude Code | `CLAUDE.md`, `.claude/skills/kontrol/SKILL.md`, `docs/README.md`, `docs/process/*` | `docs/process` | 2026-05-12 | Linear issue [DEM-16](https://linear.app/demirkol/issue/DEM-16) ile eşlendi (proje: Pusula). |
| DOC-2026-05-12-002 | DEM-17 | Obsidian kasası dokümantasyon düzeni | Süreç | Done | Codex | `README.md`, `CLAUDE.md`, `.claude/skills/kontrol/SKILL.md`, `docs/**/*.md` | `docs` | 2026-05-12 | 31 Markdown dosyasında frontmatter/link kontrolü geçti; Linear issue [DEM-17](https://linear.app/demirkol/issue/DEM-17) ile eşlendi (proje: Pusula). |
| INFRA-2026-05-12-001 | DEM-18 | İlk commit ve GitHub remote (`pusula_v2`) push | 0 — Temel altyapı | Done | Claude Code | `.gitignore`, `docs/process/05-is-kayit-defteri.md` | repo kökü | 2026-05-12 | Faz 0 iskelesinin tamamı tek commit ile yüklendi; `master` → `main`, remote: github.com/demirkolorg/pusula_v2. `.obsidian/` gitignore'a eklendi. Linear issue [DEM-18](https://linear.app/demirkol/issue/DEM-18) ile eşlendi (proje: Pusula). |
| DOC-2026-05-12-003 | DEM-19 | Genel çalışma başlangıç rehberi oluştur | Süreç | Done | Codex | `CLAUDE.md`, `.claude/skills/kontrol/SKILL.md`, `docs/README.md`, `docs/process/00-calisma-baslangic-rehberi.md`, `docs/process/03-faz-0-devir-notu.md`, `docs/process/04-otomatik-is-akisi-protokolu.md`, `docs/process/README.md` | `docs/process` | 2026-05-12 | Yeni işlerde Faz 0 devir notu yerine genel başlangıç rehberi baz alınacak. Linear issue [DEM-19](https://linear.app/demirkol/issue/DEM-19) ile eşlendi (proje: Pusula). |
| API-2026-05-12-001 | DEM-20 | Faz 1A — Workspace alanı (backend): `workspaceProcedure` + CRUD + üyelik + permission enforcement | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/architecture/07-auth.md`, `docs/process/02-mvp-faz-plani.md` | `packages/api`, `packages/domain`, `packages/db` | 2026-05-12 | İmplement edildi: `workspaceProcedure` (membership middleware), `workspace.{list,create,get,update,archive}` + `workspace.members.{list,updateRole,remove}`, activity yazımı (`workspace.created/updated/archived/member_role_changed/member_removed`) ilgili transaction'da. `ACTIVITY_EVENT_TYPES`'a 3 yeni değer + Drizzle migration `drizzle/0001_loud_kinsey_walden.sql`. Vitest kuruldu: 15 domain birim + 8 tRPC integration testi PASS. QA (code-reviewer + security-reviewer + database-reviewer) = PASS; review M1 bulgusu düzeltildi (slug çakışmasında PG `23505` → `CONFLICT` map'lenir, check-then-write yarışına karşı). verifier = PASS (`typecheck`/`lint`/`test` yeşil, secret taraması temiz). Commit: `97cd9b8` (main, push edildi → origin). Linear issue [DEM-20](https://linear.app/demirkol/issue/DEM-20) ile eşlendi (proje: Pusula); kullanıcı onayladı → Linear durumu `Done` (2026-05-12, kapanış yorumu eklendi). Kalan: davet-token akışı + `workspace.delete` ayrı kayıt (bkz. `docs/process/02-mvp-faz-plani.md` → Faz 1 alt işleri). |
| PROC-2026-05-12-001 | DEM-21 | Linear senkronu: bekleyen iş kayıtlarını Linear'a eşle + Pusula projesi | Süreç | Done | Claude Code | `docs/process/05-is-kayit-defteri.md` | `docs/process` | 2026-05-12 | Linear MCP callable hâle geldi; `Pusula` projesi açıldı (state: In Progress), DOC/INFRA/API satırları DEM-16…DEM-20 ile eşlendi, bu iş [DEM-21](https://linear.app/demirkol/issue/DEM-21). Re-auth sonrası DEM-20 → Done, FE-2026-05-12-001 → [DEM-22](https://linear.app/demirkol/issue/DEM-22); 5 Mayıs test/çöp kayıtları temizlendi (DEM-10–15: DEM-10/11/12/14/15 arşivde, DEM-13 iptal edildi + yeniden adlandırıldı). `MCP bekliyor` kalmadı. |
| FE-2026-05-12-001 | DEM-22 | Faz 1B — Auth web UI & session: sign-up/in/out, korumalı kabuk, workspace listesi/oluşturma | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md`, `docs/architecture/07-auth.md`, `docs/process/02-mvp-faz-plani.md` | `apps/web`, `packages/ui`, `packages/domain` | 2026-05-12 | Pre-dev: ilgili docs koddan önce güncellendi (`08-web-ve-mobil.md` §8.1.1 — auth ekranları & oturum, client-side session, korumalı route group, shadcn bileşen ekleme; `07-auth.md` web client notu). İmplement edildi: `@pusula/domain/schemas/auth.ts` (`signInInput`/`signUpInput` + `email/password/name` şemaları, barrel'a eklendi); `@pusula/ui`'ye shadcn `Input`/`Label`/`Card`/`Dialog`/`Alert` (Radix `react-dialog`+`react-label`, `tw-animate-css`) eklenip `src/index.ts` + `package.json` `exports`'tan dışa aktarıldı; `apps/web` route group'lar: `(auth)/{layout,sign-in,sign-up}` + paylaşılan presentational `_components/auth-form.tsx`, `(app)/{layout,page}` + `_components/{app-shell,create-workspace-dialog}` (korumalı kabuk client-side `useSession`, `?redirect=` koruması; workspace listesi `trpc.workspace.list`, oluşturma `trpc.workspace.create` + `queryClient.invalidateQueries(trpc.workspace.list.queryFilter())`); eski demo `app/page.tsx` silindi; `apps/web/src/env.ts` (Zod, `NEXT_PUBLIC_API_URL`) eklendi, `auth-client.ts` + `trpc/client.tsx` bunu kullanıyor; kullanıcı-yüzlü Türkçe metinler `apps/web/src/lib/strings.ts`'te toplandı (i18n placeholder, lib eklenmedi). Test: `apps/web`'e Vitest+RTL (`vitest.config.ts`/`vitest.setup.ts`, jsdom) kuruldu, `auth-form.test.tsx` 6 test PASS. Tüm suite: 29 test PASS (15 domain + 8 api + 6 web); `pnpm typecheck`/`lint`/`@pusula/web build` (route'lar: `/`, `/sign-in`, `/sign-up`) yeşil. Atlanan: Playwright e2e (sign-up→workspace→çıkış) ileri faz; davet-token akışı ayrı kayıt. QA (code-reviewer + security-reviewer) = PASS; bulgular düzeltildi — H1 open-redirect (`?redirect=` artık `safeRedirectPath` ile doğrulanır, `//evil.com` vb. engellenir; redirect tek noktada — `(auth)` layout'taki `RedirectIfAuthenticated`), M4 (`AppShell.signOut` catch), L4 (`z.url`), M1 (`apps/web/tsconfig.json` `types` daraltıldı). verifier = PASS (`typecheck`/`lint`/`@pusula/web build`/`test` — 15 domain + 8 api + 6 web — yeşil, secret taraması temiz). `.gitignore`'a `.claude/worktrees/` + `.claude/cache/` eklendi. Commit: `cefd148` (main, push edildi → origin). Kullanıcı onayladı → durum `Done` (2026-05-12); DEM-22'nin Linear durumu da `Done`'a çekilecek (Linear MCP re-auth bekliyor). Linear issue [DEM-22](https://linear.app/demirkol/issue/DEM-22) ile eşlendi (proje: Pusula); DEM-20'ye bağımlı (Done). Açık (low/info, ertelendi): `api-status.tsx` artık referanssız (Faz 0 demo widget'ı — bilinçli bırakıldı), `.claude/worktrees/eager-chatterjee-074e94` stray agent worktree'si (gitignore'lu; `git worktree prune` ile temizlenebilir). |
| API-2026-05-12-002 | DEM-23 | Faz 1.3 — Workspace davet akışı: davet token tablosu + invite/accept/decline + bildirim + web UI | 1 — Auth & Workspace | Done | Claude Code | `docs/domain/01-urun-modeli.md`, `docs/architecture/04-veri-katmani.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/domain/05-aktivite-kurallari.md`, `docs/architecture/08-web-ve-mobil.md`, `docs/process/02-mvp-faz-plani.md` | `packages/db`, `packages/api`, `packages/domain`, `packages/ui`, `apps/web` | 2026-05-12 | Pre-dev: davet akışı koddan önce docs'a işlendi — `workspace_invitations` entity (`01-urun-modeli.md`) + tablo/kolonlar + `invitation_status` enum (`04-veri-katmani.md`), davet procedure haritası `members.invite` / `invitations.{list,revoke,mine,accept,decline}` + e-posta-eşleşme/tek-kullanımlık/expiry kuralları (`02-yetkilendirme-kurallari.md`), yeni activity tipleri `workspace.member_invited` + `workspace.invitation_revoked` (`05-aktivite-kurallari.md`), `notification_outbox.recipient_id` nullable notu. **Backend tamam** (web davet UI'ı ayrı tur): `@pusula/domain` — `INVITATION_STATUSES` + `WORKSPACE_INVITATION_TTL_DAYS` (=7) sabitleri, `ACTIVITY_EVENT_TYPES`'a `workspace.member_invited`/`workspace.invitation_revoked` (append), `schemas/invitation.ts` (`invitationStatusSchema`, `invitationTokenSchema`, `revoke/accept/declineWorkspaceInvitationInput`); `@pusula/db` — `invitation_status` pgEnum, `workspace_invitations` tablosu (token uniq idx + (workspace,status)/(email) idx), `notification_outbox.recipient_id` `notNull` kaldırıldı (nullable; e-posta daveti hesapsız adrese gidebilir), Drizzle migration `drizzle/0002_omniscient_lady_ursula.sql` (+ QA sonrası `drizzle/0003_pretty_daredevil.sql` — `workspace_invitations_pending_email_uq` partial unique index `(workspace_id, lower(email)) WHERE status='pending'`) üretildi + lokal Postgres'e uygulandı (`pnpm db:migrate`); `@pusula/api` — `workspace.members.invite` (`admin+`, e-posta lowercase, zaten üye/zaten pending → `CONFLICT`, `node:crypto.randomBytes` token, `workspace_invitations` + `activity_events`(`workspace.member_invited`) + `notification_outbox`(`email` + hesap varsa `in_app`) tek transaction'da, token response'da dönmez) + yeni `workspace.invitations` router'ı (`list` member+, `revoke` admin+ → `workspace.invitation_revoked`, `mine`/`accept`/`decline` `protectedProcedure`; `accept` e-posta eşleşme + expiry [süresi dolmuşsa `status=expired`, tx dışında durable] + idempotent üye ekleme + `workspace.member_added`; `decline` → `status=declined`, activity yok — bilinçli). Test: `workspace.test.ts`'e toplam 17 yeni invitation integration testi (api 25 PASS; suite 46 — 15 domain + 25 api + 6 web; DB yoksa invitation/integration testleri skip). QA (code-reviewer + security-reviewer + database-reviewer) = PASS; bulgular düzeltildi — H1 (`workspace_invitations_pending_email_uq` partial unique index + `23505`→`CONFLICT` map, migration `0003`), H2 (`accept`'te `workspace_members` insert `.onConflictDoNothing()` + invitation satırı `.for('update')` — tam idempotent, eşzamanlı accept güvenli), M1 (`inviteWorkspaceMemberInput.email` → `emailSchema` trim+lowercase; `accept`/`decline` oturum e-postası da `.trim().toLowerCase()`), low (`invite` outbox satırları artık `eventId`'ye bağlı). verifier = PASS. Backend commit: `bca834a` (main, push edildi → origin). **Web davet UI tamam** (frontend-dev; `08-web-ve-mobil.md` §8.1.1 güncellendi): `apps/web/(app)/_components/{invite-member-form (presentational),invite-member-dialog,pending-invitations}.tsx` + `@pusula/ui` `Badge`; workspace kartında — `role` `owner`/`admin` ise — "davet et" dialog'u (e-posta-only, `members.invite`, `CONFLICT` inline) + workspace listesi üstünde "Bekleyen davetler" bölümü (`invitations.mine` → satır başına accept/decline, kabul → `workspace.list`+`invitations.mine` invalidate); `clientMutationId` istemcide; metinler `strings.invitations`'a eklendi; RTL `invite-member-form.test.tsx` 5 test. Web QA (code-reviewer) = PASS (yalnızca low/info: kullanılmayan `inviteSent`/`noPending` string'leri, `MineInvitation` tipi router'dan infer edilmiyor — ertelendi); verifier = PASS (`typecheck`/`lint`/`@pusula/web build`/`test` — 15 domain + 25 api + 11 web — yeşil, secret temiz). DEM-23'ün her iki parçası (backend + web UI) tamam; **kullanıcı onayladı → durum `Done` (2026-05-12)**. Bununla Faz 1'in ana işleri (1, 2, 3) tamam — kalan: `workspace.delete` (`API-2026-05-12-003` / DEM-24, `Backlog` ek iş). Atlanan/ertelenen: davet dialog'unda rol seçimi, gönderilmiş davet yönetimi UI'ı (`invitations.list`/`revoke` — workspace üyeler ekranıyla), worker email/in-app teslimi (Faz 6 — bu turda yalnızca `notification_outbox` satırı yazılır), realtime yayın (Faz 5). Docs ile çelişki yok. Linear issue [DEM-23](https://linear.app/demirkol/issue/DEM-23) (proje: Pusula, milestone: Faz 1); DEM-20'ye bağımlı (Done). DEM-23'ün Linear durumu da `Done`'a çekilecek — Linear MCP token süresi doldu, re-auth/kullanıcı tarafından. |
| API-2026-05-12-003 | DEM-24 | `workspace.delete` — workspace kalıcı silme (owner-only, ad onayı, cascade) | 1 — Auth & Workspace | Done | Claude Code | `docs/domain/02-yetkilendirme-kurallari.md`, `docs/process/02-mvp-faz-plani.md` | `packages/api`, `packages/domain` | 2026-05-12 | Pre-dev: tasarım çelişkisi (soft vs hard delete) kullanıcıya soruldu → **kullanıcı kararı: hard delete** (owner-only, `confirmName` ad onayı, `DELETE FROM workspaces` cascade; `archive` ayrı kavram; cascade nedeniyle DB içi iz yok). `02-yetkilendirme-kurallari.md` `workspace.delete` satırı + `02-mvp-faz-plani.md` madde 4 koddan önce güncellendi. Plan: `@pusula/domain` — `deleteWorkspaceInput` (`{ workspaceId, confirmName: workspaceNameSchema, clientMutationId }`); `@pusula/api` — `workspace.delete` (`workspaceProcedure`, `ctx.workspace.role !== 'owner'` → `FORBIDDEN`, `confirmName !== workspace.name` → `BAD_REQUEST`, tx `DELETE FROM workspaces WHERE id = ?` — cascade members/invitations/(ileride boards…), dön `{ id, deleted: true }`); şema değişikliği yok; activity/notification iz yok (cascade siler); Vitest (owner-only, ad onayı, silme + cascade kontrolü). `root.ts` zaten `workspace` mount. **İmplement edildi** (backend-dev): `@pusula/domain` `deleteWorkspaceInput`; `@pusula/api` `workspace.delete` (workspaceProcedure, owner-only, `confirmName` ad onayı, tx `DELETE`...cascade, returning boşsa `NOT_FOUND`); `workspace.test.ts` +4 testi (api 29 PASS). QA (code-reviewer + security-reviewer) = PASS — low/info: arşivli workspace `delete` ile silinemiyor (`workspaceProcedure` arşivliyi `NOT_FOUND` verir; ileri faz iyileştirmesi), büyük workspace cascade async/audit-log Faz 2+. verifier = PASS (`typecheck`/`lint`/`test` yeşil, secret temiz). Commit: `7c91527` (main, push edildi → origin). Kullanıcı onayladı → durum `Done` (2026-05-12). Linear: [DEM-24](https://linear.app/demirkol/issue/DEM-24) → `Done` (başlık "workspace.delete — workspace kalıcı silme (owner-only, ad onayı, cascade)" olarak güncellendi; kapanış yorumu eklendi — tasarım değişikliği [soft→hard delete] not düşüldü); DEM-20'ye bağımlı (Done). |
| PROC-2026-05-12-002 | DEM-32 | Linear: Faz 0–8 milestone + Faz 2–8 epic + Faz 1 alt iş issue'larını ekle | Süreç | Done | Claude Code | `docs/process/02-mvp-faz-plani.md`, `docs/process/05-is-kayit-defteri.md` | `docs/process` | 2026-05-12 | Pusula projesinde Faz 0…8 milestone'ları oluşturuldu; DEM-18 → Faz 0, DEM-20/DEM-22 → Faz 1 bağlandı; Faz 1 yeni issue'lar DEM-23 (davet akışı, Todo) + DEM-24 (`workspace.delete`, Backlog); Faz 2–8 epic'leri DEM-25…DEM-31 (Backlog). Linear issue [DEM-32](https://linear.app/demirkol/issue/DEM-32). |
| DOC-2026-05-12-004 | DEM-38 | Kontrol odası tab'ı — görev dokümanı + komut seti | Süreç | Done | Claude Code | `docs/kontrol-odasi/README.md` (yeni), `docs/README.md`, `docs/process/02-mvp-faz-plani.md`, `docs/process/04-otomatik-is-akisi-protokolu.md`, `docs/process/05-is-kayit-defteri.md`, `.claude/commands/*` (yeni) | `docs/kontrol-odasi`, `docs/process`, `.claude/commands` | 2026-05-12 | "Kontrol odası" tab'ının (kod yazmayan; süreç + docs + Linear senkronu) rolü + 7 komut (`/panorama`, `/doc-denetim`, `/linear-senkron`, `/faz-bol`, `/faz-baslat`, `/devir`, `/celiski`) dokümante edildi + `.claude/commands/` slash komutları. Linear workflow state'i `Sonraki Faz` (Unstarted; Backlog↔Todo arası) eklendi; Faz 2 alt işleri o state'e taşındı; durum lejantı `04`/`05`'te hizalandı. Memory'ye kaydedildi. Linear issue [DEM-38](https://linear.app/demirkol/issue/DEM-38) (Done). |
| FE-2026-05-12-003 | DEM-39 | Workspace yönetim ekranı (web): rename/archive + üye listesi/rol/çıkar | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md` | `apps/web`, `packages/ui` | 2026-05-12 | Faz 1 ek işi (`doc-denetim` F6 ile açıldı); backend hazır (DEM-20'de `workspace.update/archive` + `workspace.members.*`), UI yok. Davet UI'ı DEM-23'te eklendi; bu iş rename/archive + üye yönetimini kapsar. Pre-dev: `docs/architecture/08-web-ve-mobil.md` §8.1.1'e workspace yönetim ekranı (route `(app)/workspaces/[id]`, gösterilen procedure'ler, rol-bazlı görünürlük) eklendi. Plan: `(app)/workspaces/[id]/page.tsx` + `_components/*` — ad/slug düzenle (`workspace.update`, admin+), arşivle (`workspace.archive`, owner; sonra `/`'a dön), üye listesi (`workspace.members.list`) + rol değiştir (`workspace.members.updateRole`, shadcn `Select`) + üye çıkar/`workspaceten ayrıl` (`workspace.members.remove`), gönderilmiş davetler (`workspace.invitations.list` + `revoke`, admin+) + davet et dialog'u (InviteMemberDialog buraya taşınır); workspace listesi kartında ad → `/workspaces/[id]` linki. `packages/ui`'ye shadcn `Select`. `clientMutationId` istemcide; Türkçe metinler `strings.ts`. **İmplement edildi** (frontend-dev): `(app)/workspaces/[id]/page.tsx` + `_components/{workspace-settings(+form+test),member-list,member-row(+test),sent-invitations,archive-workspace-dialog}.tsx`; `(app)/page.tsx` kart başlığı → `/workspaces/[id]` link + davet dialog'u bu ekrana taşındı; `@pusula/ui` `Select` (Radix react-select); `InviteMemberDialog` opsiyonel `onInvited` prop'u → `invitations.list` invalidate. Web testleri 22 (auth-form 6 + invite-member-form 5 + member-row 6 + workspace-settings-form 5); suite toplam 66. QA round-1 (code-reviewer) = PASS — 3 medium (M1 gönderilmiş davetlerde "davet eden" adı gösterilmiyor / M2 `member.image` kullanılmayan alan / M3 session-pending'de self-satır flash+layout-shift) + 7 low/info. Round-2 düzeltmeleri: M1 → `workspace.invitations.list`'e `leftJoin users` + `invitedByName` döndürülür, `sent-invitations.tsx` "Davet eden: X · Son tarih: Y" gösterir (`workspace.test.ts` `invitations.list` testi de güncellendi); M2 → `member-row.tsx` yerel tipinden `image` çıkarıldı; M3+L1 → `member-list.tsx` session/`members` yüklenirken loading guard + mutation uçarken tüm satır kontrolleri disable; L2 → `formatDate(value: Date | string)`. Kalan L3–L7 bilinçli ertelendi (redundant no-op kontrolü, iki effect, 403/404 ayrımı yapmayan generic Alert, `onInvited` Promise ignore, dialog focus — kozmetik). verifier (round-1 + round-2 sonrası) = PASS (`pnpm -w typecheck`/`lint` 7/7, `pnpm -w test` 66 — 15 domain + 29 api + 22 web, `@pusula/web build` 5 route — yeşil). Commit: original implementation + M1/L2 `7c91527`; round-2 M2/M3 düzeltmeleri (`member-list.tsx`, `member-row.tsx`+test, `packages/api/.../workspace.test.ts`) commit `f715735` (push edildi). Kullanıcı onayladı → durum `Done` (2026-05-12). Atlanan: davet dialog'unda rol seçimi; `workspace.delete` UI'ı `FE-2026-05-12-004` / DEM-40'a taşındı (DEM-39'un tamamlayıcısı). Linear: [DEM-39](https://linear.app/demirkol/issue/DEM-39) → `Done` (kapanış yorumu eklendi); `blockedBy` DEM-20 (Done). |
| FE-2026-05-12-004 | DEM-40 | `workspace.delete` UI — workspace ayarları tehlikeli bölgesinde "kalıcı sil" (owner-only, ad onayı) | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md` | `apps/web` | 2026-05-12 | Faz 1 ek işi (DEM-39 tamamlayıcısı); backend hazır (`workspace.delete`, DEM-24, Done). Pre-dev: `08-web-ve-mobil.md` §8.1.2 tehlikeli bölge → "(2) Kalıcı sil" (owner-only, `confirmName` ad onayı [eşleşmeden onay disabled], başarıda `router.replace('/')` + `workspace.list` invalidate, `BAD_REQUEST`/`FORBIDDEN` inline). **İmplement edildi** (spark): `(app)/workspaces/[id]/_components/delete-workspace-dialog.tsx` (yeni — `DeleteWorkspaceForm` presentational [ad birebir yazma onayı, eşleşmeden submit disabled, `useId` label↔input + `aria-describedby`] + `DeleteWorkspaceDialog` tRPC shell [`trpc.workspace.delete`, `clientMutationId`, başarıda `/`'a dön + `workspace.list` invalidate, pending'de disabled + dialog kapanmaz, hata inline `destructive` Alert]); `page.tsx` tehlikeli bölge iki satıra ayrıldı — arşivle (üst) / kalıcı sil (alt, `border-t`), `isOwner` ise; `strings.workspace.manage.delete*`; `delete-workspace-dialog.test.tsx` 8 RTL test. QA (code-reviewer) = PASS; düzeltmeler: M1 (trigger `size="sm"` kaldırıldı — arşivlemeden daha vurgulu), L1/L2 (gereksiz `aria-label` + hidden hint duplikasyonu kaldırıldı, görünür prompt'a `id`+`aria-describedby`). verifier = PASS (`typecheck`/`lint`/`@pusula/web build`/`test` — 74 test [15 domain + 29 api + 30 web] — yeşil, secret temiz). Commit: pre-dev `6228bfa` + implementasyon `77c2627` (push edildi → origin). Kullanıcı talebiyle açıldı + tamamlandı → durum `Done` (2026-05-12). Linear: [DEM-40](https://linear.app/demirkol/issue/DEM-40) → `Done` (kapanış yorumu eklendi); `blockedBy` DEM-24 (Done). |
| FE-2026-05-12-005 | DEM-46 | Yeni kullanıcı onboarding'i: signup'ta default workspace + boş "İlk Pano" + login yönlendirme | 1 — Auth & Workspace | Done | Claude Code | `docs/domain/01-urun-modeli.md`, `docs/architecture/08-web-ve-mobil.md`, `docs/architecture/07-auth.md`, `docs/process/02-mvp-faz-plani.md` | `apps/api`, `apps/web`, `packages/domain` | 2026-05-12 | Faz 1 ek işi (onboarding boşluğu — `doc-denetim`/UX tartışmasıyla açıldı); backend hazır (`workspace.create` DEM-20 (Done), `board.create` DEM-34 (Done)). **Pre-dev (tamam):** `01-urun-modeli.md` invariant 11 (best-effort default workspace + boş "İlk Pano"; `workspace.list` boş olabilir → UI onboarding gösterir; şablon listeler/kartlar kapsam dışı), `08-web-ve-mobil.md` §8.1.3 (signup bootstrap akışı + `(app)/page.tsx` yönlendirme 0/1/2+; §8.1.1 "korumalı kabuk" satırı §8.1.3'e referans verir), `07-auth.md` (Better Auth `databaseHooks.user.create.after` bootstrap notu); `02-mvp-faz-plani.md` madde 7 → `In Progress` + Faz 1 header notu; Linear DEM-46 `Todo` → `In Progress`. **Plan:** `@pusula/domain` `ONBOARDING_WORKSPACE_NAME` (`"Çalışma Alanım"`) + `ONBOARDING_BOARD_TITLE` (`"İlk Pano"`) sabitleri; `apps/api/src/bootstrap.ts` (`bootstrapNewUser(userId)` — tek transaction: `workspaces`(sabit ad + benzersiz suffix'li slug) + `workspace_members`(owner) + `activity_events`(`workspace.created`) + `boards`("İlk Pano") + `board_members`(creator `admin`) + `activity_events`(`board.created`)) + `apps/api/src/auth.ts` `databaseHooks.user.create.after` → `bootstrapNewUser`; hata `console.error` ile loglanır, exception yeniden fırlatılmaz (best-effort — signup'ı patlatmaz); `apps/web` `(app)/page.tsx` yönlendirme (`workspace.list`: `pending`→placeholder, `error`→`Alert`+tekrar dene, 0→`OnboardingEmptyState`, 1→`useEffect` `router.replace('/workspaces/[id]')`, 2+→mevcut workspace listesi) + `_components/onboarding-empty-state.tsx` (`CreateWorkspaceDialog` CTA, Pusula/workspace kavram metni) + `strings.onboarding` (+ `strings.workspace.redirecting`); RTL: `onboarding-empty-state.test.tsx` + `(app)/page.test.tsx` (0/1/2+/pending/error senaryoları, `useTRPC`/`useQuery`/`useRouter` mock'lu). **İmplement edildi** (plana göre): `@pusula/domain/constants.ts` 2 sabit; `apps/api/src/bootstrap.ts` (yeni — `bootstrapNewUser`, tek tx; slug `calisma-alanim-{16hex}` — `randomBytes(8)`) + `apps/api/src/auth.ts` `databaseHooks.user.create.after` (try/catch + `console.error`, best-effort); `apps/web` `(app)/page.tsx` yeniden yazıldı (yönlendirme 0/1/2+), `_components/onboarding-empty-state.tsx` (yeni), `_components/create-workspace-dialog.tsx` opsiyonel `triggerLabel` prop, `lib/strings.ts` (`workspace.redirecting` + `onboarding` grubu; orphan `workspace.empty` silindi); RTL `onboarding-empty-state.test.tsx` (1) + `(app)/page.test.tsx` (5). QA: `code-reviewer` + `security-reviewer` = PASS (APPROVE) — M1/L1 slug çakışması → `randomBytes(4)`→`(8)` (64-bit); L1 orphan `workspace.empty` silindi; M2 (better-auth `after` hook post-commit varsayımı — comment'te belirtildi, ^1.6.10 ile doğru); kalan low'lar (best-effort hook'un transient hatayı yutması → ileride worker'a taşınabilir; `page.test.tsx` mock'u biraz geniş; backend bootstrap için direkt integration testi yok — best-effort yol, opsiyonel) ertelendi. `verifier` = PASS: `pnpm -w typecheck`/`lint` 7/7, `pnpm -w test` 121 (15 domain + 51 api [DB canlı] + 55 web — yeni: onboarding-empty-state 1 + page 5; geri kalan web testlerinin artışı Faz 2D paralel işinden), `@pusula/api-server` + `@pusula/web` build (route'lar `/`, `/sign-in`, `/sign-up`, `/workspaces/[id]`, `/workspaces/[id]/boards/[boardId]`) yeşil; secret temiz. Docs ile çelişki yok. Kullanıcı onayladı → durum `Done` (2026-05-12); Linear DEM-46 → `Done` + kapanış yorumu. Şablon listeler/kartlar **bu işte değil** → DEM-47 (`blockedBy` DEM-46 + DEM-35 + DEM-36). Linear issue [DEM-46](https://linear.app/demirkol/issue/DEM-46) (Pusula, milestone: Faz 1; `blockedBy` DEM-20 + DEM-34; `blocks` DEM-47; `relatedTo` DEM-35). Faz 2 paralelinde yapıldı. |
| FE-2026-05-12-006 | DEM-47 | Pano şablonu: onboarding "İlk Pano"sına varsayılan listeler (Yapılacak/Devam Eden/Bitti) + welcome kartlar | 1 — Auth & Workspace | Done | Claude Code | `docs/domain/01-urun-modeli.md`, `docs/architecture/08-web-ve-mobil.md`, `docs/process/02-mvp-faz-plani.md` | `apps/api`, `packages/domain` | 2026-05-12 | Faz 1 ek işi (DEM-46 onboarding'inin tamamlayıcısı — "İlk Pano" boş gelmesin). Bağımlılık: DEM-46 (onboarding "İlk Pano", Done) + DEM-35 (list CRUD, Done) + DEM-36 (card CRUD, Done) — hepsi karşılandı. **Pre-dev (tamam):** `01-urun-modeli.md` invariant 11 güncellendi (default workspace artık varsayılan listeler `Yapılacak`/`Devam Eden`/`Bitti` + `Yapılacak`'ta welcome kartlarla seed edilir; `list.created`/`card.created` aynı tx; şablon içeriği `@pusula/domain` sabitleri; "şablon kapsam dışı" notu kaldırıldı), `08-web-ve-mobil.md` §8.1.3 güncellendi (signup bootstrap → pano şablonu: varsayılan listeler + welcome kartlar + activity'ler; "pano boş gelir" notu kaldırıldı); `02-mvp-faz-plani.md` madde 8 → `In Progress` + Faz 1 header notu; Linear DEM-47 `Todo` → `In Progress`. **Plan:** `@pusula/domain/constants.ts` → `ONBOARDING_LIST_TITLES` (`['Yapılacak','Devam Eden','Bitti']`) + `ONBOARDING_WELCOME_CARDS` (`Yapılacak`'ta ~3-4 welcome kart başlığı — mevcut özelliklere göre Türkçe; diğer iki liste boş); `apps/api/src/bootstrap.ts` → `bootstrapNewUser` transaction'ı genişletilir: board oluşturulduktan sonra `seedOnboardingBoard(tx, …)` — 3 liste (`positionsBetween(null,null,3)` ile sıralı `position`) + her birine `list.created` activity + `Yapılacak` listesine welcome kartlar (`positionsBetween` ile `position`) + her birine `card.created` activity (actor = yeni kullanıcı; `cards.boardId` = "İlk Pano"); `boards.version` seed sırasında bump edilmez (panonun ilk hâli). Test: `apps/api`'de vitest yok → bootstrap içerik testi ertelendi (best-effort yol; DEM-46 ile aynı gerekçe — `positionBetween` `@pusula/domain`'de test edilmiş, insert pattern'leri `list.create`/`card.create` integration testleriyle aynı). **İmplement edildi** (plana göre, küçük sapma: `positionsBetween` yerine `positionBetween` cursor'u): `@pusula/domain/constants.ts` → `ONBOARDING_LIST_TITLES` + `ONBOARDING_WELCOME_CARDS` (4 welcome kart `Yapılacak`'ta; diğer iki liste boş); `apps/api/src/bootstrap.ts` → `bootstrapNewUser` tx'i genişletildi (board sonrası 3 liste + `list.created` + `Yapılacak`'a welcome kartlar + `card.created`; `cards.boardId`="İlk Pano"; `boards.version` bump yok); `apps/api/src/auth.ts` yorumu güncellendi. QA: `code-reviewer` = PASS (APPROVE) — L1 (`list.created`/`card.created` payload'ları `list.create`/`card.create` ile aynı şekle `{listId,title,position,...}` / `{cardId,listId,title,position,...}`), L2 (`auth.ts` "boş İlk Pano" stale comment) düzeltildi; info'lar (cursor vs batch pos, emoji bir kart başlığında — data, OK; backend bootstrap testi yok) ertelendi. `verifier` = PASS: `pnpm -w typecheck`/`lint` 7/7, `pnpm -w test` 121 (15 domain + 51 api + 55 web — sayı değişmedi, seed doğrudan test edilmedi), `@pusula/api-server` + `@pusula/web` build yeşil; secret temiz. Docs ile çelişki yok. Kullanıcı onayladı → durum `Done` (2026-05-12); Linear DEM-47 → `Done` + kapanış yorumu. Faz 1'in son ek işi de kapandı. Not: bu turda `docs/process/*` + `packages/domain/constants.ts`'in bir kısmı paralel oturumun `bosluk-tara`/Faz 2.5 commit'lerine (`b3fc9a4` vb.) dahil oldu (içerik tam). Linear issue [DEM-47](https://linear.app/demirkol/issue/DEM-47) (Pusula, milestone: Faz 1; `blockedBy` DEM-46 + DEM-35 + DEM-36 — hepsi `Done`). |
| FE-2026-05-12-007 | DEM-55 | Kullanıcı profili / hesap ayarları ekranı (web): ad/avatar (URL), parola değiştir, hesabı sil | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/07-auth.md`, `docs/architecture/08-web-ve-mobil.md`, `docs/architecture/03-backend.md`, `docs/architecture/02-teknoloji-kararlari.md`, `docs/domain/01-urun-modeli.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/process/02-mvp-faz-plani.md` | `apps/web`, `packages/domain`, `apps/api` | 2026-05-12 | Faz 1 ek işi (`bosluk-tara` G5). Bağımlılık: DEM-22 + DEM-39 (ikisi de Done). **Kararlar (kullanıcı, 2026-05-12):** (a) avatar = **basit URL** (yükleme yok; ileride Faz 8 MinIO attachment'a bağlanabilir); (b) hesap silmede kullanıcı bir workspace `owner`'ıysa → **engelle** (`@pusula/domain` `canDeleteOwnAccount`); (c) **yeni tRPC `user.*` router yok** — doğrudan Better Auth client (`updateUser`/`changePassword`/`deleteUser`); sunucu tarafı `apps/api/src/auth.ts`'e `user.deleteUser.enabled` + `beforeDelete` hook + `databaseHooks.user.update.before` (name/image server-side re-validation). **Pre-dev (tamam):** `07-auth.md` (Profil & hesap yönetimi), `08-web-ve-mobil.md` §8.1.7, `03-backend.md` (tRPC bölümü notu), `02-teknoloji-kararlari.md` (karar kaydı 2026-05-12), `01-urun-modeli.md` (invariant 14), `02-yetkilendirme-kurallari.md` (Hesap (User) — öz-yönetim), `02-mvp-faz-plani.md` (madde 9 + tablo). **İmplement edildi:** `@pusula/domain` → `schemas/user.ts` (`updateProfileInput`/`changePasswordInput`/`deleteAccountInput`, `userNameSchema`, `userImageUrlSchema` — `http(s)`-only şema kısıtı) + `schemas/index.ts` export + `permissions.ts` `canDeleteOwnAccount(n)=>n===0` + `permissions.test.ts` ek + `schemas/user.test.ts` (yeni); `apps/api/src/auth.ts` → `user.deleteUser.enabled` + `beforeDelete` (`assertCanDeleteAccount`: `count(workspaces where owner_id=userId)`, >0 ise `APIError BAD_REQUEST`; `workspaces.ownerId` zaten `restrict`) + `databaseHooks.user.update.before` (name/image server-side parse, hatalıysa `APIError`); `apps/web` → `(app)/account/page.tsx` + `_components/{profile-form,change-password-form,delete-account-section}.tsx` (presentational, auth-client `page.tsx`'te) + `(app)/_components/app-shell.tsx`'e "Hesap" linki (kullanıcı adı → `/account`) + `lib/strings.ts` `strings.account` + `strings.shell.accountSettings`. Testler: `@pusula/domain` 24 (yeni 9 — `canDeleteOwnAccount` + user şemaları, `javascript:`/`data:`/`file:` URL reddi dahil) + `apps/web` 156 (yeni ~22 RTL — 3 form bileşeni: prefill/validasyon/no-change/dialog/server error). **QA:** `code-reviewer` = APPROVE (0 BLOCKER/HIGH; M-3 `AlertDescription` markup düzeltildi; M-1/M-2 arşivli-workspace sayım edge dokümante edildi — kod yorumu + `assertCanDeleteAccount` JSDoc; LOW'lar kabul). `security-reviewer` = LOW risk (0 CRITICAL/HIGH; M-1 avatar URL şema kısıtı + M-2 server-side `name`/`image` re-validation eklendi; M-3 sayım tutarsızlığı dokümante; L-1 `session.freshAge` takip işi notu — client her zaman `password` gönderdiği için akut değil; L-3 ham `error.message` → MVP kabul). `verifier`: `pnpm -w typecheck` 7/7, `pnpm -w lint` 7/7, `pnpm -w build` 3/3 (`/account` route üretildi), `pnpm -w test` (domain 24 + api 51 + web 156) yeşil; secret temiz; docs ile çelişki yok. **Kullanıcı onayladı → durum `Done`** (2026-05-12); commit `3d8c7c3`; Linear DEM-55 → `Done` + kapanış yorumu. Faz 1'in son ek işi de kapandı — Faz 1 tamam. Linear issue [DEM-55](https://linear.app/demirkol/issue/DEM-55) (Pusula, milestone: Faz 1; `relatedTo` DEM-22 + DEM-39 — ikisi de `Done`). |
| DOC-2026-05-12-005 | DEM-33 | Faz 2.0 — Önce belge: Faz 2 (Board/List/Card) docs güncellemesi | 2 — Board/List/Card CRUD | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/architecture/04-veri-katmani.md`, `docs/architecture/05-board-mekanigi.md`, `docs/domain/01-urun-modeli.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/process/02-mvp-faz-plani.md`, `docs/process/05-is-kayit-defteri.md` | `docs/architecture`, `docs/domain`, `docs/process` | 2026-05-12 | Faz 2'nin "önce belge" adımı; **kontrol odası tab'ında yapıldı**: `03-backend.md`'ye "Faz 2 — board / list / card procedure'leri" tablosu (router/procedure/middleware/rol + Faz-2-dışı liste + transaction notu; activity tipleri `board.created/renamed/archived` vb. — taksonomi adlarıyla); `02-yetkilendirme-kurallari.md`'ye "Board / List / Card procedure haritası (Faz 2)" (procedure → board rolü); `04-veri-katmani.md`'ye "Faz 2 kapsamı" notu (hangi tablolar/kolonlar Faz 2, hangileri ileri faz; `position` sona ekleme); `05-board-mekanigi.md`'ye §5.0 "Board ekranı — CRUD veri akışı (Faz 2)"; `01-urun-modeli.md`'ye arşivli board salt-okunur notu. `05-aktivite-kurallari.md` taksonomisi board/list/card tiplerini zaten kapsıyor — değişiklik gerekmedi. Kapanış (2026-05-12): docs senkron tamam → **Done**; Linear [DEM-33](https://linear.app/demirkol/issue/DEM-33) → `Done` + kapanış yorumu; DEM-34 (Faz 2A) artık serbest (`blockedBy` çözüldü). Kod tarafı notu (Faz 2A/2B/2C'ye taşındı): `@pusula/domain` `ACTIVITY_EVENT_TYPES` şu an `*.updated` taşıyor; taksonomideki `board.renamed`/`list.renamed`/`card.renamed`/`card.description_changed` tipleri ilgili implementasyonda enum'a append + migration ile eklenecek (Postgres enum append-only). Linear issue [DEM-33](https://linear.app/demirkol/issue/DEM-33) (Pusula, milestone: Faz 2; durum `Done`; DEM-25 epic'i altında). |
| API-2026-05-12-004 | DEM-34 | Faz 2A — Board CRUD (backend): `boardProcedure` + `board.*` + permission enforcement | 2 — Board/List/Card CRUD | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/01-urun-modeli.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/process/02-mvp-faz-plani.md`, `docs/process/05-is-kayit-defteri.md` | `packages/api`, `packages/domain`, `packages/db` | 2026-05-12 | Başlatıldı 2026-05-12 (DEM-33 `Done` → `blockedBy` çözüldü; Linear DEM-34 `Sonraki Faz` → `In Progress`). Önce-belge DEM-33'te tamam — bu iş yalnızca kodu yazdı. **İmplement edildi** (backend-dev): `@pusula/domain` `ACTIVITY_EVENT_TYPES`'a append `board.renamed`/`list.renamed`/`card.renamed`/`card.description_changed` (Faz 2 alt kümesi; `*.updated` cruft dizide kaldı); `@pusula/db` migration `drizzle/0004_graceful_archangel.sql` (`activity_event_type` enum `ADD VALUE` ×4, append-only; DB ayaktaydı → `pnpm db:migrate` uygulandı); `@pusula/api` `middleware/board.ts` `boardProcedure` (`protectedProcedure` + boardId resolve → board (yoksa `NOT_FOUND`) → workspace üyeliği (yoksa `FORBIDDEN`) → opsiyonel `board_members` → `effectiveBoardRole` (null → `FORBIDDEN`) → `ctx.board = { id, workspaceId, role }`; `BoardContext` export); `routers/board.ts` `boardRouter` — `list` (`workspaceProcedure`; guest yalnızca explicit `board_members`, member+ workspace'teki tüm board'lar + arşivli, dön `{id,title,version,archivedAt,createdAt,role}[]` effective rol), `create` (`workspaceProcedure`; guest `FORBIDDEN`; tx `boards`+`board_members`(creator `admin`)+`activity_events`(`board.created`)), `get` (`boardProcedure` `viewer+`; `{board:{…,role},lists[](arşivli dahil,position sıralı),cards[](yalnızca aktif,tek sorgu `boardId`,position sıralı)}`), `update` (`boardProcedure` board `admin`=`canManageBoard`; `title` zorunlu→`BAD_REQUEST`; arşivli board salt-okunur `BAD_REQUEST` (tx içinde tekrar okunur, race-safe); aynı başlık idempotent `changed:false`; tx `boards` update(`title`,`version+1`)+`activity_events`(`board.renamed`,`{fromTitle,toTitle}`)), `archive` (`boardProcedure` board `admin`; `archived` bool default true; idempotent no-op `changed:false`; tx update(`archivedAt`,`version+1`)+`activity_events`(`board.archived`,`{archived}`); dön `{id,archivedAt,version,changed}`); `root.ts`'e `board: boardRouter` mount. Test: yeni `routers/board.test.ts` 9 tRPC integration testi (create member ok/creator board admin/`board.created`; create guest+outsider `FORBIDDEN`; list member tümü+inherited rol/guest yalnızca explicit; get unknown `NOT_FOUND`/non-board-member `FORBIDDEN`/`{board,lists,cards}` position sıralı; update admin+`board.renamed`+`version` artar/viewer `FORBIDDEN`/boş input `BAD_REQUEST`/aynı başlık idempotent/arşivli board `BAD_REQUEST`; archive admin set/restore/idempotent/non-admin `FORBIDDEN`). QA: `code-reviewer` = PASS (0 critical, 1 warning W1 `version` return-shape → bu turda düzeltildi: `board.list`/`board.archive` dönüşlerine `version` eklendi; 4 low öneri ertelendi — `board.get`'teki erişilemez `canViewBoard`, update unchanged-title erken dönüş, archive/renamed payload şekil farkı); `verifier` = PASS (`pnpm -w typecheck` 7/7, `lint` 7/7, `test` 83 — 15 domain + 38 api [board.test.ts 9 dahil, DB canlı] + 30 web, `@pusula/api-server build` — yeşil, secret temiz); W1 sonrası tekrar typecheck/lint/test yeşil. Docs ile çelişki yok. **Kullanıcı onayladı → durum `Done` (2026-05-12)**; commit `feat: DEM-34 — Faz 2A board CRUD backend` (main); Linear [DEM-34](https://linear.app/demirkol/issue/DEM-34) → `Done` + kapanış yorumu. Linear issue [DEM-34](https://linear.app/demirkol/issue/DEM-34) (Pusula, milestone: Faz 2; durum `Done`; DEM-25 altında; `blockedBy` DEM-33 (Done)). DEM-35 (Faz 2B) ve DEM-36 (Faz 2C) artık serbest (`blockedBy` çözüldü). |
| API-2026-05-12-005 | DEM-35 | Faz 2B — List CRUD (backend): `list.*` + board-edit permission | 2 — Board/List/Card CRUD | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/architecture/04-veri-katmani.md`, `docs/domain/02-yetkilendirme-kurallari.md` | `packages/api`, `packages/domain` | 2026-05-12 | Başlatıldı 2026-05-12 (DEM-34 `Done` → `blockedBy` çözüldü; DEM-36 ile aynı turda). Önce-belge DEM-33'te tamam. **İmplement edildi** (backend-dev): `@pusula/api` `middleware/board-access.ts` (`resolveBoardAccess(db|tx, boardId, userId)` paylaşılan helper — board → workspace üyeliği → board üyeliği → `effectiveBoardRole`; `BoardAccess { id, workspaceId, role, archivedAt }`; `boardProcedure` bunu kullanacak şekilde refactor edildi — davranış aynı, `board.test.ts` 9/9); `routers/list.ts` `listRouter` — `list.create` (`boardProcedure`, board `member+` = `canEditBoardContent`; board sonuna `position` (boş board `firstPosition()`, aksi `positionBetween(lastPos,null)` — `lastPos` aktif+arşivli tüm listelerden `position desc limit 1`); arşivli board → `BAD_REQUEST`; tx `lists` insert + `activity_events`(`list.created`) + `boards.version+1`), `list.update` (board `member+`; rename — `renameListInput`; arşivli board salt-okunur; cross-board liste → `BAD_REQUEST`; aynı başlık idempotent `changed:false`; tx update + `activity_events`(`list.renamed`) + `boards.version+1`), `list.archive` (board `member+`; `archived` bool; idempotent; tx update(`archivedAt`) + `activity_events`(`list.archived`) + `boards.version+1`); arşivli liste içi başlık düzenleme serbest (domain yalnızca arşivli listeye yeni kart eklemeyi yasaklar). `root.ts`'e `list: listRouter` mount. Test: `routers/list.test.ts` 6 entegrasyon testi. QA: code-reviewer + verifier = PASS (HIGH/MEDIUM yok; LOW#1 docs drift [`card.create` transport] → `03-backend.md` + `02-yetkilendirme-kurallari.md` güncellendi; LOW#2/#3 ek test [arşivli board update/archive reddi, arşivli liste içi kart edit] ertelendi; LOW#4/#5 kozmetik). `pnpm -w typecheck`/`lint` 7/7; `test` 96 (15 domain + 51 api [board 9 + list 6 + card 7 + workspace 29] + 30 web, DB canlı); `@pusula/api-server build` yeşil; secret temiz. Docs ile çelişki yok. Commit `feat: DEM-35 + DEM-36 — Faz 2B/2C list+card CRUD backend` (main). Kullanıcı onayladı → `Done` (2026-05-12); Linear DEM-35 → `Done` + kapanış yorumu. DEM-37 (Faz 2D — web board ekranı) artık serbest (`blockedBy` DEM-35+DEM-36 çözüldü). `list.move` (reorder) kapsam dışı → Faz 3 (DEM-26). Linear issue [DEM-35](https://linear.app/demirkol/issue/DEM-35) (Pusula, milestone: Faz 2; durum `Done`; DEM-25 altında). |
| API-2026-05-12-006 | DEM-36 | Faz 2C — Card CRUD (backend): `cardProcedure` + `card.*` + invariant enforcement | 2 — Board/List/Card CRUD | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/01-urun-modeli.md`, `docs/domain/02-yetkilendirme-kurallari.md` | `packages/api`, `packages/domain` | 2026-05-12 | Başlatıldı 2026-05-12 (DEM-34 `Done` → `blockedBy` çözüldü; DEM-35 ile aynı turda). **İmplement edildi** (backend-dev): `@pusula/api` `middleware/card.ts` `cardProcedure` (`protectedProcedure` + cardId resolve → kart (yoksa `NOT_FOUND`) → `resolveBoardAccess(card.boardId, userId)` → kullanıcının `card_members` ilişkileri → `ctx.card = { id, listId, boardId, workspaceId, archivedAt, boardRole, boardArchivedAt, relations }`); `routers/card.ts` `cardRouter` — `card.create` (`protectedProcedure` — `createCardInput` yalnızca `listId` taşıdığından: tx içinde liste okunur (yoksa `NOT_FOUND`) → `resolveBoardAccess(tx, list.boardId, userId)` → `canEditBoardContent` değilse `FORBIDDEN` → arşivli board/liste → `BAD_REQUEST`; listenin sonuna `position`; `cards` insert (**`boardId` = listenin board'u — kart ⊆ liste.board invariant'ı**) + `activity_events`(`card.created`) + `boards.version+1`), `card.get` (`cardProcedure` board `viewer+`; `{card, relations}`), `card.update` (`cardProcedure` board `member+`; ≥1 alan (`title`/`description`/`dueAt`) zorunlu (`'dueAt' in input` ile `null`'ı yokluktan ayırır); arşivli board salt-okunur; her değişen alan ayrı activity — `title`→`card.renamed`, `description`→`card.description_changed` (`""`↔`null` normalize), `dueAt` set→`card.due_set` / null→`card.due_cleared`; `boards.version+1` tek sefer), `card.archive` (`cardProcedure` board `member+`; `archived` bool; idempotent; `activity_events`(`card.archived`) + `boards.version+1`)); `root.ts`'e `card: cardRouter` mount (`appRouter` = health/auth/workspace/board/list/card). Test: `routers/card.test.ts` 7 entegrasyon testi (create listenin sonuna + invariant `card.boardId===list.boardId` + arşivli liste/board reddi + viewer `FORBIDDEN` + bilinmeyen `listId` `NOT_FOUND`; get viewer+ + non-member `FORBIDDEN` + bilinmeyen `cardId` `NOT_FOUND`; update title/desc/due → ilgili activity / boş input `BAD_REQUEST` / viewer `FORBIDDEN`; archive set/restore/idempotent). QA: code-reviewer + verifier = PASS (DEM-35 ile aynı tur; LOW#1 docs drift `card.create` transport düzeltildi; LOW#2/#3 ertelendi; kozmetikler skip). Doğrulama: yukarıdaki gibi (96 test, DB canlı). Commit `feat: DEM-35 + DEM-36 — Faz 2B/2C list+card CRUD backend` (main). Kullanıcı onayladı → `Done` (2026-05-12); Linear DEM-36 → `Done` + kapanış yorumu. `card.move`/`moveCard` kapsam dışı → Faz 3 (DEM-26). Linear issue [DEM-36](https://linear.app/demirkol/issue/DEM-36) (Pusula, milestone: Faz 2; durum `Done`; DEM-25 altında). Kart ⊆ liste.board invariant'ı + arşivli liste kuralları enforce edildi. |
| FE-2026-05-12-002 | DEM-37 | Faz 2D — Temel board ekranı (web): board listesi + board detay (CRUD, drag-drop'suz) | 2 — Board/List/Card CRUD | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md`, `docs/architecture/05-board-mekanigi.md` | `apps/web`, `packages/ui` | 2026-05-12 | Başlatıldı 2026-05-12 (DEM-35+DEM-36 `Done` → `blockedBy` çözüldü; backend Faz 2A/2B/2C tamam). Pre-dev: `08-web-ve-mobil.md`'ye §8.1.4 "Board ekranı (Faz 2D)" eklendi. **İmplement edildi** (frontend-dev): `apps/web` `(app)/workspaces/[id]/page.tsx` üstüne `<BoardListSection>` kartı (board listesi — `trpc.board.list`; "Pano oluştur" `CreateBoardDialog` → `trpc.board.create` (`boardTitleSchema` validasyon, `clientMutationId`) → `board.list` invalidate; "Pano oluştur" `workspaceRoleAtLeast(role,'member')` ise); yeni route `(app)/workspaces/[id]/boards/[boardId]/page.tsx` (`'use client'`, `use(params)`, `trpc.board.get` → `{board,lists,cards}`; pending/error [`NOT_FOUND`/`FORBIDDEN` → `Alert` + geri linki]/success; board başlığı + `RenameBoardForm` (board `admin`) + `ArchiveBoardDialog` (board `admin`; arşivle onaylı, geri yükle tek tık)) + `_components/*`: `BoardColumns` (yatay `flex overflow-x-auto`, sabit `w-72` kolonlar, `cards` → `Map<listId,cards[]>` `position` sıralı, `canEdit = boardRoleAtLeast(role,'member') && !archivedAt`), `ListColumn` (inline rename, arşivle/geri-yükle onaylı dialog, kartlar, `AddCardForm`), `AddListColumn`/`AddListForm` (`trpc.list.create` → `board.get` invalidate), `CardItem` (`dueAt` rozeti, tıkla → `EditCardDialog`, arşivle onaylı dialog), `EditCardForm`/`EditCardDialog` (başlık `Input`/`cardTitleSchema` + açıklama `Textarea`/`cardDescriptionSchema` [boş→`''`] + `dueAt` `<input type=date>` [boş→`null`]; yalnız değişen alanları gönderir, değişiklik yoksa kapatır; `viewer` salt-okunur), `format.ts` (`formatDate` tr-TR + `toDateInputValue`/`parseDateInputValue` — TZ-güvenli local Date). `strings.board.*` + `boardRoleLabels`. `@pusula/ui`'ye shadcn `Textarea` (CSS-only, yeni bağımlılık yok) → `src/index.ts` + `package.json` export. `DropdownMenu` eklenmedi — kolon/kart aksiyonları ghost `Button` + onaylı `Dialog`. Test: `apps/web`'e 19 yeni RTL testi (`create-board-dialog` 3, `add-list-form` 5, `add-card-form` 4, `edit-card-dialog` 7); web suite 55, toplam 121 (15 domain + 51 api [DB canlı] + 55 web). QA: code-reviewer = PASS (HIGH/MEDIUM yok; 5 LOW ertelendi — kullanılmayan `strings.board.card` anahtarları, restore-dialog açıklaması, mid-edit prop re-sync guard'ı [Faz 3+ realtime için], edit-card-dialog'a 2 date test'i, boş flex wrapper); verifier = PASS (`pnpm -w typecheck`/`lint` 7/7, `test` 121, `@pusula/web build` — route'lar `/`, `/sign-in`, `/sign-up`, `/workspaces/[id]`, `/workspaces/[id]/boards/[boardId]` — yeşil, secret temiz). Docs (§8.1.4) ile çelişki yok. Commit `feat: DEM-37 — Faz 2D web board ekranı` (main; benim olmayan paralel onboarding WIP'i [DEM-46/47] commit'e dahil edilmedi). Kullanıcı onayladı → `Done` (2026-05-12); Linear DEM-37 → `Done` + kapanış yorumu. **Bununla Faz 2 (DEM-25 epic) tamamen tamam** — sıradaki Faz 3 (DEM-26, `Sonraki Faz` — DEM-41…45 alt işleri). Linear issue [DEM-37](https://linear.app/demirkol/issue/DEM-37) (Pusula, milestone: Faz 2; durum `Done`; DEM-25 altında). Drag-drop yok (Faz 3 — DEM-26); optimistic UI Faz 4 (DEM-27); Playwright e2e Faz 8 (DEM-31). |

| DOC-2026-05-12-006 | DEM-49 | Faz 2.5.0 — Önce belge: kart detayı + board işbirliği docs | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/domain/05-aktivite-kurallari.md`, `docs/domain/01-urun-modeli.md`, `docs/architecture/08-web-ve-mobil.md`, `docs/architecture/04-veri-katmani.md`, `docs/architecture/02-teknoloji-kararlari.md`, `docs/process/02-mvp-faz-plani.md`, `docs/process/05-is-kayit-defteri.md` | `docs/architecture`, `docs/domain`, `docs/process` | 2026-05-12 | Faz 2.5'in "önce belge" adımı; bu turda **kod-yazma tab'ında pre-dev olarak** yapıldı (kontrol odası tab'ında değil). Kararlar kullanıcıya soruldu → (a) kart detay görünümü = **modal + `?card=<id>` query param** (intercepting route yok); (b) karta üye adayı `effectiveBoardRole !== null` olmalı; (c) `board.members.add` workspace-dışı kişiyi de davet edebilir → yeni `board_invitations` tablosu (e-posta + outbox; kabulde workspace `guest` + board üyesi). Güncellenenler: `03-backend.md` → "Faz 2.5 — comment / checklist / card.members / card.labels / label / board.members procedure'leri" tablosu (router/middleware/rol + activity + `boards.version` notu + board davet akışı); `02-yetkilendirme-kurallari.md` → effective board rolü + board rolleri (admin/member/viewer) açıklaması, board matrisine yeni satırlar (board daveti, board'dan ayrıl, yorum düzenle/sil) + "Board / Card içerik procedure haritası (Faz 2.5)" tablosu; `05-aktivite-kurallari.md` → taksonomide `board.member_invited`/`board.invitation_revoked`, comment adlandırması (`comment.created/updated/deleted`), `checklist.item_completed` cruft notu, etiket/checklist CRUD activity üretmez notu; `01-urun-modeli.md` → Board invitation entity + Board member "explicit" notu + invariant 12 (kart üyesi adayı) + invariant 13 (etiket scope + explicit board üyeliği + version); `08-web-ve-mobil.md` → §8.1.5 "Kart detay görünümü (Faz 2.5D)" (modal + `?card` param, içerik, veri akışı) + §8.1.6 "Board üye + etiket yönetimi UI (Faz 2.5E)" (üye/rol + board davetleri + etiket CRUD + etiket filtresi + arşiv toggle G7); `04-veri-katmani.md` → Faz 2.5 kapsamı notu (aktive edilen tablolar + `board_invitations` yeni tablo şeması + enum append listesi); `02-teknoloji-kararlari.md` → tarihli Karar kaydı satırı (3 karar); `02-mvp-faz-plani.md` → Faz 2.5 satırı `🚧 Devam ediyor`, "Faz 2.5 alt işleri" intro + alt madde detayları + kararlar; bu defter satırı. Obsidian frontmatter/MOC bağları korundu (yeni dosya yok). Kapanış: docs senkron tamam → **Done**; Linear [DEM-49](https://linear.app/demirkol/issue/DEM-49) → `Done` + kapanış yorumu; DEM-50/51/52 (`blockedBy` DEM-49) artık serbest. Not: paralel tab DEM-47 (pano şablonu) WIP'i (`apps/api/*`, `packages/domain/constants.ts` onboarding sabitleri, `01-urun-modeli.md` invariant 11, `08-web-ve-mobil.md` §8.1.3) commit'e dahil edilmedi — yalnızca DEM-49'a ait docs commit edildi. Linear issue [DEM-49](https://linear.app/demirkol/issue/DEM-49) (Pusula, milestone: Faz 2.5; DEM-48 altında; bağımlılık yok). |
| API-2026-05-12-007 | DEM-50 | Faz 2.5A — Comment + Checklist CRUD (backend): comment.* + checklist.* + checklist.item.* | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/02-yetkilendirme-kurallari.md` | `packages/api`, `packages/domain`, `packages/db` | 2026-05-12 | Faz 2.5'in ilk kod işi (kod-yazma tab'ı; önce-belge DEM-49'da tamam). İmplement edildi: `cardProcedure` üzerinde `comment.{list,create,update,delete}` (board `member+`; `update`/`delete` yazan veya board `admin`; soft-delete `deletedAt` + `body=''`, `list` silinmiş yorumları da döner — UI placeholder) + `checklist.{create,update,delete}` + `checklist.item.{create,toggle,update,delete,reorder}` (board `member+`; item `position` fractional `@pusula/domain`; `reorder` self/ters-komşu → `BAD_REQUEST` + `positionBetween` try/catch); transaction = domain mutasyonu + `activity_events` + `boards.version+1`; activity tipleri `comment.created/updated/deleted`, `checklist.created`, `checklist.item_added/checked/unchecked/removed` (checklist/item rename/edit/reorder activity üretmez ama version artar). `@pusula/domain` `schemas/comment.ts` + `schemas/checklist.ts`; `ACTIVITY_EVENT_TYPES`'a 4 yeni değer append + migration `drizzle/0005_typical_captain_america.sql` (enum `ADD VALUE`); `root.ts`'e `comment`+`checklist` mount. Test: tRPC integration — `comment.test.ts` 8 + `checklist.test.ts` 13. QA: code-reviewer + security-reviewer = PASS (round-2: cross-card/cross-checklist `NOT_FOUND`, outsider `FORBIDDEN`, reorder self-komşu `BAD_REQUEST` testleri + reorder try/catch). verifier = PASS (`pnpm -w typecheck`/`lint` 7/7, `test` 142 [15 domain + 72 api (DB canlı) + 55 web], `@pusula/api-server build` yeşil). Docs: `03-backend.md` + `02-yetkilendirme-kurallari.md` `comment.list` satırı (silinmiş yorumlar dahil) düzeltildi. Commit: `caa880d` (main, push edildi). Linear [DEM-50](https://linear.app/demirkol/issue/DEM-50) → `Done` + kapanış yorumu; `blockedBy` DEM-49 + DEM-36 (ikisi de Done); DEM-48 altında. Not: paralel DEM-47 (pano şablonu) tab'ının `packages/domain/constants.ts` onboarding sabitleri (paylaşılan dosya) bu commit'e karıştı — `apps/api/*` ve kontrol-odası README DEM-47 WIP'i dahil edilmedi. |
| API-2026-05-12-008 | DEM-51 | Faz 2.5B — Card members (assignee/watcher) + Label CRUD + karta etiket atama (backend) | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/02-yetkilendirme-kurallari.md` | `packages/api`, `packages/domain` | 2026-05-12 | Faz 2.5 kod işi (önce-belge DEM-49'da tamam); 2.5A ile paralel yapıldı. İmplement edildi: `cardProcedure` üzerinde `card.members.{list,add,remove}` (board `member+`; `viewer` yalnız kendini watcher yapar/çıkar; aday `effectiveBoardRole !== null` olmalı yoksa `BAD_REQUEST`; `onConflictDoNothing` idempotent; `list` yalnız `{userId,role,name}` döner — e-posta board `viewer`'lara açılmaz [privacy]) + `card.labels.{list,add,remove}` (board `member+`; etiket kartın board'una ait olmalı yoksa `BAD_REQUEST`); `boardProcedure` üzerinde `label.{list,create,update,delete}` (board `member+`; `(board,renk,ad)` benzersiz → `CONFLICT` [PG 23505]; cross-board `labelId` → `NOT_FOUND`; etiket CRUD **activity üretmez** — board metadata); transaction = domain mutasyonu + (varsa) `activity_events` + `boards.version+1`; activity tipleri `card.member_added/removed`, `card.label_added/removed` (zaten enum'da — migration yok). `@pusula/domain` `LABEL_COLORS` (10 renk palet) + `LabelColor` + `schemas/label.ts` + `schemas/card.ts`'e card-member/card-label input şemaları; `root.ts`'e `label` mount, `card.ts`'e members+labels nest. Test: tRPC integration — `card-members.test.ts` 10 + `card-labels.test.ts` 6 + `label.test.ts` 7. QA: code-reviewer + security-reviewer = PASS (round-2: N1 privacy [`card.members.list` e-posta dönmüyor] + aday-board-erişimi pozitif testi [workspace guest + explicit board viewer → eklenebilir] + arşivli board label update/delete testi). verifier = PASS (`pnpm -w typecheck`/`lint` 7/7, `test` 165 [15 domain + 95 api (DB canlı) + 55 web], `@pusula/api-server build` yeşil). Ertelenen (LOW): `isUniqueViolation` dedup (`workspace.ts` ↔ `label.ts`), `clientMutationId` dedup (Faz 4/5). Commit: `b3fc9a4` (main, push edildi). Linear [DEM-51](https://linear.app/demirkol/issue/DEM-51) → `Done` + kapanış yorumu; `blockedBy` DEM-49 + DEM-34 + DEM-36 (hepsi Done); DEM-48 altında. Not: paralel DEM-47 (pano şablonu) WIP'i — `constants.ts` onboarding hunk'ı paylaşılan dosya olduğu için karıştı; `apps/api/*` + kontrol-odası README DEM-47 WIP'i dahil edilmedi. |
| API-2026-05-12-009 | DEM-52 | Faz 2.5C — Board üye yönetimi + board davetleri (backend): board.members.* + board.invitations.* + board rolleri | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/02-yetkilendirme-kurallari.md` | `packages/api`, `packages/domain`, `packages/db` | 2026-05-12 | Faz 2.5 kod işi (önce-belge DEM-49'da tamam; karar: `board.members.add` workspace-dışı kişiyi de davet edebilir → yeni `board_invitations` tablosu, e-posta + outbox, kabulde workspace `guest` + board üyesi). İmplement edildi: `boardProcedure` üzerinde `board.members.{list,add,updateRole,remove}` (`list` board `viewer+` — explicit `board_members` `{userId,role,name}` [e-posta yok, gizlilik] + inherited workspace owner/admin `inherited:true`; `add` board `admin`, e-postayla → hesap+ws üyesi direkt / hesap var ws değil → workspace `guest` + board üye / hesap yok → `board_invitations` `pending` + `notification_outbox`; race-guard `CONFLICT`; `updateRole`/`remove` board `admin`, yalnız explicit satır, son explicit admin korunur `.for('update')`) + `board.invitations.{list,revoke}` (`boardProcedure`) + `{mine,accept,decline}` (`protectedProcedure`; `workspace_invitations` disiplini — gizli token, süreli, tek kullanımlık, `(board,lower(email)) WHERE pending` partial unique; `accept` e-posta eşleşmeli, expired→`BAD_REQUEST`, arşivli board/ws → `BAD_REQUEST`, idempotent `.onConflictDoNothing()`; kabulde davetli ws `guest` + board üye); transaction = domain mutasyonu + (varsa) `activity_events` + (davet) `notification_outbox` + `boards.version+1`. Yeni `board_invitations` tablosu + migration `drizzle/0006_dazzling_ma_gnuci.sql` (enum `ADD VALUE` ×3: `board.member_role_changed`/`member_invited`/`invitation_revoked` + `CREATE TABLE board_invitations` + indeksler + partial unique). `@pusula/domain` `schemas/board.ts`'e board-member/board-invitation input şemaları (`emailSchema`/`invitationTokenSchema`/`WORKSPACE_INVITATION_TTL_DAYS` yeniden kullanıldı); `board.ts`'e `members`/`invitations` nest. Test: `board-members.test.ts` 8 + `board-invitations.test.ts` 7. QA: code-reviewer = PASS (round-2; round-1 FAIL — H1 docs-drift düzeltildi [`board.members.list` e-posta yok + `inherited` flag], M1 accept arşiv kontrolü, M2 case-(a) board insert race-guard, M3 son-admin TOCTOU `.for('update')` eklendi; ertelenen LOW: `isUniqueViolation` dedup, payload key drift); security-reviewer = PASS (token/IDOR/privilege-escalation/TOCTOU temiz; INFO: `add` case-(b) auto-guest = Trello modeli, `01-urun-modeli.md` invariant 13'te belgeli). verifier = PASS (`pnpm -w typecheck`/`lint` 7/7, `test` suite yeşil [+15 api board-members/invitations], `@pusula/api-server build` yeşil, secret temiz). Docs: `03-backend.md` + `02-yetkilendirme-kurallari.md` `board.members.list` satırı düzeltildi. Commit: `c321154` (main, push edildi). Linear [DEM-52](https://linear.app/demirkol/issue/DEM-52) → `Done` + kapanış yorumu; `blockedBy` DEM-49 + DEM-34 (ikisi de Done); DEM-48 altında; 2.5A/2.5B ile paralel yapıldı. Not: paralel kontrol odası tab'ının `docs/process/*` + `docs/kontrol-odasi/README.md` catch-up WIP'i bu commit'e dahil edilmedi. DEM-54 (`blockedBy` DEM-51+52+37+49) artık serbest. |
| FE-2026-05-12-008 | DEM-53 | Faz 2.5D — Kart detay görünümü (web): modal — açıklama/due-date/üyeler/etiketler/checklist/yorum/activity | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md` (§8.1.5), `docs/architecture/03-backend.md` (`checklist.list` + `card.activity.list`) | `apps/web`, `packages/api` | 2026-05-12 | Faz 2.5 web işi (önce-belge DEM-49'da tamam — kart detayı = **modal + `?card=<id>` query param**, intercepting route yok). İmplement edildi: shadcn `Dialog` modal (board arkada, `?card=<cardId>` derin link; geçersiz id → "kart bulunamadı"); başlık/açıklama/due-date inline edit (3 ayrı `card.update` mutation) + etiketler (`card.labels.*`+`label.*`, `LABEL_COLORS`, yeni etiket mini-form, `CONFLICT` inline) + üyeler assignee/watcher (`card.members.*`+`board.members.list`; `viewer` yalnız self-watch) + checklist'ler (`checklist.*`+`checklist.item.*`, X/Y ilerleme, item toggle/edit/sil; reorder ertelendi) + yorumlar (`comment.*`, soft-delete "silindi" placeholder, "(düzenlendi)", yazan/admin edit-sil) + kart activity feed (`card.activity.list`, type→Türkçe özet). Yeni 2 read-only query: `checklist.list` + `card.activity.list` (`cardProcedure`). `EditCardDialog` kaldırıldı (modal kapsadı). Yeni shadcn paketi yok (native checkbox + mevcut `Select`). Optimistic UI yok; `clientMutationId` istemcide; metinler `strings.card.*`. Test: 11 RTL dosyası ~51 test; suite 223 (15 domain + 110 api + 98 web). QA: code-reviewer = PASS (round-2; round-1 M1 `card-detail-checklists.tsx` 518→5 dosya, M2 activity skeleton, M3 ortak mutation→3 instance; ertelenen LOW: `replace` vs `push`, nested Dialog onayı, `card.get`'e `effectiveBoardRole`); verifier = PASS (typecheck/lint 7/7, build yeşil, secret temiz). Docs: `03-backend.md`'ye `checklist.list`/`card.activity.list`/`card.members.list`/`card.labels.list`/`label.list` satırları. Commit `7881461` (main). Kullanıcı onayladı → `Done` (2026-05-12); Linear DEM-53 → `Done` + kapanış yorumu. `blockedBy` DEM-50 + DEM-51 + DEM-37 + DEM-49 (hepsi Done); DEM-48 altında; DEM-54 (kart-detay tarafı tamam) artık serbest. |
| FE-2026-05-12-009 | DEM-54 | Faz 2.5E — Board üye + etiket yönetimi UI (web): board ayarlarına üye/rol + board davetleri + etiket CRUD + etiket filtresi | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md` (§8.1.6), `docs/architecture/03-backend.md` (`board.get` `cards[].labels`), `docs/domain/02-yetkilendirme-kurallari.md` | `apps/web`, `packages/api` | 2026-05-12 | Faz 2.5'in son alt işi. İmplement edildi: board detay sayfasında "Board ayarları" girişi (board `admin`) → `Dialog`: **Üyeler** (`board.members.list` {userId,role,name,inherited}; inherited kilitli; `admin` rol değiştir/çıkar — `board.members.updateRole`/`remove`; son explicit admin UI'da kilitli; üye ekle e-posta+rol → `board.members.add`, sonuç `added`/`added_as_guest`/`invited` inline) + **gönderilmiş davetler** (`board.invitations.list` + `revoke`) + **etiketler** (`label.list`/`create`/`update`/`delete`, `LABEL_COLORS`, `CONFLICT` inline); **PendingInvitations** genişletildi → "Workspace davetleri" + "Board davetleri" (`board.invitations.{mine,accept,decline}`); **board ekranında etiket filtresi** (çoklu seçim, OR, client-side; palet `board.get` `cards[].labels`'tan) + "arşivli listeleri göster/gizle" toggle (`bosluk-tara` G7; arşivli **kartlar** kapsam dışı — ileri faz); `card-item`'a renk çipleri. **Backend (additive):** `board.get` `cards` projection'ına `labels:[{labelId,name,color}]` (tek sorgu `card_labels ⋈ labels`, server'da `Map`; mevcut testler kırılmadı, N+1 yok). Optimistic UI yok; `clientMutationId` istemcide; metinler `strings.board.settings.*`/`filter.*`/`invitations.*`. Yeni shadcn paketi yok. Test: ~35 yeni RTL/unit test + `board.test.ts` +1; suite 283 (16 domain + 111 api + 156 web — bir kısmı paralel DEM-55 testleri). QA: code-reviewer = PASS ("ship it"; ertelenen LOW: 4 dead string key, etiket filtresi raw state temizliği, confirm pending feedback); verifier = PASS (typecheck/lint 7/7, build yeşil, secret temiz). Docs: `08-web-ve-mobil.md` §8.1.6 + `03-backend.md` (`cards[].labels`) + `02-yetkilendirme-kurallari.md`. Commit `ca9fe45` (main). Kullanıcı onayladı → `Done` (2026-05-12); Linear DEM-54 → `Done` + kapanış yorumu. **Bununla Faz 2.5 (DEM-48 epic) tamam** — DEM-49–54 hepsi `Done`. `blockedBy` DEM-51 + DEM-52 + DEM-37 + DEM-49 (hepsi Done); DEM-48 altında. |
| DOC-2026-05-12-007 | DEM-58 | Faz 2.7 epic'i — UI tasarım dili & board/kart cilalama (oluştur) | 2.7 — UI tasarım dili & board/kart cilalama | Todo | Claude Code | `docs/process/02-mvp-faz-plani.md`, `docs/kontrol-odasi/README.md`, `docs/process/05-is-kayit-defteri.md` (2.7.0'da ayrıca: yeni `docs/architecture/13-ui-tasarim-dili.md`, `02-teknoloji-kararlari.md`, `08-web-ve-mobil.md`, `CLAUDE.md`) | `docs/process`, `docs/kontrol-odasi`, Linear | 2026-05-12 | Kontrol odası tab'ı: kullanıcı talebiyle Faz 2.5 ↔ Faz 3 arasına yeni bir **UI cilalama fazı** eklendi (mevcut web UI fonksiyonel ama görsel olarak ham — "HTML+JS var, CSS yok"). İki ajan analizi (eski Pusula `D:\projects\pusula` UI'ı + mevcut `pusula_v2` web UI envanteri) + 3 ekran görüntüsü + Trello karşılaştırması → 4 karar kullanıcıdan: referans = karma (eski proje layout/anatomi/token baz + Trello iyileştirmeleri); tema = yeni palet (2.7.0'da seçenekler → kullanıcı seçer); rich text = Tiptap (mimari karar — headless editör); sıra = 2.7.0 önce-belge önce, uygulama Faz 2.5 web bittiğinden serbest. Linear: "Faz 2.7 — UI tasarım dili & board/kart cilalama" milestone + epic [DEM-58](https://linear.app/demirkol/issue/DEM-58) (`Todo` — 2026-05-12 `Sonraki Faz`'tan alındı, geliştirmesi başlıyor; priority High); alt işler `faz-bol 2.7`'de. Docs: `02-mvp-faz-plani.md` Faz 2.7 satırı + "Faz 2.7 alt işleri" iskeleti + footer; `kontrol-odasi/README.md` §5; `02-teknoloji-kararlari.md` Karar kaydı (Faz 2.7 + Tiptap + palet) + `CLAUDE.md` §2 #8 Tiptap notu eklendi (2026-05-12). **Faz 2.7.0 önce-belge tamam (2026-05-12):** yeni `docs/architecture/13-ui-tasarim-dili.md` yazıldı (09 numarası `09-depolama-ve-arama.md`'de olduğu için 13'e alındı) — design token sistemi + board/kolon/kart anatomisi + kart detay modalı yapısı + ortak desenler + `packages/ui` bileşen spec'leri (`Avatar`/`SectionHeader`/`Progress`/`EmptyState`/`MetaChip`/`LabelChip`/`CardCompleteToggle` + shadcn `Tooltip`/`DropdownMenu`/`Checkbox`/`Tabs`) + Tiptap entegrasyonu; kullanıcı seçimi: palet=Trello-vari (parlak mavi primary + canlı çoklu accent + 12-renk etiket paleti; board zemini açık mavi-gri), yoğunluk=compact-balanced (kart `p-2`/kolon `w-72`/gap-3), font=**Inter**, rich text storage=**Tiptap JSON** (parse-time plain-text fallback, migration yok); `02-teknoloji-kararlari.md`'ye 2.7.0 Karar kaydı + `architecture/README.md` indeksi (satır 13) + `08-web-ve-mobil.md` §8.1 pointer'ı eklendi. **`faz-bol 2.7` yapıldı (2026-05-12):** DEM-58 epic'i 5 alt issue'ya bölündü — [DEM-61](https://linear.app/demirkol/issue/DEM-61) (2.7.0 önce-belge, `Done`) · [DEM-62](https://linear.app/demirkol/issue/DEM-62) (2.7A tema+token+`packages/ui`, `Todo`, `blockedBy` DEM-61) · [DEM-63](https://linear.app/demirkol/issue/DEM-63) (2.7B board ekranı, `Todo`, `blockedBy` DEM-62+DEM-37) · [DEM-64](https://linear.app/demirkol/issue/DEM-64) (2.7C kart detay modalı+Tiptap, `Todo`, `blockedBy` DEM-62+DEM-53) · [DEM-65](https://linear.app/demirkol/issue/DEM-65) (2.7D workspace/app-shell+a11y, `Todo`, `blockedBy` DEM-63+DEM-64); hepsi `Faz 2.7 — ...` milestone'unda, parent DEM-58, priority Medium, assignee Abdullah. `02-mvp-faz-plani.md` "Faz 2.7 alt işleri" DEM numaralarıyla güncellendi. Uygulama (2.7A–D) Faz 2.5 web bittiğinden serbest; her alt iş başladığında bu deftere satır açılır. Linear issue [DEM-58](https://linear.app/demirkol/issue/DEM-58) (Pusula, milestone Faz 2.7); başlık "Faz 2.7 — ... (epic)". |
| FE-2026-05-12-011 | DEM-62 | Faz 2.7A — Tema + design token + `packages/ui` rafinasyonu (yeni `theme.css` paleti + Inter + `Avatar`/`SectionHeader`/`Progress`/`EmptyState`/`MetaChip`/`LabelChip`/`CardCompleteToggle` + shadcn `Tooltip`/`DropdownMenu`/`Checkbox`/`Tabs` + `label-colors.ts` → token) | 2.7 — UI tasarım dili & board/kart cilalama | Done | Claude Code | `docs/architecture/13-ui-tasarim-dili.md` §13.1 + §13.4 (bağlayıcı spec — DEM-61'de yazıldı, bu iş onu uygular; QA M2 ile §13.1/§13.4'teki `label-colors.ts` path'i + `LABEL_PALETTE` notu düzeltildi), `docs/process/02-mvp-faz-plani.md` (durum), `docs/process/05-is-kayit-defteri.md` | `packages/ui`, `apps/web` (font setup, `_components/label-colors.ts`, `vitest.setup.ts`, `components/ui/*.test.tsx`) | 2026-05-12 | Faz 2.7'nin ilk kod işi (önce-belge DEM-61 `Done`). İmplement edildi (2026-05-12): **`packages/ui/src/styles/theme.css`** baştan yazıldı — Trello-vari OKLCH paleti (`:root` light + `.dark`; `--primary` parlak mavi `oklch(0.55 0.16 245)` / dark `0.62 0.15 245`; `--background` board zemini `oklch(0.96 0.02 240)` / dark `0.18 0.01 250`; `--card` beyaz / dark `0.22 0.01 250`; `--foreground` `0.18 0.01 250` / dark `0.97 0 0`; `--muted`/`--muted-foreground`/`--popover`/`--secondary`/`--accent`/`--border`/`--input`/`--ring` token'ları; `--success` `0.62 0.16 150`, `--warning` `0.78 0.14 75`, `--destructive` `0.58 0.21 27` + `-foreground` eşleri); **12-renk etiket paleti** `--palet-{kirmizi,turuncu,sari,lime,yesil,sky,mavi,indigo,mor,pembe,gri,siyah}` + her birinin `-foreground` eşi (light+dark, kontrast garantili; `--palet-mavi`≈`--primary`, `--palet-yesil`≈`--success`); **radius** base `0.5rem` + `@theme inline`'da `-sm`(6px)/`-md`/`-lg`(10px)/`-xl`(14px); **shadow** `--shadow-card`/`-card-hover`/`-popover`/`-drag` (`:root`+`.dark`) + `@theme inline` map (Tailwind `shadow-card` vb. utility); **font** `--font-sans` = `var(--font-inter), …` + `@theme inline` map; `@theme inline`'a `--color-success`/`--color-warning` + 12×2 `--color-palet-*` map (`bg-palet-yesil`, `text-palet-yesil-foreground`, `bg-palet-yesil/15` çalışır); eski `--chart-*`/`--sidebar*` token'ları kaldırıldı (kullanılmıyordu); `@layer base`'e `body { … font-sans }`. `theme.css` Tailwind'i import etmiyor (web `globals.css` ediyor). **`apps/web/src/app/layout.tsx`** → `next/font/google` `Inter` self-host (`subsets:['latin']`, `variable:'--font-inter'`, `display:'swap'`); `<html className={inter.variable}>` + `<body … font-sans>`; Next build font'u indirip lokal serve etti (online — sorun yok). **`apps/web/.../boards/[boardId]/_components/label-colors.ts`** → `LABEL_SWATCH` ham Tailwind (`bg-green-500`…) yerine `bg-palet-*` token utility'leri (string literal — Tailwind scanner görsün); domain `LabelColor` 10 değeri → palet adı: `green→yesil`/`yellow→sari`/`orange→turuncu`/`red→kirmizi`/`purple→mor`/`blue→mavi`/`sky→sky`/`lime→lime`/`pink→pembe`/`black→siyah`; ek olarak `LABEL_PALETTE: Record<LabelColor,PaletteName>` (2.7B/2.7C `LabelChip` için; `PaletteName` `@pusula/ui`'den import — QA M1); `LABEL_SWATCH[color]` imzası değişmedi — mevcut kullanıcılar (`board-filter-bar`, `board-settings/*`, `card-detail-labels`, `card-item`) bozulmadı; **domain `LABEL_COLORS` değişmedi**. **Yeni `packages/ui` bileşenleri** (her biri ayrı dosya + `exports` + `index.ts` re-export): `avatar.tsx` (`Avatar` — custom; `image` URL → `<img object-cover>`, yoksa isimden 1-2 baş harf büyük + isimden deterministik hash → 12 `--palet-*`'tan biri, isim yoksa `bg-muted text-muted-foreground`; `xs`16/`sm`24/`md`32/`lg`40px; `ring`→`ring-2 ring-card`; `PaletteName` tipi burada tanımlandı, `label-chip` re-export ediyor), `section-header.tsx` (`SectionHeader` — icon + UPPERCASE label `text-xs font-semibold uppercase tracking-wide text-muted-foreground` + `action` slot; `mb-2 flex justify-between`), `progress.tsx` (`Progress` — custom; `role="progressbar"` `aria-valuenow/min/max`; `h-1 bg-muted rounded-full overflow-hidden` + iç dolgu `complete?bg-success:bg-primary` `style.width=pct%`; `complete` verilmezse `value>=max`), `empty-state.tsx` (`EmptyState` — icon `text-muted-foreground/60` + `text-sm text-muted-foreground` mesaj + opsiyonel action; `flex-col items-center gap-2 py-6 text-center`), `meta-chip.tsx` (`MetaChip`+`MetaRow` — `MetaRow` flex shell variant `card`(`text-[10px] text-muted-foreground`)/`modal`; `MetaChip` icon+children, kart sürümü `text-[10px]`, modal sürümü `h-8 rounded-md px-2 text-xs`, `interactive`(modal)→`<button>` + `hover:bg-muted hover:text-foreground`, `tone`: `overdue`→`bg-destructive/12 text-destructive rounded-sm px-1 py-px font-medium`, `soon`→placeholder; `due`/`count`/`members` "variant"ları çağıran `icon`+içerikle elde eder), `label-chip.tsx` (`LabelChip` — `color:PaletteName`, `solid`(`bg-palet-{c} text-palet-{c}-foreground rounded-sm px-1.5 py-0.5 text-[10px] font-medium`)/`soft`(`bg-palet-{c}/15 text-palet-{c}`), `name` yoksa kısa renkli bar `h-2 w-8 rounded-sm bg-palet-{c}`; **Tailwind scanner için** 12 renk × variant `Record<PaletteName,{solid,soft,swatch,bar}>` literal map — dinamik string concat yok; ayrıca `LabelSwatch` `size-2.5 rounded-full bg-palet-{c}` export), `card-complete-toggle.tsx` (`CardCompleteToggle` — `role="checkbox" aria-checked`; boş `border-2 border-muted-foreground/40 hover:border-foreground rounded-full`, tamamlanmış `bg-success border-2 border-success text-success-foreground` + `<CheckIcon size-3>`; `size-4`(sm)/`size-5`(md); `alwaysVisible` false→`opacity-0 group-hover/kart:opacity-100 aria-checked:opacity-100`, true→hep görünür; `focus-visible:ring-2 ring-ring/60`; `onCheckedChange` toggle). **shadcn primitive'leri** (Radix tabanlı — "yalnız shadcn/ui" içinde; new-york + tema token'larına uyarlandı): `tooltip.tsx` (`Tooltip`/`TooltipTrigger`/`TooltipContent`/`TooltipProvider` — `@radix-ui/react-tooltip`; content `bg-popover text-popover-foreground … shadow-popover`), `dropdown-menu.tsx` (tüm shadcn parçaları — `@radix-ui/react-dropdown-menu`; content `shadow-popover`; destructive item `data-[variant=destructive]` token uyumu), `checkbox.tsx` (`Checkbox` — `@radix-ui/react-checkbox`; `data-[state=checked]:bg-primary data-[state=checked]:border-primary` + `focus-visible:ring-ring/60`), `tabs.tsx` (`Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` — `@radix-ui/react-tabs`; `TabsList` `bg-secondary p-[3px]`, aktif trigger `bg-card shadow-xs`). **Mevcut shadcn rafinasyonu** (minimal, davranış/API değişmedi): `dialog.tsx` content `shadow-popover` + `rounded-xl` + kapat butonu `focus-visible:ring-ring/60`; `card.tsx` `shadow-card`; `select.tsx` content `shadow-popover`; `--radius` `0.625→0.5rem` değiştiği için diğerleri otomatik uyumlu — başka dokunuş yok. **deps:** `packages/ui` → `@radix-ui/react-tooltip`/`-dropdown-menu`/`-checkbox`/`-tabs` (`pnpm install` ile kuruldu — 6 paket eklendi). **Test:** `apps/web/src/components/ui/` altında 9 RTL test dosyası — `avatar.test.tsx` (isim→baş harf + `bg-palet-` class; `image`→`<img src>`; `size-10`), `progress.test.tsx` (`aria-valuenow` + `style.width`; `complete`→`bg-success`; in-progress→`bg-primary`; explicit `complete`), `section-header.test.tsx` (uppercase label + action + icon), `empty-state.test.tsx` (mesaj + icon + action), `label-chip.test.tsx` (solid/soft class + `name` yoksa renkli bar + `LabelSwatch`), `card-complete-toggle.test.tsx` (`role=checkbox` + `aria-checked` + `onCheckedChange(true)` + `alwaysVisible`), `tabs.test.tsx` (panel görünür + tab değişimi), `checkbox.test.tsx` (toggle + `onCheckedChange`), `dropdown-menu.test.tsx` (trigger açılır + item handler), `tooltip.test.tsx` (trigger focus → content görünür); `apps/web/vitest.setup.ts`'e `ResizeObserver` no-op shim eklendi (Radix tooltip jsdom'da gerekli). Mevcut testler kırılmadı. **Doğrulama:** `pnpm install` OK (6 paket); `pnpm -w typecheck` 7/7 PASS; `pnpm -w lint` 7/7 PASS; `pnpm -w test` 40 dosya / 182 test PASS (10 yeni dosya / ~25 yeni test dahil); `pnpm --filter @pusula/web build` PASS (Next 16, Inter font fetch dahil — online; offline ortamda fail edebilir, o durumda `next/font/local`'a geçilir). **Fonksiyon değişmedi** — tRPC/mutation/permission/domain dokunulmadı; salt görsel/yapısal. Sapma: yok (spec'e sadık; OKLCH değerleri spec'teki "önerilen"e yakın ince-ayarlı; `Avatar` `PaletteName` tipini `avatar.tsx`'te tanımladım, `label-chip` ondan re-export ediyor — tek kaynak). Açık risk: build Inter fetch'e bağlı (online ortamda sorunsuz). **QA:** code-reviewer = APPROVE WITH NITS → düzeltildi: M1 (`label-colors.ts`'teki `PaletteName` 12-ad duplikesi kaldırıldı; `import type { PaletteName } from '@pusula/ui'` — tek kaynak `avatar.tsx`; `LABEL_PALETTE` korundu, 2.7B/2.7C `LabelChip` kullanacak) + M2 (`docs/architecture/13-ui-tasarim-dili.md` §13.1/§13.4'teki `apps/web/src/lib/label-colors.ts` path'i gerçek `…/_components/label-colors.ts` konumuna düzeltildi + `LABEL_PALETTE` / 12→10 token eşleme + `indigo`/`gri` yedek notu eklendi); ertelendi/kabul: L1 (Avatar `aria-label` span'da — pratikte buton/link içinde), L2 (`<img>` `loading=lazy`/`onError` fallback), L3 (MetaChip cast), L4 (TabsList `bg-secondary` — deliberate token), NIT'ler. verifier = PASS (`pnpm install` OK; `pnpm -w typecheck` 7/7; `pnpm -w lint` 7/7; `pnpm -w test` 317 test [domain 24 + api 111 + web 182, 10 yeni `components/ui/*.test.tsx` dahil]; `pnpm --filter @pusula/web build` yeşil; güvenlik temiz). M1/M2 fix sonrası `@pusula/web` typecheck+lint tekrar yeşil. Bağımlılık: DEM-61 (Done). `blocks` DEM-63 + DEM-64; `blockedBy` DEM-61. Kullanıcı onayladı (2026-05-12) → `Done`; commit `feat: DEM-62 — Faz 2.7A …` + push (main); Linear DEM-62 → `Done` + kapanış yorumu. **Bununla DEM-63 (2.7B) + DEM-64 (2.7C) `blockedBy` çözüldü — `Todo` (paralel başlatılabilir).** |
| FE-2026-05-12-012 | DEM-63 | Faz 2.7B — Board ekranı cilalama (zemin/üst bar + kolon/kart anatomisi + metadata satırı + "GECİKTİ" rozeti + filter bar + loading skeleton + hover/focus) + `board.get` additive zenginleştirme (kart metadata sayaçları) | 2.7 — UI tasarım dili & board/kart cilalama | Done | Claude Code | `docs/architecture/13-ui-tasarim-dili.md` §13.2 (bağlayıcı spec — DEM-61; QA-öncesi `CardCompleteToggle` kartta yok + ek sayacı yok notları eklendi), `docs/architecture/03-backend.md` (önce-belge — `board.get` additive `checklistTotal`/`checklistDone`/`commentCount`/`members[]`), `docs/architecture/05-board-mekanigi.md` §5.0 (önce-belge — aynı), `docs/process/02-mvp-faz-plani.md` (durum), `docs/process/05-is-kayit-defteri.md` | `apps/web` (board ekranı `_components/*`), `packages/api` (`board.get` additive), gerekirse `packages/ui` | 2026-05-12 | Faz 2.7'nin board-ekranı kod işi (önce-belge DEM-61 `Done`; 2.7A `Done` — token + bileşenler hazır). Başlatıldı 2026-05-12 (Linear DEM-63 `Todo` → `In Progress`). **Kullanıcı kararları (2026-05-12, kontrol odası dışı bu turda):** (a) kart metadata satırı (checklist progress / yorum sayısı / üye avatarları) verisi `board.get`'te yoktu → **additive zenginleştir**: `cards[]`'a `checklistTotal`/`checklistDone` (`checklists ⋈ checklist_items` GROUP BY card), `commentCount` (`comments` `deleted_at IS NULL`), `members: { userId, name, image, role }[]` (`card_members ⋈ users` — avatar yığını için ad+görsel; e-posta dönmez, privacy — DEM-51 deseni); toplu sorgular, N+1 yok; imza additive (mevcut alanlar değişmez) — DEM-54 `cards[].labels` deseni. "Önce belge" yapıldı: `03-backend.md` `board.get` satırı + `05-board-mekanigi.md` §5.0. (b) `CardCompleteToggle` board kartında **render edilmez** — Pusula'da kart-seviyesi "tamamlandı" özelliği yok (`cards`'ta `completed`/`completedAt` kolonu yok; yalnız `checklist_items.completed`); bileşen `packages/ui`'de kalır (modal / checklist madde checkbox'ında kullanılır); "tamamlanmış kart" görünümü (line-through vb.) da atlanır; kart-completion isterse ileride ayrı domain işi. (c) Cover görseli + ek sayacı `PaperclipIcon` chip'i kapsam dışı (attachment Faz 8). `13-ui-tasarim-dili.md` §13.2 bu kararlarla kod-öncesi güncellendi. **Kapsam (§13.2 — kalan):** board zemini `bg-background` + `BoardTopBar` (sticky; sol `BoardIdentity` board ikonu/renk + "Pano" label + ad + ⭐ favori [altyapı Faz 8/DEM-57 — görsel/gizli]; orta `BoardViewSwitch` "Pano/Liste/Etiketler" — Liste/Etiketler placeholder/disabled; sağ `BoardActions` Davet/Paylaş → board ayarları · arama gizli [Faz 6.5] · aktivite ikonu · ⋮ `DropdownMenu` yeniden adlandır/arşivle/board ayarları); kolon anatomisi (`w-72 rounded-lg border bg-muted/40 max-h-[calc(100vh-9rem)]`; başlık `p-2` ad+kart sayısı + shield/daralt/⋮; gövde `gap-2 px-2 pb-2 overflow-y-auto`; footer `AddCardForm`/"+ Kart ekle" ghost; arşivli liste `bg-muted/20 border-dashed` salt-okunur; sona "+ Liste ekle" dashed kolon); kart anatomisi (`CardItem` `bg-card rounded-md border p-2 shadow-card hover:border-foreground/30 hover:shadow-card-hover group/kart`: etiket chip'leri [`LabelChip` solid] → başlık `line-clamp-3` → **metadata satırı** `text-[10px] text-muted-foreground gap-x-2 gap-y-1`: due chip [`CalendarIcon`; gecikmiş→`bg-destructive/12 text-destructive` + "GECİKTİ" rozeti `bg-destructive text-destructive-foreground text-[9px] uppercase`; 24–72h→`--warning` nokta] · açıklama-var [`AlignLeftIcon`] · checklist progress [`CheckSquareIcon` x/y; tamsa `text-success`] · yorum sayısı [`MessageSquareIcon` n] · üye avatarları [`Avatar size-xs -space-x-1` + "+N"]); filter bar cilalama (DEM-54'ten var — etiket çipleri aktif/pasif + arşiv toggle); loading skeleton (`animate-pulse`); hover/focus (`focus-visible:ring-2 ring-ring/60`); `Tooltip` ile ikon butonları; `strings.board.*` genişletme. **Optimistic UI yok** (Faz 4 — mutation → invalidate → refetch); **drag-drop davranışı yok** (Faz 3 — DEM-26; §13.2 drag spec'leri yalnız hedef görsel). RTL testleri (CardItem metadata, kolon, üst bar) + `board.get` integration test güncellemesi + verifier. **Fonksiyon davranışı değişmez** — `board.get` additive (semantik aynı), mutation/permission dokunulmadı. **İmplement edildi (2026-05-12):** backend `board.get` `cards[]`'a `checklistTotal`/`checklistDone`/`commentCount`/`members[]` (toplu sorgular `Promise.all` ile paralel; boş board kısa devre; e-posta yok) + `board.test.ts` güncellemesi; web: `page.tsx` + yeni `_components/board-top-bar.tsx` (back link + `BoardIdentity` [renk noktası + "PANO" eyebrow + ad + admin&aktif inline rename + ⭐ favori `disabled` Tooltip "Yakında"] + `BoardViewSwitch` ["Pano" aktif, "Liste"/"Etiketler" `aria-disabled` Tooltip] + `BoardActions` [Davet et → board ayarları, arama/aktivite `disabled` Tooltip, ⋮ `DropdownMenu` yeniden adlandır/arşivle/geri al/board ayarları]; arşivli → rozet) + `_components/board-skeleton.tsx` (`role=status aria-busy` + kolon iskeletleri) + `_components/card-meta-row.tsx` (due chip + "GECİKTİ" rozeti + ≤72h amber soon-dot + açıklama-var + checklist progress [tamsa `text-success`] + yorum sayısı + üye avatar yığını [son 3 + "+N"]; hepsi `Tooltip`); `card-item.tsx` → `role=button tabIndex=0` + Enter/Space → `?card=` push, etiket chip'leri `LabelChip` solid, başlık `line-clamp-3`, `CardMetaRow`, arşivle = hover ikon + onaylı `Dialog` (`stopPropagation`); `list-column.tsx` → `rounded-lg border bg-muted/40 max-h-[calc(100vh-9rem)]` (arşivli `border-dashed bg-muted/20` salt-okunur) + başlık ad+sayı + ⋮ `DropdownMenu` + footer `AddCardForm`; `board-columns.tsx` `gap-3` + boş board `EmptyState`; `board-filter-bar.tsx` `bg-card rounded-md border` + etiket çipleri `LabelChip` soft/aktif + arşiv toggle `@pusula/ui` `Checkbox`; `rename-board-form`/`archive-board-dialog` (+`useRestoreBoard` hook)/`board-settings-dialog` opsiyonel controlled `open`/`editing`/`hideTrigger` props (geriye dönük uyumlu); `strings.board.*` genişletildi (`topBar.*`/`column.*`/`card.*`/`skeleton.*`/`filter.archivedListsToggle`/`card.overdueBadge`/`card.unnamedLabel` vb.); 4 RTL test dosyası (`card-item` güncel + yeni `list-column`/`board-top-bar`/`board-skeleton` + `board-filter-bar` güncel) + `board.test.ts` (`get: cards carry additive metadata`). **QA:** code-reviewer = APPROVE WITH NITS → düzeltildi: M1 (`board.get` 4 ardışık sorgu → `Promise.all` paralel) + L2 (adsız etiket fallback `copy.dueLabel` → `copy.unnamedLabel` / `LabelChip` `name=… || undefined`); ertelendi/kabul: L3 (soon eşiği kodda ≤72h — spec metni "24–72h"; davranış daha mantıklı), N4–7 (ölü `strings` anahtarları, nested-interactive kart, çift Tooltip/`sr-only`, Avatar aria). verifier = PASS (typecheck 8/8; lint 7/7; test 336 [domain 24 + api 112 (board.test 11 dahil — gerçek DB) + web 200]; `@pusula/web` build yeşil; güvenlik temiz — `members` e-posta dönmüyor, Drizzle parametreli sorgu). M1/L2 fix sonrası typecheck/lint/test/build tekrar yeşil. Bağımlılık: DEM-61 + DEM-62 + DEM-37 (hepsi Done). `blockedBy` DEM-62 + DEM-37. Kullanıcı onayladı (2026-05-12) → `Done`; commit `feat: DEM-63 — Faz 2.7B …` + push (main); Linear DEM-63 → `Done` + kapanış yorumu. |
| FE-2026-05-12-013 | DEM-64 | Faz 2.7C — Kart detay modalı yeniden yapı (iki-kolon + meta chip satırı + AÇIKLAMA/KONTROL LİSTESİ + sağ panel sekme strip/yorum composer/aktivite feed + Tiptap rich text) | 2.7 — UI tasarım dili & board/kart cilalama | Done | Claude Code | `docs/architecture/13-ui-tasarim-dili.md` §13.3 + §13.5 (bağlayıcı spec — DEM-61'de yazıldı), `docs/architecture/02-teknoloji-kararlari.md` (Tiptap karar kaydı — DEM-61'de eklendi; bu iş onu uygular), `docs/process/02-mvp-faz-plani.md` (durum), `docs/process/05-is-kayit-defteri.md` | `apps/web` (kart detay modalı `_components/card-detail/*` + `lib/strings.ts`), gerekirse `packages/ui` (`RichTextEditor`/`RichTextContent`) | 2026-05-12 | Faz 2.7'nin kart-detay-modalı kod işi (önce-belge DEM-61 `Done`; 2.7A `Done` — token + bileşenler [`Tabs`/`Checkbox`/`DropdownMenu`/`Tooltip`/`Avatar`/`SectionHeader`/`Progress`/`EmptyState`/`MetaChip`/`LabelChip`/`CardCompleteToggle`] hazır). Başlatıldı 2026-05-12 (Linear DEM-64 `In Progress`). DEM-53'ün [Done] tek-kolon modali üzerine **yapısal yeniden kurulum** (§13.3): modal kabı `w-[min(960px,92vw)] h-[min(85vh,800px)] flex flex-col p-0`; `CardModalHeader` (kapak rengi seçiliyse `bg-palet-{ad}` çubuk; sol `ListIcon`+breadcrumb pano/liste; sağ `BellIcon` [ileri faz] + `LinkIcon` [derin linki kopyala] + ⋮ `DropdownMenu` [arşivle/geri al; taşı/kopyala ileri faz] + `X`); içerik iki-kolon `grid md:grid-cols-[1fr_360px]` — **sol** (`overflow-y-auto px-5 py-4 space-y-5`: sticky başlık [`CardCompleteToggle` görsel — kart-completion özelliği yok, modal'da checklist madde checkbox'ı için kullanılır; başlık inline-edit `text-lg font-semibold`], meta chip satırı [`ShieldIcon`+üye→picker · `CalendarIcon`+due→date picker (gecikmiş kırmızı + "GECİKTİ" rozeti / soon amber nokta) · `TagIcon`+etiket→picker · `PaletteIcon`+kapak rengi→renk picker (kapak rengi: yeni — `cards`'ta kolon yok → ya client-only state ya kapsam dışı; agent karar verecek/raporlayacak)], "AÇIKLAMA" [`SectionHeader` + `RichTextEditor` Tiptap toolbar B I S `<>`/H1-3/liste/link; boşken "Açıklama ekle…"], "KONTROL LİSTESİ" [`SectionHeader` + toplam `Progress` mini-bar + `x/y` + "+ Liste ekle"; her checklist `border rounded-md p-3` — başlık edit + `x/y` + `Progress` (tamsa `bg-success`) + maddeler (`Checkbox` yuvarlak + madde edit + atanan `Avatar size-xs` + chip + sil hover) + "+ Madde ekle"]) — **sağ panel `CardModalSidebar`** (`bg-muted/40 backdrop-blur md:border-l overflow-y-auto`: sticky header + `Tabs` strip [Yorumlar N / Aktivite N / Ekler N / Tümü N]; üstte yorum composer [`Avatar size-sm` + mini `RichTextEditor` (B I + link, Placeholder "Yorum yaz, @ ile etiketle…" — @mention Faz 6) + alt toolbar + "Gönder"]; sekmeye göre liste — Yorumlar (kartlar, silinmiş→"silindi", "(düzenlendi)", yazan/admin edit-sil hover, `RichTextContent` render), Aktivite (actor `Avatar size-xs` + ad + Türkçe özet + zaman), Ekler (ileri faz — boş `EmptyState`), Tümü (birleşik kronolojik); boş durumlar `EmptyState`); mobil (`md:` altı) tek-kolon, sağ panel alta düşer. **Tiptap entegrasyonu** (§13.5): `@tiptap/react` + `@tiptap/starter-kit` + `@tiptap/extension-link` + `@tiptap/extension-placeholder` (deps eklenir); `RichTextEditor` (toolbar + editable `EditorContent`) + `RichTextContent` (read-only `EditorContent`); **storage = Tiptap JSON** — `cards.description`/`comments.body` `text` kolonları DEĞİŞMEZ, Tiptap `getJSON()` JSON string olarak saklanır; mevcut plain-text içerikler render/edit anında düz paragrafa parse edilir (parse-time fallback); **migration YOK**; XSS yok (Tiptap controlled şema + read-only render `EditorContent` ile — `dangerouslySetInnerHTML` yok); procedure imzaları aynı (`card.update`/`comment.create`/`comment.update` string in/out — mantık değişmez). `strings.card.detail.*` genişletme; `clientMutationId` istemcide; **optimistic UI yok** (Faz 4 — mutation → invalidate → refetch); @mention yok (Faz 6); ek/attachment listesi boş (Faz 8). RTL testleri (modal yapısı, Tiptap editör, sekme strip, meta chip'ler) + verifier + security-reviewer (Tiptap/XSS). 2.7C kapsamında **backend mantığı değişmez** — yeni/değişen tRPC procedure yok (Tiptap = `text` kolonlarda JSON string, imzalar aynı). **İmplement edildi (2026-05-12):** `packages/ui/src/components/rich-text-editor.tsx` (`RichTextEditor` toolbar + editable `EditorContent` / `RichTextContent` read-only; Tiptap v3 StarterKit + Placeholder; Link config `isAllowedUri`/`shouldAutoLink` ile `http`/`https`/`mailto` whitelist — `javascript:` href düşürülür; `rel="noopener noreferrer nofollow"`; `parseRichTextValue` parse-time plain-text fallback; `'use client'` + `immediatelyRender:false`; prose stilleri token'lı + `.tiptap` placeholder kuralı `theme.css`'te) + `index.ts` export + `apps/web`/`packages/ui` Tiptap v3 deps; `card-detail-dialog.tsx` baştan yazıldı (iki-kolon `grid md:grid-cols-[1fr_360px]` — sol: sticky `CardDetailTitle` → `CardDetailLabels`/`Members`/`DueDate` bölümleri → AÇIKLAMA Tiptap → KONTROL LİSTESİ (`@pusula/ui` `Checkbox`/`Progress`/completer `Avatar`); sağ: `CardModalSidebar` `Tabs` strip [Yorumlar/Aktivite/Ekler/Tümü] + her zaman üstte yorum composer + sekme içerikleri); `CardModalHeader` (breadcrumb pano/liste — `board.get` cache'inden + arşivli rozeti + `BellIcon` disabled + `LinkIcon` derin link kopyala + ⋮ `DropdownMenu` arşivle [yıkıcı, onaylı `Dialog`]/geri al, taşı/kopyala disabled + `X`); `CardModalSidebar` (sekme strip + composer [`RichTextEditor` mini + "Gönder" boşken disabled] + Yorumlar [`RichTextContent` render, silinmiş→"silindi", "(düzenlendi)", yazan/admin edit-sil, no-op edit `isSameRichText` ile normalize edildi] / Aktivite [`summarizeCardActivity` Türkçe özet — DEM-66/67 yeni tipleri `card.completed`/`uncompleted`/`cover_changed`/`cover_cleared` için case eklendi] / Ekler [boş `EmptyState`] / Tümü [birleşik kronolojik]); `card-detail-description.tsx` Tiptap full toolbar; `checklist-block`/`checklist-item-row`/`card-detail-checklists`/`card-detail-activity`/`checklist-types` yeni stile; `strings.card.detail.*`/`richText.*`/`tabs.*`/`composer.*` genişletildi; `vitest.setup.ts` ProseMirror jsdom shim'leri; RTL testleri (`card-modal-header`/`card-modal-sidebar`/`rich-text-editor` yeni + `card-detail-{comments,description,checklists,activity}` güncel). **Kararlar:** `CardCompleteToggle` modal sticky başlığına **disabled placeholder** olarak eklendi (`disabled`+`aria-disabled`+no-op `onCheckedChange`, `<span>` sarmalı + `Tooltip` "Kart tamamlama yakında geliyor.") + kapak-rengi `PaletteIcon` meta-chip'i de disabled placeholder + Tooltip "yakında" — gerçek backend wiring (`card.complete`/`uncomplete` + `card.update({coverColor})`; DEM-66/67 backend hazır) ileri faz/ayrı küçük iş follow-up; `card-modal-meta-chips.tsx` retry turunda **wire edildi** (başlık altında üye/due/etiket chip'leri → tıklayınca `metaSection` state ile inline picker [`CardDetailMembers`/`CardDetailDueDate`/`CardDetailLabels`], `aria-expanded`; `Popover` yeni primitive eklenmedi — bilinçli) → artık dead code değil; `@tiptap/extension-link` retry turunda dep'lerden kaldırıldı (Tiptap 3.x'te `Link` StarterKit içinde — `StarterKit.configure({ link: {...} })`); kullanılmayan eski `strings.card.detail.*` anahtarları silindi. **QA (2 tur):** code-reviewer 1. tur = APPROVE WITH NITS → M1 (yorum/açıklama edit no-op normalize → legacy plain-text yorumda yanlış-pozitif `comment.update`) + M2/L1/L2 (`CardCompleteToggle` modale eklenmemiş + `card-modal-meta-chips.tsx` ölü kod + kullanılmayan `strings`) + N1 (`@tiptap/extension-link` 3.x'te gereksiz) + N4 (checklist butonları klavye focus'unda soluk) + L (Tiptap Link `javascript:` href sertleştirme + test) + L (yeni activity tipleri `summarizeCardActivity`'ye) → **düzeltme turu: hepsi çözüldü** (yeni `rich-text-helpers.ts` `isSameRichText`; `CardModalMetaChips`+`CardCompleteToggle` wire; `extension-link` kaldırıldı; `group-focus-within/item`; regresyon testleri — legacy plain-text yorum/açıklama no-op + stored XSS `javascript:` reddi); code-reviewer 2. tur = **APPROVE** (3 NIT bloklayıcı değil — disabled buton üzerinde Tooltip tetiklenmesi, boş-ama-JSON description `hasContent` edge case, `+` meta-chip etiket picker'ı açıyor); ertelendi: M/L (`RichTextContent` yorum başına ProseMirror instance — perf, follow-up: statik render), L (`commentBodySchema`/`cardDescriptionSchema` `max(20K)` JSON için darca olabilir — backend tarafı, ayrı). verifier = PASS (typecheck 7/7; lint 7/7; test 365 [domain 24 + api 118 + web 223 / 46 dosya]; `@pusula/web` build yeşil; migration `0007` tutarlı; güvenlik temiz — `dangerouslySetInnerHTML` yok, Link sanitize var, Drizzle parametreli). **DEM-66/67 birlikte commit edildi** (kullanıcı kararı — şema/domain/test iç içe; bkz. `FE-2026-05-12-014`). Bağımlılık: DEM-61 + DEM-62 + DEM-53 (hepsi Done). `blockedBy` DEM-62 + DEM-53. 2.7B ile paralel (2.7B `Done`). Kullanıcı onayladı → `Done`; commit `feat: DEM-64 (+ DEM-66/67) — …` + push (main); Linear DEM-64 → `Done` + kapanış yorumu. |
| FE-2026-05-12-014 | DEM-66 + DEM-67 | Kart-seviyesi tamamlama backend (`cards.completed/completedAt/completedBy` + `card.complete`/`uncomplete`) + kart kapağı rengi backend (`cards.coverColor` + `CARD_COVER_COLORS`) — `bosluk-tara` 2. tur, Faz 2.7 "fonksiyon değişmez" istisnası | 2.7 — UI tasarım dili & board/kart cilalama | Done | Claude Code | `docs/architecture/04-veri-katmani.md` (`cards.completed*`/`coverColor`), `docs/architecture/03-backend.md` (`card.complete`/`uncomplete` + `board.get` additive), `docs/domain/01-urun-modeli.md` (kart tamamlama/kapak rengi invariant), `docs/domain/05-aktivite-kurallari.md` (`card.completed/uncompleted/cover_changed/cover_cleared`), `docs/process/05-is-kayit-defteri.md` | `packages/db` (şema + migration `0007_icy_wallflower.sql` + meta), `packages/domain` (`CARD_COVER_COLORS` + `ACTIVITY_EVENT_TYPES` + `schemas/card.ts`), `packages/api` (`card.ts` `card.complete`/`uncomplete` + `card.update` `coverColor`; `board.get`/`card.get` additive) | 2026-05-12 | `bosluk-tara` 2. tur'da açılan DEM-66 + DEM-67 (kontrol odası tab'ı; Linear'da ikisi de `Done`, `completedAt` 2026-05-12). Tek migration `0007`: `cards` → `completed boolean not null default false` + `completed_at timestamptz` + `completed_by text references users on delete set null` + `cover_color text` (nullable). `@pusula/domain`: `CARD_COVER_COLORS` (12 ad — `@pusula/ui` `PaletteName` ile elle senkron, `indigo`/`gri` dahil), `cardCoverColorSchema = z.enum(...)`, `updateCardInput.coverColor` nullable+optional, `completeCardInput`/`uncompleteCardInput`, `ACTIVITY_EVENT_TYPES` +4 (`card.completed`/`uncompleted`/`cover_changed`/`cover_cleared`, append-only). `@pusula/api`: `card.complete`/`card.uncomplete` (`cardProcedure`, board `member+` = `canEditBoardContent`, arşivli board `BAD_REQUEST` kapısı, tx: `cards` update + `activity_events` + `boards.version+1`, idempotent no-op; dönüş `{id,completed,completedAt,completedBy,changed}`); `card.update` `coverColor` (`'coverColor' in input` key-presence ile `null` da gerçek değişiklik; activity `card.cover_changed`/`card.cover_cleared`); `board.get`/`card.get` `cards` projection'a `completed`/`completedAt`/`completedBy`/`coverColor` (additive — doğrudan kolonlardan, ek sorgu yok; 2.7B `checklistTotal`/`commentCount`/`members`/`labels` korundu). `card.test.ts`: complete/uncomplete (set + no-op + activity + version + `card.get` projeksiyonu + viewer `FORBIDDEN`), coverColor (set/no-op/clear + activity payload + invalid → `BAD_REQUEST` + viewer `FORBIDDEN`), arşivli board kapısı, fresh card defaults. **2.7C ile aynı commit'te** (`feat: DEM-64 (+ DEM-66/67) — …`). QA: code-reviewer = APPROVE WITH NITS (LOW: `card.complete` `card.update` ile kopya kod / dönüş şekli farklı / arşivli *kart* engellenmiyor [sadece arşivli *board* — `card.update` politikasıyla tutarlı] / outbox-realtime yok [mevcut pattern]; NIT: kullanılmayan `@tiptap/extension-link` dep) + verifier = PASS. UI henüz kullanmıyor (`CardCompleteToggle` modal/kartta render edilmiyor; kapak-rengi picker yok) — follow-up: 2.7C/2.7B'ye veya ayrı işe wire. **Faz 2.7 "fonksiyon değişmez" ilkesinin bilinçli istisnası** (UI bileşeninin işlevsel olması için backend; `13-ui-tasarim-dili.md` zaten UI'yı tanımlıyor). Bağımlılık: yok. |
| FE-2026-05-12-015 | DEM-65 | Faz 2.7D — Workspace / app-shell ekranları yeni tema uyumu + a11y rötuş (+ `Dialog` "Kapat" → `strings.common.close`) | 2.7 — UI tasarım dili & board/kart cilalama | Done | Claude Code | `docs/architecture/13-ui-tasarim-dili.md` §13.1/§13.4 (token + `SectionHeader` deseni — DEM-61'de yazıldı, son rötuş bunları uygular), `docs/process/02-mvp-faz-plani.md` (durum + Faz 2.7 epic), `docs/process/05-is-kayit-defteri.md` | `apps/web` (app-shell, workspace/account ekranları, `lib/strings.ts`), `packages/ui` (gerekirse `Dialog`) | 2026-05-12 | Faz 2.7'nin **son alt işi** (önce-belge DEM-61 `Done`; 2.7A/2.7B/2.7C `Done`). Başlatıldı 2026-05-12 (Linear DEM-65 `Todo` → `In Progress`). Kapsam (küçük — **yalnız tema/görsel uyum + a11y; yeni özellik yok**): app-shell (`(app)/_components/app-shell.tsx` — board ekranını full-width'e çek / `max-w-5xl` kısıtını gevşet; nav/header tema uyumu) + workspace listesi (`(app)/page.tsx` + `onboarding-empty-state` → `EmptyState` bileşeni) + workspace yönetim ekranı (`workspaces/[id]/page.tsx` + alt bölümler — `SectionHeader` deseni, hover/focus, shadow/spacing) + hesap ekranı (`(app)/account` — aynı) yeni renk paleti / spacing ritmi / `shadow-card`/`shadow-popover` / hover-focus state'leri; accessibility pass (focus ring `focus-visible:ring-2 ring-ring/60` tüm odaklanabilirlerde, keyboard nav board+modal, ARIA doğrulama, kontrast light+dark WCAG AA — gerekirse `BoardSkeleton` `motion-reduce:animate-none`); shadcn `Dialog`'taki hardcoded "Kapat" sr-only string → `strings.common.close` (`@pusula/ui` `Dialog` prop'tan alır veya `packages/ui` token'sız metin almadığından `apps/web` tarafında `DialogContent`'e geçer — agent karar verir). RTL + verifier; accessibility-auditor opsiyonel. **Fonksiyon değişmez** — tRPC/mutation/permission yok; mevcut davranış korunur. **İmplement edildi (2026-05-12):** `packages/ui` `DialogContent`'e `closeLabel?: string` prop (default `'Kapat'`); `apps/web` `strings.common.close = 'Kapat'` + tüm açık-uçlu `DialogContent` kullanımlarına `closeLabel={strings.common.close}` (`card-detail-dialog` `showCloseButton={false}` istisna); `app-shell.tsx` header tema token'larına + `usePathname()` ile board route'unda full-width `<main>` (diğerlerinde `max-w-5xl` ortalı) + nav focus-ring'leri; `boards/[boardId]/page.tsx` full-bleed `<main>`'i dolduran `flex flex-1 flex-col gap-4 px-4 py-4`; `(app)/page.tsx` + `board-list-section.tsx` kart hover (`hover:border-foreground/30 hover:shadow-card-hover`) + link focus-ring; `onboarding-empty-state.tsx` → `@pusula/ui` `EmptyState` (`Card` shell + `SparklesIcon`); `workspaces/[id]/page.tsx` + `account/page.tsx`/`delete-account-section.tsx` link rötuşları; `board-skeleton.tsx` `motion-reduce:animate-none`; `board-settings-dialog.tsx` `<h3>` → `@pusula/ui` `SectionHeader`; `board-label-row`/`create-board-label-form`/`card-detail-labels` renk-picker + `×` butonlarına `focus-visible:ring`; yeni `apps/web/src/components/ui/dialog.test.tsx` (closeLabel prop) + mevcut testler uyumlu. **QA:** code-reviewer = APPROVE WITH NITS → ertelendi/kabul: M1 (`SectionHeader` heading semantiği taşımıyor — `<span uppercase>`; gerekirse §13.4'e "gerekirse `aria-labelledby` ekle" notu + ileride `SectionHeader`'a opsiyonel `headingLevel`), L1 (header `max-w-7xl` vs sayfa `max-w-5xl` hizalama — bilinçli, header global chrome), L2 (board page pending/error `py-6` vs içerik `py-4` — küçük zıplama), N (closeLabel default geçici-TR / `cn` tek-arg / dialog.test konumu — yerleşik desen). verifier = PASS (typecheck 7/7; lint 7/7; test 368 [domain 24 + api 118 + web 226 — dialog.test 3 yeni dahil]; `@pusula/web` build yeşil; güvenlik temiz). Bağımlılık: DEM-61 + DEM-62 + DEM-63 + DEM-64 (hepsi Done). `blockedBy` DEM-63 + DEM-64. Kullanıcı onayladı → `Done`; commit `feat: DEM-65 — Faz 2.7D …` + push (main); Linear DEM-65 → `Done` + kapanış yorumu. **Bununla Faz 2.7 (DEM-58 epic) tamam → DEM-58 `Done`.** |
| DOC-2026-05-12-008 | DEM-59 | Deployment: Dokploy "Docker Compose" servis tipi kararı + üretim deploy runbook'u (docs) | Süreç (infra/deployment) | Done | Claude Code | `docs/architecture/10-platform.md` (§10.3), `docs/architecture/02-teknoloji-kararlari.md` (Sabit kararlar + ADR-lite), yeni `docs/architecture/12-deployment-runbook.md`, `docs/architecture/README.md`, `docs/architecture/01-genel-bakis.md`, `docs/architecture/11-referanslar.md`, `docs/process/05-is-kayit-defteri.md` | `docs/architecture`, `docs/process` | 2026-05-12 | Kullanıcı sorusu: "VDS'te Dokploy kurulu ama her servisi tek tek ayağa kaldırmak çok uğraştırdı; doğrudan docker compose mı, yoksa Dokploy + Docker birlikte mi?" → öneri **B: Dokploy "Docker Compose" servis tipi** kabul edildi. Karar: Dokploy'da her servisi ayrı "Application" olarak tanımlama yok; **tek bir "Docker Compose" servisi** → GitHub repo + branch + repo kökündeki `compose.prod.yml` → stack tek ünite olarak deploy edilir. Dokploy payı: Traefik + Let's Encrypt TLS, git-push-to-deploy webhook'u, build logu, env yönetimi, rollback. Güncellenenler: `10-platform.md` §10.3 "Deployment (Dokploy — 'Docker Compose' servis tipi)" yeniden yazıldı (Compose modu + monorepo image'ları `turbo prune --docker` + migration tek seferlik job + named volume/yedek + prod≠dev compose); `02-teknoloji-kararlari.md` → Sabit kararlar tablosu "Deployment" satırı + tarihli ADR-lite kaydı (2026-05-12, DEM-59); **yeni `12-deployment-runbook.md`** → 12.1 yaklaşım/önkoşul, 12.2 `compose.prod.yml` + `apps/api`(api+worker paylaşımlı)/`apps/web` `Dockerfile` template'leri + migration disiplini, 12.3 Aşama 0 VDS temizliği (v1'i indir: bakım moduna al → `pg_dump`/MinIO yedeği → durdur → yedek doğrula → sil → container/image/volume/network temizliği [körlemesine `volume prune` yok] → disk/port), 12.4 Aşama 1 Dokploy v2 Compose servisi, 12.5 Aşama 2 env tablosu, 12.6 Aşama 3 domain + TLS, 12.7 Aşama 4 ilk deploy + migration + MinIO bucket, 12.8 Aşama 5 smoke test, 12.9 Aşama 6 erişimi açma, 12.10 sürekli deploy (git push → build logu), 12.11 rollback, 12.12 yedekleme/volume, 12.13 sorun giderme tablosu; `architecture/README.md` → indekse satır 12 + "Kaçınılması gerekenler"e 2 madde (servis-servis Application tanımlama; prod'da yerel `docker-compose.yml`'i kullanma); `01-genel-bakis.md` teknoloji özeti "Deployment" satırı; `11-referanslar.md` → Dokploy Compose / Traefik routers+ACME / Turborepo Docker / Next.js standalone linkleri; bu defter satırı. **Docs-only** — gerçek `compose.prod.yml` + `apps/{web,api,worker}/Dockerfile` (multi-stage `turbo prune --docker`) + migration one-off job ayrı infra issue'sunda. Obsidian frontmatter/MOC bağları: yeni dosya `12-deployment-runbook.md` frontmatter + `parent`/`related` + README indeksi tamam. Kapanış: kullanıcı yaklaşımı onayladı → docs senkron tamam → **Done**; Linear [DEM-59](https://linear.app/demirkol/issue/DEM-59) → `Done` + kapanış yorumu. Takip işi (kod, v2 yazımı ilerleyince): [DEM-60](https://linear.app/demirkol/issue/DEM-60) — `compose.prod.yml` + `apps/{web,api,worker}/Dockerfile` + migration one-off job (`Backlog`, related to DEM-59); bu defterde geliştirmesi başlayınca satır açılır. Commit `b0b8f61` (main; kontrol odası tab'ı tarafından commit edildi). |
| DOC-2026-05-12-009 | DEM-66–73 | bosluk-tara 2. tur — ürün boşluğu taraması (8 yeni issue) | Süreç (ürün/kontrol odası) | Done | Claude Code | `docs/process/02-mvp-faz-plani.md` (Faz 2.7/Faz 1/Faz 3 alt işleri + Faz 6/8 satırları), `docs/kontrol-odasi/README.md` §5, `docs/process/05-is-kayit-defteri.md` | `docs/process`, `docs/kontrol-odasi`, Linear | 2026-05-12 | Kontrol odası tab'ı: kullanıcı talebiyle 2. `bosluk-tara` (kart tamamlama vb. boşluklar). Kod tarandı (doğrulandı: `cards` tablosunda `completed`/`coverColor` kolonu **yok**; `(auth)` route group'unda forgot/reset-password route'u **yok**; Better Auth e-posta doğrulama config'i **yok**). 8 boşluk → kullanıcı **hepsini onayladı** → 8 yeni Linear issue: **[DEM-66](https://linear.app/demirkol/issue/DEM-66)** kart tamamlama backend (`cards.completed*` + `card.complete`/`uncomplete` + activity + domain invariant) → Faz 2.7 (DEM-58 altı, `Todo`, Medium; UI 2.7B/2.7C bunu bekliyor — "fonksiyon değişmez" istisnası); **[DEM-67](https://linear.app/demirkol/issue/DEM-67)** kart kapağı rengi backend (`cards.coverColor`) → Faz 2.7 (DEM-58 altı, `Todo`, Medium; DEM-66 ile tek migration; kapak görseli ayrı → Faz 8); **[DEM-68](https://linear.app/demirkol/issue/DEM-68)** şifre sıfırlama akışı (forgot/reset password + Resend transactional) → Faz 1 ek iş (`Todo`, Medium, no parent; `blockedBy` DEM-22); **[DEM-69](https://linear.app/demirkol/issue/DEM-69)** Faz 3E — `card.moveToList` (cross-board) + `card.copy` → Faz 3 (DEM-26 altı, `Sonraki Faz`, Medium, `blockedBy` DEM-42); **[DEM-70](https://linear.app/demirkol/issue/DEM-70)** board-level activity feed (`board.activity.list` + board ekranı drawer) → Faz 6 (DEM-29 altı, `Backlog`, Low, `blockedBy` DEM-37); **[DEM-71](https://linear.app/demirkol/issue/DEM-71)** arşivli kartlar görünümü + geri yükleme (`board.get includeArchivedCards`) → Faz 8 (DEM-31 altı, `Backlog`, Low, `blockedBy` DEM-37+DEM-54); **[DEM-72](https://linear.app/demirkol/issue/DEM-72)** signup e-posta doğrulama (Better Auth `emailVerification` + Resend) → Faz 8 (DEM-31 altı, `Backlog`, Low, `blockedBy` DEM-22; DEM-68 ile birlikte yapılabilir); **[DEM-73](https://linear.app/demirkol/issue/DEM-73)** Trello-stili klavye kısayolları (n/c/// vb.) → Faz 8 (DEM-31 altı, `Backlog`, Low, `blockedBy` DEM-37+DEM-53). Hepsi assignee = proje sahibi. `02-mvp-faz-plani.md` "Faz 2.7 alt işleri"ne madde 6/7 (DEM-66/67) + "bosluk-tara 2. tur" notu + Faz 1 alt işleri madde 10 (DEM-68) + Faz 3 alt işleri Faz 3E (DEM-69) + Faz 6/8 main table satırları güncellendi. Kapsamda işaretlenenler (gürültü değil): drag-drop=Faz 3, optimistic=Faz 4, realtime=Faz 5, bildirim+@mention+due-reminder=Faz 6, arama=Faz 6.5, mobil=Faz 7, attachment+audit+favoriler+E2E=Faz 8, board-bg özelleştirme=Faz 8/2.7-noted, Liste/Etiketler board görünümleri=ileri faz. Kapanış: kullanıcı onayladı → **Done**. Kod yazılmadı. Not: DEM-68/72 Resend env (`RESEND_API_KEY`) gerektirir; auth e-postaları request-path'te Better Auth tarafından gönderilir (Faz 6 bildirim outbox'tan ayrı — `02-teknoloji-kararlari.md` Karar kaydı DEM-68 pre-dev'inde). |
| FE-2026-05-12-016 | DEM-68 | Şifre sıfırlama akışı — `(auth)/forgot-password` + `(auth)/reset-password` + sign-in "Şifremi unuttum" linki + Better Auth `sendResetPassword` → Resend transactional | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/07-auth.md` (Şifre sıfırlama akışı), `docs/architecture/08-web-ve-mobil.md` §8.1.1, `docs/architecture/02-teknoloji-kararlari.md` (Karar kaydı 2026-05-12 — Resend auth e-postaları request-path, bildirim outbox'tan ayrı), `docs/process/02-mvp-faz-plani.md` (Faz 1 alt işleri madde 10 + Faz 1 main table), `docs/process/05-is-kayit-defteri.md` | `apps/api` (`src/auth.ts` `emailAndPassword.sendResetPassword` → Resend; `src/env.ts` `RESEND_API_KEY`/`EMAIL_FROM`; `package.json` `resend` dep), `apps/web` (`(auth)/forgot-password`, `(auth)/reset-password`, `(auth)/sign-in` linki, `lib/strings.ts` `auth.*`), `packages/domain` (`schemas/auth.ts` `forgotPasswordInput`/`resetPasswordInput`) | 2026-05-12 | `bosluk-tara` 2. tur'da açılan Faz 1 ek işi ([DEM-68](https://linear.app/demirkol/issue/DEM-68)). **Ön koşul karşılandı:** `RESEND_API_KEY` kullanıcı tarafından sağlandı + `.env`'e (gitignored) eklendi (2026-05-12). Pre-dev (tamam — kontrol odası tab'ı): `07-auth.md`'ye "Şifre sıfırlama akışı" bölümü (Better Auth `requestPasswordReset`/`resetPassword` + `sendResetPassword` → Resend + best-effort dev fallback + token notu + e-posta var/yok ayırt etmeme) + `08-web-ve-mobil.md` §8.1.1'e `forgot-password`/`reset-password` ekranları + sign-in "Şifremi unuttum" linki + `02-teknoloji-kararlari.md` Karar kaydı (transactional auth e-postaları request-path'te Resend ile, Faz 6 bildirim outbox'tan AYRI; yeni `user.*` router yok; `resend` paketi `apps/api`'ye + env) + `02-mvp-faz-plani.md` Faz 1 alt işleri madde 10 + Faz 1 main table satırı. **İmplement edildi (2026-05-12):** Backend (@backend-dev): `apps/api/src/env.ts` → `RESEND_API_KEY: z.string().min(1).optional()` + `EMAIL_FROM: z.string().min(1).default('Pusula <no-reply@pusula.local>')`; yeni `apps/api/src/auth-emails.ts` (`sendResetPasswordEmail({ to, url })` + saf `resetPasswordEmailHtml`/`resetPasswordEmailText` builder'ları [`url` HTML'e `escapeHtml`'li — defense-in-depth] + `getResend()` lazy/memoized [`env.RESEND_API_KEY` yoksa `null`] + `__resetResendClientForTests`; key yoksa best-effort: `NODE_ENV==='production'` ise yalnız "e-posta gönderilemedi" loglanır [token YOK], dev'de link de loglanır; key varsa `resend.emails.send({ from: env.EMAIL_FROM, to, subject, html, text })` + `try/catch` + `{data,error}` kontrolü + `console.error`; **hiçbir durumda throw etmez**); `apps/api/src/auth.ts` → `emailAndPassword.sendResetPassword({ user, url, token }, request)` callback'i `sendResetPasswordEmail`'i çağırır + `revokeSessionsOnPasswordReset: true`; `apps/api/package.json` → `resend@^6` dep + `vitest` devDep + `test` script + yeni `apps/api/vitest.config.ts`. `@pusula/domain` `schemas/auth.ts` → `forgotPasswordInput = { email: emailSchema }` + `resetPasswordInput = { token: z.string().min(1), newPassword: passwordSchema }` + tipler (barrel yıldız re-export, ek değişiklik gerekmedi) + `schemas/auth.test.ts`. Web (@frontend-dev): yeni `(auth)/forgot-password/page.tsx` + `_components/forgot-password-form.tsx` (`authClient.requestPasswordReset({ email, redirectTo: ${window.location.origin}/reset-password })` — `redirectTo` **mutlak** [Better Auth bunu sunucu tarafında `baseURL`=`API_URL` ile çözer; göreli yol API sunucusuna giderdi → 404]; başarı/başarısızlık AYNI nötr "gönderildi" mesajı — e-posta enumeration koruması), yeni `(auth)/reset-password/page.tsx` + `_components/reset-password-form.tsx` (`useSearchParams`+`<Suspense>` ile `?token=`; token yok/boş → "geçersiz/eksik bağlantı" + forgot linki; varsa yeni parola + tekrar [client-side eşleşme] → `authClient.resetPassword({ newPassword, token })` → `/sign-in?reset=1`; Better Auth token-hatasında inline `Alert` + "yeni bağlantı iste"), `(auth)/sign-in/page.tsx` → "Şifremi unuttum?" linki + `?reset=1` flash `Alert` (+ `<Suspense>` bölme), `apps/web/next.config.ts` → `headers()` `Referrer-Policy: no-referrer` (`/reset-password` + `/forgot-password` — token query'de, Referer sızıntısına karşı), `lib/strings.ts` additive `auth.forgotPassword.*`/`auth.resetPassword.*`/`auth.signIn.forgotPassword`/`resetDone`; `(auth)` deseni (presentational form + page wrapper + `useId` + zod + `aria-*` + `Alert`); RTL testleri (`forgot-password-form`/`reset-password-form`/`forgot-password/page`/`reset-password/page`/`sign-in/page` — 22 yeni test). **Backend mantığı request-path'te** — yeni/değişen tRPC procedure yok; Faz 6 bildirim outbox'tan AYRI. **QA (2 tur):** 1. tur — code-reviewer **REQUEST CHANGES** (H1 HIGH: `redirectTo:'/reset-password'` relative → link API sunucusuna 404, akış kırık), security-reviewer **APPROVE WITH CHANGES** (M1 MEDIUM: `RESEND_API_KEY` boşsa reset token'ı `console.warn`'a düşüyor — prod misconfig riski; + L1 token URL query'de → Referer; L2 rate-limit Faz 8; L3 `(auth)/layout` edge), verifier **PASS** (407 test). Düzeltme turu (@backend-dev): H1 → `redirectTo` mutlak (`${window.location.origin}/reset-password`) + `auth.ts` yorumu + test + `07-auth.md`/`08-web-ve-mobil.md` senkron; M1 → token log'u sadece `NODE_ENV !== 'production'` + yeni "prod'da token loglanmıyor" testi; L1 → `next.config.ts` `Referrer-Policy: no-referrer`. 2. tur — code-reviewer **APPROVE WITH NITS** (H1/M1/L1 hepsi ÇÖZÜLDÜ; tek NIT = `08-web-ve-mobil.md` doc-drift → düzeltildi), security-reviewer **APPROVE** (risk temiz, yeni açık yok), verifier **PASS** (typecheck 7/7; lint 7/7; test **408** [domain 34 + api 118 + api-server 8 + web 248]; `@pusula/web` + `@pusula/api-server` build yeşil; güvenlik temiz — secret koda yazılmadı, `.env` gitignored, HTML escape var). Açık follow-up'lar (kapsam dışı, not edildi): L2 (`/api/auth/forgot-password` rate-limit → Faz 8 — Hono kabuğunda global rate-limit eklenirken), L3 (oturumlu kullanıcı reset linkinden de yönlendiriliyor — UX edge), opsiyonel (`env.ts`'te `RESEND_API_KEY` prod'da `superRefine` ile zorunlu — şu an log-guard yeterli), `EMAIL_FROM` gerçek gönderim için doğrulanmış domain gerektirir (deployment ayrıntısı); DEM-72 (signup e-posta doğrulama, Faz 8) aynı Resend kurulumunu kullanır. Kullanıcı onayladı → `Done`; commit `feat: DEM-68 — şifre sıfırlama akışı …` + push (main); Linear DEM-68 → `Done` + kapanış yorumu. `blockedBy` DEM-22 (Done). **Follow-up (aynı turda — kullanıcı talebiyle):** `EMAIL_FROM` → `Pusula <onboarding@resend.dev>` (Resend test göndereni — yalnız hesap sahibinin adresine teslim eder); web port 3000'de kaldı (kullanıcı kararı — v1'in `:2500`'ü taşınmadı); dev e-posta alıcı override eklendi — `apps/api/src/env.ts` `EMAIL_DEV_OVERRIDE` (opsiyonel, `@pusula/domain` `emailSchema`) + `apps/api/src/auth-emails.ts` `resolveRecipient` (dev'de `EMAIL_DEV_OVERRIDE` set'liyse `to`=override + uyarı logu; prod'da **kategorik olarak yok sayılır**) + `env.example` + `07-auth.md` notu + 6 yeni test; QA code-reviewer **APPROVE** + verifier **PASS** (414 test, build yeşil); commit `feat: dev e-posta alıcı override (EMAIL_DEV_OVERRIDE) — DEM-68 follow-up` + push (main); ayrı Linear issue açılmadı (tiny dev-ergonomi eki) — DEM-68 yorumuna not düşüldü. **Bununla Faz 1 (ana + ek işler) tamamen `Done`.** |
| FE-2026-05-13-001 | DEM-74 | Faz 2.7C-2 — Kart detay modalı tasarım uyumu (`13-ui-tasarim-dili.md` §13.3'e çekme) + DEM-66/67 (kart tamamlama / kapak rengi backend) UI'ye wire | 2.7 — UI tasarım dili & board/kart cilalama | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md` §8.1.5 (DEM-66/67 artık wire — "disabled placeholder" notu güncellenir), `docs/architecture/13-ui-tasarim-dili.md` §13.2 (l.165 — `CardCompleteToggle`/kapak-rengi şeridi artık wire) + §13.3 (gerekirse, kontrol odası tab'ı koordinasyonuyla), `docs/process/02-mvp-faz-plani.md` (Faz 2.7 alt işleri + Faz 2.7 satırı follow-up notu), `docs/process/05-is-kayit-defteri.md` | `apps/web` (`.../boards/[boardId]/_components/card-detail/*` + `card-item.tsx` + `card-meta-row.tsx` — kapak şeridi/tamamlanmış görünüm, minimal + `lib/strings.ts` `card.detail.*`/`board.card.*`), gerekirse `packages/ui` (`RichTextEditor`/Tiptap toolbar overflow fix) | 2026-05-13 | DEM-64 (Faz 2.7C) kart detay modalını **fonksiyonel** teslim etti ama tarayıcıdaki sonuç `13-ui-tasarim-dili.md` §13.3 ile örtüşmüyor: modal çok dar (~600px), section header'larda metin taşması, kapak-renkli başlık çubuğu yok ("Kapak rengi yakında geliyor" placeholder), sağ panel sekmesi "İşlemler" (spec "Aktivite"), sol kolon altında yatay scrollbar. Bu iş modalı §13.3'e **çeker** + DEM-66/67 backend'ini (`d6dc102` — `cards.coverColor` + `card.update({coverColor})` + `card.complete`/`uncomplete` + `board.get`/`card.get` projection'da `coverColor`/`completed`) UI'ye **wire eder**. **Fonksiyon değişmez** — backend procedure'lerine dokunulmaz; salt görsel/yapısal. 8 madde: (1) modal genişlik/grid (`w-[min(960px,92vw)]` + `[1fr_360px]` + sol `overflow-y-auto px-5 py-4 space-y-5`); (2) metin taşması → `SectionHeader` aksiyon slotu ikon-only/`DropdownMenu`, inline metin değil; (3) kapak-renkli başlık + `PaletteIcon` meta-chip → 12-renk `--palet-*` picker (`LabelSwatch`) → `card.update({coverColor})` (clear=null) + kart `card-item.tsx` kapak şeridi; (4) `CardCompleteToggle` → `card.complete`/`uncomplete`, tamamlanmış → `bg-success` tik + başlık `line-through` (modal + kart + kart metadata); (5) sekme adları "Aktivite" (`strings.card.detail.tabs.activity`); (6) sol kolon yatay scrollbar kök sebebi (muhtemelen grid `1fr` track'i `min-w-0` eksik / Tiptap içerik); (7) §13.3 cilalama; (8) mobil tek kolon. Linear: [DEM-74](https://linear.app/demirkol/issue/DEM-74) (Pusula, milestone Faz 2.7, parent DEM-58, priority Medium, assignee Abdullah; `relatedTo` DEM-53/64/66/67) → 2026-05-13 `Todo` → `In Progress`, geliştirme başladı. **İmplement edildi (2026-05-13, @frontend-dev):** `card-detail-dialog.tsx` — `DialogContent` className `flex h-[min(85vh,800px)] w-[min(960px,92vw)] max-w-none sm:max-w-none flex-col gap-0 overflow-hidden p-0` (`sm:max-w-none` ŞART — shadcn `DialogContent` base'inde `sm:max-w-lg` var, `twMerge` farklı breakpoint variant'larını çakışan saymaz → modal ~512px'e kapanıyordu; bu **kök sebepti**) + içerik grid `md:grid-cols-[1fr_360px]` + sol kolon/sticky div'e `min-w-0` (yatay scrollbar'ın diğer kök sebebi: `1fr` track'in implicit `min-width:auto`'su) + `CardModalHeader` `coverColor` wire (`asCoverColor` runtime guard) + gerçek `CardCompleteToggle` wire (`card.complete`/`card.uncomplete` ayrı mutation instance'ları, `completePending`, inline `Alert`) + `'cover'` meta-section + `CardDetailCoverColor` render + `updateCoverColor` (`card.update`) instance + `CardDetailTitle`'a `completed` geçişi; disabled placeholder Tooltip sarmalları kaldırıldı. `card-modal-meta-chips.tsx` — `CardMetaSection`'a `'cover'`; `PaletteIcon` chip artık `interactive`/`aria-expanded` (disabled değil), kapak rengi varsa `LabelSwatch` gösterir; `coverColorSoon`/`completeToggleSoon` placeholder kodu silindi. Yeni `card-detail-cover-color.tsx` — inline kapak rengi picker (12 `CARD_COVER_COLORS` swatch + "Kaldır", `Record<CardCoverColor,string>` literal `bg-palet-*` map, `aria-pressed` + focus ring; presentational). `card-modal-header.test.tsx` — coverColor varlık/yokluk → `bg-palet-{ad}` vs `bg-background border-b`. `card-modal-sidebar.tsx` — `TabLabel` helper (sayı muted `<span>`), sekme adı strings'ten ("Aktivite"), "all" boş ikonu `ActivityIcon`. `card-detail-title.tsx` — `completed` prop → `<h2>`/`<textarea>` `line-through text-muted-foreground`; "Düzenle" text butonu → `PencilIcon` ikon-only `ghost` + `Tooltip`; `placeholder` kaldırıldı (sadece `aria-label` — bleed yok); okuma `<h2>` ve editör `<textarea>` aynı `px-2 py-1 text-lg leading-tight` (zıplama yok). `card-detail-description.tsx` — "Düzenle"/"Açıklama ekle" → `PencilIcon`/`PlusIcon` ikon-only `ghost` + `Tooltip` (action slot taşması yok). `card-detail-labels.tsx` — kart etiket chip'leri `LabelChip variant="solid"` (renk `LABEL_PALETTE` ile `PaletteName`'e; bilinmeyen → fallback `span`); picker "yeni etiket" renk seçici `LABEL_SWATCH` (mevcut). `card-detail-comments.tsx` — yorum "Düzenle"/"Sil" → ikon-only `ghost` + `Tooltip`; composer alt toolbar'ına disabled `PaperclipIcon` placeholder (ek yükleme Faz 8). `checklist-add-forms.tsx`/`checklist-block.tsx`/`checklist-item-row.tsx` — add/rename/delete tetikleri ikon-only `ghost` + `Tooltip` (taşma yok); sil yine onaylı `Dialog`. `card-item.tsx` — `BoardCard` tipine `completed: boolean` + `coverColor: string | null`; kapak şeridi `-mx-2 -mt-2 mb-1.5 h-3 rounded-t-md bg-palet-{ad}` (bilinmeyen renk → render yok; literal map); başlık satırına `CardCompleteToggle` (`alwaysVisible={completed}`, `disabled={!canEdit||pending}`, `stopPropagation`, `card.complete`/`card.uncomplete` + `board.get` invalidate); tamamlanmış → başlık `line-through text-muted-foreground`. `packages/ui/src/components/rich-text-editor.tsx` — `PROSE_CLASS`'a `[&_pre]:max-w-full [&_pre]:overflow-x-auto` (uzun kod bloğu modalı taşırmaz). `packages/ui/src/components/card-complete-toggle.tsx` — `disabled` prop'u `<button>`'a + `aria-disabled={disabled||undefined}` (QA L2; `role="checkbox"` custom widget). `apps/web/src/lib/strings.ts` — `card.detail.tabs.activity` `'İşlemler'`→`'Aktivite'`, `card.activity.empty` `'Bu kartta henüz işlem yok.'`→`'Henüz aktivite yok.'`, eklendi: `card.detail.modal.completeUntoggle`/`coverColorPickerTitle`/`coverColorOf`/`coverColorClear`/`attachmentAdd` + `board.card.completeToggle`/`completeUntoggle`; silindi: `card.detail.modal.completeToggleSoon`/`coverColorSoon`. **Backend dokunulmadı** — `packages/api`/`db`/`domain` değişmedi; `card.get`/`card.update`/`card.complete`/`card.uncomplete`/`board.get` zaten DEM-64/66/67'de gerekli alanları döndürüyor (`completed`/`coverColor`). Yeni/güncel RTL: `card-detail-dialog.test.tsx` (genişlik class'ları + grid + cover picker açma), `card-modal-meta-chips.test.tsx` (cover chip interaktif/`aria-expanded`/swatch), `card-detail-cover-color.test.tsx` (12 swatch + no-op guard + remove görünürlüğü + `aria-pressed` + read-only + hata), `card-detail-title.test.tsx`/`card-modal-header.test.tsx`/`card-modal-sidebar.test.tsx`/`card-item.test.tsx`/`list-column.test.tsx` güncel. **QA (1 tur + nit-fix):** code-reviewer = APPROVE WITH NITS (CRITICAL/HIGH/MEDIUM 0; LOW: L1 cover-update no-op zaten guard'lı [non-issue], L2 `CardCompleteToggle` disabled'da `aria-disabled` yok; NIT: N1 7 ölü `strings` key'i, N2 gereksiz `as PaletteName` cast, N3 redundant `aria-disabled` placeholder'da [zararsız], N4 swatch `aria-label` ham token [pre-existing pattern — `card-detail-labels` aynı]) → **nit-fix turu:** N1 (7 key silindi), L2 (`card-complete-toggle.tsx` `aria-disabled`), N2 (cast + kullanılmayan `PaletteName` import kaldırıldı) çözüldü; N3/N4/L1 kabul/pre-existing. verifier = PASS (typecheck 7/7; lint 7/7; test **440** [domain 34 + api 118 + api-server 14 + web 274 / 71 dosya]; `@pusula/web` build yeşil; güvenlik temiz — `dangerouslySetInnerHTML` yok [JSDoc'ta], Tiptap controlled render, secret yok; git kapsam doğru — `packages/api`/`db`/`domain` dokunulmadı). Bağımlılık: DEM-61/62/63/64 + DEM-66/67 (hepsi Done). `relatedTo` DEM-53/64/66/67. Kullanıcı onayladı (2026-05-13, tarayıcı doğrulaması dahil) → `Done`; commit `feat: DEM-74 — Faz 2.7C-2: kart detay modalı tasarım uyumu + DEM-66/67 wire` (`e9849d2`) + push (main); Linear DEM-74 → `Done` + kapanış yorumu; docs senkron: bu satır + `02-mvp-faz-plani.md` (DEM-74 → Done, Faz 2.7 satırı follow-up'tan çıkarıldı) + `08-web-ve-mobil.md` §8.1.5 ("disabled placeholder" → wired DEM-74) + `13-ui-tasarim-dili.md` §13.2 l.165 ("henüz wire edilmedi" → wired DEM-74). Açık follow-up'lar (kapsam dışı, not edildi): `RichTextContent` per-comment ProseMirror instance perf'i (statik render — Faz 8 / ayrı), `commentBodySchema`/`cardDescriptionSchema` `max(20K)` JSON için darca olabilir (backend tarafı — ayrı), renk-adı yerelleştirme sözlüğü (cover + label picker `aria-label` ham token — N4). **Bununla DEM-58 (Faz 2.7) epic tamamen `Done`.** |
| DOC-2026-05-13-001 | DEM-41 | Faz 3.0 — Önce belge: Faz 3 (Drag-Drop / position) docs güncellemesi | 3 — Drag-Drop | Review | Claude Code | `docs/architecture/05-board-mekanigi.md` §5.1, `docs/domain/03-siralama-kurallari.md` (Semantik + Concurrent move + Compaction), `docs/architecture/08-web-ve-mobil.md` §8.1.8 (board ekranı drag-drop — **eklendi 2026-05-13**), `docs/process/02-mvp-faz-plani.md` (Faz 3 satırı 🚧 + Faz 3 alt işleri 1–6), `docs/kontrol-odasi/README.md` §5, `docs/process/05-is-kayit-defteri.md` | `docs/**` (+ `@pusula/domain` şema şekli notu — kod değişikliği yok) | 2026-05-13 | `faz-baslat 3` (kontrol odası tab'ı, 2026-05-13). **Yapılanlar:** `05-board-mekanigi.md` §5.1 yeniden yazıldı — Faz 3 dağılımı (3.0→3A→(3B∥3C)→3D, 3E⊇3A) + UI zorunlu ilkeleri (drag sırasında yalnız local state, `onDragEnd` tek mutation `list.move`/`card.move`, optimistic+rollback, `clientMutationId` realtime-echo ayıklama, `before`/`after` komşu + opsiyonel `newPosition`) + `moveCard`/`moveList` şekilleri (`@pusula/domain` `schemas/card.ts`/`schemas/list.ts`) + server move akışı (`card.move`: board-edit permission → kart `fromListId`'de değilse `CONFLICT` → `toListId` arşivli değil + aynı board → `positionBetween`/doğrula → tx `cards.list_id`+`position`+`activity card.moved`+`boards.version` → idempotent `clientMutationId`; `list.move` benzeri) + faz kapsamı (Faz 3A yalnız domain+activity; realtime Faz 5, outbox Faz 6, compaction Faz 3C) + test listesi. `03-siralama-kurallari.md`: Semantik'e `moveCard`/`moveList` girdileri + "hedef liste aynı board" + cross-board = `card.moveToList` (Faz 3E / DEM-69); Concurrent move'a `CONFLICT` (tRPC error code) → client board refetch netleştirildi (server yeniden-hesaplama yoluna gitmez); Compaction'a somut tetik — `POSITION_COMPACTION_MAX_LEN` (öneri default 50 karakter) worker config param'ı + ikincil satır-sayısı tetiği + debounce + `boards.version` artışı. `@pusula/domain`: `moveCardInput` (`cardId`/`fromListId`/`toListId`/`beforeCardId?`/`afterCardId?`/`newPosition?`/`clientMutationId`) ve `moveListInput` (`listId`/`beforeListId?`/`afterListId?`/`newPosition?`/`clientMutationId`) **zaten mevcut & doğru** — yeni şema gerekmedi; kontrol odası kod yazmaz. Faz planı (Faz 3 satırı `🚧` + footer + "Faz 3 alt işleri" 1–6, DEM-69 madde 6 olarak eklendi) + kontrol odası README §5 (Faz 3 tab'ları satırı + bu tab notu) + bu defter + Linear (DEM-41 → `In Progress`, DEM-42–45 + DEM-69 → `Todo`) güncellendi. **§8.1.8 eklendi (2026-05-13):** `08-web-ve-mobil.md` §8.1.8 "Board ekranı drag-drop (Faz 3B)" — Pragmatic DnD `draggable`/`dropTargetForElements`/`monitorForElements` + `closestEdge` (kart üst/alt, kolon sol/sağ) + auto-scroll; `onDrop` → komşu `before`/`after` + `positionBetween` ile `newPosition` → **tek** mutation `card.move`/`list.move` (cross-list yalnız aynı board; başka board → `card.moveToList`, Faz 3E); optimistic cache snapshot + `onError` rollback + `onSettled` `board.get` invalidate; `CONFLICT` (kart artık `fromListId`'de değil) → rollback + board refetch + kullanıcı bilgisi; yetki/arşiv (board `member+`, `viewer` salt-okunur — `draggable` mount yok; arşivli liste drop hedefi değil); erişilebilir alternatif kart/kolon ⋮ "Taşı"/"Sola-Sağa taşı" menüsü; metinler `strings.board.dnd.*`; test ayrımı RTL (komşu/edge → before/after + `positionBetween` sarmalayıcı + cache rollback) — uçtan uca Playwright Faz 3D ([DEM-45](https://linear.app/demirkol/issue/DEM-45)). **Önce-belge tamam — kullanıcı onaylayınca DEM-41 `Done`.** **Not (DEM-42 tab'ı için):** `packages/domain/src/position.ts` + `schemas/card.ts` `moveCardInput` JSDoc'ları silinmiş `docs/PUSULA_TEKNIK_MIMARI.md` §5/§6'ya atıf veriyor → güncel docs'a (`05-board-mekanigi §5.1` / `03-siralama-kurallari`) düzeltilebilir (kod tarafı — kontrol odası dokunmaz). Bağımlılık: yok; 3A backend ([DEM-42](https://linear.app/demirkol/issue/DEM-42)) bu önce-belgeye dayanıp başlayabilir. Linear: [DEM-41](https://linear.app/demirkol/issue/DEM-41) `In Progress` (Demirkol takımında ayrı `Review` state yok → kavramsal `Review` notta belirtilir) + önce-belge-tamam yorumu (2026-05-13); parent epic [DEM-26](https://linear.app/demirkol/issue/DEM-26) `Sonraki Faz` → `In Progress` (senkron düzeltmesi — Faz 3 başladı); milestone Faz 3. |
| API-2026-05-13-001 | DEM-42 | Faz 3A — `list.move` + `card.move` (backend): reorder + listeler arası taşıma + position hesabı | 3 — Drag-Drop | Done | Claude Code | `docs/architecture/05-board-mekanigi.md` §5.1 (server move akışı — bağlayıcı), `docs/domain/03-siralama-kurallari.md` (Semantik / Concurrent move / Compaction), `docs/architecture/03-backend.md` ("Faz 3 — `list.move` / `card.move`" procedure tablosu — **eklendi 2026-05-13**), `docs/domain/02-yetkilendirme-kurallari.md` ("Drag-drop / move procedure haritası (Faz 3)" — **eklendi 2026-05-13**), `packages/api/src/lib/position.ts` (yeni), `packages/domain/src/position.test.ts` (yeni), `docs/process/02-mvp-faz-plani.md`, `docs/process/05-is-kayit-defteri.md` | `packages/api`, `packages/domain` | 2026-05-13 | **`Done` (commit `7375941`, 2026-05-13 — kullanıcı onayladı).** Başlatıldı 2026-05-13 — DEM-41 `Review` (docs `05`/`03` hazır); bu turda `03-backend.md` "Faz 3 — `list.move` / `card.move`" procedure tablosu + `02-yetkilendirme-kurallari.md` "Drag-drop / move procedure haritası (Faz 3)" eklendi (koddan önce). Faz 3'ün ilk kod işi. **Middleware:** `list.move` → `boardProcedure` (`moveListInput` `boardId` taşır, `createListInput` ile aynı disiplin); `card.move` → `cardProcedure` (`moveCardInput` `cardId`-anahtarlı; doc taslağındaki `boardProcedure` yazımı `cardProcedure` okunur — tüm `card.*` ile tutarlı). Board `member+` = `canEditBoardContent`. `card.move` (`moveCardInput`): (1) permission (`ctx.card.boardRole`), (2) kart hâlâ `fromListId`'de değilse `CONFLICT`, (3) `toListId` arşivli değil + `ctx.card.boardId`'ye ait, arşivli board reddi, (4) `newPosition` doğrula veya `positionBetween` ile hesapla, (5) tx: `cards.list_id`+`position` + `activity_events` `card.moved` (from/to + eski/yeni position) + `boards.version+1`, (6) idempotent (`clientMutationId` → no-op; zaten o liste+konum → `changed:false`). `list.move` (`moveListInput`+`boardId`): board içi reorder, `positionBetween`/doğrula, tx + `activity_events` `list.moved` + `boards.version+1`, idempotent. Faz 3A yalnız domain+activity — realtime Faz 5, outbox Faz 6, compaction tetiği Faz 3C (DEM-44; bu turda eşiği aşan position üretilince enqueue hook'u için no-op stub bırakılabilir ya da DEM-44 ekler). `ACTIVITY_EVENT_TYPES` + `05-aktivite-kurallari.md`'de `list.moved`/`card.moved` zaten var (kontrol edildi 2026-05-13). Test: domain birim (`positionBetween`/`positionsBetween` edge case'ler) + tRPC integration (liste-içi reorder, cross-list, kart taşınmış→`CONFLICT`, arşivli hedef, başka board reddi, idempotency, `viewer` permission reddi). **Not:** `position.ts`/`schemas/card.ts` JSDoc'larındaki stale `PUSULA_TEKNIK_MIMARI.md` atıfları güncel docs'a düzeltildi. **DEM-74 ile paralel** — `apps/web` dokunulmaz, çakışmaz. Dev-QA loop: @backend-dev → @code-reviewer + @verifier → onay → commit → Linear `Done`/`Review` + kapanış. Linear: [DEM-42](https://linear.app/demirkol/issue/DEM-42), parent DEM-26, milestone Faz 3, `blockedBy` Faz 2A/2B/2C + DEM-41. Bağımlılık: Faz 2A/2B/2C (`Done`) + Faz 3.0 (`05`+`03` kısmı tamam). 3B/3C/3E buna bağımlı. **QA PASS (2026-05-13):** code-reviewer **APPROVE** (CRITICAL/HIGH/MEDIUM 0; 2 ops. LOW follow-up — arşivli *kartı* taşıma engellenmiyor [bilinçli, `card.update`/`card.complete` ile tutarlı]; `resolveMovePosition` ters-komşu catch→`BAD_REQUEST` router seviyesinde test edilmemiş) + verifier **PASS** (464 test 0 hata — `@pusula/domain` 44 [`position.test.ts` +11] / `@pusula/api` 132 [`list.move` +5 / `card.move` +9]; `pnpm -w typecheck`+`lint`+`build` temiz; güvenlik temiz — secret/`console.log` yok, `realtime_events`/`notification_outbox` sadece JSDoc'ta; yeni migration yok; kapsam doğru — `apps/*`/`packages/ui`/`packages/db` dokunulmadı). Yeni dosyalar: `packages/api/src/lib/position.ts` (`assertPositionBetween`/`resolveMovePosition`), `packages/domain/src/position.test.ts`. Linear yorum eklendi (2026-05-13). **Kapanış:** kullanıcı onayladı → commit `7375941` (`feat: DEM-42 — Faz 3A: list.move + card.move (drag-drop backend) + docs`, 13 dosya +852/−33), Linear DEM-42 `Done`. **Follow-up:** Faz 3C (DEM-44) compaction enqueue çağrısını `list.move`/`card.move`'a ekleyecek (şu an `// TODO(DEM-44/Faz 3C)` stub); ops. arşivli kart move guard'ı + `resolveMovePosition` ters-komşu router testi (LOW). |
| FE-2026-05-13-002 | DEM-43 | Faz 3B — Drag-drop UI (web): Pragmatic DnD ile liste/kart reorder + listeler arası taşıma | 3 — Drag-Drop | In Progress | Claude Code | `docs/architecture/05-board-mekanigi.md` §5.1 (UI ilkeleri — bağlayıcı), `docs/architecture/08-web-ve-mobil.md` §8.1.8 (board ekranı drag-drop — DEM-41'de yazıldı; bağlayıcı), `docs/process/05-is-kayit-defteri.md` | `apps/web` (`.../boards/[boardId]/_components/` board kolon bileşenleri + `card-item.tsx` + `list-column.tsx` + `board-columns.tsx` + drag handler/hook + `lib/strings.ts`), `package.json` (`@atlaskit/pragmatic-drag-and-drop` + `-hitbox` + `-auto-scroll` dep) | 2026-05-13 | Başlatıldı 2026-05-13 (DEM-42 `Done` → `blockedBy` çözüldü; önce-belge `08-web §8.1.8` DEM-41'de tamam — yeni docs gerekmez). Board ekranına (DEM-37) Atlassian Pragmatic Drag and Drop: kolon reorder · kart reorder (liste içi) · kart cross-list taşıma. Drag sırasında **backend mutation atılmaz** — local state + placeholder/drop-indicator; `onDrop` → `before`/`after` komşu + `positionBetween` → `list.move`/`card.move` (tek istek) + optimistic cache update (`queryClient.setQueryData(trpc.board.get…)`) + `onSettled` invalidate + `onError` rollback + `CONFLICT` → rollback+refetch+"başkası taşıdı" bilgisi; `clientMutationId` istemcide (`crypto.randomUUID()`). Yetki: `viewer` → draggable mount edilmez; arşivli liste drop hedefi değil. Erişilebilir alternatif: kart ⋮ "Taşı" picker + kolon ⋮ "sola/sağa taşı" (`strings.board.dnd.*`). dnd-kit vb. **yok**. Tam optimistic cache modeli Faz 4 — bu fazda invalidate+refetch yeterli ama drag UX akıcı. Test: RTL — komşu/edge → `before`/`after` + `positionBetween` saf fonksiyon, cache optimistic update + rollback (mock `queryClient`); Pragmatic DnD uçtan uca Playwright (Faz 3D/DEM-45). Dev-QA loop: @frontend-dev → @code-reviewer + @verifier → commit → Linear `Done` + kapanış. Linear: [DEM-43](https://linear.app/demirkol/issue/DEM-43), parent DEM-26, milestone Faz 3, `blockedBy` DEM-42 (`Done`) + DEM-37 (`Done`). Bağımlılık: Faz 3A + Faz 2D. 3D (DEM-45) buna bağımlı. |
| API-2026-05-13-002 | DEM-44 | Faz 3C — Position compaction worker job | 3 — Drag-Drop | Todo | Claude Code | `docs/domain/03-siralama-kurallari.md` "Compaction" (tetik/eşik — bağlayıcı), `docs/architecture/06-bildirim-altyapisi.md` (worker job'ları — DEM-44 turunda satır), `docs/architecture/10-platform.md`, `docs/process/05-is-kayit-defteri.md` | `apps/worker` (BullMQ job), gerekirse `packages/db`/`packages/domain` (helper), `apps/api` (move sonrası enqueue hook — DEM-42 ile koordine) | 2026-05-13 | Fractional `position` string'i eşiği (`POSITION_COMPACTION_MAX_LEN`, öneri default 50) aşan key üretildiğinde, ilgili scope (liste içi kartlar / board içi listeler) için compaction job kuyruğa atılır → job o scope'u `positionsBetween(null,null,n)` ile kısa/eşit aralıklı pozisyonlara yeniden yazar; `boards.version+1`; aynı scope kuyruktaysa tekrar enqueue edilmez (debounce). Activity event üretmez (teknik bakım); realtime echo Faz 5+. İkincil tetik (ops.): scope satır sayısı > eşik → periyodik dengeleme. Enqueue noktası `card.move`/`list.move` (DEM-42) içinde — DEM-42 ile koordine (ya DEM-42 hook bırakır ya DEM-44 ekler). Test: uzun-key senaryosu compaction tetikler + yeniden yazım sırayı korur + idempotent. **DEM-74 ile çakışmaz.** Linear: [DEM-44](https://linear.app/demirkol/issue/DEM-44), parent DEM-26, milestone Faz 3, `blockedBy` DEM-42. Bağımlılık: Faz 3A; 3B ile paralel. |
| FE-2026-05-13-003 | DEM-45 | Faz 3D — Playwright drag-drop testleri (kurulum + reorder/cross-list/rollback) | 3 — Drag-Drop | Todo | Claude Code | `docs/architecture/05-board-mekanigi.md` §5.1 (test listesi), `docs/process/05-is-kayit-defteri.md` (+ gerekirse `08-web-ve-mobil.md` test notu) | `apps/web` veya repo kökü (Playwright config + fixture'lar + e2e spec'leri), CI temel entegrasyon | 2026-05-13 | Playwright e2e harness'ı kur (yoksa) + drag-drop akışları: (1) liste reorder, (2) kart reorder (aynı liste), (3) kart cross-list taşıma, (4) `card.move` başarısızlık → optimistic rollback, (ops.) eşzamanlı kullanıcı / kart taşınmışken move (`CONFLICT`). Fixture: auth'lu kullanıcı + seed board/list/card. Kurulum yeri (apps/web vs repo kökü) DEM-45 turunda kararlaştırılır. Geniş e2e suite → Faz 8 (DEM-31). **BLOCKED:** Faz 3B (DEM-43). Linear: [DEM-45](https://linear.app/demirkol/issue/DEM-45), parent DEM-26, milestone Faz 3, `blockedBy` DEM-43. Bağımlılık: Faz 3B. |
| API-2026-05-13-003 | DEM-69 | Faz 3E — `card.moveToList` (cross-board) + `card.copy` | 3 — Drag-Drop | Todo | Claude Code | `docs/architecture/03-backend.md` (`card.moveToList`/`card.copy` procedure — DEM-69 turunda), `docs/domain/03-siralama-kurallari.md` (cross-board move kuralı — notu DEM-41'de eklendi), `docs/domain/01-urun-modeli.md` / `docs/domain/02-yetkilendirme-kurallari.md` (cross-board invariant + yetki), `docs/process/05-is-kayit-defteri.md` | `packages/api`, `packages/domain`, gerekirse `apps/web` (kart ⋮ menüsü "Taşı"/"Kopyala") | 2026-05-13 | `card.move` (DEM-42) yalnız board-içi; **başka board'a taşıma** + **kart kopyalama** ayrı. `card.moveToList` (`cardProcedure`/`protectedProcedure`; hedef listenin board'una `resolveBoardAccess` ile erişim + board `member+` + arşiv kontrolü; kartın `board_id`+`list_id`+`position` değişir; checklist/yorum/etiket/üye bağları — etiketler board-scoped olduğu için düşürülür mü / eşlenir mi — issue'da netleştirilir; `activity_events`). `card.copy` (yeni kart + içerik kopyası: açıklama/checklist; yorum/aktivite kopyalanmaz; hedef liste/board seçimi; `activity_events` `card.created`). Trello "Move"/"Copy"; 2.7C modal ⋮ "taşı/kopyala (ileri faz)" → bu iş. `bosluk-tara` 2. tur. Linear: [DEM-69](https://linear.app/demirkol/issue/DEM-69), parent DEM-26, milestone Faz 3, `blockedBy` DEM-42. Bağımlılık: Faz 3A (DEM-42). |
| PROC-2026-05-13-001 | DEM-75 | Token disiplini — workflow docs inceltme + iş kayıt defteri kompakt planı | Süreç | In Progress | Claude Code | `CLAUDE.md` §1/§4, `docs/process/04-otomatik-is-akisi-protokolu.md`, `.claude/skills/kontrol/SKILL.md`, bu dosya | `docs/process` | 2026-05-13 | Yapıldı: docs okuma "eksen README + gereken numaralı dosya"a daraltıldı; Linear MCP yalnızca Pre-Dev + closeout — ara turlarda Linear API yok, durum bu defterde tutulur, Linear'a closeout'ta toplu yansır. Bekliyor (kullanıcı onayı): bu dosyanın "Not" kolonu kompakt ~73k→~12-15k token (kapalı satırlarda 1-2 cümle + commit/DEM ref; aktif satırlar tam). Proje dışı: `~/.claude/rules/archive/learned-*` + `vibecosystem-welcome.md` silindi, bayat `.claude/worktrees/eager-chatterjee-074e94` git-metadata temizlendi. Detay → [DEM-75](https://linear.app/demirkol/issue/DEM-75). |

> Faz 2 epic'i ([DEM-25](https://linear.app/demirkol/issue/DEM-25)) bölündü → DEM-33–37 yukarıdaki tabloda
> (hepsi `Done`; DEM-25 altında — Faz 2 tamamlandı). Faz 2.5 epic'i ([DEM-48](https://linear.app/demirkol/issue/DEM-48)) `Done`: DEM-49–54 hepsi `Done` (2026-05-12; defter satırları güncel). Faz 1 tamamen `Done` (DEM-55 — `FE-2026-05-12-007`; DEM-68 şifre sıfırlama — `FE-2026-05-12-016` dahil). **Faz 2.7 epic'i ([DEM-58](https://linear.app/demirkol/issue/DEM-58)) `Done` (2026-05-12)** — UI cilalama tamamlandı: 2.7.0 önce-belge (`13-ui-tasarim-dili.md`, `DOC-2026-05-12-007`) + 2.7A ([DEM-62](https://linear.app/demirkol/issue/DEM-62) — tema+token+`packages/ui`, `FE-2026-05-12-011`, commit `0e28564`) + 2.7B ([DEM-63](https://linear.app/demirkol/issue/DEM-63) — board ekranı + `board.get` additive, `FE-2026-05-12-012`, commit `389ee00`) + 2.7C ([DEM-64](https://linear.app/demirkol/issue/DEM-64) — kart detay modalı+Tiptap, `FE-2026-05-12-013`, commit `96c4bd6`) + 2.7D ([DEM-65](https://linear.app/demirkol/issue/DEM-65) — workspace/app-shell+a11y, `FE-2026-05-12-015`) hepsi `Done`; DEM-66/67 (kart tamamlama/kapak rengi backend, `FE-2026-05-12-014`, ayrı commit `d6dc102`) `Done`; DEM-74 (2.7C-2 — kart detay modalı §13.3 tasarım uyumu + DEM-66/67 wire, `FE-2026-05-13-001`, commit `e9849d2`) `Done` (2026-05-13 — modal genişlik/iki-kolon + kapak-renkli başlık + kapak rengi picker + `CardCompleteToggle` wire + "İşlemler"→"Aktivite" + overflow fix; modal tamamen §13.3'te). **Faz 3 epic'i ([DEM-26](https://linear.app/demirkol/issue/DEM-26)) `🚧 In Progress` (2026-05-13 başladı — `faz-baslat 3`; Linear `Sonraki Faz` → `In Progress` senkron düzeltmesi)** — 3.0 önce-belge ([DEM-41](https://linear.app/demirkol/issue/DEM-41) / `DOC-2026-05-13-001`) `Review` (kontrol odası tab'ı; `05-board-mekanigi §5.1` + `03-siralama-kurallari` + `08-web §8.1.8` "Board ekranı drag-drop" + `@pusula/domain` move-input şekilleri tamam — önce-belge bitti, kullanıcı onayı bekliyor), 3A ([DEM-42](https://linear.app/demirkol/issue/DEM-42) / `API-2026-05-13-001`) `Done` (2026-05-13, commit `7375941` — koddan önce `03-backend.md` "Faz 3 — `list.move` / `card.move`" procedure tablosu + `02-yetkilendirme-kurallari.md` "Drag-drop / move procedure haritası (Faz 3)" eklendi; `list.move`→`boardProcedure`, `card.move`→`cardProcedure`; code-reviewer APPROVE + verifier PASS [464 test]; follow-up: 3C compaction enqueue); 3B ([DEM-43](https://linear.app/demirkol/issue/DEM-43) / `FE-2026-05-13-002`) `🚧 In Progress` (2026-05-13 başladı — DEM-42 `Done` ile bloklayıcı çözüldü; `08-web §8.1.8` DEM-41'de yazılmıştı, yeni docs gerekmez); 3C ([DEM-44](https://linear.app/demirkol/issue/DEM-44) / `API-2026-05-13-002`) · 3D ([DEM-45](https://linear.app/demirkol/issue/DEM-45) / `FE-2026-05-13-003`) · 3E ([DEM-69](https://linear.app/demirkol/issue/DEM-69) / `API-2026-05-13-003`) `Todo` (sıra: 3A → 3B∥3C → 3D, 3E⊇3A; 3C/3E 3A'ya bağımlı, 3D 3B'ye). Faz 4–8 faz-seviyesi epic'leri Linear'da `Backlog`'tadır
> ([DEM-27](https://linear.app/demirkol/issue/DEM-27)=Faz 4, [DEM-28](https://linear.app/demirkol/issue/DEM-28)=Faz 5, [DEM-29](https://linear.app/demirkol/issue/DEM-29)=Faz 6,
> [DEM-30](https://linear.app/demirkol/issue/DEM-30)=Faz 7, [DEM-31](https://linear.app/demirkol/issue/DEM-31)=Faz 8; Pusula projesi, ilgili faz milestone'una bağlı).
> Faz statüsü [`02-mvp-faz-plani.md`](02-mvp-faz-plani.md)'de tutulur; ilgili fazın geliştirmesi başlayınca epic alt issue'lara
> bölünür (henüz başlamamış faz'ın işleri `Sonraki Faz`'da, mevcut fazınkiler `Todo`/`In Progress`'te) ve her alt iş bu defterde satır olarak açılır.

## Güncelleme kuralları

- Aynı iş için ikinci satır açma; mevcut satırı güncelle.
- `Son senkron` tarihi Linear veya docs tarafındaki son bilinçli güncelleme tarihidir.
- Faz değişirse `docs/process/02-mvp-faz-plani.md` de aynı çalışma turunda güncellenir.
- Kapanan işlerde test/verification sonucu Linear kapanış yorumunda, kısa notu bu dosyada tutulur.
