---
title: "05 — İş Kayıt Defteri"
description: "Linear işlerinin repo içindeki takip aynası."
aliases:
  - "İş Kayıt Defteri"
  - "Work Register"
tags:
  - "pusula"
  - "process/work-register"
  - "linear"
type: "register"
axis: "process"
status: "active"
parent: "[[docs/process/README|Süreç]]"
updated: 2026-05-12
---
# 05 — İş Kayıt Defteri

> Eksen: **süreç**. Bu dosya Linear'ın repo içindeki takip aynasıdır. Her geliştirme,
> refactor, bug fix, dokümantasyon ve infra işi için tek satır tutulur.

Durumlar `Backlog`, `Sonraki Faz`, `Todo`, `In Progress`, `Review`, `Done`, `Canceled` setinden seçilir
(Linear karşılıkları ve "ayrı state olmayan" durumlar için kural → [`04-otomatik-is-akisi-protokolu.md`](04-otomatik-is-akisi-protokolu.md) "Durum sözlüğü";
Demirkol takımında `Review` için ayrı state yok → Linear'da `In Progress` + notta belirtilir; `Blocked` için ayrı state yok → bloke iş `Todo`/`Backlog`'ta kalır + `blockedBy` ilişkisi + notta belirtilir).
Linear MCP erişilemiyorsa `Linear` alanına `MCP bekliyor` yazılır ve ilk uygun oturumda Linear issue ile eşlenir.

| İş ID | Linear | Başlık | Faz | Durum | Sahip | Etkilenen belgeler | Etkilenen katmanlar | Son senkron | Not |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DOC-2026-05-12-001 | DEM-16 | Otomatik iş akışı protokolünü kur | Süreç | Done | Claude Code | `CLAUDE.md`, `.claude/skills/kontrol/SKILL.md`, `docs/README.md`, `docs/process/*` | `docs/process` | 2026-05-12 | Linear issue [DEM-16](https://linear.app/demirkol/issue/DEM-16) ile eşlendi (proje: Pusula). |
| DOC-2026-05-12-002 | DEM-17 | Obsidian kasası dokümantasyon düzeni | Süreç | Done | Codex | `README.md`, `CLAUDE.md`, `.claude/skills/kontrol/SKILL.md`, `docs/**/*.md` | `docs` | 2026-05-12 | 31 Markdown dosyasında frontmatter/link kontrolü geçti; Linear issue [DEM-17](https://linear.app/demirkol/issue/DEM-17) ile eşlendi (proje: Pusula). |
| INFRA-2026-05-12-001 | DEM-18 | İlk commit ve GitHub remote (`pusula_v2`) push | 0 — Temel altyapı | Done | Claude Code | `.gitignore`, `docs/process/05-is-kayit-defteri.md` | repo kökü | 2026-05-12 | Faz 0 iskelesinin tamamı tek commit ile yüklendi; `master` → `main`, remote: github.com/demirkolorg/pusula_v2. `.obsidian/` gitignore'a eklendi. Linear issue [DEM-18](https://linear.app/demirkol/issue/DEM-18) ile eşlendi (proje: Pusula). |
| DOC-2026-05-12-003 | DEM-19 | Genel çalışma başlangıç rehberi oluştur | Süreç | Done | Codex | `CLAUDE.md`, `.claude/skills/kontrol/SKILL.md`, `docs/README.md`, `docs/process/00-calisma-baslangic-rehberi.md`, `docs/process/03-faz-0-devir-notu.md`, `docs/process/04-otomatik-is-akisi-protokolu.md`, `docs/process/README.md` | `docs/process` | 2026-05-12 | Yeni işlerde Faz 0 devir notu yerine genel başlangıç rehberi baz alınacak. Linear issue [DEM-19](https://linear.app/demirkol/issue/DEM-19) ile eşlendi (proje: Pusula). |
| API-2026-05-12-001 | DEM-20 | Faz 1A — Workspace alanı (backend): `workspaceProcedure` + CRUD + üyelik + permission enforcement | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/architecture/07-auth.md`, `docs/process/02-mvp-faz-plani.md` | `packages/api`, `packages/domain`, `packages/db` | 2026-05-12 | İmplement edildi: `workspaceProcedure` (membership middleware), `workspace.{list,create,get,update,archive}` + `workspace.members.{list,updateRole,remove}`, activity yazımı (`workspace.created/updated/archived/member_role_changed/member_removed`) ilgili transaction'da. `ACTIVITY_EVENT_TYPES`'a 3 yeni değer + Drizzle migration `drizzle/0001_loud_kinsey_walden.sql`. Vitest kuruldu: 15 domain birim + 8 tRPC integration testi PASS. QA (code-reviewer + security-reviewer + database-reviewer) = PASS; review M1 bulgusu düzeltildi (slug çakışmasında PG `23505` → `CONFLICT` map'lenir, check-then-write yarışına karşı). verifier = PASS (`typecheck`/`lint`/`test` yeşil, secret taraması temiz). Commit: `97cd9b8` (main, push edildi → origin). Linear issue [DEM-20](https://linear.app/demirkol/issue/DEM-20) ile eşlendi (proje: Pusula); kullanıcı onayladı → Linear durumu `Done` (2026-05-12, kapanış yorumu eklendi). Kalan: davet-token akışı + `workspace.delete` ayrı kayıt (bkz. `docs/process/02-mvp-faz-plani.md` → Faz 1 alt işleri). |
| PROC-2026-05-12-001 | DEM-21 | Linear senkronu: bekleyen iş kayıtlarını Linear'a eşle + Pusula projesi | Süreç | Done | Claude Code | `docs/process/05-is-kayit-defteri.md` | `docs/process` | 2026-05-12 | Linear MCP callable hâle geldi; `Pusula` projesi açıldı (state: In Progress), DOC/INFRA/API satırları DEM-16…DEM-20 ile eşlendi, bu iş [DEM-21](https://linear.app/demirkol/issue/DEM-21). Re-auth sonrası DEM-20 → Done, FE-2026-05-12-001 → [DEM-22](https://linear.app/demirkol/issue/DEM-22); 5 Mayıs test/çöp kayıtları temizlendi (DEM-10–15: DEM-10/11/12/14/15 arşivde, DEM-13 iptal edildi + yeniden adlandırıldı). `MCP bekliyor` kalmadı. |
| FE-2026-05-12-001 | DEM-22 | Faz 1B — Auth web UI & session: sign-up/in/out, korumalı kabuk, workspace listesi/oluşturma | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md`, `docs/architecture/07-auth.md`, `docs/process/02-mvp-faz-plani.md` | `apps/web`, `packages/ui`, `packages/domain` | 2026-05-12 | Pre-dev: ilgili docs koddan önce güncellendi (`08-web-ve-mobil.md` §8.1.1 — auth ekranları & oturum, client-side session, korumalı route group, shadcn bileşen ekleme; `07-auth.md` web client notu). İmplement edildi: `@pusula/domain/schemas/auth.ts` (`signInInput`/`signUpInput` + `email/password/name` şemaları, barrel'a eklendi); `@pusula/ui`'ye shadcn `Input`/`Label`/`Card`/`Dialog`/`Alert` (Radix `react-dialog`+`react-label`, `tw-animate-css`) eklenip `src/index.ts` + `package.json` `exports`'tan dışa aktarıldı; `apps/web` route group'lar: `(auth)/{layout,sign-in,sign-up}` + paylaşılan presentational `_components/auth-form.tsx`, `(app)/{layout,page}` + `_components/{app-shell,create-workspace-dialog}` (korumalı kabuk client-side `useSession`, `?redirect=` koruması; workspace listesi `trpc.workspace.list`, oluşturma `trpc.workspace.create` + `queryClient.invalidateQueries(trpc.workspace.list.queryFilter())`); eski demo `app/page.tsx` silindi; `apps/web/src/env.ts` (Zod, `NEXT_PUBLIC_API_URL`) eklendi, `auth-client.ts` + `trpc/client.tsx` bunu kullanıyor; kullanıcı-yüzlü Türkçe metinler `apps/web/src/lib/strings.ts`'te toplandı (i18n placeholder, lib eklenmedi). Test: `apps/web`'e Vitest+RTL (`vitest.config.ts`/`vitest.setup.ts`, jsdom) kuruldu, `auth-form.test.tsx` 6 test PASS. Tüm suite: 29 test PASS (15 domain + 8 api + 6 web); `pnpm typecheck`/`lint`/`@pusula/web build` (route'lar: `/`, `/sign-in`, `/sign-up`) yeşil. Atlanan: Playwright e2e (sign-up→workspace→çıkış) ileri faz; davet-token akışı ayrı kayıt. QA (code-reviewer + security-reviewer) = PASS; bulgular düzeltildi — H1 open-redirect (`?redirect=` artık `safeRedirectPath` ile doğrulanır, `//evil.com` vb. engellenir; redirect tek noktada — `(auth)` layout'taki `RedirectIfAuthenticated`), M4 (`AppShell.signOut` catch), L4 (`z.url`), M1 (`apps/web/tsconfig.json` `types` daraltıldı). verifier = PASS (`typecheck`/`lint`/`@pusula/web build`/`test` — 15 domain + 8 api + 6 web — yeşil, secret taraması temiz). `.gitignore`'a `.claude/worktrees/` + `.claude/cache/` eklendi. Commit: `cefd148` (main, push edildi → origin). Kullanıcı onayladı → durum `Done` (2026-05-12); DEM-22'nin Linear durumu da `Done`'a çekilecek (Linear MCP re-auth bekliyor). Linear issue [DEM-22](https://linear.app/demirkol/issue/DEM-22) ile eşlendi (proje: Pusula); DEM-20'ye bağımlı (Done). Açık (low/info, ertelendi): `api-status.tsx` artık referanssız (Faz 0 demo widget'ı — bilinçli bırakıldı), `.claude/worktrees/eager-chatterjee-074e94` stray agent worktree'si (gitignore'lu; `git worktree prune` ile temizlenebilir). |
| API-2026-05-12-002 | DEM-23 | Faz 1.3 — Workspace davet akışı: davet token tablosu + invite/accept/decline + bildirim + web UI | 1 — Auth & Workspace | Done | Claude Code | `docs/domain/01-urun-modeli.md`, `docs/architecture/04-veri-katmani.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/domain/05-aktivite-kurallari.md`, `docs/architecture/08-web-ve-mobil.md`, `docs/process/02-mvp-faz-plani.md` | `packages/db`, `packages/api`, `packages/domain`, `packages/ui`, `apps/web` | 2026-05-12 | Pre-dev: davet akışı koddan önce docs'a işlendi — `workspace_invitations` entity (`01-urun-modeli.md`) + tablo/kolonlar + `invitation_status` enum (`04-veri-katmani.md`), davet procedure haritası `members.invite` / `invitations.{list,revoke,mine,accept,decline}` + e-posta-eşleşme/tek-kullanımlık/expiry kuralları (`02-yetkilendirme-kurallari.md`), yeni activity tipleri `workspace.member_invited` + `workspace.invitation_revoked` (`05-aktivite-kurallari.md`), `notification_outbox.recipient_id` nullable notu. **Backend tamam** (web davet UI'ı ayrı tur): `@pusula/domain` — `INVITATION_STATUSES` + `WORKSPACE_INVITATION_TTL_DAYS` (=7) sabitleri, `ACTIVITY_EVENT_TYPES`'a `workspace.member_invited`/`workspace.invitation_revoked` (append), `schemas/invitation.ts` (`invitationStatusSchema`, `invitationTokenSchema`, `revoke/accept/declineWorkspaceInvitationInput`); `@pusula/db` — `invitation_status` pgEnum, `workspace_invitations` tablosu (token uniq idx + (workspace,status)/(email) idx), `notification_outbox.recipient_id` `notNull` kaldırıldı (nullable; e-posta daveti hesapsız adrese gidebilir), Drizzle migration `drizzle/0002_omniscient_lady_ursula.sql` (+ QA sonrası `drizzle/0003_pretty_daredevil.sql` — `workspace_invitations_pending_email_uq` partial unique index `(workspace_id, lower(email)) WHERE status='pending'`) üretildi + lokal Postgres'e uygulandı (`pnpm db:migrate`); `@pusula/api` — `workspace.members.invite` (`admin+`, e-posta lowercase, zaten üye/zaten pending → `CONFLICT`, `node:crypto.randomBytes` token, `workspace_invitations` + `activity_events`(`workspace.member_invited`) + `notification_outbox`(`email` + hesap varsa `in_app`) tek transaction'da, token response'da dönmez) + yeni `workspace.invitations` router'ı (`list` member+, `revoke` admin+ → `workspace.invitation_revoked`, `mine`/`accept`/`decline` `protectedProcedure`; `accept` e-posta eşleşme + expiry [süresi dolmuşsa `status=expired`, tx dışında durable] + idempotent üye ekleme + `workspace.member_added`; `decline` → `status=declined`, activity yok — bilinçli). Test: `workspace.test.ts`'e toplam 17 yeni invitation integration testi (api 25 PASS; suite 46 — 15 domain + 25 api + 6 web; DB yoksa invitation/integration testleri skip). QA (code-reviewer + security-reviewer + database-reviewer) = PASS; bulgular düzeltildi — H1 (`workspace_invitations_pending_email_uq` partial unique index + `23505`→`CONFLICT` map, migration `0003`), H2 (`accept`'te `workspace_members` insert `.onConflictDoNothing()` + invitation satırı `.for('update')` — tam idempotent, eşzamanlı accept güvenli), M1 (`inviteWorkspaceMemberInput.email` → `emailSchema` trim+lowercase; `accept`/`decline` oturum e-postası da `.trim().toLowerCase()`), low (`invite` outbox satırları artık `eventId`'ye bağlı). verifier = PASS. Backend commit: `bca834a` (main, push edildi → origin). **Web davet UI tamam** (frontend-dev; `08-web-ve-mobil.md` §8.1.1 güncellendi): `apps/web/(app)/_components/{invite-member-form (presentational),invite-member-dialog,pending-invitations}.tsx` + `@pusula/ui` `Badge`; workspace kartında — `role` `owner`/`admin` ise — "davet et" dialog'u (e-posta-only, `members.invite`, `CONFLICT` inline) + workspace listesi üstünde "Bekleyen davetler" bölümü (`invitations.mine` → satır başına accept/decline, kabul → `workspace.list`+`invitations.mine` invalidate); `clientMutationId` istemcide; metinler `strings.invitations`'a eklendi; RTL `invite-member-form.test.tsx` 5 test. Web QA (code-reviewer) = PASS (yalnızca low/info: kullanılmayan `inviteSent`/`noPending` string'leri, `MineInvitation` tipi router'dan infer edilmiyor — ertelendi); verifier = PASS (`typecheck`/`lint`/`@pusula/web build`/`test` — 15 domain + 25 api + 11 web — yeşil, secret temiz). DEM-23'ün her iki parçası (backend + web UI) tamam; **kullanıcı onayladı → durum `Done` (2026-05-12)**. Bununla Faz 1'in ana işleri (1, 2, 3) tamam — kalan: `workspace.delete` (`API-2026-05-12-003` / DEM-24, `Backlog` ek iş). Atlanan/ertelenen: davet dialog'unda rol seçimi, gönderilmiş davet yönetimi UI'ı (`invitations.list`/`revoke` — workspace üyeler ekranıyla), worker email/in-app teslimi (Faz 6 — bu turda yalnızca `notification_outbox` satırı yazılır), realtime yayın (Faz 5). Docs ile çelişki yok. Linear issue [DEM-23](https://linear.app/demirkol/issue/DEM-23) (proje: Pusula, milestone: Faz 1); DEM-20'ye bağımlı (Done). DEM-23'ün Linear durumu da `Done`'a çekilecek — Linear MCP token süresi doldu, re-auth/kullanıcı tarafından. |
| API-2026-05-12-003 | DEM-24 | `workspace.delete` — workspace kalıcı silme (owner-only, ad onayı, cascade) | 1 — Auth & Workspace | Done | Claude Code | `docs/domain/02-yetkilendirme-kurallari.md`, `docs/process/02-mvp-faz-plani.md` | `packages/api`, `packages/domain` | 2026-05-12 | Pre-dev: tasarım çelişkisi (soft vs hard delete) kullanıcıya soruldu → **kullanıcı kararı: hard delete** (owner-only, `confirmName` ad onayı, `DELETE FROM workspaces` cascade; `archive` ayrı kavram; cascade nedeniyle DB içi iz yok). `02-yetkilendirme-kurallari.md` `workspace.delete` satırı + `02-mvp-faz-plani.md` madde 4 koddan önce güncellendi. Plan: `@pusula/domain` — `deleteWorkspaceInput` (`{ workspaceId, confirmName: workspaceNameSchema, clientMutationId }`); `@pusula/api` — `workspace.delete` (`workspaceProcedure`, `ctx.workspace.role !== 'owner'` → `FORBIDDEN`, `confirmName !== workspace.name` → `BAD_REQUEST`, tx `DELETE FROM workspaces WHERE id = ?` — cascade members/invitations/(ileride boards…), dön `{ id, deleted: true }`); şema değişikliği yok; activity/notification iz yok (cascade siler); Vitest (owner-only, ad onayı, silme + cascade kontrolü). `root.ts` zaten `workspace` mount. **İmplement edildi** (backend-dev): `@pusula/domain` `deleteWorkspaceInput`; `@pusula/api` `workspace.delete` (workspaceProcedure, owner-only, `confirmName` ad onayı, tx `DELETE`...cascade, returning boşsa `NOT_FOUND`); `workspace.test.ts` +4 testi (api 29 PASS). QA (code-reviewer + security-reviewer) = PASS — low/info: arşivli workspace `delete` ile silinemiyor (`workspaceProcedure` arşivliyi `NOT_FOUND` verir; ileri faz iyileştirmesi), büyük workspace cascade async/audit-log Faz 2+. verifier = PASS (`typecheck`/`lint`/`test` yeşil, secret temiz). Commit: `7c91527` (main, push edildi → origin). Kullanıcı onayladı → durum `Done` (2026-05-12). Linear: [DEM-24](https://linear.app/demirkol/issue/DEM-24) → `Done` (başlık "workspace.delete — workspace kalıcı silme (owner-only, ad onayı, cascade)" olarak güncellendi; kapanış yorumu eklendi — tasarım değişikliği [soft→hard delete] not düşüldü); DEM-20'ye bağımlı (Done). |
| PROC-2026-05-12-002 | DEM-32 | Linear: Faz 0–8 milestone + Faz 2–8 epic + Faz 1 alt iş issue'larını ekle | Süreç | Done | Claude Code | `docs/process/02-mvp-faz-plani.md`, `docs/process/05-is-kayit-defteri.md` | `docs/process` | 2026-05-12 | Pusula projesinde Faz 0…8 milestone'ları oluşturuldu; DEM-18 → Faz 0, DEM-20/DEM-22 → Faz 1 bağlandı; Faz 1 yeni issue'lar DEM-23 (davet akışı, Todo) + DEM-24 (`workspace.delete`, Backlog); Faz 2–8 epic'leri DEM-25…DEM-31 (Backlog). Linear issue [DEM-32](https://linear.app/demirkol/issue/DEM-32). |
| DOC-2026-05-12-004 | DEM-38 | Kontrol odası tab'ı — görev dokümanı + komut seti | Süreç | Done | Claude Code | `docs/kontrol-odasi/README.md` (yeni), `docs/README.md`, `docs/process/02-mvp-faz-plani.md`, `docs/process/04-otomatik-is-akisi-protokolu.md`, `docs/process/05-is-kayit-defteri.md`, `.claude/commands/*` (yeni) | `docs/kontrol-odasi`, `docs/process`, `.claude/commands` | 2026-05-12 | "Kontrol odası" tab'ının (kod yazmayan; süreç + docs + Linear senkronu) rolü + 7 komut (`/panorama`, `/doc-denetim`, `/linear-senkron`, `/faz-bol`, `/faz-baslat`, `/devir`, `/celiski`) dokümante edildi + `.claude/commands/` slash komutları. Linear workflow state'i `Sonraki Faz` (Unstarted; Backlog↔Todo arası) eklendi; Faz 2 alt işleri o state'e taşındı; durum lejantı `04`/`05`'te hizalandı. Memory'ye kaydedildi. Linear issue [DEM-38](https://linear.app/demirkol/issue/DEM-38) (Done). |
| FE-2026-05-12-003 | DEM-39 | Workspace yönetim ekranı (web): rename/archive + üye listesi/rol/çıkar | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md` | `apps/web`, `packages/ui` | 2026-05-12 | Faz 1 ek işi (`doc-denetim` F6 ile açıldı); backend hazır (DEM-20'de `workspace.update/archive` + `workspace.members.*`), UI yok. Davet UI'ı DEM-23'te eklendi; bu iş rename/archive + üye yönetimini kapsar. Pre-dev: `docs/architecture/08-web-ve-mobil.md` §8.1.1'e workspace yönetim ekranı (route `(app)/workspaces/[id]`, gösterilen procedure'ler, rol-bazlı görünürlük) eklendi. Plan: `(app)/workspaces/[id]/page.tsx` + `_components/*` — ad/slug düzenle (`workspace.update`, admin+), arşivle (`workspace.archive`, owner; sonra `/`'a dön), üye listesi (`workspace.members.list`) + rol değiştir (`workspace.members.updateRole`, shadcn `Select`) + üye çıkar/`workspaceten ayrıl` (`workspace.members.remove`), gönderilmiş davetler (`workspace.invitations.list` + `revoke`, admin+) + davet et dialog'u (InviteMemberDialog buraya taşınır); workspace listesi kartında ad → `/workspaces/[id]` linki. `packages/ui`'ye shadcn `Select`. `clientMutationId` istemcide; Türkçe metinler `strings.ts`. **İmplement edildi** (frontend-dev): `(app)/workspaces/[id]/page.tsx` + `_components/{workspace-settings(+form+test),member-list,member-row(+test),sent-invitations,archive-workspace-dialog}.tsx`; `(app)/page.tsx` kart başlığı → `/workspaces/[id]` link + davet dialog'u bu ekrana taşındı; `@pusula/ui` `Select` (Radix react-select); `InviteMemberDialog` opsiyonel `onInvited` prop'u → `invitations.list` invalidate. Web testleri 22 (auth-form 6 + invite-member-form 5 + member-row 6 + workspace-settings-form 5); suite toplam 66. QA round-1 (code-reviewer) = PASS — 3 medium (M1 gönderilmiş davetlerde "davet eden" adı gösterilmiyor / M2 `member.image` kullanılmayan alan / M3 session-pending'de self-satır flash+layout-shift) + 7 low/info. Round-2 düzeltmeleri: M1 → `workspace.invitations.list`'e `leftJoin users` + `invitedByName` döndürülür, `sent-invitations.tsx` "Davet eden: X · Son tarih: Y" gösterir (`workspace.test.ts` `invitations.list` testi de güncellendi); M2 → `member-row.tsx` yerel tipinden `image` çıkarıldı; M3+L1 → `member-list.tsx` session/`members` yüklenirken loading guard + mutation uçarken tüm satır kontrolleri disable; L2 → `formatDate(value: Date | string)`. Kalan L3–L7 bilinçli ertelendi (redundant no-op kontrolü, iki effect, 403/404 ayrımı yapmayan generic Alert, `onInvited` Promise ignore, dialog focus — kozmetik). verifier (round-1 + round-2 sonrası) = PASS (`pnpm -w typecheck`/`lint` 7/7, `pnpm -w test` 66 — 15 domain + 29 api + 22 web, `@pusula/web build` 5 route — yeşil). Commit: original implementation + M1/L2 `7c91527`; round-2 M2/M3 düzeltmeleri (`member-list.tsx`, `member-row.tsx`+test, `packages/api/.../workspace.test.ts`) commit `f715735` (push edildi). Kullanıcı onayladı → durum `Done` (2026-05-12). Atlanan: davet dialog'unda rol seçimi; `workspace.delete` UI'ı `FE-2026-05-12-004` / DEM-40'a taşındı (DEM-39'un tamamlayıcısı). Linear: [DEM-39](https://linear.app/demirkol/issue/DEM-39) → `Done` (kapanış yorumu eklendi); `blockedBy` DEM-20 (Done). |
| FE-2026-05-12-004 | DEM-40 | `workspace.delete` UI — workspace ayarları tehlikeli bölgesinde "kalıcı sil" (owner-only, ad onayı) | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md` | `apps/web` | 2026-05-12 | Faz 1 ek işi (DEM-39 tamamlayıcısı); backend hazır (`workspace.delete`, DEM-24, Done). Pre-dev: `08-web-ve-mobil.md` §8.1.2 tehlikeli bölge → "(2) Kalıcı sil" (owner-only, `confirmName` ad onayı [eşleşmeden onay disabled], başarıda `router.replace('/')` + `workspace.list` invalidate, `BAD_REQUEST`/`FORBIDDEN` inline). **İmplement edildi** (spark): `(app)/workspaces/[id]/_components/delete-workspace-dialog.tsx` (yeni — `DeleteWorkspaceForm` presentational [ad birebir yazma onayı, eşleşmeden submit disabled, `useId` label↔input + `aria-describedby`] + `DeleteWorkspaceDialog` tRPC shell [`trpc.workspace.delete`, `clientMutationId`, başarıda `/`'a dön + `workspace.list` invalidate, pending'de disabled + dialog kapanmaz, hata inline `destructive` Alert]); `page.tsx` tehlikeli bölge iki satıra ayrıldı — arşivle (üst) / kalıcı sil (alt, `border-t`), `isOwner` ise; `strings.workspace.manage.delete*`; `delete-workspace-dialog.test.tsx` 8 RTL test. QA (code-reviewer) = PASS; düzeltmeler: M1 (trigger `size="sm"` kaldırıldı — arşivlemeden daha vurgulu), L1/L2 (gereksiz `aria-label` + hidden hint duplikasyonu kaldırıldı, görünür prompt'a `id`+`aria-describedby`). verifier = PASS (`typecheck`/`lint`/`@pusula/web build`/`test` — 74 test [15 domain + 29 api + 30 web] — yeşil, secret temiz). Commit: pre-dev `6228bfa` + implementasyon `77c2627` (push edildi → origin). Kullanıcı talebiyle açıldı + tamamlandı → durum `Done` (2026-05-12). Linear: [DEM-40](https://linear.app/demirkol/issue/DEM-40) → `Done` (kapanış yorumu eklendi); `blockedBy` DEM-24 (Done). |
| FE-2026-05-12-005 | DEM-46 | Yeni kullanıcı onboarding'i: signup'ta default workspace + boş "İlk Pano" + login yönlendirme | 1 — Auth & Workspace | Done | Claude Code | `docs/domain/01-urun-modeli.md`, `docs/architecture/08-web-ve-mobil.md`, `docs/architecture/07-auth.md`, `docs/process/02-mvp-faz-plani.md` | `apps/api`, `apps/web`, `packages/domain` | 2026-05-12 | Faz 1 ek işi (onboarding boşluğu — `doc-denetim`/UX tartışmasıyla açıldı); backend hazır (`workspace.create` DEM-20 (Done), `board.create` DEM-34 (Done)). **Pre-dev (tamam):** `01-urun-modeli.md` invariant 11 (best-effort default workspace + boş "İlk Pano"; `workspace.list` boş olabilir → UI onboarding gösterir; şablon listeler/kartlar kapsam dışı), `08-web-ve-mobil.md` §8.1.3 (signup bootstrap akışı + `(app)/page.tsx` yönlendirme 0/1/2+; §8.1.1 "korumalı kabuk" satırı §8.1.3'e referans verir), `07-auth.md` (Better Auth `databaseHooks.user.create.after` bootstrap notu); `02-mvp-faz-plani.md` madde 7 → `In Progress` + Faz 1 header notu; Linear DEM-46 `Todo` → `In Progress`. **Plan:** `@pusula/domain` `ONBOARDING_WORKSPACE_NAME` (`"Çalışma Alanım"`) + `ONBOARDING_BOARD_TITLE` (`"İlk Pano"`) sabitleri; `apps/api/src/bootstrap.ts` (`bootstrapNewUser(userId)` — tek transaction: `workspaces`(sabit ad + benzersiz suffix'li slug) + `workspace_members`(owner) + `activity_events`(`workspace.created`) + `boards`("İlk Pano") + `board_members`(creator `admin`) + `activity_events`(`board.created`)) + `apps/api/src/auth.ts` `databaseHooks.user.create.after` → `bootstrapNewUser`; hata `console.error` ile loglanır, exception yeniden fırlatılmaz (best-effort — signup'ı patlatmaz); `apps/web` `(app)/page.tsx` yönlendirme (`workspace.list`: `pending`→placeholder, `error`→`Alert`+tekrar dene, 0→`OnboardingEmptyState`, 1→`useEffect` `router.replace('/workspaces/[id]')`, 2+→mevcut workspace listesi) + `_components/onboarding-empty-state.tsx` (`CreateWorkspaceDialog` CTA, Pusula/workspace kavram metni) + `strings.onboarding` (+ `strings.workspace.redirecting`); RTL: `onboarding-empty-state.test.tsx` + `(app)/page.test.tsx` (0/1/2+/pending/error senaryoları, `useTRPC`/`useQuery`/`useRouter` mock'lu). **İmplement edildi** (plana göre): `@pusula/domain/constants.ts` 2 sabit; `apps/api/src/bootstrap.ts` (yeni — `bootstrapNewUser`, tek tx; slug `calisma-alanim-{16hex}` — `randomBytes(8)`) + `apps/api/src/auth.ts` `databaseHooks.user.create.after` (try/catch + `console.error`, best-effort); `apps/web` `(app)/page.tsx` yeniden yazıldı (yönlendirme 0/1/2+), `_components/onboarding-empty-state.tsx` (yeni), `_components/create-workspace-dialog.tsx` opsiyonel `triggerLabel` prop, `lib/strings.ts` (`workspace.redirecting` + `onboarding` grubu; orphan `workspace.empty` silindi); RTL `onboarding-empty-state.test.tsx` (1) + `(app)/page.test.tsx` (5). QA: `code-reviewer` + `security-reviewer` = PASS (APPROVE) — M1/L1 slug çakışması → `randomBytes(4)`→`(8)` (64-bit); L1 orphan `workspace.empty` silindi; M2 (better-auth `after` hook post-commit varsayımı — comment'te belirtildi, ^1.6.10 ile doğru); kalan low'lar (best-effort hook'un transient hatayı yutması → ileride worker'a taşınabilir; `page.test.tsx` mock'u biraz geniş; backend bootstrap için direkt integration testi yok — best-effort yol, opsiyonel) ertelendi. `verifier` = PASS: `pnpm -w typecheck`/`lint` 7/7, `pnpm -w test` 121 (15 domain + 51 api [DB canlı] + 55 web — yeni: onboarding-empty-state 1 + page 5; geri kalan web testlerinin artışı Faz 2D paralel işinden), `@pusula/api-server` + `@pusula/web` build (route'lar `/`, `/sign-in`, `/sign-up`, `/workspaces/[id]`, `/workspaces/[id]/boards/[boardId]`) yeşil; secret temiz. Docs ile çelişki yok. Kullanıcı onayladı → durum `Done` (2026-05-12); Linear DEM-46 → `Done` + kapanış yorumu. Şablon listeler/kartlar **bu işte değil** → DEM-47 (`blockedBy` DEM-46 + DEM-35 + DEM-36). Linear issue [DEM-46](https://linear.app/demirkol/issue/DEM-46) (Pusula, milestone: Faz 1; `blockedBy` DEM-20 + DEM-34; `blocks` DEM-47; `relatedTo` DEM-35). Faz 2 paralelinde yapıldı. |
| FE-2026-05-12-006 | DEM-47 | Pano şablonu: onboarding "İlk Pano"sına varsayılan listeler (Yapılacak/Devam Eden/Bitti) + welcome kartlar | 1 — Auth & Workspace | Done | Claude Code | `docs/domain/01-urun-modeli.md`, `docs/architecture/08-web-ve-mobil.md`, `docs/process/02-mvp-faz-plani.md` | `apps/api`, `packages/domain` | 2026-05-12 | Faz 1 ek işi (DEM-46 onboarding'inin tamamlayıcısı — "İlk Pano" boş gelmesin). Bağımlılık: DEM-46 (onboarding "İlk Pano", Done) + DEM-35 (list CRUD, Done) + DEM-36 (card CRUD, Done) — hepsi karşılandı. **Pre-dev (tamam):** `01-urun-modeli.md` invariant 11 güncellendi (default workspace artık varsayılan listeler `Yapılacak`/`Devam Eden`/`Bitti` + `Yapılacak`'ta welcome kartlarla seed edilir; `list.created`/`card.created` aynı tx; şablon içeriği `@pusula/domain` sabitleri; "şablon kapsam dışı" notu kaldırıldı), `08-web-ve-mobil.md` §8.1.3 güncellendi (signup bootstrap → pano şablonu: varsayılan listeler + welcome kartlar + activity'ler; "pano boş gelir" notu kaldırıldı); `02-mvp-faz-plani.md` madde 8 → `In Progress` + Faz 1 header notu; Linear DEM-47 `Todo` → `In Progress`. **Plan:** `@pusula/domain/constants.ts` → `ONBOARDING_LIST_TITLES` (`['Yapılacak','Devam Eden','Bitti']`) + `ONBOARDING_WELCOME_CARDS` (`Yapılacak`'ta ~3-4 welcome kart başlığı — mevcut özelliklere göre Türkçe; diğer iki liste boş); `apps/api/src/bootstrap.ts` → `bootstrapNewUser` transaction'ı genişletilir: board oluşturulduktan sonra `seedOnboardingBoard(tx, …)` — 3 liste (`positionsBetween(null,null,3)` ile sıralı `position`) + her birine `list.created` activity + `Yapılacak` listesine welcome kartlar (`positionsBetween` ile `position`) + her birine `card.created` activity (actor = yeni kullanıcı; `cards.boardId` = "İlk Pano"); `boards.version` seed sırasında bump edilmez (panonun ilk hâli). Test: `apps/api`'de vitest yok → bootstrap içerik testi ertelendi (best-effort yol; DEM-46 ile aynı gerekçe — `positionBetween` `@pusula/domain`'de test edilmiş, insert pattern'leri `list.create`/`card.create` integration testleriyle aynı). **İmplement edildi** (plana göre, küçük sapma: `positionsBetween` yerine `positionBetween` cursor'u): `@pusula/domain/constants.ts` → `ONBOARDING_LIST_TITLES` + `ONBOARDING_WELCOME_CARDS` (4 welcome kart `Yapılacak`'ta; diğer iki liste boş); `apps/api/src/bootstrap.ts` → `bootstrapNewUser` tx'i genişletildi (board sonrası 3 liste + `list.created` + `Yapılacak`'a welcome kartlar + `card.created`; `cards.boardId`="İlk Pano"; `boards.version` bump yok); `apps/api/src/auth.ts` yorumu güncellendi. QA: `code-reviewer` = PASS (APPROVE) — L1 (`list.created`/`card.created` payload'ları `list.create`/`card.create` ile aynı şekle `{listId,title,position,...}` / `{cardId,listId,title,position,...}`), L2 (`auth.ts` "boş İlk Pano" stale comment) düzeltildi; info'lar (cursor vs batch pos, emoji bir kart başlığında — data, OK; backend bootstrap testi yok) ertelendi. `verifier` = PASS: `pnpm -w typecheck`/`lint` 7/7, `pnpm -w test` 121 (15 domain + 51 api + 55 web — sayı değişmedi, seed doğrudan test edilmedi), `@pusula/api-server` + `@pusula/web` build yeşil; secret temiz. Docs ile çelişki yok. Kullanıcı onayladı → durum `Done` (2026-05-12); Linear DEM-47 → `Done` + kapanış yorumu. Faz 1'in son ek işi de kapandı. Not: bu turda `docs/process/*` + `packages/domain/constants.ts`'in bir kısmı paralel oturumun `bosluk-tara`/Faz 2.5 commit'lerine (`b3fc9a4` vb.) dahil oldu (içerik tam). Linear issue [DEM-47](https://linear.app/demirkol/issue/DEM-47) (Pusula, milestone: Faz 1; `blockedBy` DEM-46 + DEM-35 + DEM-36 — hepsi `Done`). |
| FE-2026-05-12-007 | DEM-55 | Kullanıcı profili / hesap ayarları ekranı (web): ad/avatar (URL), parola değiştir, hesabı sil | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/07-auth.md`, `docs/architecture/08-web-ve-mobil.md`, `docs/architecture/03-backend.md`, `docs/architecture/02-teknoloji-kararlari.md`, `docs/domain/01-urun-modeli.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/process/02-mvp-faz-plani.md` | `apps/web`, `packages/domain`, `apps/api` | 2026-05-12 | Faz 1 ek işi (`bosluk-tara` G5). Bağımlılık: DEM-22 + DEM-39 (ikisi de Done). **Kararlar (kullanıcı, 2026-05-12):** (a) avatar = **basit URL** (yükleme yok; ileride Faz 8 MinIO attachment'a bağlanabilir); (b) hesap silmede kullanıcı bir workspace `owner`'ıysa → **engelle** (`@pusula/domain` `canDeleteOwnAccount`); (c) **yeni tRPC `user.*` router yok** — doğrudan Better Auth client (`updateUser`/`changePassword`/`deleteUser`); sunucu tarafı `apps/api/src/auth.ts`'e `user.deleteUser.enabled` + `beforeDelete` hook + `databaseHooks.user.update.before` (name/image server-side re-validation). **Pre-dev (tamam):** `07-auth.md` (Profil & hesap yönetimi), `08-web-ve-mobil.md` §8.1.7, `03-backend.md` (tRPC bölümü notu), `02-teknoloji-kararlari.md` (karar kaydı 2026-05-12), `01-urun-modeli.md` (invariant 14), `02-yetkilendirme-kurallari.md` (Hesap (User) — öz-yönetim), `02-mvp-faz-plani.md` (madde 9 + tablo). **İmplement edildi:** `@pusula/domain` → `schemas/user.ts` (`updateProfileInput`/`changePasswordInput`/`deleteAccountInput`, `userNameSchema`, `userImageUrlSchema` — `http(s)`-only şema kısıtı) + `schemas/index.ts` export + `permissions.ts` `canDeleteOwnAccount(n)=>n===0` + `permissions.test.ts` ek + `schemas/user.test.ts` (yeni); `apps/api/src/auth.ts` → `user.deleteUser.enabled` + `beforeDelete` (`assertCanDeleteAccount`: `count(workspaces where owner_id=userId)`, >0 ise `APIError BAD_REQUEST`; `workspaces.ownerId` zaten `restrict`) + `databaseHooks.user.update.before` (name/image server-side parse, hatalıysa `APIError`); `apps/web` → `(app)/account/page.tsx` + `_components/{profile-form,change-password-form,delete-account-section}.tsx` (presentational, auth-client `page.tsx`'te) + `(app)/_components/app-shell.tsx`'e "Hesap" linki (kullanıcı adı → `/account`) + `lib/strings.ts` `strings.account` + `strings.shell.accountSettings`. Testler: `@pusula/domain` 24 (yeni 9 — `canDeleteOwnAccount` + user şemaları, `javascript:`/`data:`/`file:` URL reddi dahil) + `apps/web` 156 (yeni ~22 RTL — 3 form bileşeni: prefill/validasyon/no-change/dialog/server error). **QA:** `code-reviewer` = APPROVE (0 BLOCKER/HIGH; M-3 `AlertDescription` markup düzeltildi; M-1/M-2 arşivli-workspace sayım edge dokümante edildi — kod yorumu + `assertCanDeleteAccount` JSDoc; LOW'lar kabul). `security-reviewer` = LOW risk (0 CRITICAL/HIGH; M-1 avatar URL şema kısıtı + M-2 server-side `name`/`image` re-validation eklendi; M-3 sayım tutarsızlığı dokümante; L-1 `session.freshAge` takip işi notu — client her zaman `password` gönderdiği için akut değil; L-3 ham `error.message` → MVP kabul). `verifier`: `pnpm -w typecheck` 7/7, `pnpm -w lint` 7/7, `pnpm -w build` 3/3 (`/account` route üretildi), `pnpm -w test` (domain 24 + api 51 + web 156) yeşil; secret temiz; docs ile çelişki yok. **Kullanıcı onayladı → durum `Done`** (2026-05-12); commit `3d8c7c3`; Linear DEM-55 → `Done` + kapanış yorumu. Faz 1'in son ek işi de kapandı — Faz 1 tamam. Linear issue [DEM-55](https://linear.app/demirkol/issue/DEM-55) (Pusula, milestone: Faz 1; `relatedTo` DEM-22 + DEM-39 — ikisi de `Done`). |
| DOC-2026-05-12-005 | DEM-33 | Faz 2.0 — Önce belge: Faz 2 (Board/List/Card) docs güncellemesi | 2 — Board/List/Card CRUD | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/architecture/04-veri-katmani.md`, `docs/architecture/05-board-mekanigi.md`, `docs/domain/01-urun-modeli.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/process/02-mvp-faz-plani.md`, `docs/process/05-is-kayit-defteri.md` | `docs/architecture`, `docs/domain`, `docs/process` | 2026-05-12 | Faz 2'nin "önce belge" adımı; **kontrol odası tab'ında yapıldı**: `03-backend.md`'ye "Faz 2 — board / list / card procedure'leri" tablosu (router/procedure/middleware/rol + Faz-2-dışı liste + transaction notu; activity tipleri `board.created/renamed/archived` vb. — taksonomi adlarıyla); `02-yetkilendirme-kurallari.md`'ye "Board / List / Card procedure haritası (Faz 2)" (procedure → board rolü); `04-veri-katmani.md`'ye "Faz 2 kapsamı" notu (hangi tablolar/kolonlar Faz 2, hangileri ileri faz; `position` sona ekleme); `05-board-mekanigi.md`'ye §5.0 "Board ekranı — CRUD veri akışı (Faz 2)"; `01-urun-modeli.md`'ye arşivli board salt-okunur notu. `05-aktivite-kurallari.md` taksonomisi board/list/card tiplerini zaten kapsıyor — değişiklik gerekmedi. Kapanış (2026-05-12): docs senkron tamam → **Done**; Linear [DEM-33](https://linear.app/demirkol/issue/DEM-33) → `Done` + kapanış yorumu; DEM-34 (Faz 2A) artık serbest (`blockedBy` çözüldü). Kod tarafı notu (Faz 2A/2B/2C'ye taşındı): `@pusula/domain` `ACTIVITY_EVENT_TYPES` şu an `*.updated` taşıyor; taksonomideki `board.renamed`/`list.renamed`/`card.renamed`/`card.description_changed` tipleri ilgili implementasyonda enum'a append + migration ile eklenecek (Postgres enum append-only). Linear issue [DEM-33](https://linear.app/demirkol/issue/DEM-33) (Pusula, milestone: Faz 2; durum `Done`; DEM-25 epic'i altında). |
| API-2026-05-12-004 | DEM-34 | Faz 2A — Board CRUD (backend): `boardProcedure` + `board.*` + permission enforcement | 2 — Board/List/Card CRUD | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/01-urun-modeli.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/process/02-mvp-faz-plani.md`, `docs/process/05-is-kayit-defteri.md` | `packages/api`, `packages/domain`, `packages/db` | 2026-05-12 | Başlatıldı 2026-05-12 (DEM-33 `Done` → `blockedBy` çözüldü; Linear DEM-34 `Sonraki Faz` → `In Progress`). Önce-belge DEM-33'te tamam — bu iş yalnızca kodu yazdı. **İmplement edildi** (backend-dev): `@pusula/domain` `ACTIVITY_EVENT_TYPES`'a append `board.renamed`/`list.renamed`/`card.renamed`/`card.description_changed` (Faz 2 alt kümesi; `*.updated` cruft dizide kaldı); `@pusula/db` migration `drizzle/0004_graceful_archangel.sql` (`activity_event_type` enum `ADD VALUE` ×4, append-only; DB ayaktaydı → `pnpm db:migrate` uygulandı); `@pusula/api` `middleware/board.ts` `boardProcedure` (`protectedProcedure` + boardId resolve → board (yoksa `NOT_FOUND`) → workspace üyeliği (yoksa `FORBIDDEN`) → opsiyonel `board_members` → `effectiveBoardRole` (null → `FORBIDDEN`) → `ctx.board = { id, workspaceId, role }`; `BoardContext` export); `routers/board.ts` `boardRouter` — `list` (`workspaceProcedure`; guest yalnızca explicit `board_members`, member+ workspace'teki tüm board'lar + arşivli, dön `{id,title,version,archivedAt,createdAt,role}[]` effective rol), `create` (`workspaceProcedure`; guest `FORBIDDEN`; tx `boards`+`board_members`(creator `admin`)+`activity_events`(`board.created`)), `get` (`boardProcedure` `viewer+`; `{board:{…,role},lists[](arşivli dahil,position sıralı),cards[](yalnızca aktif,tek sorgu `boardId`,position sıralı)}`), `update` (`boardProcedure` board `admin`=`canManageBoard`; `title` zorunlu→`BAD_REQUEST`; arşivli board salt-okunur `BAD_REQUEST` (tx içinde tekrar okunur, race-safe); aynı başlık idempotent `changed:false`; tx `boards` update(`title`,`version+1`)+`activity_events`(`board.renamed`,`{fromTitle,toTitle}`)), `archive` (`boardProcedure` board `admin`; `archived` bool default true; idempotent no-op `changed:false`; tx update(`archivedAt`,`version+1`)+`activity_events`(`board.archived`,`{archived}`); dön `{id,archivedAt,version,changed}`); `root.ts`'e `board: boardRouter` mount. Test: yeni `routers/board.test.ts` 9 tRPC integration testi (create member ok/creator board admin/`board.created`; create guest+outsider `FORBIDDEN`; list member tümü+inherited rol/guest yalnızca explicit; get unknown `NOT_FOUND`/non-board-member `FORBIDDEN`/`{board,lists,cards}` position sıralı; update admin+`board.renamed`+`version` artar/viewer `FORBIDDEN`/boş input `BAD_REQUEST`/aynı başlık idempotent/arşivli board `BAD_REQUEST`; archive admin set/restore/idempotent/non-admin `FORBIDDEN`). QA: `code-reviewer` = PASS (0 critical, 1 warning W1 `version` return-shape → bu turda düzeltildi: `board.list`/`board.archive` dönüşlerine `version` eklendi; 4 low öneri ertelendi — `board.get`'teki erişilemez `canViewBoard`, update unchanged-title erken dönüş, archive/renamed payload şekil farkı); `verifier` = PASS (`pnpm -w typecheck` 7/7, `lint` 7/7, `test` 83 — 15 domain + 38 api [board.test.ts 9 dahil, DB canlı] + 30 web, `@pusula/api-server build` — yeşil, secret temiz); W1 sonrası tekrar typecheck/lint/test yeşil. Docs ile çelişki yok. **Kullanıcı onayladı → durum `Done` (2026-05-12)**; commit `feat: DEM-34 — Faz 2A board CRUD backend` (main); Linear [DEM-34](https://linear.app/demirkol/issue/DEM-34) → `Done` + kapanış yorumu. Linear issue [DEM-34](https://linear.app/demirkol/issue/DEM-34) (Pusula, milestone: Faz 2; durum `Done`; DEM-25 altında; `blockedBy` DEM-33 (Done)). DEM-35 (Faz 2B) ve DEM-36 (Faz 2C) artık serbest (`blockedBy` çözüldü). |
| API-2026-05-12-005 | DEM-35 | Faz 2B — List CRUD (backend): `list.*` + board-edit permission | 2 — Board/List/Card CRUD | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/architecture/04-veri-katmani.md`, `docs/domain/02-yetkilendirme-kurallari.md` | `packages/api`, `packages/domain` | 2026-05-12 | Başlatıldı 2026-05-12 (DEM-34 `Done` → `blockedBy` çözüldü; DEM-36 ile aynı turda). Önce-belge DEM-33'te tamam. **İmplement edildi** (backend-dev): `@pusula/api` `middleware/board-access.ts` (`resolveBoardAccess(db|tx, boardId, userId)` paylaşılan helper — board → workspace üyeliği → board üyeliği → `effectiveBoardRole`; `BoardAccess { id, workspaceId, role, archivedAt }`; `boardProcedure` bunu kullanacak şekilde refactor edildi — davranış aynı, `board.test.ts` 9/9); `routers/list.ts` `listRouter` — `list.create` (`boardProcedure`, board `member+` = `canEditBoardContent`; board sonuna `position` (boş board `firstPosition()`, aksi `positionBetween(lastPos,null)` — `lastPos` aktif+arşivli tüm listelerden `position desc limit 1`); arşivli board → `BAD_REQUEST`; tx `lists` insert + `activity_events`(`list.created`) + `boards.version+1`), `list.update` (board `member+`; rename — `renameListInput`; arşivli board salt-okunur; cross-board liste → `BAD_REQUEST`; aynı başlık idempotent `changed:false`; tx update + `activity_events`(`list.renamed`) + `boards.version+1`), `list.archive` (board `member+`; `archived` bool; idempotent; tx update(`archivedAt`) + `activity_events`(`list.archived`) + `boards.version+1`); arşivli liste içi başlık düzenleme serbest (domain yalnızca arşivli listeye yeni kart eklemeyi yasaklar). `root.ts`'e `list: listRouter` mount. Test: `routers/list.test.ts` 6 entegrasyon testi. QA: code-reviewer + verifier = PASS (HIGH/MEDIUM yok; LOW#1 docs drift [`card.create` transport] → `03-backend.md` + `02-yetkilendirme-kurallari.md` güncellendi; LOW#2/#3 ek test [arşivli board update/archive reddi, arşivli liste içi kart edit] ertelendi; LOW#4/#5 kozmetik). `pnpm -w typecheck`/`lint` 7/7; `test` 96 (15 domain + 51 api [board 9 + list 6 + card 7 + workspace 29] + 30 web, DB canlı); `@pusula/api-server build` yeşil; secret temiz. Docs ile çelişki yok. Commit `feat: DEM-35 + DEM-36 — Faz 2B/2C list+card CRUD backend` (main). Kullanıcı onayladı → `Done` (2026-05-12); Linear DEM-35 → `Done` + kapanış yorumu. DEM-37 (Faz 2D — web board ekranı) artık serbest (`blockedBy` DEM-35+DEM-36 çözüldü). `list.move` (reorder) kapsam dışı → Faz 3 (DEM-26). Linear issue [DEM-35](https://linear.app/demirkol/issue/DEM-35) (Pusula, milestone: Faz 2; durum `Done`; DEM-25 altında). |
| API-2026-05-12-006 | DEM-36 | Faz 2C — Card CRUD (backend): `cardProcedure` + `card.*` + invariant enforcement | 2 — Board/List/Card CRUD | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/01-urun-modeli.md`, `docs/domain/02-yetkilendirme-kurallari.md` | `packages/api`, `packages/domain` | 2026-05-12 | Başlatıldı 2026-05-12 (DEM-34 `Done` → `blockedBy` çözüldü; DEM-35 ile aynı turda). **İmplement edildi** (backend-dev): `@pusula/api` `middleware/card.ts` `cardProcedure` (`protectedProcedure` + cardId resolve → kart (yoksa `NOT_FOUND`) → `resolveBoardAccess(card.boardId, userId)` → kullanıcının `card_members` ilişkileri → `ctx.card = { id, listId, boardId, workspaceId, archivedAt, boardRole, boardArchivedAt, relations }`); `routers/card.ts` `cardRouter` — `card.create` (`protectedProcedure` — `createCardInput` yalnızca `listId` taşıdığından: tx içinde liste okunur (yoksa `NOT_FOUND`) → `resolveBoardAccess(tx, list.boardId, userId)` → `canEditBoardContent` değilse `FORBIDDEN` → arşivli board/liste → `BAD_REQUEST`; listenin sonuna `position`; `cards` insert (**`boardId` = listenin board'u — kart ⊆ liste.board invariant'ı**) + `activity_events`(`card.created`) + `boards.version+1`), `card.get` (`cardProcedure` board `viewer+`; `{card, relations}`), `card.update` (`cardProcedure` board `member+`; ≥1 alan (`title`/`description`/`dueAt`) zorunlu (`'dueAt' in input` ile `null`'ı yokluktan ayırır); arşivli board salt-okunur; her değişen alan ayrı activity — `title`→`card.renamed`, `description`→`card.description_changed` (`""`↔`null` normalize), `dueAt` set→`card.due_set` / null→`card.due_cleared`; `boards.version+1` tek sefer), `card.archive` (`cardProcedure` board `member+`; `archived` bool; idempotent; `activity_events`(`card.archived`) + `boards.version+1`)); `root.ts`'e `card: cardRouter` mount (`appRouter` = health/auth/workspace/board/list/card). Test: `routers/card.test.ts` 7 entegrasyon testi (create listenin sonuna + invariant `card.boardId===list.boardId` + arşivli liste/board reddi + viewer `FORBIDDEN` + bilinmeyen `listId` `NOT_FOUND`; get viewer+ + non-member `FORBIDDEN` + bilinmeyen `cardId` `NOT_FOUND`; update title/desc/due → ilgili activity / boş input `BAD_REQUEST` / viewer `FORBIDDEN`; archive set/restore/idempotent). QA: code-reviewer + verifier = PASS (DEM-35 ile aynı tur; LOW#1 docs drift `card.create` transport düzeltildi; LOW#2/#3 ertelendi; kozmetikler skip). Doğrulama: yukarıdaki gibi (96 test, DB canlı). Commit `feat: DEM-35 + DEM-36 — Faz 2B/2C list+card CRUD backend` (main). Kullanıcı onayladı → `Done` (2026-05-12); Linear DEM-36 → `Done` + kapanış yorumu. `card.move`/`moveCard` kapsam dışı → Faz 3 (DEM-26). Linear issue [DEM-36](https://linear.app/demirkol/issue/DEM-36) (Pusula, milestone: Faz 2; durum `Done`; DEM-25 altında). Kart ⊆ liste.board invariant'ı + arşivli liste kuralları enforce edildi. |
| FE-2026-05-12-002 | DEM-37 | Faz 2D — Temel board ekranı (web): board listesi + board detay (CRUD, drag-drop'suz) | 2 — Board/List/Card CRUD | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md`, `docs/architecture/05-board-mekanigi.md` | `apps/web`, `packages/ui` | 2026-05-12 | Başlatıldı 2026-05-12 (DEM-35+DEM-36 `Done` → `blockedBy` çözüldü; backend Faz 2A/2B/2C tamam). Pre-dev: `08-web-ve-mobil.md`'ye §8.1.4 "Board ekranı (Faz 2D)" eklendi. **İmplement edildi** (frontend-dev): `apps/web` `(app)/workspaces/[id]/page.tsx` üstüne `<BoardListSection>` kartı (board listesi — `trpc.board.list`; "Pano oluştur" `CreateBoardDialog` → `trpc.board.create` (`boardTitleSchema` validasyon, `clientMutationId`) → `board.list` invalidate; "Pano oluştur" `workspaceRoleAtLeast(role,'member')` ise); yeni route `(app)/workspaces/[id]/boards/[boardId]/page.tsx` (`'use client'`, `use(params)`, `trpc.board.get` → `{board,lists,cards}`; pending/error [`NOT_FOUND`/`FORBIDDEN` → `Alert` + geri linki]/success; board başlığı + `RenameBoardForm` (board `admin`) + `ArchiveBoardDialog` (board `admin`; arşivle onaylı, geri yükle tek tık)) + `_components/*`: `BoardColumns` (yatay `flex overflow-x-auto`, sabit `w-72` kolonlar, `cards` → `Map<listId,cards[]>` `position` sıralı, `canEdit = boardRoleAtLeast(role,'member') && !archivedAt`), `ListColumn` (inline rename, arşivle/geri-yükle onaylı dialog, kartlar, `AddCardForm`), `AddListColumn`/`AddListForm` (`trpc.list.create` → `board.get` invalidate), `CardItem` (`dueAt` rozeti, tıkla → `EditCardDialog`, arşivle onaylı dialog), `EditCardForm`/`EditCardDialog` (başlık `Input`/`cardTitleSchema` + açıklama `Textarea`/`cardDescriptionSchema` [boş→`''`] + `dueAt` `<input type=date>` [boş→`null`]; yalnız değişen alanları gönderir, değişiklik yoksa kapatır; `viewer` salt-okunur), `format.ts` (`formatDate` tr-TR + `toDateInputValue`/`parseDateInputValue` — TZ-güvenli local Date). `strings.board.*` + `boardRoleLabels`. `@pusula/ui`'ye shadcn `Textarea` (CSS-only, yeni bağımlılık yok) → `src/index.ts` + `package.json` export. `DropdownMenu` eklenmedi — kolon/kart aksiyonları ghost `Button` + onaylı `Dialog`. Test: `apps/web`'e 19 yeni RTL testi (`create-board-dialog` 3, `add-list-form` 5, `add-card-form` 4, `edit-card-dialog` 7); web suite 55, toplam 121 (15 domain + 51 api [DB canlı] + 55 web). QA: code-reviewer = PASS (HIGH/MEDIUM yok; 5 LOW ertelendi — kullanılmayan `strings.board.card` anahtarları, restore-dialog açıklaması, mid-edit prop re-sync guard'ı [Faz 3+ realtime için], edit-card-dialog'a 2 date test'i, boş flex wrapper); verifier = PASS (`pnpm -w typecheck`/`lint` 7/7, `test` 121, `@pusula/web build` — route'lar `/`, `/sign-in`, `/sign-up`, `/workspaces/[id]`, `/workspaces/[id]/boards/[boardId]` — yeşil, secret temiz). Docs (§8.1.4) ile çelişki yok. Commit `feat: DEM-37 — Faz 2D web board ekranı` (main; benim olmayan paralel onboarding WIP'i [DEM-46/47] commit'e dahil edilmedi). Kullanıcı onayladı → `Done` (2026-05-12); Linear DEM-37 → `Done` + kapanış yorumu. **Bununla Faz 2 (DEM-25 epic) tamamen tamam** — sıradaki Faz 3 (DEM-26, `Sonraki Faz` — DEM-41…45 alt işleri). Linear issue [DEM-37](https://linear.app/demirkol/issue/DEM-37) (Pusula, milestone: Faz 2; durum `Done`; DEM-25 altında). Drag-drop yok (Faz 3 — DEM-26); optimistic UI Faz 4 (DEM-27); Playwright e2e Faz 8 (DEM-31). |

| DOC-2026-05-12-006 | DEM-49 | Faz 2.5.0 — Önce belge: kart detayı + board işbirliği docs | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/02-yetkilendirme-kurallari.md`, `docs/domain/05-aktivite-kurallari.md`, `docs/domain/01-urun-modeli.md`, `docs/architecture/08-web-ve-mobil.md`, `docs/architecture/04-veri-katmani.md`, `docs/architecture/02-teknoloji-kararlari.md`, `docs/process/02-mvp-faz-plani.md`, `docs/process/05-is-kayit-defteri.md` | `docs/architecture`, `docs/domain`, `docs/process` | 2026-05-12 | Faz 2.5'in "önce belge" adımı; bu turda **kod-yazma tab'ında pre-dev olarak** yapıldı (kontrol odası tab'ında değil). Kararlar kullanıcıya soruldu → (a) kart detay görünümü = **modal + `?card=<id>` query param** (intercepting route yok); (b) karta üye adayı `effectiveBoardRole !== null` olmalı; (c) `board.members.add` workspace-dışı kişiyi de davet edebilir → yeni `board_invitations` tablosu (e-posta + outbox; kabulde workspace `guest` + board üyesi). Güncellenenler: `03-backend.md` → "Faz 2.5 — comment / checklist / card.members / card.labels / label / board.members procedure'leri" tablosu (router/middleware/rol + activity + `boards.version` notu + board davet akışı); `02-yetkilendirme-kurallari.md` → effective board rolü + board rolleri (admin/member/viewer) açıklaması, board matrisine yeni satırlar (board daveti, board'dan ayrıl, yorum düzenle/sil) + "Board / Card içerik procedure haritası (Faz 2.5)" tablosu; `05-aktivite-kurallari.md` → taksonomide `board.member_invited`/`board.invitation_revoked`, comment adlandırması (`comment.created/updated/deleted`), `checklist.item_completed` cruft notu, etiket/checklist CRUD activity üretmez notu; `01-urun-modeli.md` → Board invitation entity + Board member "explicit" notu + invariant 12 (kart üyesi adayı) + invariant 13 (etiket scope + explicit board üyeliği + version); `08-web-ve-mobil.md` → §8.1.5 "Kart detay görünümü (Faz 2.5D)" (modal + `?card` param, içerik, veri akışı) + §8.1.6 "Board üye + etiket yönetimi UI (Faz 2.5E)" (üye/rol + board davetleri + etiket CRUD + etiket filtresi + arşiv toggle G7); `04-veri-katmani.md` → Faz 2.5 kapsamı notu (aktive edilen tablolar + `board_invitations` yeni tablo şeması + enum append listesi); `02-teknoloji-kararlari.md` → tarihli Karar kaydı satırı (3 karar); `02-mvp-faz-plani.md` → Faz 2.5 satırı `🚧 Devam ediyor`, "Faz 2.5 alt işleri" intro + alt madde detayları + kararlar; bu defter satırı. Obsidian frontmatter/MOC bağları korundu (yeni dosya yok). Kapanış: docs senkron tamam → **Done**; Linear [DEM-49](https://linear.app/demirkol/issue/DEM-49) → `Done` + kapanış yorumu; DEM-50/51/52 (`blockedBy` DEM-49) artık serbest. Not: paralel tab DEM-47 (pano şablonu) WIP'i (`apps/api/*`, `packages/domain/constants.ts` onboarding sabitleri, `01-urun-modeli.md` invariant 11, `08-web-ve-mobil.md` §8.1.3) commit'e dahil edilmedi — yalnızca DEM-49'a ait docs commit edildi. Linear issue [DEM-49](https://linear.app/demirkol/issue/DEM-49) (Pusula, milestone: Faz 2.5; DEM-48 altında; bağımlılık yok). |
| API-2026-05-12-007 | DEM-50 | Faz 2.5A — Comment + Checklist CRUD (backend): comment.* + checklist.* + checklist.item.* | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/02-yetkilendirme-kurallari.md` | `packages/api`, `packages/domain`, `packages/db` | 2026-05-12 | Faz 2.5'in ilk kod işi (kod-yazma tab'ı; önce-belge DEM-49'da tamam). İmplement edildi: `cardProcedure` üzerinde `comment.{list,create,update,delete}` (board `member+`; `update`/`delete` yazan veya board `admin`; soft-delete `deletedAt` + `body=''`, `list` silinmiş yorumları da döner — UI placeholder) + `checklist.{create,update,delete}` + `checklist.item.{create,toggle,update,delete,reorder}` (board `member+`; item `position` fractional `@pusula/domain`; `reorder` self/ters-komşu → `BAD_REQUEST` + `positionBetween` try/catch); transaction = domain mutasyonu + `activity_events` + `boards.version+1`; activity tipleri `comment.created/updated/deleted`, `checklist.created`, `checklist.item_added/checked/unchecked/removed` (checklist/item rename/edit/reorder activity üretmez ama version artar). `@pusula/domain` `schemas/comment.ts` + `schemas/checklist.ts`; `ACTIVITY_EVENT_TYPES`'a 4 yeni değer append + migration `drizzle/0005_typical_captain_america.sql` (enum `ADD VALUE`); `root.ts`'e `comment`+`checklist` mount. Test: tRPC integration — `comment.test.ts` 8 + `checklist.test.ts` 13. QA: code-reviewer + security-reviewer = PASS (round-2: cross-card/cross-checklist `NOT_FOUND`, outsider `FORBIDDEN`, reorder self-komşu `BAD_REQUEST` testleri + reorder try/catch). verifier = PASS (`pnpm -w typecheck`/`lint` 7/7, `test` 142 [15 domain + 72 api (DB canlı) + 55 web], `@pusula/api-server build` yeşil). Docs: `03-backend.md` + `02-yetkilendirme-kurallari.md` `comment.list` satırı (silinmiş yorumlar dahil) düzeltildi. Commit: `caa880d` (main, push edildi). Linear [DEM-50](https://linear.app/demirkol/issue/DEM-50) → `Done` + kapanış yorumu; `blockedBy` DEM-49 + DEM-36 (ikisi de Done); DEM-48 altında. Not: paralel DEM-47 (pano şablonu) tab'ının `packages/domain/constants.ts` onboarding sabitleri (paylaşılan dosya) bu commit'e karıştı — `apps/api/*` ve kontrol-odası README DEM-47 WIP'i dahil edilmedi. |
| API-2026-05-12-008 | DEM-51 | Faz 2.5B — Card members (assignee/watcher) + Label CRUD + karta etiket atama (backend) | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/02-yetkilendirme-kurallari.md` | `packages/api`, `packages/domain` | 2026-05-12 | Faz 2.5 kod işi (önce-belge DEM-49'da tamam); 2.5A ile paralel yapıldı. İmplement edildi: `cardProcedure` üzerinde `card.members.{list,add,remove}` (board `member+`; `viewer` yalnız kendini watcher yapar/çıkar; aday `effectiveBoardRole !== null` olmalı yoksa `BAD_REQUEST`; `onConflictDoNothing` idempotent; `list` yalnız `{userId,role,name}` döner — e-posta board `viewer`'lara açılmaz [privacy]) + `card.labels.{list,add,remove}` (board `member+`; etiket kartın board'una ait olmalı yoksa `BAD_REQUEST`); `boardProcedure` üzerinde `label.{list,create,update,delete}` (board `member+`; `(board,renk,ad)` benzersiz → `CONFLICT` [PG 23505]; cross-board `labelId` → `NOT_FOUND`; etiket CRUD **activity üretmez** — board metadata); transaction = domain mutasyonu + (varsa) `activity_events` + `boards.version+1`; activity tipleri `card.member_added/removed`, `card.label_added/removed` (zaten enum'da — migration yok). `@pusula/domain` `LABEL_COLORS` (10 renk palet) + `LabelColor` + `schemas/label.ts` + `schemas/card.ts`'e card-member/card-label input şemaları; `root.ts`'e `label` mount, `card.ts`'e members+labels nest. Test: tRPC integration — `card-members.test.ts` 10 + `card-labels.test.ts` 6 + `label.test.ts` 7. QA: code-reviewer + security-reviewer = PASS (round-2: N1 privacy [`card.members.list` e-posta dönmüyor] + aday-board-erişimi pozitif testi [workspace guest + explicit board viewer → eklenebilir] + arşivli board label update/delete testi). verifier = PASS (`pnpm -w typecheck`/`lint` 7/7, `test` 165 [15 domain + 95 api (DB canlı) + 55 web], `@pusula/api-server build` yeşil). Ertelenen (LOW): `isUniqueViolation` dedup (`workspace.ts` ↔ `label.ts`), `clientMutationId` dedup (Faz 4/5). Commit: `b3fc9a4` (main, push edildi). Linear [DEM-51](https://linear.app/demirkol/issue/DEM-51) → `Done` + kapanış yorumu; `blockedBy` DEM-49 + DEM-34 + DEM-36 (hepsi Done); DEM-48 altında. Not: paralel DEM-47 (pano şablonu) WIP'i — `constants.ts` onboarding hunk'ı paylaşılan dosya olduğu için karıştı; `apps/api/*` + kontrol-odası README DEM-47 WIP'i dahil edilmedi. |
| API-2026-05-12-009 | DEM-52 | Faz 2.5C — Board üye yönetimi + board davetleri (backend): board.members.* + board.invitations.* + board rolleri | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/03-backend.md`, `docs/domain/02-yetkilendirme-kurallari.md` | `packages/api`, `packages/domain`, `packages/db` | 2026-05-12 | Faz 2.5 kod işi (önce-belge DEM-49'da tamam; karar: `board.members.add` workspace-dışı kişiyi de davet edebilir → yeni `board_invitations` tablosu, e-posta + outbox, kabulde workspace `guest` + board üyesi). İmplement edildi: `boardProcedure` üzerinde `board.members.{list,add,updateRole,remove}` (`list` board `viewer+` — explicit `board_members` `{userId,role,name}` [e-posta yok, gizlilik] + inherited workspace owner/admin `inherited:true`; `add` board `admin`, e-postayla → hesap+ws üyesi direkt / hesap var ws değil → workspace `guest` + board üye / hesap yok → `board_invitations` `pending` + `notification_outbox`; race-guard `CONFLICT`; `updateRole`/`remove` board `admin`, yalnız explicit satır, son explicit admin korunur `.for('update')`) + `board.invitations.{list,revoke}` (`boardProcedure`) + `{mine,accept,decline}` (`protectedProcedure`; `workspace_invitations` disiplini — gizli token, süreli, tek kullanımlık, `(board,lower(email)) WHERE pending` partial unique; `accept` e-posta eşleşmeli, expired→`BAD_REQUEST`, arşivli board/ws → `BAD_REQUEST`, idempotent `.onConflictDoNothing()`; kabulde davetli ws `guest` + board üye); transaction = domain mutasyonu + (varsa) `activity_events` + (davet) `notification_outbox` + `boards.version+1`. Yeni `board_invitations` tablosu + migration `drizzle/0006_dazzling_ma_gnuci.sql` (enum `ADD VALUE` ×3: `board.member_role_changed`/`member_invited`/`invitation_revoked` + `CREATE TABLE board_invitations` + indeksler + partial unique). `@pusula/domain` `schemas/board.ts`'e board-member/board-invitation input şemaları (`emailSchema`/`invitationTokenSchema`/`WORKSPACE_INVITATION_TTL_DAYS` yeniden kullanıldı); `board.ts`'e `members`/`invitations` nest. Test: `board-members.test.ts` 8 + `board-invitations.test.ts` 7. QA: code-reviewer = PASS (round-2; round-1 FAIL — H1 docs-drift düzeltildi [`board.members.list` e-posta yok + `inherited` flag], M1 accept arşiv kontrolü, M2 case-(a) board insert race-guard, M3 son-admin TOCTOU `.for('update')` eklendi; ertelenen LOW: `isUniqueViolation` dedup, payload key drift); security-reviewer = PASS (token/IDOR/privilege-escalation/TOCTOU temiz; INFO: `add` case-(b) auto-guest = Trello modeli, `01-urun-modeli.md` invariant 13'te belgeli). verifier = PASS (`pnpm -w typecheck`/`lint` 7/7, `test` suite yeşil [+15 api board-members/invitations], `@pusula/api-server build` yeşil, secret temiz). Docs: `03-backend.md` + `02-yetkilendirme-kurallari.md` `board.members.list` satırı düzeltildi. Commit: `c321154` (main, push edildi). Linear [DEM-52](https://linear.app/demirkol/issue/DEM-52) → `Done` + kapanış yorumu; `blockedBy` DEM-49 + DEM-34 (ikisi de Done); DEM-48 altında; 2.5A/2.5B ile paralel yapıldı. Not: paralel kontrol odası tab'ının `docs/process/*` + `docs/kontrol-odasi/README.md` catch-up WIP'i bu commit'e dahil edilmedi. DEM-54 (`blockedBy` DEM-51+52+37+49) artık serbest. |
| FE-2026-05-12-008 | DEM-53 | Faz 2.5D — Kart detay görünümü (web): modal — açıklama/due-date/üyeler/etiketler/checklist/yorum/activity | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md` (§8.1.5), `docs/architecture/03-backend.md` (`checklist.list` + `card.activity.list`) | `apps/web`, `packages/api` | 2026-05-12 | Faz 2.5 web işi (önce-belge DEM-49'da tamam — kart detayı = **modal + `?card=<id>` query param**, intercepting route yok). İmplement edildi: shadcn `Dialog` modal (board arkada, `?card=<cardId>` derin link; geçersiz id → "kart bulunamadı"); başlık/açıklama/due-date inline edit (3 ayrı `card.update` mutation) + etiketler (`card.labels.*`+`label.*`, `LABEL_COLORS`, yeni etiket mini-form, `CONFLICT` inline) + üyeler assignee/watcher (`card.members.*`+`board.members.list`; `viewer` yalnız self-watch) + checklist'ler (`checklist.*`+`checklist.item.*`, X/Y ilerleme, item toggle/edit/sil; reorder ertelendi) + yorumlar (`comment.*`, soft-delete "silindi" placeholder, "(düzenlendi)", yazan/admin edit-sil) + kart activity feed (`card.activity.list`, type→Türkçe özet). Yeni 2 read-only query: `checklist.list` + `card.activity.list` (`cardProcedure`). `EditCardDialog` kaldırıldı (modal kapsadı). Yeni shadcn paketi yok (native checkbox + mevcut `Select`). Optimistic UI yok; `clientMutationId` istemcide; metinler `strings.card.*`. Test: 11 RTL dosyası ~51 test; suite 223 (15 domain + 110 api + 98 web). QA: code-reviewer = PASS (round-2; round-1 M1 `card-detail-checklists.tsx` 518→5 dosya, M2 activity skeleton, M3 ortak mutation→3 instance; ertelenen LOW: `replace` vs `push`, nested Dialog onayı, `card.get`'e `effectiveBoardRole`); verifier = PASS (typecheck/lint 7/7, build yeşil, secret temiz). Docs: `03-backend.md`'ye `checklist.list`/`card.activity.list`/`card.members.list`/`card.labels.list`/`label.list` satırları. Commit `7881461` (main). Kullanıcı onayladı → `Done` (2026-05-12); Linear DEM-53 → `Done` + kapanış yorumu. `blockedBy` DEM-50 + DEM-51 + DEM-37 + DEM-49 (hepsi Done); DEM-48 altında; DEM-54 (kart-detay tarafı tamam) artık serbest. |
| FE-2026-05-12-009 | DEM-54 | Faz 2.5E — Board üye + etiket yönetimi UI (web): board ayarlarına üye/rol + board davetleri + etiket CRUD + etiket filtresi | 2.5 — Kart detayı + board işbirliği | Done | Claude Code | `docs/architecture/08-web-ve-mobil.md` (§8.1.6), `docs/architecture/03-backend.md` (`board.get` `cards[].labels`), `docs/domain/02-yetkilendirme-kurallari.md` | `apps/web`, `packages/api` | 2026-05-12 | Faz 2.5'in son alt işi. İmplement edildi: board detay sayfasında "Board ayarları" girişi (board `admin`) → `Dialog`: **Üyeler** (`board.members.list` {userId,role,name,inherited}; inherited kilitli; `admin` rol değiştir/çıkar — `board.members.updateRole`/`remove`; son explicit admin UI'da kilitli; üye ekle e-posta+rol → `board.members.add`, sonuç `added`/`added_as_guest`/`invited` inline) + **gönderilmiş davetler** (`board.invitations.list` + `revoke`) + **etiketler** (`label.list`/`create`/`update`/`delete`, `LABEL_COLORS`, `CONFLICT` inline); **PendingInvitations** genişletildi → "Workspace davetleri" + "Board davetleri" (`board.invitations.{mine,accept,decline}`); **board ekranında etiket filtresi** (çoklu seçim, OR, client-side; palet `board.get` `cards[].labels`'tan) + "arşivli listeleri göster/gizle" toggle (`bosluk-tara` G7; arşivli **kartlar** kapsam dışı — ileri faz); `card-item`'a renk çipleri. **Backend (additive):** `board.get` `cards` projection'ına `labels:[{labelId,name,color}]` (tek sorgu `card_labels ⋈ labels`, server'da `Map`; mevcut testler kırılmadı, N+1 yok). Optimistic UI yok; `clientMutationId` istemcide; metinler `strings.board.settings.*`/`filter.*`/`invitations.*`. Yeni shadcn paketi yok. Test: ~35 yeni RTL/unit test + `board.test.ts` +1; suite 283 (16 domain + 111 api + 156 web — bir kısmı paralel DEM-55 testleri). QA: code-reviewer = PASS ("ship it"; ertelenen LOW: 4 dead string key, etiket filtresi raw state temizliği, confirm pending feedback); verifier = PASS (typecheck/lint 7/7, build yeşil, secret temiz). Docs: `08-web-ve-mobil.md` §8.1.6 + `03-backend.md` (`cards[].labels`) + `02-yetkilendirme-kurallari.md`. Commit `ca9fe45` (main). Kullanıcı onayladı → `Done` (2026-05-12); Linear DEM-54 → `Done` + kapanış yorumu. **Bununla Faz 2.5 (DEM-48 epic) tamam** — DEM-49–54 hepsi `Done`. `blockedBy` DEM-51 + DEM-52 + DEM-37 + DEM-49 (hepsi Done); DEM-48 altında. |
| FE-2026-05-12-010 | DEM-55 | Kullanıcı profili / hesap ayarları ekranı (web): ad/avatar + parola değiştir + hesap silme | 1 — Auth & Workspace | Done | Claude Code | `docs/architecture/07-auth.md`, `docs/architecture/08-web-ve-mobil.md` (§8.1.7), `docs/architecture/03-backend.md`, `docs/architecture/02-teknoloji-kararlari.md` (Karar kaydı), `docs/domain/01-urun-modeli.md` (invariant 14), `docs/domain/02-yetkilendirme-kurallari.md` (Hesap — öz-yönetim), `docs/process/02-mvp-faz-plani.md` | `apps/web`, `apps/api`, `packages/domain` | 2026-05-12 | Faz 1 ek işi (`bosluk-tara` G5; `02-mvp-faz-plani.md` madde 9'da `FE-2026-05-12-007` olarak da geçti — kanonik kayıt bu satır). Kararlar (kullanıcı onayı): (a) avatar = basit URL (yükleme yok; ileride Faz 8 MinIO); (b) hesap silmede kullanıcı bir workspace `owner`'ıysa engellenir (`workspaces.ownerId` `onDelete:'restrict'` + Better Auth `beforeDelete` hook açıklayıcı hata; domain `canDeleteOwnAccount`); (c) yeni tRPC `user.*` router yok — ad/avatar/parola/silme doğrudan Better Auth client (`updateUser`/`changePassword`/`deleteUser`), sunucu yalnız `user.deleteUser.enabled` + `beforeDelete`. İmplement edildi: `(app)/account` sayfası + `ProfileForm`/`ChangePasswordForm`/`DeleteAccountSection` (presentational + shell); app-shell'e "Hesap" linki; `@pusula/domain` `schemas/user.ts` (`updateProfileInput`/`changePasswordInput`/`deleteAccountInput`) + `permissions.ts` `canDeleteOwnAccount`; `apps/api/src/auth.ts` `user.deleteUser.enabled` + `beforeDelete` hook; yalnız shadcn/ui; metinler `strings.account`. Test: `@pusula/domain` Vitest (`canDeleteOwnAccount`) + `apps/web` RTL (form bileşenleri). QA: code-reviewer = APPROVE; security-reviewer = LOW; verifier = PASS (typecheck/lint/build/test yeşil). Commit `3d8c7c3` + kapanış `f118fa6` (main). Kullanıcı onayladı → `Done` (2026-05-12); Linear DEM-55 → `Done`. **Bununla Faz 1 tamamen tamam.** `blockedBy` DEM-22 + DEM-39 (ikisi de Done). |
| DOC-2026-05-12-007 | DEM-58 | Faz 2.7 epic'i — UI tasarım dili & board/kart cilalama (oluştur) | 2.7 — UI tasarım dili & board/kart cilalama | Todo | Claude Code | `docs/process/02-mvp-faz-plani.md`, `docs/kontrol-odasi/README.md`, `docs/process/05-is-kayit-defteri.md` (2.7.0'da ayrıca: yeni `docs/architecture/09-ui-tasarim-dili.md`, `02-teknoloji-kararlari.md`, `08-web-ve-mobil.md`, `CLAUDE.md`) | `docs/process`, `docs/kontrol-odasi`, Linear | 2026-05-12 | Kontrol odası tab'ı: kullanıcı talebiyle Faz 2.5 ↔ Faz 3 arasına yeni bir **UI cilalama fazı** eklendi (mevcut web UI fonksiyonel ama görsel olarak ham — "HTML+JS var, CSS yok"). İki ajan analizi (eski Pusula `D:\projects\pusula` UI'ı + mevcut `pusula_v2` web UI envanteri) + 3 ekran görüntüsü + Trello karşılaştırması → 4 karar kullanıcıdan: referans = karma (eski proje layout/anatomi/token baz + Trello iyileştirmeleri); tema = yeni palet (2.7.0'da seçenekler → kullanıcı seçer); rich text = Tiptap (mimari karar — headless editör); sıra = 2.7.0 önce-belge önce, uygulama Faz 2.5 web bittiğinden serbest. Linear: "Faz 2.7 — UI tasarım dili & board/kart cilalama" milestone + epic [DEM-58](https://linear.app/demirkol/issue/DEM-58) (`Todo` — 2026-05-12 `Sonraki Faz`'tan alındı, geliştirmesi başlıyor; priority High); alt işler `faz-bol 2.7`'de. Docs: `02-mvp-faz-plani.md` Faz 2.7 satırı + "Faz 2.7 alt işleri" iskeleti + footer; `kontrol-odasi/README.md` §5; `02-teknoloji-kararlari.md` Karar kaydı (Faz 2.7 + Tiptap + palet) + `CLAUDE.md` §2 #8 Tiptap notu eklendi (2026-05-12). Sıradaki somut adım: Faz 2.7.0 önce-belge (`09-ui-tasarim-dili.md` + palet/yoğunluk/font seçenekleri, kontrol odasında). Linear issue [DEM-58](https://linear.app/demirkol/issue/DEM-58) (Pusula, milestone Faz 2.7). |
| DOC-2026-05-12-008 | DEM-59 | Deployment: Dokploy "Docker Compose" servis tipi kararı + üretim deploy runbook'u (docs) | Süreç (infra/deployment) | Done | Claude Code | `docs/architecture/10-platform.md` (§10.3), `docs/architecture/02-teknoloji-kararlari.md` (Sabit kararlar + ADR-lite), yeni `docs/architecture/12-deployment-runbook.md`, `docs/architecture/README.md`, `docs/architecture/01-genel-bakis.md`, `docs/architecture/11-referanslar.md`, `docs/process/05-is-kayit-defteri.md` | `docs/architecture`, `docs/process` | 2026-05-12 | Kullanıcı sorusu: "VDS'te Dokploy kurulu ama her servisi tek tek ayağa kaldırmak çok uğraştırdı; doğrudan docker compose mı, yoksa Dokploy + Docker birlikte mi?" → öneri **B: Dokploy "Docker Compose" servis tipi** kabul edildi. Karar: Dokploy'da her servisi ayrı "Application" olarak tanımlama yok; **tek bir "Docker Compose" servisi** → GitHub repo + branch + repo kökündeki `compose.prod.yml` → stack tek ünite olarak deploy edilir. Dokploy payı: Traefik + Let's Encrypt TLS, git-push-to-deploy webhook'u, build logu, env yönetimi, rollback. Güncellenenler: `10-platform.md` §10.3 "Deployment (Dokploy — 'Docker Compose' servis tipi)" yeniden yazıldı (Compose modu + monorepo image'ları `turbo prune --docker` + migration tek seferlik job + named volume/yedek + prod≠dev compose); `02-teknoloji-kararlari.md` → Sabit kararlar tablosu "Deployment" satırı + tarihli ADR-lite kaydı (2026-05-12, DEM-59); **yeni `12-deployment-runbook.md`** → 12.1 yaklaşım/önkoşul, 12.2 `compose.prod.yml` + `apps/api`(api+worker paylaşımlı)/`apps/web` `Dockerfile` template'leri + migration disiplini, 12.3 Aşama 0 VDS temizliği (v1'i indir: bakım moduna al → `pg_dump`/MinIO yedeği → durdur → yedek doğrula → sil → container/image/volume/network temizliği [körlemesine `volume prune` yok] → disk/port), 12.4 Aşama 1 Dokploy v2 Compose servisi, 12.5 Aşama 2 env tablosu, 12.6 Aşama 3 domain + TLS, 12.7 Aşama 4 ilk deploy + migration + MinIO bucket, 12.8 Aşama 5 smoke test, 12.9 Aşama 6 erişimi açma, 12.10 sürekli deploy (git push → build logu), 12.11 rollback, 12.12 yedekleme/volume, 12.13 sorun giderme tablosu; `architecture/README.md` → indekse satır 12 + "Kaçınılması gerekenler"e 2 madde (servis-servis Application tanımlama; prod'da yerel `docker-compose.yml`'i kullanma); `01-genel-bakis.md` teknoloji özeti "Deployment" satırı; `11-referanslar.md` → Dokploy Compose / Traefik routers+ACME / Turborepo Docker / Next.js standalone linkleri; bu defter satırı. **Docs-only** — gerçek `compose.prod.yml` + `apps/{web,api,worker}/Dockerfile` (multi-stage `turbo prune --docker`) + migration one-off job ayrı infra issue'sunda. Obsidian frontmatter/MOC bağları: yeni dosya `12-deployment-runbook.md` frontmatter + `parent`/`related` + README indeksi tamam. Kapanış: kullanıcı yaklaşımı onayladı → docs senkron tamam → **Done**; Linear [DEM-59](https://linear.app/demirkol/issue/DEM-59) → `Done` + kapanış yorumu. Takip işi (kod, v2 yazımı ilerleyince): [DEM-60](https://linear.app/demirkol/issue/DEM-60) — `compose.prod.yml` + `apps/{web,api,worker}/Dockerfile` + migration one-off job (`Backlog`, related to DEM-59); bu defterde geliştirmesi başlayınca satır açılır. Commit `b0b8f61` (main; kontrol odası tab'ı tarafından commit edildi). |

> Faz 2 epic'i ([DEM-25](https://linear.app/demirkol/issue/DEM-25)) bölündü → DEM-33–37 yukarıdaki tabloda
> (hepsi `Done`; DEM-25 altında — Faz 2 tamamlandı). Faz 2.5 epic'i ([DEM-48](https://linear.app/demirkol/issue/DEM-48)) `Done`: DEM-49–54 hepsi `Done` (2026-05-12; defter satırları güncel). Faz 1 tamamen `Done` (DEM-55 dahil — `FE-2026-05-12-010`). **Faz 2.7 epic'i ([DEM-58](https://linear.app/demirkol/issue/DEM-58)) `Todo`** — UI cilalama, sıradaki faz; geliştirmesi başlıyor (2.7.0 önce-belge — `09-ui-tasarim-dili.md` + palet seçenekleri); alt işler `faz-bol 2.7`'de. Faz 3 epic'i ([DEM-26](https://linear.app/demirkol/issue/DEM-26)) `Sonraki Faz` (Faz 2.7'den sonra; alt işler DEM-41–45 de `Sonraki Faz` — bölünmüş, geliştirmesi başlayınca `Todo`'ya alınır + bu deftere satır); Faz 4–8 faz-seviyesi epic'leri Linear'da `Backlog`'tadır
> ([DEM-27](https://linear.app/demirkol/issue/DEM-27)=Faz 4, [DEM-28](https://linear.app/demirkol/issue/DEM-28)=Faz 5, [DEM-29](https://linear.app/demirkol/issue/DEM-29)=Faz 6,
> [DEM-30](https://linear.app/demirkol/issue/DEM-30)=Faz 7, [DEM-31](https://linear.app/demirkol/issue/DEM-31)=Faz 8; Pusula projesi, ilgili faz milestone'una bağlı).
> Faz statüsü [`02-mvp-faz-plani.md`](02-mvp-faz-plani.md)'de tutulur; ilgili fazın geliştirmesi başlayınca epic alt issue'lara
> bölünür (henüz başlamamış faz'ın işleri `Sonraki Faz`'da, mevcut fazınkiler `Todo`/`In Progress`'te) ve her alt iş bu defterde satır olarak açılır.

## Güncelleme kuralları

- Aynı iş için ikinci satır açma; mevcut satırı güncelle.
- `Son senkron` tarihi Linear veya docs tarafındaki son bilinçli güncelleme tarihidir.
- Faz değişirse `docs/process/02-mvp-faz-plani.md` de aynı çalışma turunda güncellenir.
- Kapanan işlerde test/verification sonucu Linear kapanış yorumunda, kısa notu bu dosyada tutulur.
