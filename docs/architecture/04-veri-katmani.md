---
title: '04 — Veri Katmanı'
description: 'PostgreSQL, Drizzle, temel tablolar, position implementasyonu ve transaction disiplini.'
aliases:
  - 'Veri Katmanı'
  - 'PostgreSQL Drizzle'
tags:
  - 'pusula'
  - 'architecture/data'
  - 'database'
type: 'architecture'
axis: 'architecture'
status: 'active'
parent: '[[docs/architecture/README|Tasarım / Teknik Mimari]]'
updated: 2026-05-20
---

# 04 — Veri Katmanı (PostgreSQL + Drizzle)

> Eksen: **tasarım / teknik**. Sıralamanın **iş anlamı** (before/after, compaction tetiği,
> concurrent move çözümü) için [`../domain/03-siralama-kurallari.md`](../domain/03-siralama-kurallari.md). Domain modeli için [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md).

## Neden PostgreSQL + Drizzle

Trello benzeri üründe transaction, row-level consistency, index ve sıralama mantığı kritik →
relational model doğru seçim. Drizzle: type-safe SQL, migration kontrolü, transaction netliği.

- Şema `packages/db/src/schema` altında. Paylaşılan Drizzle instance `casing: 'snake_case'` — TS'te **camelCase kolon anahtarı** yaz, DB kolonları snake_case.
- Better Auth tabloları (`users`, `sessions`, `accounts`, `verifications`) da `@pusula/db` içinde.
- `DATABASE_URL` `@pusula/db` tarafından Zod ile doğrulanır. Tooling: `pnpm db:generate` / `db:migrate` / `db:push` / `db:studio` / `db:seed`.

## Temel tablolar

```txt
users · sessions · accounts · verifications            (Better Auth)
workspaces · workspace_members · workspace_invitations
boards · board_members · labels
lists
cards · card_members · card_labels · checklists · checklist_items
comments · attachments
activity_events · realtime_events
notifications · notification_preferences · notification_outbox · push_tokens
search_documents
quick_notes                                            (kişiye özel hızlı notlar)
```

Örnek kritik kolonlar:

```txt
lists:  id, board_id, title, position, color, icon, icon_color, archived_at, created_at, updated_at
cards:  id, board_id, list_id, title, description, position, due_at, completed, completed_at, completed_by, cover_color, archived_at, created_at, updated_at
boards: id, workspace_id, title, background, version, ... (background nullable text — `null` = varsayılan, `gradient:<ad>` / `solid:<ad>` / `image:<attachmentId>`; version → client realtime sequence kontrolü)
workspace_invitations: id, workspace_id, email, role, token (uniq), invited_by_id, status (invitation_status enum: pending/accepted/declined/revoked/expired), expires_at, accepted_by_id, accepted_at, created_at, updated_at
activity_events:  id, workspace_id, board_id, card_id, actor_id, type, payload, created_at
realtime_events:  id, workspace_id, board_id, card_id, actor_id, type, payload, client_mutation_id, sequence, created_at, published_at  (outbox — Faz 5; published_at NULL = bekleyen)
notification_outbox: id, event_id, channel, recipient_id (nullable — e-posta daveti hesabı olmayan adrese gidebilir; alıcı payload'taki email), type, payload, status, attempts, scheduled_at, processed_at, created_at
search_documents: id, workspace_id, board_id, card_id, entity_type, entity_id, title, body, labels, search_vector, archived_at, updated_at
```

> `workspace_invitations`: `token` rastgele ve gizli (yalnızca davet e-postasında), tek kullanımlık;
> bir (workspace_id, email) çifti için aynı anda en fazla bir `pending` satır. Detay → [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Workspace davet akışı).

> **Faz 2 (Board/List/Card CRUD) kapsamı:** statik CRUD için `boards(id, workspace_id, title, version, archived_at?, created_at, updated_at)`, `lists(id, board_id, title, position, archived_at, created_at, updated_at)`, `cards(id, board_id, list_id, title, description, position, due_at, archived_at, created_at, updated_at)` ve `board_members` (oluşturan `admin`) kullanılır. `labels`, `card_labels`, `card_members`, `checklists`, `checklist_items`, `comments`, `attachments` ileri fazlardır. `position` her zaman `@pusula/domain/position` ile üretilir; Faz 2 yalnızca **sona ekleme** yapar (ilk eleman `firstPosition`, sonrakiler son elemanın ardına) — araya ekleme/reorder Faz 3. Faz 2 transaction'ı yalnızca `domain mutasyonu + activity_events` içerir; `realtime_events` / `notification_outbox` Faz 5/6'da devreye girer.

> **Faz 2.5 (Kart detayı + board işbirliği — [DEM-48](https://linear.app/demirkol/issue/DEM-48)) kapsamı:** Faz 0 iskeletindeki şu tablolar **aktive edilir**: `comments(id, card_id, author_id, body, edited_at?, deleted_at?, created_at, updated_at)` (soft-delete), `checklists(id, card_id, title, position, created_at, updated_at)`, `checklist_items(id, checklist_id, content, position, completed, completed_at?, completed_by?, created_at, updated_at)`, `card_members(card_id, user_id, role[card_role: assignee/watcher], created_at, updated_at)` (PK `(card_id, user_id, role)`), `labels(id, board_id, name, color, created_at, updated_at)` (`(board_id, color, name)` benzersiz), `card_labels(card_id, label_id)` (PK `(card_id, label_id)`), `board_members(board_id, user_id, role[board_role: admin/member/viewer], created_at, updated_at)` (PK `(board_id, user_id)` — Faz 2'de yalnızca "oluşturan `admin`" kullanılıyordu; bu fazda `add`/`updateRole`/`remove` ile yönetilir). `position` (checklist + checklist_item) yine `@pusula/domain/position` ile; checklist/item yalnızca sona ekleme + `reorder` (Faz 2'deki `move` ertelemesi burada geçerli değil — checklist item içi reorder Faz 2.5'te). `attachments` hâlâ ileri faz (Faz 8 — MinIO). Faz 2.5 transaction'ı yine yalnızca `domain mutasyonu + activity_events` içerir; `realtime_events` / `notification_outbox` Faz 5/6'da devreye girer (board daveti hariç — aşağı bak). Board içeriği değişen mutation'lar `boards.version`'ı artırır.
>
> **Yeni tablo — `board_invitations`** (Faz 2.5C, [DEM-52](https://linear.app/demirkol/issue/DEM-52); `workspace_invitations` ile aynı şekil): `id, board_id (→ boards, cascade), email, role (board_role), token (uniq, gizli), invited_by_id (→ users), status (invitation_status enum: pending/accepted/declined/revoked/expired), expires_at, accepted_by_id (→ users, nullable), accepted_at (nullable), created_at, updated_at` + index'ler `(board_id, status)`, `lower(email)` ve partial unique `(board_id, lower(email)) WHERE status = 'pending'`. Davet kabulü `notification_outbox` (`board_invitation`, channel `email`; alıcının hesabı varsa ek `in_app` satır) yazar — gerçek teslim worker'la (Faz 6); kabul transaction'ı `workspace_members` (`guest`, gerekirse) + `board_members` + `activity_events` (`board.member_added` + gerekirse `workspace.member_added`) içerir. `NOTIFICATION_TYPES` zaten `board_invitation` taşıyor — yeni enum gerekmez; `ACTIVITY_EVENT_TYPES`'a `board.member_invited`/`board.invitation_revoked` (+ `board.member_role_changed`, `checklist.item_added`/`item_checked`/`item_unchecked`/`item_removed`) **append** edilir (Postgres enum append-only — migration `ADD VALUE`).

> **Faz 6 (Bildirim — [DEM-29](https://linear.app/demirkol/issue/DEM-29)) kapsamı:** Faz 0 iskeletindeki `notifications` + `notification_outbox` + `notification_preferences` tabloları **aktive edilir**; ayrıca yeni `push_tokens` tablosu Faz 6B'de ([DEM-91](https://linear.app/demirkol/issue/DEM-91)) eklenir. Şemalar:
>
> - **`notifications`** (in-app notification merkezi): `id (uuid pk), user_id (→ users, cascade), type (text — 'card.member_added' / 'comment.mentioned' / 'due_reminder_1h' / vb.), payload (jsonb — actor, kartId, link, vb.), read_at (timestamptz, nullable — okundu/okunmadı), created_at (timestamptz default now())`. Index'ler: `(user_id, read_at) WHERE read_at IS NULL` (unread badge count için partial index), `(user_id, created_at DESC)` (list pagination için).
> - **`notification_outbox`** (Faz 0 iskelet — aktive): `id (uuid pk), event_id (text — `activity_events.id` veya scheduler için sentinel), recipient_id (→ users, set null — silinmiş kullanıcının audit'i kalır), channel (notification_channel enum: 'in_app' | 'email' | 'push'), type (text), payload (jsonb), status (text — 'pending' | 'delivered' | 'failed' default 'pending'), attempts (integer default 0), scheduled_at (timestamptz default now()), processed_at (timestamptz, nullable), created_at (timestamptz default now())`. Index'ler: `(processed_at) WHERE processed_at IS NULL` (sweeper sorgusu — Faz 5 `realtime_events` pattern'iyle simetrik), `(recipient_id, type, created_at DESC)` (cooldown 60s dedupe sorgusu için), `(scheduled_at)` (gelecekteki scheduled bildirimler için — sonraki tur).
> - **`notification_preferences`** (Faz 0 iskelet — aktive): `id (uuid pk), user_id (→ users, cascade), workspace_id (→ workspaces, cascade, nullable), board_id (→ boards, cascade, nullable), card_id (→ cards, cascade, nullable), mute_level (text — 'all' | 'mentions_only' | 'none' default 'none'), mention_only (boolean default false), push_enabled (boolean default true), email_enabled (boolean default true), created_at, updated_at`. UNIQUE constraint `(user_id, workspace_id, board_id, card_id)` (her scope için tek tercih). Tercih ekranı UI **Faz 6'da yok** — default değerlerle çalışır; UI sonraki tur.
> - **`push_tokens`** (Faz 6B — yeni migration): `id (uuid pk), user_id (→ users, cascade), token (text uniq — ExponentPushToken[xxx] formatı), platform (text CHECK in ('ios','android','web')), device_name (text, nullable — kullanıcıya gösterilebilir; "Abdullah'ın iPhone"), created_at (timestamptz default now()), last_used_at (timestamptz, nullable — push gönderildiğinde güncellenir), revoked_at (timestamptz, nullable — logout / DeviceNotRegistered hatası)`. Index'ler: `(user_id) WHERE revoked_at IS NULL` (aktif token sorgusu için partial), `(token)` (unique — duplicate registration'ı reject).
>
> **Transaction disiplini Faz 6:** mutation tx içinde `activity_events` insert'in yanına `notification-rules.ts` çıktısı için `notification_outbox` insert(ler) (per-recipient × per-channel — birden fazla satır olabilir, örn. atama hem in-app hem email için iki satır); cooldown 60s pre-check `notification_outbox`'tan `WHERE recipient_id=? AND type=? AND created_at > NOW() - INTERVAL '60 seconds'` (`comment.mentioned` + davet + due-reminder hariç). Worker tx commit sonrası `pusula-notifications` job alır, channel'a göre fan-out: `in_app` → `notifications` insert + Faz 5 `emitToUser(userId, 'notification.created')`; `email`/`push` → ilgili Faz 6B kanal kuyruğu. Idempotent: aynı outbox satırı iki kez publish'lense `processed_at IS NULL` kontrolüyle skip; recipient cache'inde `event_id` ile in-app dedupe (aynı event'ten aynı user'a tek `notifications` satırı). **Retention:** `notification_outbox` `processed_at IS NOT NULL AND created_at < NOW() - INTERVAL '30 days'` → cleanup job sonraki tur; `notifications` retention belirlemedi (kullanıcı görüntülediği sürece tutulur — read_at + 90 gün sonra cleanup önerilir, ileri faz). Detay → [`06-bildirim-altyapisi.md`](06-bildirim-altyapisi.md) "Notification processor (Faz 6)" + [`../domain/04-bildirim-kurallari.md`](../domain/04-bildirim-kurallari.md).

> **Faz 6.5 (Arama — [DEM-56](https://linear.app/demirkol/issue/DEM-56)) kapsamı:** Faz 0 iskeletindeki `search_documents` tablosu PostgreSQL FTS için **aktive edilir**. `search_documents` denormalize okuma modelidir; source of truth board/list/card/comment/label tablolarıdır. Şema:
>
> - **`search_documents`**: `id (uuid pk), workspace_id (→ workspaces, cascade), board_id (→ boards, cascade, nullable), card_id (→ cards, cascade, nullable), entity_type (text — 'board' | 'list' | 'card' | 'comment' | 'label'), entity_id (uuid), title (text), body (text nullable), labels (text[] default '{}'), search_vector (tsvector), archived_at (timestamptz nullable), updated_at (timestamptz default now())`.
> - Unique: `(entity_type, entity_id)`; scope index'leri `(workspace_id)`, `(board_id)`, `(card_id)` ve aktif sonuçlar için partial index `(workspace_id, board_id, entity_type, updated_at) WHERE archived_at IS NULL`; FTS index `GIN(search_vector)`.
> - `search_vector` app/helper katmanında `to_tsvector('simple', ...)` ile üretilir: title ağırlığı `A`, labels `B`, body `C`. PostgreSQL built-in Türkçe stemming olmadığı için Faz 6.5 başlangıç config'i `simple`; `unaccent`/Meilisearch ölçülen ihtiyaçla açılır.
> - `pg_trgm` opsiyoneldir; typo/fuzzy veya çok kısa title fallback'i kanıtlanırsa sonraki migration ile açılır. DEM-103 kararı: ana yol PostgreSQL FTS + GIN.
> - DEM-104 uygulaması (`0016_dem104_search_documents_fts.sql`) Faz 0 iskeletini bu modele taşır: `search_entity_type` enum'una `list` ekler, `card_id`, `labels text[]`, `search_vector tsvector`, GIN ve active-scope indexlerini bağlar; destructive tablo rebuild yoktur.
>
> **Transaction disiplini Faz 6.5:** request-path mutation tx içinde değişen entity için `upsertSearchDocument` / `deleteSearchDocument` çalışır. Board/list/card/comment/label create/update/archive/delete kapsamda; comment soft-delete search document'ı siler; arşivli board/list/card `archived_at` ile varsayılan sonuç dışına çıkar. Ağır backfill/reindex worker (`apps/worker`) idempotent çalışır ve aynı `(entity_type, entity_id)` unique key üzerinden upsert eder. Detay → [`09-depolama-ve-arama.md`](09-depolama-ve-arama.md) §9.2 + [`../domain/06-arama-kapsami.md`](../domain/06-arama-kapsami.md).

> **Faz 5 (Realtime — [DEM-28](https://linear.app/demirkol/issue/DEM-28)) kapsamı:** Faz 0 iskeletindeki `realtime_events` tablosu **aktive edilir** ve **outbox pattern** ile genişletilir: `id (uuid pk), workspace_id (→ workspaces, cascade), board_id (→ boards, cascade, nullable — workspace-scope event'ler için), card_id (→ cards, cascade, nullable), actor_id (→ users, set null — kullanıcı silinince audit kalır, anonimleşir), type (text — 'card.moved' / 'list.archived' / 'board.updated' / …), payload (jsonb), client_mutation_id (text, nullable — Faz 4A altyapısı), sequence (integer — board-scoped `boards.version` aynası), created_at (timestamptz default now()), published_at (timestamptz, nullable — outbox; NULL = bekleyen, NOT NULL = worker publish etti)`. Index'ler: `(published_at) WHERE published_at IS NULL` (sweeper sorgusu için partial index — bekleyen satırları hızlı bul), `(board_id, sequence)` (seq sorgusu için), `(created_at)` (retention cleanup için). **Transaction disiplini:** mutation tx içinde `realtime_events` insert (Faz 4'te boş, Faz 5'te aktif); worker tx commit sonrası `pusula-realtime-publish` job alır, publish eder, `published_at = NOW()` damgalar. Idempotent: aynı satır iki kez publish'lenirse client `seq` ile dedupe. **Retention:** `published_at IS NOT NULL AND created_at < NOW() - INTERVAL '7 days'` satırları periyodik cleanup job (Faz 8). **Faz 5'te insert eklenen mutation'lar:** `card.move`/`moveToList`/`copy`/`create`/`update`/`archive`/`complete`/`uncomplete` + `list.move`/`create`/`update`/`archive` + `board.create`/`update`/`archive`. Comment / checklist / label / member event'leri Faz 6'ya bırakılır. Detay → [`06-bildirim-altyapisi.md`](06-bildirim-altyapisi.md) "Realtime event yayın katmanı (Faz 5)" + [`05-board-mekanigi.md`](05-board-mekanigi.md) §5.3.

> **Faz 2.7 (kart tamamlama + kapak rengi — [DEM-66](https://linear.app/demirkol/issue/DEM-66) / [DEM-67](https://linear.app/demirkol/issue/DEM-67)) kapsamı:** Faz 2.7 "fonksiyon değişmez" ilkesinin iki bilinçli istisnası (UI tarafı — `CardCompleteToggle` + kapak-renkli başlık/şerit — `13-ui-tasarim-dili.md`'de tanımlıydı ama backend yoktu). `cards` tablosuna **dört yeni kolon** eklenir (tek migration `0007_*` — `cards` `ALTER TABLE ADD COLUMN` ×4 + `ALTER TYPE activity_event_type ADD VALUE` ×4): `completed boolean NOT NULL DEFAULT false`, `completed_at timestamptz` (nullable), `completed_by text REFERENCES users(id) ON DELETE SET NULL` (nullable — kullanıcı silinince geçmiş kalır ama anonimleşir, `card_members`/`activity` deseni gibi değil; FK `set null`), `cover_color text` (nullable; değer seti `@pusula/domain` `CARD_COVER_COLORS` — `@pusula/ui` `PaletteName` ile birebir uyumlu 12 ad: `kirmizi/turuncu/sari/lime/yesil/sky/mavi/indigo/mor/pembe/gri/siyah`; `labels.color` gibi düz `text` — DB-CHECK yok, doğrulama Zod katmanında [`cardCoverColorSchema`]). Kart **kapak görseli** (resim) ayrı ve **bu kapsamda değil** — attachment altyapısı gerektirir (Faz 8). `completed`/`completed_at`/`completed_by` birlikte set/clear edilir (`card.complete` → `completed=true, completed_at=now(), completed_by=actor`; `card.uncomplete` → hepsini `false/null`). Procedure haritası → [`03-backend.md`](03-backend.md); aktivite tipleri (`card.completed`/`card.uncompleted`/`card.cover_changed`/`card.cover_cleared`) → [`../domain/05-aktivite-kurallari.md`](../domain/05-aktivite-kurallari.md); domain invariant'ı → [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) (invariant 15). Salt backend — `apps/web`/`packages/ui` Faz 2.7B/2.7C'de UI'yı bağlar.

> **Faz 2.7 follow-up #4 — pano arka plan rengi ([DEM-100](https://linear.app/demirkol/issue/DEM-100)) kapsamı:** `boards` tablosuna `background text` nullable kolonu eklenir. DB kolonu snake_case `background`, Drizzle/TS alanı camelCase `background`. Kanonik format uygulama katmanında doğrulanır: `null` = varsayılan zemin, `gradient:<ad>` = `BOARD_BACKGROUND_GRADIENTS` preset'i, `solid:<paletAd>` = `CARD_COVER_COLORS` düz paleti. DB-level CHECK yok; `labels.color`/`cards.cover_color` disipliniyle Zod validation source of truth kalır. Aynı migration activity enum'a `board.background_changed` ve `board.background_cleared` değerlerini append eder. `board.update` transaction'ı background değiştiğinde `boards.background`, `boards.version`, `activity_events` ve `realtime_events` satırlarını aynı tx içinde yazar. **Faz 8.X (DEM-202) genişletmesi (DEM-242 önce-belge, 2026-05-20):** üçüncü kanonik varyant `image:<attachmentId>` eklenir; `boardBackgroundSchema` regex bu UUID-tabanlı formu kabul edecek şekilde genişler. Image varyantı için `attachments` tablosu `kind='board_background'` satırlarına işaret eder (aşağıdaki Faz 11 paragrafının Faz 8.X genişletmesine bakın). Activity payload'ı `image:<id>` taşıyabilir; gradient/solid → image, image → gradient/solid, image → null geçişleri `board.background_changed` üretir, image → null `board.background_cleared` üretir.

> **DEM-98 (Trello-stili liste rengi) kapsamı:** `lists` tablosuna `color text` kolonu eklenir (nullable; `null` = mevcut `bg-muted/30` varsayılan kolon zemini). Değer seti `@pusula/domain` `LIST_COLORS` paletindeki 10 adla sınırlıdır (`yesil/sari/turuncu/kirmizi/mor/mavi/sky/lime/pembe/gri`); DB-CHECK yok, doğrulama Zod/API katmanında yapılır. Aynı migration `ACTIVITY_EVENT_TYPES` enum'una `list.color_changed` ve `list.color_cleared` değerlerini append eder (`packages/db/drizzle/0012_silent_chameleon.sql`). Procedure haritası → [`03-backend.md`](03-backend.md); activity payload'ları → [`../domain/05-aktivite-kurallari.md`](../domain/05-aktivite-kurallari.md); realtime payload'ı → [`05-board-mekanigi.md`](05-board-mekanigi.md) §5.3; domain invariant'ı → [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) invariant 17.
> **DEM-109 (liste ikonu ve ikon rengi) kapsamı:** `lists` tablosuna `icon text` ve `icon_color text` nullable kolonları eklenir. `null`/`null` varsayılan ikonsuz kolon durumudur; `icon = null` iken `icon_color` da `null` kalır. Değer setleri `@pusula/domain` `LIST_ICONS` ve `LIST_ICON_COLORS` ile sınırlıdır; DB-CHECK yok, doğrulama Zod/API katmanında yapılır. Aynı migration `ACTIVITY_EVENT_TYPES` enum'una `list.icon_changed` ve `list.icon_cleared` değerlerini append eder. Procedure haritası → [`03-backend.md`](03-backend.md); activity payload'ları → [`../domain/05-aktivite-kurallari.md`](../domain/05-aktivite-kurallari.md); realtime payload'ı → [`05-board-mekanigi.md`](05-board-mekanigi.md) §5.3; domain invariant'ı → [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) invariant 18.

> **Faz 11 (Kart Eki — genel attachment) kapsamı:** Faz 0 iskeletindeki `attachments` tablosu (id, card_id, board_id, uploader_id, storage_key, file_name, mime_type, size, created_at, updated_at) DEM-110 kart-kapak-resmi yolundan **genel kart eki yoluna** genişletilir. Yeni kolonlar (migration `0027_dem147_faz11A_attachments_general.sql` — wired, DEM-147 / Faz 11A; 0021–0026 Faz 10 hand-written migration'lar tarafından alındığı için sıradaki müsait numara 0027):
>
> - **`description text NULL`** — kullanıcının opsiyonel açıklaması; ≤500 karakter; plain text (Tiptap **YOK** — yalnız alt-yazı/caption). `NULL` = açıklama yok. Doğrulama Zod katmanında (`attachmentDescriptionSchema = z.string().trim().max(500).optional()`); DB-CHECK yok (`labels.color`/`cards.cover_color` disipliniyle aynı).
> - **(opsiyonel) `committed_at timestamptz NULL`** — two-phase commit state işareti. `attachment.initiate` satır oluşturduğunda `NULL`; `attachment.commit` başarılı olunca `NOW()` damgalanır. Worker orphan sweeper `committed_at IS NULL AND created_at < NOW() - INTERVAL '1 hour'` satırları + storage objelerini siler. Tek alternatif: ayrı `attachments_drafts` tablosu (reddedildi — iki yoldan veri okuma karmaşık). **Karar (11.0):** `committed_at` kolonu eklenir; mevcut DEM-110 single-shot path `commit` çağrısı eklenerek two-phase'e göç eder (geriye-dönük migration aynı tx'te tüm satırlara `committed_at = created_at` atar).
> - Yeni index'ler: `attachments(card_id, committed_at DESC) WHERE committed_at IS NOT NULL` (kart için ek listesi — yalnız commit edilmiş satırlar), `attachments(committed_at) WHERE committed_at IS NULL` (orphan sweeper partial index).
>
> **Allowlist + boyut sabitleri (`@pusula/domain/constants.ts`):**
>
> - `ATTACHMENT_MIME_TYPES = ['image/jpeg', 'image/png', 'image/webp', 'image/gif', 'application/pdf', 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet', 'application/vnd.openxmlformats-officedocument.presentationml.presentation'] as const` (8 tip; SVG/ODF/plain text/csv/zip V1 dışı — [`02-teknoloji-kararlari.md`](02-teknoloji-kararlari.md) Karar kaydı 2026-05-15).
> - `ATTACHMENT_KIND = { image, pdf, office } as const` (UI ikon + önizleme stratejisi seçer).
> - `ATTACHMENT_MAX_BYTES = 50 * 1024 * 1024` (50 MiB tek limit).
> - `CARD_COVER_IMAGE_MIME_TYPES` + `CARD_COVER_IMAGE_MAX_BYTES = 50 MiB` — kart kapağı picker dar yolu (cover-only image) genel attachment yoluna paralel kalır; cover picker UI `attachment.list` filtreli `mimeType LIKE 'image/%' AND size <= 50 MiB` ile beslenir. Kapak boyut limiti `ATTACHMENT_MAX_BYTES` ile hizalı (50 MiB).
>
> **Transaction disiplini Faz 11:** `attachment.initiate` yalnız draft satır (`committed_at IS NULL`) + presigned PUT URL — `activity_events` / `realtime_events` / `notification_outbox` insert **etmez**. `attachment.commit` aynı tx içinde `committed_at = NOW()` + `activity_events.attachment.added` + `realtime_events` outbox (Faz 5B simetri) + `notification_outbox` (watcher fan-out, Faz 6 simetri) + `boards.version + 1`. `attachment.delete` aynı tx içinde `attachments` satırı DELETE + `activity_events.attachment.removed` + `realtime_events` outbox + `boards.version + 1`; storage temizliği post-commit `pusula-attachment-cleanup` BullMQ job (best-effort, idempotent — bkz. [`06-bildirim-altyapisi.md`](06-bildirim-altyapisi.md)). Activity event tipleri (`attachment.added`/`attachment.removed`) Faz 0 enum'unda zaten var — yeni `ADD VALUE` migration **gerekmez**. `cards.coverImageAttachmentId` FK `ON DELETE SET NULL` korunur (DEM-110); kapak yapılmış attachment silindiğinde kart kapak şeridi otomatik `null`'a düşer. Detay → [`09-depolama-ve-arama.md`](09-depolama-ve-arama.md) §9.1 + [`../domain/07-ek-kurallari.md`](../domain/07-ek-kurallari.md).

> **Faz 8.X (DEM-202 — board görsel arka plan) genişletmesi ([DEM-242](https://linear.app/demirkol/issue/DEM-242) önce-belge, 2026-05-20):** Mevcut `attachments` tablosu **board background image** için yeniden kullanılır — yeni `board_background_attachments` tablosu açılmaz. Yeni kolon: **`kind text NOT NULL DEFAULT 'card_attachment' CHECK (kind IN ('card_attachment','board_background'))`**. `card_id` kolonu **nullable** yapılır; DB CHECK constraint `(kind = 'card_attachment') = (card_id IS NOT NULL)` zorlar — `card_attachment` satırları `card_id`'yi zorunlu kılar, `board_background` satırları `card_id IS NULL` taşır. `board_id` NOT NULL kalır (her iki kind da bir board'a aittir). `uploader_id` aynen (board admin yükler). 2-fazlı commit (`committed_at`), `attachments_orphan_sweep_idx`, `pusula-attachment-cleanup` BullMQ queue, presigned URL üretici (`createPresignedGetUrl`), `attachment.initiate` / `attachment.commit` paterni hepsi **paylaşılır** — board background için duplikat altyapı yok. Yeni board-scoped tRPC procedure'leri (`board.backgroundUploadInitiate` / `board.backgroundUploadCommit`, [`03-backend.md`](03-backend.md) Faz 8.X) `kind='board_background'` satırları üretir; `cards.coverImageAttachmentId` ve `attachment.list` query'leri `WHERE kind='card_attachment'` filtreli kalır (kart UI'sı board background satırlarını listelemez). **Yeni index:** `attachments(board_id, committed_at DESC) WHERE kind='board_background' AND committed_at IS NOT NULL` — bir board'ın aktif image background'unu çözmek için. **Allowlist + boyut (board_background):** `BOARD_BACKGROUND_MIME_TYPES = ['image/jpeg', 'image/png', 'image/webp', 'image/avif']` (animasyonlu GIF dahil değil; mobil Safari 16+ ile AVIF dahil), `BOARD_BACKGROUND_MAX_BYTES = 10 * 1024 * 1024` (10 MiB). Permission: image upload board `admin` (`canManageBoard`). **Eski image temizlik:** board admin yeni image yüklerse veya `background = null` ile temizlerse, eski `kind='board_background'` satırı `attachments` DELETE'lenir (aynı tx) ve storage temizliği `pusula-attachment-cleanup` BullMQ job ile (Faz 11C deseninin tam karşılığı). Migration `0028_dem242_faz8X_attachments_kind.sql` (planlanan numara; sıra DEM-203/quick_notes ve mevcut Faz 11 migration'larına göre 8.X.B implement aşamasında netleşir): `ADD COLUMN kind`, `ALTER COLUMN card_id DROP NOT NULL`, CHECK constraint ekle, yeni index. Geriye dönük: mevcut tüm satırlar default `kind='card_attachment'` alır, `card_id` NOT NULL invariant'ı CHECK ile korunur. Detay → [`02-teknoloji-kararlari.md`](02-teknoloji-kararlari.md) Karar kaydı 2026-05-20 + [`09-depolama-ve-arama.md`](09-depolama-ve-arama.md) §9.1.

> **Faz 8E (audit log — [DEM-282](https://linear.app/demirkol/issue/DEM-282)) kapsamı:** Kritik mutation'ların kalıcı/immutable forensic izi için yeni **`audit_log`** tablosu eklenir (`activity_events` ile dublike değil — daha sıkı kapsam + append-only + workspace-owner görüntüleme). Şema:
>
> - **`audit_log`**: `id text pk (primaryId nanoid)`, `workspace_id text NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE`, `actor_id text REFERENCES users(id) ON DELETE SET NULL` (kullanıcı silinince anonimleşir, kayıt kalır), `action text NOT NULL` (12 değerli enum — `@pusula/domain` `AUDIT_ACTIONS`), `target_type text NOT NULL` (7 değerli enum — `AUDIT_TARGET_TYPES`), `target_id text NOT NULL`, `before jsonb`, `after jsonb`, `ip text`, `user_agent text`, `created_at timestamptz default now()`.
> - Index: `(workspace_id, created_at DESC)` (workspace owner akışı sorgusu), `(target_type, target_id)` (forward-compat — bir entity için tüm audit'leri çekme).
> - **Append-only invariant:** `audit_log_reject_mutation` PL/pgSQL fonksiyonu + `BEFORE UPDATE` trigger (`audit_log_no_update`) RAISE EXCEPTION ile reddeder. Tek istisna: `actor_id` ON DELETE SET NULL cascade (`OLD.actor_id NOT NULL → NEW.actor_id NULL` + diğer tüm kolonlar aynı) — `0042` migration cascade istisnasını ekler. DELETE trigger 0044'te düşürüldü (workspace cascade'i bloklamasın diye); forensic DELETE guarantee app convention (`appendAudit` dışında DELETE yazmama — code review + grep gate). Migration zinciri: `0041` (tablo + ilk trigger) + `0042` (actor cascade istisnası) + `0043` (workspace FK CASCADE) + `0044` (DELETE trigger drop).
> - **`workspace_id` FK = CASCADE** (kullanıcı kararı 2026-05-24): workspace silindiğinde audit kayıtları da temizlenir. 8.0'da RESTRICT seçilmişti ama pratikte uygulanamadı (self-FK ihlali + mevcut test teardown'u patlatıyor). Sonuç: `workspace.delete` audit kaydı önce yazılır, sonra DELETE workspace cascade audit'i (kendisi dahil) wipe eder. Bugün 10 caller bağlı (workspace.delete + workspace/board members + invitations + share + attachment).
> - **`board.delete` + `card.delete`** AUDIT_ACTIONS enum'da forward-compat — kodda hard delete yok, archive reversible criterion-1 dışı (2026-05-24 kararı).
> - **Retention:** workspace yaşadığı sürece süresiz. Workspace silindiğinde CASCADE ile audit gider — forensic okuyucu workspace owner zaten silinmiş olur. Satır başına ~500 byte → storage maliyeti ihmal edilebilir.
>
> **Transaction disiplini Faz 8E:** `appendAudit` helper mutation transaction'ı **içinde** çağrılır (`packages/api/src/lib/audit-log.ts`). Worker outbox YOK — fire-and-forget tutarlılığı bozar. Detay → [`17-audit-log-mimarisi.md`](17-audit-log-mimarisi.md) + [`03-backend.md`](03-backend.md) Faz 8E.

> **DEM-203 (Mobil hızlı notlar — yeni tablo `quick_notes`) kapsamı:** Mobil merkezi "Ekle" akışındaki **Hızlı Not** entity'si için yeni `quick_notes` tablosu eklenir. Hızlı Not **kişiye özeldir** ve workspace/board/list'ten bağımsızdır — yalnız sahibi erişir; `activity_events` / `realtime_events` / `notification_outbox` ile ilişkisi yoktur. Şema:
>
> - **`quick_notes`**: `id (uuid pk — mevcut id desenine uyar)`, `user_id (text → Better Auth `users`, `ON DELETE CASCADE` — kullanıcı silinince notları da gider)`, `content (text NOT NULL — not metni)`, `created_at (timestamptz default now())`, `updated_at (timestamptz default now())`.
> - Index: `(user_id)` — bir kullanıcının notlarını listelemek için. `quick_note.list` `created_at DESC` ile yeniden eskiye sıralar.
> - Tablo hiçbir board/list/workspace'e bağlı değil; tek FK `user_id`'dir. Activity/outbox/realtime tablolarıyla ilişkisi yok.
>
> **Transaction disiplini DEM-203:** `quick_note.create/update/delete` tek satır mutasyonudur; activity/outbox/realtime insert **etmez**. `quick_note.convertToCard` istisnadır: bir Hızlı Not hedef listeye dönüştürülürken **tek transaction** içinde `cards` satırı oluşturulur (notun `content`'i kart başlığı) ve `quick_notes` satırı silinir; kart oluşturma normal kart-create transaction'ı gibi `activity_events` + `realtime_events` + `notification_outbox` üretir, not silme ise **sessizdir** (kişiye özel kayıt — activity/event yok). Kart konumu DEM-205 ile opsiyonel `beforeCardId`/`afterCardId`/`newPosition` parametreleriyle belirlenir (web panelinde nota karta sürükleme — komşu kartlardan fractional `position` server-side çözülür); parametre verilmezse kart listenin sonuna eklenir. Procedure haritası → [`03-backend.md`](03-backend.md).

## Sıralama implementasyonu (position)

`position` kolonu **integer değildir**. Akıcı drag-drop için LexoRank benzeri string (fractional)
pozisyon: `@pusula/domain/position` helper'ları (`positionBetween`, `positionsBetween`,
`firstPosition` — `fractional-indexing` paketiyle).

```txt
card A position = "a0"
card B position = "a8"
A ile B arasına eklenen kart position = "a4"
```

Faydası: her taşımada tüm liste yeniden numaralanmaz; optimistic UI kolay; concurrent move
yönetilebilir; büyük listelerde performans iyi. Aralık tükenince ilgili liste için worker'da
**background compaction**. Compaction tetiği ve concurrent move semantiği iş kuralıdır →
[`../domain/03-siralama-kurallari.md`](../domain/03-siralama-kurallari.md).

## Transaction disiplini

- Domain mutasyonu + `activity_events` + `realtime_events` + `notification_outbox` insert'leri **mümkünse aynı transaction**'da.
- Transaction'lar kısa tutulur (ağır iş worker'a).
- Mutation'lar idempotent tasarlanır; aynı `clientMutationId` ile iki kez gelen mutation duplicate activity/bildirim üretmez. Bkz. [`05-board-mekanigi.md`](05-board-mekanigi.md).
