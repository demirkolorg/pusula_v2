---
title: "08 — Web ve Mobil"
description: "Next.js web ve ileri faz Expo mobile teknik kuralları."
aliases:
  - "Web ve Mobil"
  - "Frontend"
tags:
  - "pusula"
  - "architecture/frontend"
  - "mobile"
type: "architecture"
axis: "architecture"
status: "active"
parent: "[[docs/architecture/README|Tasarım / Teknik Mimari]]"
updated: 2026-05-14
---
# 08 — Web ve Mobil

> Eksen: **tasarım / teknik**. Board mekaniği (drag-drop/optimistic/realtime) ayrı: [`05-board-mekanigi.md`](05-board-mekanigi.md).

---

## 8.1 Web (`apps/web` → `@pusula/web`)

Next.js App Router. App `apps/web/src/app`; `@/*` → `apps/web/src/*`.

Sorumluluklar: board ekranı, drag-drop deneyimi, workspace & board yönetimi, notification center,
activity feed, search, settings, auth ekranları.

> **UI tasarım dili (token'lar, board/kolon/kart/modal anatomisi, ortak desenler, `packages/ui` bileşen spec'leri) → [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md)** (Faz 2.7). Bu dosya web tarafının teknik/işlevsel ihtiyaçlarını tutar; *görsel/anatomi* kararları 13'te. Faz 2.7 uygulamasında §8.1.4–8.1.6'daki ekranlar 13'teki tasarım diline çekilir.
>
> **Dark/light tema desteği:** root layout'ta `next-themes` `ThemeProvider` (`attribute="class"`, `defaultTheme="light"`, `enableSystem={false}`, `themes=["light","dark"]`, `storageKey="pusula-theme"`); app-shell header sağ tarafında `ThemeToggle` (Sun/Moon ikon swap, `Button variant=ghost size=icon`); persistence = localStorage; auth route'larında toggle yok (ilk tur). Tüm ekranlar (`(auth)/*`, `(app)/*`, board, board settings, card detail modal, filter bar, Tiptap prose) light + dark token cascade'inden besleniyor — yeni renk eklenmez; §13.7 [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md#137-tema-modu-lightdark) "önce belge"si + DEM-96 uygulaması. Mod seti **ikili** (`system` algılaması yok), default `light`.
>
> **App-shell v2 — workspace + board switcher + user nav menu ([DEM-97](https://linear.app/demirkol/issue/DEM-97), Faz 2.7 follow-up #3):** app-shell header üç yatay grup taşır — **sol** (brand `LayoutGridIcon+"Pusula"` + `Separator` + `WorkspaceSwitcher` + `BoardSwitcher`), **sağ** (`NotificationBellPlaceholder` + `ThemeToggle` + `UserNavMenu` avatar dropdown). Switcher'lar shadcn `team-switcher` (sidebar-07 bloğu) desenini header'a port eder (yeni shadcn primitive yok — `Button`/`DropdownMenu`/`Tooltip` zaten var). `BoardTopBar`'ın **sol identity bloğu kaldırılır** (workspace `Link` rozet + "PANO" eyebrow + inline-rename başlık + favori `StarIcon`) — switcher trigger'ı board adını + workspace bağlamını zaten gösterir; çift bilgi olmasın. View switch + actions (`Davet/Search/Activity/⋮`) `BoardTopBar`'da yerinde kalır; inline-rename ⋮ menü "Yeniden adlandır" akışıyla açılır (mevcut `RenameBoardForm` `hideTrigger` prop'u zaten destekliyor). Avatar dropdown'da Hesap ayarları + Çıkış (mevcut header'daki ayrı link + buton birleşir). Bildirim placeholder `Faz 6D — DEM-93` ile gerçek `NotificationBell`'e dönüşür. Detay ve bileşen spec'leri → §13.8 [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md#138-app-shell-v2-workspace--board-switcher--user-nav-menu). Route → switcher state matrix:
>
> | Route | WorkspaceSwitcher trigger | BoardSwitcher trigger |
> | --- | --- | --- |
> | `/` (workspace list / onboarding) | "Workspace seç" placeholder (tıklanır → dropdown) | disabled, tooltip "Workspace seçin" |
> | `/workspaces/[id]` | aktif workspace adı + rol | "Pano seç" placeholder |
> | `/workspaces/[id]/boards/[boardId]` | aktif workspace adı + rol | aktif pano adı + renk noktası |
> | `/account` | "Workspace seç" placeholder | disabled |
>
> Mobile (`md:` altı): `BoardSwitcher` ikon-only, `WorkspaceSwitcher` ikon-only; sağ grup (bildirim + tema + avatar) her zaman görünür. Faz 6D'de `NotificationBell` placeholder yerine gerçek bileşen geçer (§8.1.11).

Kullanım ilkeleri:

- Ana API Next içinde **değil**, `apps/api` içinde. Route Handler'lar yalnızca web-BFF veya özel web endpoint'leri için.
- Server Component'ler initial shell/data için kullanılabilir; board ekranı interaktif olduğundan client component ağırlıklıdır.

Board ekranı teknik ihtiyaçları: stable layout · horizontal scroll · kart virtualization ihtimali ·
keyboard accessibility · drag overlay · multi-list reorder · optimistic cache update · realtime reconciliation.

### UI: yalnızca shadcn/ui

- **Yalnızca shadcn/ui** + Tailwind CSS (v4) + lucide-react. MUI, Chakra UI, Ant Design, Mantine, Headless UI veya başka component library **yok**. Radix primitive'ler yalnızca shadcn/ui bileşenlerinin parçası olarak.
- Ürüne özel bileşenler shadcn/ui üzerine inşa edilir. Paylaşılan web bileşenleri `@pusula/ui`; design token'lar `@pusula/ui/theme.css`; shadcn ekleme: `components.json` `packages/ui`'da konfigüre.
- **i18n & hardcode:** UI bileşenleri hardcode metin **içermez**; entity-bağımsız ve yerelleştirme (i18n) standartlarına uygun. Metinler çeviri katmanından gelir, etiketler/format locale-aware olur.
- Accessibility: keyboard navigation, focus yönetimi, ARIA — özellikle board ekranı ve modal akışları.

### 8.1.1 Auth ekranları & oturum yönetimi (Faz 1)

Better Auth HTTP route'ları `apps/api` (`${API_URL}/api/auth/*`); web tarafı `apps/web/src/lib/auth-client.ts` (`better-auth/react`) ile bunları çağırır. Web ve API farklı origin'de (dev: `:3000` / `:3001`) — session cookie API origin'inde set edilir, tRPC ve auth istekleri `credentials: 'include'` ile gider; bu yüzden **oturum yönetimi client-side**'dır (Next.js RSC, API origin'inin cookie'sine erişemez — server-side session kontrolü bu fazda yok).

- **Public route'lar:** `app/(auth)/sign-in`, `app/(auth)/sign-up`. `(auth)/layout.tsx` zaten oturum açıksa `/`'a yönlendirir. Formlar native `<form>` + `@pusula/domain` zod şemalarıyla client-side validasyon; `authClient.signUp.email(...)` / `signIn.email(...)`; hata mesajı inline; başarıda `?redirect=` (varsa) ya da `/`'a gider.
- **Korumalı kabuk:** `app/(app)/layout.tsx` (client component) `authClient.useSession()` kullanır — `pending` → iskelet/spinner; oturum yok → `router.replace('/sign-in?redirect=…')`; oturum var → app shell (header: kullanıcı adı + çıkış). `app/(app)/page.tsx` = `(app)/` varış noktası — workspace sayısına göre dallanır (onboarding / tek workspace'e yönlendir / workspace listesi), bkz. §8.1.3. Çıkış: `authClient.signOut()` → `/sign-in`.
- **Workspace oluşturma:** shadcn `Dialog` + ad input → `trpc.workspace.create` mutation (`clientMutationId` istemcide üretilir, ör. `crypto.randomUUID()`); başarıda `workspace.list` invalidate. Tam optimistic UI Faz 4'te.
- **Workspace daveti (Faz 1.3 web):** workspace kartında — kullanıcı `admin+` ise — "Davet et" → shadcn `Dialog`, e-posta input (rol varsayılan `member`; rol seçimi sonraki iterasyon) → `trpc.workspace.members.invite`; hata inline (`CONFLICT` → "zaten üye/davetli"). Workspace listesinde ayrıca "Bekleyen davetler" bölümü → `trpc.workspace.invitations.mine` (kullanıcının kendi e-postasına gelen `pending` davetler: workspace adı, rol, davet eden, son tarih) + "Kabul et" / "Reddet" → `invitations.accept` / `invitations.decline` (`clientMutationId` ile); kabul başarılı → `workspace.list` (+ `invitations.mine`) invalidate. Gönderilmiş davetleri yönetme (`invitations.list`/`revoke`) workspace üyeler ekranıyla birlikte (sonraki iterasyon). Backend sözleşmesi → [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Workspace davet akışı).
- **Şifre sıfırlama (Faz 1 ek iş — [DEM-68](https://linear.app/demirkol/issue/DEM-68)):** `(auth)/forgot-password` — e-posta input (`@pusula/domain` `emailSchema` ile istemcide doğrula) → ``authClient.requestPasswordReset({ email, redirectTo: `${window.location.origin}/reset-password` })`` (`redirectTo` **mutlak** — web app origin'i; Better Auth `redirectTo`'yu sunucu tarafında `baseURL` [`API_URL`] göre çözer, göreli yol API sunucusuna işaret ederdi; mutlak web URL'i `trustedOrigins`/`APP_URL` origin kontrolünden geçer — bkz. [`07-auth.md`](07-auth.md)) → "Bağlantı e-posta adresine gönderildi" durum mesajı (e-posta kayıtlı mı belli edilmez — kullanıcı listesi sızdırılmaz; başarı/başarısızlık aynı mesajı verir). `(auth)/reset-password` — `?token=` URL'den okunur (`useSearchParams` + `<Suspense>`); token yoksa "geçersiz/eksik bağlantı" + `/forgot-password`'a link; varsa yeni parola + tekrar input (`passwordSchema` ile doğrula) → `authClient.resetPassword({ newPassword, token })` → başarıda `/sign-in?reset=1`'e yönlendir ("parolan güncellendi" bildirimi sign-in'de bir kez gösterilir), Better Auth token-geçersiz/süresi-dolmuş hatasında açıklayıcı inline mesaj + "yeni bağlantı iste". `/reset-password` ve `/forgot-password` rotaları `Referrer-Policy: no-referrer` ile servis edilir (token query'de — Referer sızıntısına karşı; `apps/web/next.config.ts`). Sign-in ekranına **"Şifremi unuttum"** linki (parola alanının altına). Yalnız shadcn/ui + mevcut bileşenler; metinler `strings.auth.*` (yeni `forgotPassword.*` / `resetPassword.*` grupları). `(auth)/layout.tsx` zaten oturum açıksa bu sayfalardan da `/`'a yönlendirir. Backend (Better Auth `sendResetPassword` → Resend transactional) → [`07-auth.md`](07-auth.md) (Şifre sıfırlama akışı).
- **Form kütüphanesi:** Faz 1 auth formları basit (email/parola) — native form + zod yeterli; ayrı form kütüphanesi (react-hook-form vb.) eklenmez. Karmaşık form ihtiyacı doğarsa ayrı karar.
- **shadcn bileşenleri** (`@pusula/ui`): `Button` mevcut; bu işle eklenenler (`Input`, `Label`, `Card`, `Dialog`, …) — `pnpm dlx shadcn@latest add …` `packages/ui` içinden, sonra `src/index.ts`'den export, `apps/web` zaten `@source` ile `packages/ui/src`'i Tailwind taramasına dâhil ediyor.
- **Test:** Auth formları için React Testing Library (render + validasyon + `authClient` mock'lu submit). Playwright e2e (sign-up → workspace listesi → workspace oluştur → çıkış) ileri faz / Faz 8 sertleştirmeyle birlikte.

### 8.1.2 Workspace yönetim ekranı (Faz 1)

Backend'de hazır olan workspace yönetim procedure'lerinin (`workspace.update`/`archive`, `workspace.members.{list,updateRole,remove}`, `workspace.invitations.{list,revoke}`) UI'ı. Backend sözleşmesi → [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Workspace yetkilendirme + davet akışı), [`03-backend.md`](03-backend.md).

- **Route:** `app/(app)/workspaces/[id]/page.tsx` (client component) — workspace detay/ayarlar görünümü. `workspaceId` = path param; `trpc.workspace.get` ile shell verisi (ad, slug, rol, üye sayısı). `app/(app)/page.tsx` workspace listesinde kart başlığı bu route'a `Link`'tir. Faz 2'de board listesi/board ekranı bu route'un altına gelir (`workspaces/[id]/boards/[id]`).
- **Rol-bazlı görünürlük (UI seviyesi; sunucu zaten enforce eder):** ayarlar (rename/slug) ve davet/rol/üye-çıkar aksiyonları yalnızca `owner`/`admin`'e gösterilir; arşivleme yalnızca `owner`'a; gönderilmiş davet listesi `member+`'e görünür ama "iptal et" yalnızca `admin+`'e. Herkes (owner hariç) "workspace'ten ayrıl" görür. `owner` rolü UI'da değiştirilemez/çıkarılamaz olarak işaretlenir.
- **Ayarlar:** ad + slug düzenleme formu (`@pusula/domain` `workspaceNameSchema`/`workspaceSlugSchema` ile client-side validasyon) → `trpc.workspace.update`; `CONFLICT` (slug çakışması) inline.
- **Tehlikeli bölge** (yalnızca `owner`): (1) **Arşivle** → `trpc.workspace.archive` (onaylı `Dialog`) → başarıda `router.replace('/')` + `workspace.list` invalidate. (2) **Kalıcı sil** (`workspace.delete` — geri dönüşsüz; backend DEM-24, UI DEM-40) → onaylı `Dialog` içinde workspace adını birebir yazma input'u (`confirmName`; eşleşmeden onay butonu disabled); `trpc.workspace.delete({ workspaceId, confirmName, clientMutationId })` → başarıda `router.replace('/')` + `workspace.list` invalidate; `BAD_REQUEST` (ad eşleşmiyor) / `FORBIDDEN` inline `Alert`. "Sil" "Arşivle"den ayrı ve daha vurgulu (destructive) gösterilir.
- **Üyeler:** `trpc.workspace.members.list` → satır başına ad/e-posta/rol; `admin+` ise non-owner satırlarda rol değiştir (shadcn `Select` — `assignableWorkspaceRoleSchema`: `admin`/`member`/`guest`) → `trpc.workspace.members.updateRole`, ve "çıkar" → `trpc.workspace.members.remove`. Kendi satırında (owner değilse) "workspace'ten ayrıl" → `members.remove` (kendi `userId`) → başarıda `router.replace('/')` + `workspace.list` invalidate. Mutation sonrası `members.list` invalidate.
- **Gönderilmiş davetler:** `trpc.workspace.invitations.list` → bekleyen davetler (e-posta, rol, davet eden, son tarih); `admin+` ise "iptal et" → `trpc.workspace.invitations.revoke` → `invitations.list` invalidate. "Üye davet et" dialog'u (§8.1.1'deki `InviteMemberDialog`, davet sonrası `invitations.list` invalidate eder) bu ekrana taşınır; workspace listesi kartından kaldırılır.
- **shadcn:** bu işle eklenenler: `Select` (`@radix-ui/react-select`) — `packages/ui`'a eklenip `src/index.ts`'den export edilir.
- **`clientMutationId`:** her mutation istemcide üretilir (`crypto.randomUUID()`). Tam optimistic UI Faz 4 (DEM-27); bu fazda mutation sonrası ilgili query'ler invalidate edilir.
- **Test:** sunum (presentational) bileşenleri için React Testing Library (render + rol-bazlı görünürlük + validasyon + callback).

### 8.1.3 Yeni kullanıcı onboarding'i & signup bootstrap (Faz 1)

Yeni kayıt olan kullanıcı boş bir ekrana düşmesin diye signup'ta otomatik bir default workspace + boş "İlk Pano" oluşturulur; login sonrası `(app)/` varış noktası kullanıcının workspace sayısına göre dallanır. Domain kuralı (best-effort default workspace, `workspace.list` boş olabilir) → [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) (invariant 11).

- **Signup bootstrap (`apps/api`):** Better Auth instance'ı (`apps/api/src/auth.ts`) `databaseHooks.user.create.after` ile yeni kullanıcı için **tek transaction'da**: `workspaces` (sabit ad `"Çalışma Alanım"` — `@pusula/domain` `ONBOARDING_WORKSPACE_NAME`; benzersiz slug, suffix'li) + `workspace_members` (rol `owner`) + `activity_events`(`workspace.created`) + `boards` (`"İlk Pano"` — `ONBOARDING_BOARD_TITLE`) + `board_members` (creator `admin`) + `activity_events`(`board.created`) **+ pano şablonu**: varsayılan listeler (`Yapılacak` / `Devam Eden` / `Bitti` — `ONBOARDING_LIST_TITLES`; `position` fractional, `@pusula/domain/position`) + `Yapılacak` listesinde birkaç welcome/örnek kart (`ONBOARDING_WELCOME_CARDS`) + ilgili `list.created` / `card.created` activity'leri (actor = yeni kullanıcı). Şablon içeriği `@pusula/domain` sabitleri (i18n placeholder; kullanıcı-yüzlü → Türkçe). Bootstrap **best-effort**: hata loglanır, exception **yeniden fırlatılmaz** — signup başarılı sayılır, kullanıcı yine `(app)/`'a düşer (orada 0-workspace onboarding'ini görür). Mantık `apps/api/src/bootstrap.ts`'te; Better Auth tarafı → [`07-auth.md`](07-auth.md). Domain kuralı (best-effort default workspace + pano şablonu) → [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) (invariant 11).
- **`(app)/page.tsx` yönlendirme:** `trpc.workspace.list` yüklenir; sonuca göre:
  - **`pending`** → kısa "yükleniyor" placeholder.
  - **`error`** → hata `Alert`'i + tekrar dene.
  - **0 workspace** → onboarding boş-durumu (`_components/onboarding-empty-state.tsx`: Pusula/workspace kavramını anlatan kart + "Workspace oluştur" CTA → `CreateWorkspaceDialog`). Bootstrap çalıştıysa bu nadirdir, ama kullanıcı son workspace'inden ayrılınca da bu duruma düşer. `PendingInvitations` bu durumda da gösterilir.
  - **1 workspace** → `useEffect` içinde `router.replace('/workspaces/[id]')` (o workspace'e doğrudan götür); geçişte kısa placeholder. (İleride workspace'in tek panosu varsa doğrudan o panoya götürmek opsiyonel iyileştirme — Faz 2D board ekranıyla.)
  - **2+ workspace** → mevcut workspace listesi (başlık + `CreateWorkspaceDialog` + `PendingInvitations` + kart grid'i).
- **Metinler:** onboarding metinleri `strings.onboarding` (+ gerekirse `strings.workspace.redirecting`); hardcode metin yok.
- **`clientMutationId`:** onboarding'deki workspace oluşturma `CreateWorkspaceDialog` üzerinden (istemcide üretilir); yeni mutation yok.
- **Test:** `onboarding-empty-state` için React Testing Library (render + CTA); `(app)/page.tsx` yönlendirme mantığı için RTL (0 / 1 / 2+ / pending / error senaryoları; `useTRPC` / `useQuery` / `useRouter` mock'lu). Backend bootstrap için ileride tRPC/integration testi opsiyonel (best-effort yol).

### 8.1.4 Board ekranı (Faz 2D)

Backend Faz 2A/2B/2C tamam (`board.{list,create,get,update,archive}`, `list.{create,update,archive}`, `card.{create,get,update,archive}`). Bu faz web tarafında **salt CRUD** board ekranını kurar — drag-drop **yok** (Faz 3 — [DEM-26](https://linear.app/demirkol/issue/DEM-26)), optimistic UI **zorunlu değil** (Faz 4 — [DEM-27](https://linear.app/demirkol/issue/DEM-27); bu fazda mutation → `await` → ilgili query invalidate → refetch). Backend sözleşmesi → [`03-backend.md`](03-backend.md) (Faz 2 — board / list / card procedure'leri), [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Board / List / Card procedure haritası); board ekranı veri akışı → [`05-board-mekanigi.md`](05-board-mekanigi.md) §5.0.

- **Board listesi:** `app/(app)/workspaces/[id]/page.tsx` üst bölümüne (ayarlar/üyeler kartlarından **önce**) "Panolar" bölümü — `trpc.board.list` ({ workspaceId }); kart başına board adı → `Link` `/workspaces/[id]/boards/[boardId]`; "Pano oluştur" → shadcn `Dialog` + ad input (`@pusula/domain` `boardTitleSchema` ile client-side validasyon) → `trpc.board.create` (`clientMutationId` istemcide) → `board.list` invalidate. Arşivli board'lar listede soluk/ayrı işaretli ve salt-okunur; board satırındaki `role` alanına göre aksiyonlar gizlenir/gösterilir (gerçek kapı server-side).
- **Board detay:** `app/(app)/workspaces/[id]/boards/[boardId]/page.tsx` (client component) — `trpc.board.get` ({ boardId }) tek seferde `{ board: {…, role}, lists: [...] (arşivli dahil, `position` sıralı), cards: [...] (yalnızca aktif, `position` sıralı) }` döndürür. Yatay kaydırılan kolon (liste) düzeni; her kolonda listenin kartları (`cards` `listId`'ye göre gruplanır). Kolon/kart ölçüleri stabil — hover/edit'te layout shift yok. Üstte board başlığı (admin ise inline yeniden adlandırma) + "geri" linki (`/workspaces/[id]`). Board yoksa/erişim yoksa `NOT_FOUND`/`FORBIDDEN` → `Alert` + geri linki.
- **List CRUD** (board `member+` ise göster — `board.role`): "Liste ekle" (kolon listesi sonuna inline form → `trpc.list.create` { boardId, title }) · "yeniden adlandır" (kolon başlığı inline edit → `trpc.list.update` { listId, title }) · "arşivle" (kolon menüsü → `trpc.list.archive` { listId, archived: true }) — her biri sonrası `trpc.board.get` invalidate.
- **Card CRUD** (board `member+`): "Kart ekle" (kolon altına inline form → `trpc.card.create` { listId, title }) · "düzenle" (kart tıkla → dialog: başlık/açıklama/`due_at` → `trpc.card.update`) · "arşivle" (kart menüsü/dialog → `trpc.card.archive` { cardId, archived: true }) — her biri sonrası `trpc.board.get` invalidate.
- **Yetki:** UI board rolüne göre aksiyonları gizler/gösterir (`viewer` salt-okunur); arşivli board/liste salt-okunur (server zaten reddeder, UI da aksiyonları kapatır). Mutation hatası inline `Alert`.
- **shadcn:** mevcut bileşenler büyük ölçüde yeterli (`Button`/`Card`/`Dialog`/`Input`/`Label`/`Alert`/`Badge`); gerekirse `DropdownMenu` (`@radix-ui/react-dropdown-menu`) ve/veya `Textarea` eklenir (`packages/ui` → `pnpm dlx shadcn add …`, `src/index.ts` export). Türkçe metinler `apps/web/src/lib/strings.ts` (`strings.board`); UI bileşenleri hardcode metin içermez.
- **`clientMutationId`:** her mutation istemcide üretilir (`crypto.randomUUID()`). Tam optimistic UI Faz 4 (DEM-27).
- **Test:** React Testing Library — board listesi (render + "Pano oluştur" akışı + boş durum), board detay (kolon/kart render + boş board), liste/kart oluşturma akışları (presentational form bileşenleri izole, callback + validasyon). Playwright e2e (board oluştur → liste/kart ekle) ileri faz (Faz 8 — [DEM-31](https://linear.app/demirkol/issue/DEM-31)).
- **Layout & scroll davranışı ([DEM-88](https://linear.app/demirkol/issue/DEM-88) — 2026-05-13):** Board ekranı viewport-bound — `AppShell` board route'ta `h-svh overflow-hidden` (`_components/app-shell.tsx`'te `fullBleed` regex'iyle koşullu; diğer rotalar `min-h-svh` eski davranışı korur). Sayfa (body/page) scroll **YOK**. Flex zinciri her column parent'ında `min-h-0` taşır (Tailwind/CSS'te flex item default'u `min-height: auto` — bu override edilmezse overflow zinciri kırılır, child parent yüksekliğini aşar). `BoardColumns` strip `flex items-start gap-3 overflow-x-auto overflow-y-hidden pusula-scrollbar` (yatay ince scrollbar; dikey strip yapmaz — `items-start` ile kolonlar top-aligned ve içeriği kadar yüksek). `ListColumn` 3-segment: kolon `flex max-h-full flex-col` (içeriği kadar; viewport sınırına dayanınca sınırlanır — `h-full` değil, aksi halde boş kolonlar viewport-tall görünür) · header `shrink-0` · cards area `flex min-h-0 flex-col overflow-y-auto pusula-scrollbar` (içerik kadar yer kapla, sınıra dayanınca kart listesi içinde scroll — `flex-1` yok çünkü içerik az olsa cards area boş alanı doldurup kolonu şişirirdi) · footer (Kart ekle) `shrink-0`. Custom scrollbar utility `.pusula-scrollbar` (`packages/ui/src/styles/theme.css` `@layer utilities` — 6px thin, transparent track, soft OKLCH thumb; `--scrollbar-thumb` + `--scrollbar-thumb-hover` token'lar light + dark) — strip yatay scrollbar'a da uygulanır. Tasarım dili spec'i → [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) §13.2 (kolon anatomisi + scrollbar token).

### 8.1.5 Kart detay görünümü (Faz 2.5D)

Board ekranında (§8.1.4 Board ekranı) bir karta tıklanınca açılan kart detay görünümü — Trello kart modali. Backend Faz 2.5A/2.5B (`comment.*`, `checklist.*`/`checklist.item.*`, `card.members.*`, `card.labels.*`, `label.*`) tamam olmalı. Backend sözleşmesi → [`03-backend.md`](03-backend.md) (Faz 2.5 procedure'leri), [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Board / Card içerik procedure haritası — Faz 2.5).

- **Sunum: modal + URL query param.** Kart detayı shadcn `Dialog` ile board ekranının **üstünde** açılır (board arkada kalır — Trello hissi). Açık kart `?card=<cardId>` query param'ıyla URL'e yansır (paylaşılabilir/derin link; sayfa yenilenince modal açık gelir). Next.js intercepting/parallel route kullanılmaz — `useSearchParams` + `router.push`/`replace` (shallow) ile yönetilir; modal kapanınca param kaldırılır. Param geçersiz kart id'siyse modal açılmaz (sessiz) ya da kısa "kart bulunamadı" gösterir.
- **Veri:** `trpc.card.get` ({ cardId }) — kart + kullanıcının kart ilişkileri (`card_members`); ayrıca `trpc.comment.list` ({ cardId }), checklist'ler (kart payload'ında veya ayrı query — `card.get` zaten kartı döndürüyor; checklist/etiket/üye için ek alanlar eklenir ya da ayrı query'lerle çekilir; implementasyonda netleşir), board etiketleri (`board.get`'e `labels` eklenir ya da `label.list`), kart activity feed (ayrı `activity.list` ({ cardId }) — board/kart query'sini şişirme; **G2** boşluk-tara notu burada). Permission `card.get` ile (board `viewer+`) — `viewer` salt-okunur.
- **İçerik (modal):** başlık (board `member+` ise inline edit → `card.update`) · açıklama (inline edit, boş→`null`/`''` normalize → `card.update`) · due-date picker (`<input type=date>` veya küçük takvim → `card.update` `dueAt` set/clear) · üyeler — `assignee`/`watcher` listesi + ekle (board üyeleri arasından seç → `card.members.add`) / çıkar (`card.members.remove`); `viewer` yalnızca "kendini watcher yap / bırak" görür · etiketler — kartın etiketleri + board etiket listesinden ata/kaldır (`card.labels.add`/`remove`) · checklist'ler — checklist ekle/yeniden adlandır/sil (`checklist.*`), item ekle/işaretle/düzenle/sil/sırala (`checklist.item.*`), her checklist için ilerleme (X/Y) · yorumlar — listele (`comment.list`, eskiden yeniye) + ekle (`comment.create`, düz metin) + düzenle/sil (yazan veya board `admin` — `comment.update`/`delete`; silinmiş yorum "silindi" placeholder'ı) · kart activity feed (kronolojik geçmiş — okunabilir özet satırları) · "arşivle" (zaten board ekranında var; modalda da olabilir → `card.archive`).
- **Yetki & arşiv:** UI board rolüne göre aksiyonları gizler (`viewer` salt-okunur; "kendini watcher yap" hariç); arşivli board/liste/kart salt-okunur (server zaten reddeder, UI da kapatır). Mutation hatası inline `Alert`.
- **Reconcile:** Optimistic UI **bu fazda zorunlu değil** (Faz 4 — [DEM-27](https://linear.app/demirkol/issue/DEM-27)) — her mutation → `await` → ilgili query'leri (`card.get`/`comment.list`/`board.get`/checklist/activity) invalidate → refetch. `clientMutationId` her mutation'da istemcide üretilir (`crypto.randomUUID()`).
- **shadcn:** mevcutlar büyük ölçüde yeterli (`Dialog`/`Button`/`Input`/`Textarea`/`Label`/`Card`/`Alert`/`Badge`/`Select`); gerekirse `Checkbox` (checklist item), `Popover` (etiket/üye seçici), `Tabs` (yorum/activity sekmeleri) eklenir (`packages/ui` → `pnpm dlx shadcn add …`, `src/index.ts` export — yalnızca shadcn/ui + Tailwind + lucide-react). Türkçe metinler `apps/web/src/lib/strings.ts` (`strings.card`); UI bileşenleri hardcode metin içermez.
- **Test:** React Testing Library — modal aç/kapa (+ `?card` param senkronu), açıklama/due inline edit, üye/etiket ekle-kaldır, checklist item toggle, yorum ekle/sil, activity feed render; presentational form/list bileşenleri izole (callback + validasyon, tRPC mock'lu). Playwright e2e ileri faz (Faz 8).
- **Faz 2.7C ([DEM-64](https://linear.app/demirkol/issue/DEM-64)) + 2.7C-2 ([DEM-74](https://linear.app/demirkol/issue/DEM-74)) — yapısal yeniden kurulum + tasarım uyumu:** bu modal Faz 2.7'de tasarım diline çekildi (bağlayıcı spec → [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) §13.3 + §13.5; ortak bileşenler §13.4). Yapı: shadcn `Dialog` içinde **iki-kolon** (`grid md:grid-cols-[1fr_360px]`; modal `w-[min(960px,92vw)]` + `sm:max-w-none` — shadcn `DialogContent` base'inin `sm:max-w-lg`'ini geçersiz kılmak için ŞART; sol kolon `min-w-0 overflow-y-auto px-5 py-4 space-y-5`), **kapak-renkli başlık çubuğu** (breadcrumb pano/liste + bell/link/⋮/×; kapak rengi seçiliyse `bg-palet-{ad} text-palet-{ad}-foreground`, yoksa `bg-background border-b` — DEM-67 backend'i `cards.coverColor`, DEM-74'te wire), **sol kolon** = sticky başlık + `CardCompleteToggle` (DEM-66 `card.complete`/`card.uncomplete` ile wire — tamamlanmış → `bg-success` tik + başlık `line-through text-muted-foreground`) + meta chip satırı (üye/due/etiket + kapak-rengi → 12-renk `--palet-*` picker → `card.update({coverColor})`) + AÇIKLAMA (`RichTextEditor` — Tiptap, §13.5) + KONTROL LİSTESİ (`SectionHeader` aksiyon-slotu ikon-only `ghost` + `Tooltip` — inline metin değil; `Progress` + atanan `Avatar`), **sağ panel** ≈360px = `Tabs` sekme strip (Yorumlar/**Aktivite**/Ekler/Tümü — DEM-74'te "İşlemler"→"Aktivite") + yorum composer (`Avatar` + mini `RichTextEditor` + B/I/`PaperclipIcon` disabled placeholder + Gönder) + aktivite feed + `EmptyState`; mobilde (`md:` altı) tek kolon (sağ panel alta düşer). Kart üzerinde (`card-item.tsx`): kapak görseli yoksa `coverColor` varsa üst şerit `-mx-2 -mt-2 mb-1.5 h-3 rounded-t-md bg-palet-{ad}`; `CardCompleteToggle` hover'da görünür (tamamlanmışsa hep), tamamlanmış → başlık `line-through`. **Backend sözleşmesi değişmez** — `card.update` / `comment.*` / `card.complete` / `card.uncomplete` procedure imzaları aynı (DEM-64/66/67'de eklendi, DEM-74 yalnız UI-wire); `description`/`body` artık **Tiptap JSON** olarak saklanır ama `text` kolonu değişmez, migration yok, mevcut plain-text içerikler render/edit anında düz paragrafa parse edilir (parse-time fallback; §13.5 + [`02-teknoloji-kararlari.md`](02-teknoloji-kararlari.md) Karar kaydı 2026-05-12). Optimistic UI yine yok (Faz 4); `clientMutationId` istemcide.

### 8.1.6 Board üye + etiket yönetimi UI (Faz 2.5E — [DEM-54](https://linear.app/demirkol/issue/DEM-54))

Board ayarları/detay ekranına üye/rol yönetimi + etiket yönetimi + board ekranında etiket filtresi. Backend Faz 2.5B (`label.*`) + Faz 2.5C (`board.members.*`, `board.invitations.*`) tamam. Backend sözleşmesi → [`03-backend.md`](03-backend.md) (Faz 2.5 procedure'leri), [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md).

- **Konum:** board detay sayfasında (§8.1.4 Board ekranı), üst bar aksiyon alanında board `admin` için üç net giriş bulunur: `Davet et`, `Paylaş`, `Pano ayarları`. `Davet et` ayar dropdown'unu **Davetler** sekmesinde açar; `Paylaş` board linkini panoya kopyalar; `Pano ayarları` shadcn `DropdownMenu` içinde `Tabs` ile "Üyeler" + "Davetler" + "Erişim talepleri" + "Etiketler" + "Arka plan" + "Pano işlemleri" bölümlerini taşır. Eski `Davet et / paylaş` birleşik butonu ve ayrı `⋮` board menüsü kaldırılır; rename/archive/restore gibi board lifecycle aksiyonları tek yüzey olarak "Pano işlemleri" altında kalır. Dropdown yalnızca board `admin`'e mount edilir (gerçek kapı backend; UI rol-bazlı gizler — §8.1.2 modeli). Satır içi destructive onaylar mevcut shadcn `Dialog` pattern'ini korur.
- **Üyeler** (`board-settings/board-members-section.tsx` + sunum `board-member-row.tsx`): `trpc.board.members.list` → satır başına ad/rol; inherited (workspace owner/admin) satırlar "workspace yöneticisi — düzenlenemez" işaretli (rol/çıkar yok). `admin` ise: rol değiştir (shadcn `Select` — `boardRoleSchema`: `admin`/`member`/`viewer`; tek explicit `admin` ve inherited satır kilitli) → `board.members.updateRole`; çıkar → `board.members.remove` (kendi satırında "board'dan ayrıl" → başarıda `/workspaces/[id]`'e yönlendir + `board.list`/`workspace.list` invalidate); üye ekle (e-posta + rol — `add-board-member-form.tsx`) → `board.members.add`, sonuç `kind` discriminated union: `added` / `added_as_guest` / `invited` → inline durum mesajı; `CONFLICT` → "zaten üye / zaten davetli". Mutation sonrası `board.members.list` + `board.get` (+ ekleme sonrası `board.invitations.list`) invalidate.
- **Gönderilmiş board davetleri** (`board-settings/board-sent-invitations.tsx`): `trpc.board.invitations.list` → bekleyen davetler (e-posta, rol, davet eden, son tarih); `admin` ise "iptal et" → `board.invitations.revoke` → `invitations.list` invalidate. Kullanıcının kendi board davetleri (`board.invitations.mine`) workspace davetleriyle aynı yerde — `(app)/page.tsx` "Bekleyen davetler" bölümü (§8.1.1'deki `PendingInvitations`) genişletildi: "Workspace davetleri" + "Board davetleri" alt grupları; board satırı: board adı + workspace adı + rol + davet eden + son tarih; "Kabul et"/"Reddet" → `board.invitations.accept`/`decline` (`clientMutationId` ile); kabul sonrası `workspace.list` + `board.invitations.mine` + `workspace.invitations.mine` invalidate.
- **Board erişim talepleri** (`board-settings/board-access-requests-section.tsx`): `trpc.board.accessRequests.list` → board linkinden talep gönderen kullanıcılar (ad/e-posta, mesaj). `admin` satır başına yalnızca board rolünü (`member`/`viewer`) seçer; `Onayla` → `board.accessRequests.approve` (backend tek transaction'da gerekirse workspace `guest` + board membership oluşturur) ve `accessRequests.list` + `board.members.list` + `board.get` invalidate eder. `Reddet` → `board.accessRequests.reject` ve yalnızca talep listesini yeniler.
- **Etiketler** (`board-settings/board-labels-section.tsx` + sunum `board-label-row.tsx` / `create-board-label-form.tsx`): board ayarları dropdown'unda `trpc.label.list` → satır başına renk swatch + ad (boşsa "(adsız)"); board `member+` (dropdown admin-only olduğu için zaten sağlanır; arşivli board salt-okunur): etiket ekle (renk seçici — `@pusula/domain` `LABEL_COLORS` paleti + opsiyonel ad) → `label.create`; satır içi düzenle (renk + ad) → `label.update`; sil (onaylı — kartlardan da kalkar) → `label.delete`. `CONFLICT` (aynı renk+ad) inline. Mutation sonrası `label.list` + `board.get` (kart rozetleri) invalidate. Renk→Tailwind swatch eşlemesi paylaşılan `_components/label-colors.ts`'te (`LABEL_SWATCH`; eskiden `card-detail/` altındaydı, yukarı taşındı — kart detayı + board ayarları + board ekranı ortak kullanır).
- **Etiket filtresi (board ekranı)** (`board-filter-bar.tsx` + saf `board-filter.ts`): board kolonlarının üstünde bir filtre çubuğu — etiket çipleri (çoklu seçim; bir kart **en az bir** seçili etikete sahipse gösterilir — client-side filtre, `board.get` verisi üzerinde). Etiket paleti `board.get`'in döndürdüğü `cards[].labels`'tan türetilir (board ekranı `label.list` çağırmaz). Filtre durumu local state (URL param değil — sade tutuldu). Kart rozetleri: `card-item.tsx` artık `card.labels`'ı küçük renk çipleri olarak gösterir.
- **Arşivli öğeler toggle (G7):** aynı filtre çubuğunda "arşivli listeleri göster/gizle" toggle'ı (+ "N arşivli liste" sayacı) — board ekranı varsayılan olarak arşivli listeleri **gizler**; toggle açıkken arşivli listeler soluk gösterilir ve mevcut restore aksiyonu (`list.archive` `archived:false`) erişilebilir olur. **Arşivli kartlar bu fazda yapılmadı** — `board.get` arşivli kartları zaten döndürmüyor; arşivli-kart görünümü için `board.get`'e `includeArchivedCards` flag'i ileri faz/follow-up (board.get'i fazla genişletmemek için). Tam "arşiv kutusu" ekranı da isteğe bağlı ileride.
- **`board.get` additive alanı:** board ekranı etiket filtresi + kart rozetleri için `board.get`'in `cards` projection'ına her kart için `labels: { labelId, name, color }[]` eklendi (`card_labels ⋈ labels`, board genelinde tek sorgu, server'da Map ile gruplanır) — mevcut shape additive, eski testler kırılmaz; bkz. [`03-backend.md`](03-backend.md) (`board.get`).
- **shadcn:** yalnızca mevcut bileşenler — `DropdownMenu`/`Tabs`/`Dialog`/`Select`/`Button`/`Input`/`Label`/`Badge`/`Alert` (yeni paket eklenmedi; renk seçici/arşiv toggle için `Popover`/`Switch` yerine native `button`/`input` + mevcut bileşenler — §8.1.5'teki gibi). Türkçe metinler `strings.board.settings` / `strings.board.filter` / `strings.invitations` (board grupları); hardcode metin yok. `clientMutationId` her collaborative mutation'da istemcide (`crypto.randomUUID()`). Optimistic UI yok (Faz 4) — mutation → `await` → ilgili query invalidate → refetch; hata inline `Alert`.
- **Test:** React Testing Library — board üye satırı (rol-bazlı görünürlük + inherited/last-admin işareti + rol değiştir/çıkar callback), board üye ekle formu (e-posta/rol validasyon + submit), board etiket satırı (düzenle/sil callback + no-op save), board etiket oluştur formu (renk/ad + conflict), board filtre çubuğu (çip seçim/temizle + arşiv toggle), board filtre saf fonksiyonları (`board-filter.ts` — OR semantiği + arşivli liste filtresi/sayacı), kart rozetleri. Backend için `board.test.ts`'e `board.get` `cards[].labels` testi eklendi. Playwright e2e ileri faz (Faz 8).

### 8.1.7 Profil / hesap ayarları ekranı (Faz 1)

Kullanıcının kendi hesabını yönetebileceği ekran — `bosluk-tara` (G5) ile açıldı ([DEM-55](https://linear.app/demirkol/issue/DEM-55)). Bu ekran **tRPC kullanmaz**; doğrudan Better Auth client (`apps/web/src/lib/auth-client.ts`) uçlarına gider — sözleşme → [`07-auth.md`](07-auth.md) (Profil & hesap yönetimi); şemalar `@pusula/domain` (`updateProfileInput` / `changePasswordInput` / `deleteAccountInput`). Hesap silme domain kuralı → [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Hesap (User) — öz-yönetim), [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) (invariant 14).

- **Route:** `app/(app)/account/page.tsx` (client component) — `authClient.useSession()` ile mevcut kullanıcı (oturum kabuğu `(app)/layout.tsx` zaten korur). App-shell header'ına "Hesap" linki (`/account`) eklenir (kullanıcı adının yanında).
- **Profil:** ad input (`@pusula/domain` `userNameSchema` ile client-side validasyon) + avatar URL input (boş → kaldır; URL formatı doğrulanır) → `authClient.updateUser({ name, image: image || null })`; başarıda `authClient.useSession().refetch()` (header güncellensin) + başarı mesajı. Avatar **basit URL** (yükleme yok — Faz 8 attachment'a bağlanabilir; karar 2026-05-12).
- **Parola değiştir:** mevcut parola + yeni parola (+ yeni parola tekrar — client-side eşleşme kontrolü) → `authClient.changePassword({ currentPassword, newPassword, revokeOtherSessions: true })`; hatalar inline (`currentPassword` yanlış → "Mevcut parola hatalı"); başarıda alanlar temizlenir + başarı mesajı.
- **Tehlikeli bölge — hesabı sil:** onaylı `Dialog` içinde parola input'u (re-auth) → `authClient.deleteUser({ password })`; başarıda `/sign-in`'e dön. Kullanıcı bir workspace'in `owner`'ıysa sunucu (`beforeDelete` hook) `BAD_REQUEST` döndürür → inline `Alert` ("Önce sahibi olduğun çalışma alanlarını silmen/devretmen gerekiyor" + workspace listesine link). UI bunu önceden de işaret edebilir (kullanıcının `owner` olduğu workspace sayısı `trpc.workspace.list`'in `role` alanından okunabilir) ama gerçek kapı sunucudadır.
- **shadcn:** mevcutlar yeterli (`Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardContent`/`Button`/`Input`/`Label`/`Alert`/`Dialog`); yeni bileşen yok. Türkçe metinler `apps/web/src/lib/strings.ts` (`strings.account`); UI bileşenleri hardcode metin içermez. `clientMutationId` yok (Better Auth uçları — tRPC collaborative mutation değil).
- **Test:** React Testing Library — presentational form bileşenleri (`ProfileForm` / `ChangePasswordForm` / `DeleteAccountSection`): render + validasyon + callback (auth-client mock'lu). `canDeleteOwnAccount` domain kuralı için `@pusula/domain` Vitest. Playwright e2e ileri faz (Faz 8).

### 8.1.8 Board ekranı drag-drop (Faz 3B — [DEM-43](https://linear.app/demirkol/issue/DEM-43))

Board ekranına (§8.1.4 Board ekranı / §8.1.5 kart detayı; tasarım dili [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) §13.2) sürükle-bırak: **kolon (liste) reorder · kart reorder (liste içi) · kart cross-list taşıma**. Yalnızca **Atlassian Pragmatic Drag and Drop** (`@atlaskit/pragmatic-drag-and-drop`; dnd-kit vb. **yok** — proje kararı). Backend sözleşmesi `list.move` + `card.move` (Faz 3A — [DEM-42](https://linear.app/demirkol/issue/DEM-42)) → [`03-backend.md`](03-backend.md); mekanik / server akışı + zorunlu UI ilkeleri → [`05-board-mekanigi.md`](05-board-mekanigi.md) §5.1; sıralamanın iş anlamı (komşu pozisyon, eşzamanlı `CONFLICT`, compaction) → [`../domain/03-siralama-kurallari.md`](../domain/03-siralama-kurallari.md).

- **Bağımlılık & kapsam:** Faz 3A ([DEM-42](https://linear.app/demirkol/issue/DEM-42) — `list.move`/`card.move`) ve Faz 2D ([DEM-37](https://linear.app/demirkol/issue/DEM-37) — board ekranı) hazır olmalı. Tam optimistic cache modeli **Faz 4** ([DEM-27](https://linear.app/demirkol/issue/DEM-27)); bu fazda drag UX akıcıdır ama kalıcılaştırma `trpc.board.get` cache'i lokal güncelle → mutation → `onSettled` invalidate + refetch (server'ın kesin `position`'ıyla reconcile) düzeyinde tutulur. Realtime echo (Faz 5 — [DEM-28](https://linear.app/demirkol/issue/DEM-28)) ve compaction worker (Faz 3C — [DEM-44](https://linear.app/demirkol/issue/DEM-44)) ayrı işler; bu UI yalnızca `clientMutationId` üretir/gönderir (ileri-uyum, henüz tüketici yok).
- **Drag sırasında:** backend mutation **atılmaz**. React state yalnız local güncellenir — sürüklenen kartın/kolonun "hayalet" gösterimi + hedef konumda placeholder; kart/kolon ölçüleri stabil (layout shift yok). Pragmatic DnD `draggable` (kart/kolon) + `dropTargetForElements` (kolon gövdesi = kart hedefi; board şeridi = kolon hedefi) + `monitorForElements` ile yönetilir; `closestEdge` ile kart için "üst/alt", kolon için "sol/sağ" edge'i belirlenir. Otomatik kenar-kaydırma (auto-scroll) için Pragmatic DnD'nin scroll paketi kullanılır.
- **Custom drag preview — kart için body-portal pattern** (DEM-87): Pragmatic DnD'nin `setCustomNativeDragPreview` HTML5 drag-image bitmap yolu kart için terk edildi (alpha kanalı, soft-shadow keskinleşmesi, `rotate` ile transparent köşe sızıntısı bug'ları). Yerine: `disableNativeDragPreview` (1×1 transparent gif → browser preview görünmez) + `useBoardDnd` hook'unun lazy `createCardDragOverlayController()` factory'si — `position: fixed; pointer-events: none; z-index: 9999` body element'ı oluşturur, `createRoot` + `flushSync` ile `CardDragPreview` React komponentini canlı tree'de mount eder, `monitorForElements.onDrag` callback'inde DOM `style.transform = translate(...)` ile cursor pozisyonuna pinler (her frame React state yok → board re-render yok). `onDrop` ve hook unmount cleanup'ta portal kapatılır. Sürüklenen kartın orijinali "**rüya modu**"na geçer (eski Pusula `dnd-kit DragOverlay` deneyiminden esinlenildi): outer `<article>` `border-dashed border-primary/60 bg-primary/5` placeholder olur, içerik wrapper `invisible` (yer korunur, layout shift yok). Kolon preview'i hâlâ DOM clone (`renderLiftedPreview` + `setCustomNativeDragPreview` — kolon büyük + yatay tilt soft, bug pratikte hissedilmiyor). Tasarım dili → [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) §13.2.
- **`onDrop` (drag bitişi):** hedef konuma göre `before`/`after` komşular belirlenir; client `@pusula/domain/position` `positionBetween(before?.position, after?.position)` ile `newPosition` hesaplar (server doğrular veya yeniden hesaplar). Konum değişmediyse (aynı yere bırakıldı) mutation atılmaz. **Tek** mutation çalışır:
  - kart → `trpc.card.move({ cardId, fromListId, toListId, beforeCardId?, afterCardId?, newPosition?, clientMutationId })` — `toListId === fromListId` ise liste-içi reorder, farklıysa cross-list (yalnız **aynı board**; başka board'a taşıma ileri faz `card.moveToList`, Faz 3E — [DEM-69](https://linear.app/demirkol/issue/DEM-69)).
  - kolon → `trpc.list.move({ listId, beforeListId?, afterListId?, newPosition?, clientMutationId })`.
- **Optimistic + rollback:** `onDrop` anında cache snapshot alınır + `queryClient.setQueryData(trpc.board.get …)` ile kart/liste yeni sıraya çekilir (kartın `listId`+`position` veya listenin `position` güncellenir, ilgili liste içi sıra yeniden sıralanır), sonra mutation gönderilir. `onSettled` → `trpc.board.get` invalidate. `onError` → snapshot cache'e **geri yazılır** + düşük gürültülü hata (`strings.board.dnd.error`). `CONFLICT` (kart artık `fromListId`'de değil — eşzamanlı taşıma; bkz. `03-siralama-kurallari.md`) → rollback + `trpc.board.get` refetch + "kart başkası tarafından taşındı, görünüm yenilendi" bilgisi (kart sessizce kaybolmaz).
- **Yetki & arşiv:** drag yalnızca board `member+` için etkin (`board.role`; `viewer` salt-okunur → `draggable` mount edilmez); arşivli liste **drop hedefi olamaz** (UI dnd hedefini kapatır + server zaten reddeder). Mutation hatası inline.
- **Erişilebilirlik / alternatif:** sürükle-bırak yanında klavye-ve-buton tabanlı taşıma — kart ⋮ menüsünde "Taşı" (liste + konum picker → `card.move`), kolon ⋮ menüsünde "Sola/Sağa taşı" (→ `list.move`); klavye ve mobil kullanıcıları için (mobil drag-drop öncelikli değil — §8.2). Drag handle'lar ARIA etiketli; sürükleme durumu için `aria-live` duyurusu (best-effort).
- **shadcn / metinler:** yeni component library yok — mevcut board bileşenleri (`card-item.tsx`, kolon bileşenleri) Pragmatic DnD ile sarılır; ⋮ menüleri mevcut `DropdownMenu` ile. Türkçe metinler `apps/web/src/lib/strings.ts` (`strings.board.dnd.*` — "taşı" / "sola taşı" / hata-çakışma mesajları); UI bileşenleri hardcode metin içermez. `clientMutationId` her move'da istemcide (`crypto.randomUUID()`).
- **Test:** React Testing Library — komşu/edge → `before`/`after` seçimi ve `positionBetween` sarmalayıcısı (saf fonksiyon, izole), cache optimistic update + rollback (mock `queryClient`). Pragmatic DnD etkileşiminin uçtan uca doğrulaması Playwright'ta (Faz 3D — [DEM-45](https://linear.app/demirkol/issue/DEM-45)): liste reorder · kart reorder (aynı liste) · cross-list move · `card.move` başarısızlık → rollback · (ops.) `CONFLICT`/eşzamanlı kullanıcı. Geniş e2e suite → Faz 8 ([DEM-31](https://linear.app/demirkol/issue/DEM-31)).

---

### 8.1.9 Optimistic UI (Faz 4 — [DEM-27](https://linear.app/demirkol/issue/DEM-27))

Faz 3B drag-drop (§8.1.8) `card.move`/`list.move` için optimistic akışı kurdu; Faz 4 bunu **tüm board/list/card collaborative mutation'larına** genelleştirir, ortak cache modelini + helper'ları çıkarır. Cache modeli + query key konvansiyonu + lifecycle protokolü + `clientMutationId` semantiği + `CONFLICT` davranışı → [`05-board-mekanigi.md`](05-board-mekanigi.md) §5.2.

- **Modül konvansiyonu:** `apps/web/src/lib/board-cache/` — tüm cache erişimi tek modülden geçer:
  - `keys.ts` — query key factory (`boardKey(id)` / `boardsKey(workspaceId)` / `cardKey(id)`); component'larda literal array yok.
  - `types.ts` — `BoardCache` / `ListCache` / `CardCache` tipleri (tRPC `board.get` / `card.get` output'undan türetilmiş, tek kaynak `@pusula/api`).
  - `primitives.ts` — pure cache update fonksiyonları (`updateCardInCache` / `moveCardInCache` / `moveListInCache` / `addCardToCache` / `removeCardFromCache` / `archiveCardInCache` / `archiveListInCache` / `addListToCache` / `updateListInCache` / `updateBoardInCache`); queryClient ile değil cache snapshot ile çağrılabilir (4D Vitest pure-function testleriyle [DEM-81](https://linear.app/demirkol/issue/DEM-81) doğrulanır).
  - `mutations.ts` — `useOptimisticBoardMutation` higher-order hook: lifecycle iskeletini standartlaştırır (`onMutate` snapshot + apply + rollback context; `onError` rollback + toast; `onSettled` invalidate). Component'lar mutation-spesifik `apply` callback'i geçerek kendi mutation'ını kurar; `clientMutationId` (`crypto.randomUUID()`) hook tarafından transparan eklenir (component her seferinde elden eklemez).
- **Optimistic kapsam (apps/web ekranları):**
  - **§8.1.4 board ekranı** (DEM-37/63/74) — `card.create` / `card.update` / `card.archive` / `card.complete` / `card.uncomplete` (tamamla toggle § 8.1.5 modal + kart üzeri inline; 2.7C-2 / DEM-74 wire) / `list.create` / `list.update` / `list.archive`.
  - **§8.1.8 drag-drop** (DEM-43) — `card.move` / `list.move` (Faz 3B'de zaten optimistic; 4C `useOptimisticBoardMutation`'a göç ettirir, kullanıcıya görünen davranış aynı; sadece kod ortak hook'a taşınır).
  - **§8.1.5 kart detay modalı** (DEM-53/64/74) — `card.update` (description/title/due/cover) + `card.complete` / `uncomplete` + `card.archive`.
  - **Board listesi (`workspaces/[id]`)** — `board.create` / `board.update` / `board.archive`.
  - Kart ⋮ menüsü "Listeye taşı" / "Kopyala" (Faz 3E backend `card.moveToList` / `card.copy` UI follow-up) — Faz 4'te optimistic kurulur.
- **Kapsam dışı (Faz 5/6'da optimistic'lenecek):** `comment.*` · `checklist.*` · `card.label.*` · `board.members.*` · `card.members.*` — realtime echo + activity feed ile birlikte ele alınır (Faz 5 — [DEM-28](https://linear.app/demirkol/issue/DEM-28); Faz 6 — [DEM-29](https://linear.app/demirkol/issue/DEM-29)). Faz 4'te bunlar mevcut `await mutate(...)` + `queryClient.invalidate(...)` pattern'ini korur (network beklemesi var ama hata yolu basit, optimistic kompleksiyonu yok).
- **UX davranışı:**
  - **Optimistic uygulanmış mutation hata aldıysa** (`CONFLICT` dışı): rollback (cache eski hale) + toast (`strings.board.optimistic.error` — "Değişiklik kaydedilemedi. Tekrar dener misin?") + opsiyonel "Tekrar dene" CTA. Toast `@pusula/ui` (`sonner` wrapper); destructive variant. **Otomatik retry yok** (yan etki tekrarı riski).
  - **`CONFLICT`:** rollback + `board.get` refetch + bilgi toast (`strings.board.conflict.refreshed` — "Liste başka bir yerde güncellendi, yeniden yüklendi.") — neutral variant.
  - **Loading state:** optimistic uygulandığı için spinner / disabled state **göstermez**; yalnızca mutation gerçekten yavaşsa (>1s) form/butonu pasif yapma davranışı mevcut pattern'lerden değişmez. Drag-drop'ta drag-handle blokajı yok (Faz 3B davranışı korunur).
- **Türkçe metinler:** yeni toast metinleri `apps/web/src/lib/strings.ts` altında `strings.board.optimistic.*` ve `strings.board.conflict.*`; UI bileşenleri hardcode metin içermez ([`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md)).
- **Test (Faz 4D — [DEM-81](https://linear.app/demirkol/issue/DEM-81)):** Vitest + Testing Library + msw/tRPC mock — failure modları: network hata (rollback), `CONFLICT` (refetch + toast), race (eşzamanlı `card.move`), rollback doğrulaması (cache snapshot eski state), `clientMutationId` injection (UUID v4 regex). Mutation başına en az 1 senaryo: `card.move` / `card.create` / `card.archive` / `list.move` / `board.update`. Cache primitives'in pure-function birim testleri 4B'de ([DEM-79](https://linear.app/demirkol/issue/DEM-79)). E2E (Playwright) Faz 8'e ([DEM-31](https://linear.app/demirkol/issue/DEM-31)), realtime echo ayıklama testleri Faz 5'e ([DEM-28](https://linear.app/demirkol/issue/DEM-28)) bırakılır.
- **Wired (Faz 4B — [DEM-79](https://linear.app/demirkol/issue/DEM-79), 2026-05-13):** `apps/web/src/lib/board-cache/` modülü kuruldu — `keys.ts` `useBoardCacheKeys()` hook + `BoardCacheKeys` arayüzü (`board(id)`/`boards(workspaceId)`/`card(id)` tRPC `queryFilter` wrapper'ları; literal key array yok); `types.ts` `BoardCache`/`ListCache`/`CardCache`/`BoardSummary`/`CardDetailCache` `RouterOutputs` türevli (tek kaynak `@pusula/api`); `primitives.ts` 10 pure transform `applyCardMove`/`applyListMove`/`applyCardPatch`/`applyCardAdd`/`applyCardRemove`/`applyCardArchive`/`applyListAdd`/`applyListPatch`/`applyListArchive`/`applyBoardPatch` (yukarıdaki `updateCardInCache`/... isimleri tasarım metniydi; implementasyon `apply*` prefixiyle landed — `queryClient` almazlar, snapshot üzerinde pure çalışırlar; same-list+same-position no-op ref-stable); `mutations.ts` `useOptimisticBoardMutation` higher-order hook iskeleti (snapshot+cancel+apply / rollback + `CONFLICT` branch [data.code → invalidate + `onConflict`; aksi `onMutationError`] / `board.get` + opsiyonel `card.get` invalidate) + `withClientMutationId` UUID v4 helper (4C tüm collaborative mutation'larda zorunlu üretim). Faz 3B `board-dnd-cache.ts` + `board-dnd-cache.test.ts` **silindi** (göç); `use-board-dnd.ts` artık `applyCardMove`/`applyListMove` + `cacheKeys.board(boardId)` üzerinden — davranış aynı. 9 mevcut dosyada `trpc.board.get.queryFilter`/`card.get.queryFilter`/`board.list.queryFilter` çağrıları `cacheKeys.board`/`card`/`boards` factory'sine çekildi (add-list-column, archive-board-dialog, board-labels-section, board-members-section, card-detail-dialog, card-item, list-column, rename-board-form, create-board-dialog). Vitest 26 pure-function birim testi PASS. Optimistic WIRE 4C ([DEM-80](https://linear.app/demirkol/issue/DEM-80)), failure testleri 4D ([DEM-81](https://linear.app/demirkol/issue/DEM-81)).

---

### 8.1.10 Realtime board sync (Faz 5 — [DEM-28](https://linear.app/demirkol/issue/DEM-28))

Faz 4 optimistic UI (§8.1.9) tek-kullanıcı UX'i çözdü; Faz 5 **çoklu-kullanıcı sync**'i ekler: Alice kartı taşır → Bob `<500ms` içinde canlı görür (worker queue + outbox sayesinde `<1s` dayanıklı). Backend: outbox + worker + Socket.IO server → [`03-backend.md`](03-backend.md) "Faz 5" + [`06-bildirim-altyapisi.md`](06-bildirim-altyapisi.md) "Realtime event yayın katmanı"; tablo şeması → [`04-veri-katmani.md`](04-veri-katmani.md) (`realtime_events`); event envelope + reconciliation mekaniği → [`05-board-mekanigi.md`](05-board-mekanigi.md) §5.3.

- **Modül konvansiyonu (`apps/web/src/lib/realtime/`):**
  - `client.ts` — `socket.io-client` tek `io()` instance; `API_URL`'a bağlanır; `withCredentials: true` (session cookie); `transports: ['websocket']` (Faz 5 başlangıcında long-polling fallback yok — Redis adapter sticky session sorununu azaltır); auto-reconnect (varsayılan); `disconnect()` cleanup.
  - `useBoardRealtime(boardId)` — board sayfasında mount edilen React hook:
    - Mount: socket.connect (henüz değilse) → `socket.emit('board:join', { boardId })` → server `resolveBoardAccess` kontrolü + join ack → event listener register (`card.*`, `list.*`, `board.*`)
    - Unmount: `socket.emit('board:leave', { boardId })` + listener detach
  - `event-handlers.ts` — event türüne göre `apps/web/src/lib/board-cache/primitives.ts` (Faz 4B / [DEM-79](https://linear.app/demirkol/issue/DEM-79)) çağrısı: `card.moved` → `moveCardInCache` · `card.created` → `addCardToCache` · `card.updated` → `updateCardInCache` · `card.archived` → `archiveCardInCache` · `card.completed`/`uncompleted` → `updateCardInCache({completedAt})` · `list.*` → ilgili primitive · `board.updated`/`archived` → `updateBoardInCache`.
  - `in-flight-store.ts` — `useOptimisticBoardMutation` (Faz 4C / [DEM-80](https://linear.app/demirkol/issue/DEM-80)) ile entegre: aktif mutation'ların `clientMutationId` set'i (mutation `onMutate`'te add, `onSettled`'da delete). Realtime event listener bu set'ten gelen `clientMutationId`'leri **skip** eder (echo ayıklama).
- **Echo ayıklama (kritik):** Alice kart taşır → optimistic update cache'i hemen güncelliyor → mutation server'a gidiyor → tx commit + outbox insert → worker publish → Alice'in socket'i de event'i alıyor. **Eğer echo skip yapılmazsa** Alice'in cache'i ikinci kez güncellenir (idempotent ama gereksiz; race condition'da yanlış sonuç). `if (event.clientMutationId && inFlightSet.has(event.clientMutationId)) return;` zorunlu.
- **`seq` gap tespiti:** Board cache'de `lastAppliedSeq` (board'un `version` aynası). Gelen event:
  - `event.seq === lastAppliedSeq + 1` → uygula, `lastAppliedSeq = event.seq`.
  - `event.seq > lastAppliedSeq + 1` → **gap** (kaçırılan event'ler) → `boardKey` invalidate + `board.get` refetch → `lastAppliedSeq` server response'un `version`'ına eşitlenir.
  - `event.seq <= lastAppliedSeq` → stale/duplicate → skip.
- **Reconnect resync:** Socket reconnect event'inde board sayfasındaki `boardKey` invalidate + refetch (kaçırılan event'ler için tek refetch yeterli; outbox replay client'a değil server-side cache miss'e karşı).
- **Yetki & arşiv:** `viewer` rolü realtime event'leri görür (cache update olur, UI ona göre render eder), ama `useOptimisticBoardMutation` zaten mutation atamıyor; arşivli board'a join talebi server tarafında reddedilir (`apps/web` sayfa zaten arşivli board'ı yönlendirir veya salt-okunur gösterir).
- **UX davranışı:**
  - Realtime event uygulandığında **toast yok** (alice'in eylemi bob için "doğal" akış — gürültü çıkarmaz). İstisna: kart bob'un açık olduğu modalda değişirse subtle bilgi banner'ı (sonraki tur).
  - Çevrimdışı: socket disconnect olursa subtle "Bağlantı koptu, tekrar bağlanılıyor..." banner'ı (`strings.realtime.disconnected`); reconnect olduğunda banner kaybolur. Disconnect süresince mutation'lar optimistic çalışmaya devam eder ama server'a gitmez (4C `useOptimisticBoardMutation` zaten retry'lı değil — kullanıcı manuel "tekrar dene"). Sonraki tur: çevrimdışı kuyruk (Faz 8).
- **Türkçe metinler:** `apps/web/src/lib/strings.ts` `strings.realtime.*` (`disconnected` / `reconnecting` / banner mesajları).
- **Test (Faz 5D — [DEM-86](https://linear.app/demirkol/issue/DEM-86)):**
  - Vitest birim: event handler'ların cache primitives doğru çağırması (her event tipi için); echo ayıklama (in-flight set'ten gelen event skip); `seq` gap → refetch tetiklenir; `seq` stale → skip; reconnect → `boardKey` invalidate; `useBoardRealtime` mount/unmount cleanup.
  - Playwright e2e iki kullanıcı (`alice` + `bob` fixture; ortak board): card move sync · list create sync · card archive sync · reconnect resync · echo ayıklama doğruluğu. Faz 3D ([DEM-45](https://linear.app/demirkol/issue/DEM-45)) Playwright harness'ı üstüne.
- **Wired (Faz 5C — [DEM-85](https://linear.app/demirkol/issue/DEM-85), 2026-05-13):** `apps/web/src/lib/realtime/` modülü kuruldu: `client.ts` (`socket.io-client` singleton `getRealtimeSocket()` — `withCredentials: true` + `transports: ['websocket']` + `autoConnect: false`; `REALTIME_EVENT_CHANNEL = 'realtime:event'` apps/api `emit.ts` ile birebir aynı tek-channel envelope ile); `in-flight-store.ts` (modül-scope `Set<string>` — `add/has/remove/clear`); `event-handlers.ts` `dispatchRealtimeEvent(qc, filters, envelope)` (12 event tipi → `board-cache/primitives` çağrısı; `card.completed`/`uncompleted` superjson `Date` dönüşümü; `card.updated` `card.get` cache cascade; unknown event tipi → `console.warn` + skip — forward compat); `use-board-realtime.ts` `useBoardRealtime(boardId)` (mount: singleton connect + `board:join` emit + `realtime:event`/`connect`/`disconnect` listener'ları register; her envelope için **echo skip → seq gating [seq+1 apply + cache.board.version bump / seq>+1 invalidate / seq≤version stale drop / no-baseline invalidate] → reconnect handler invalidate+rejoin**; unmount: listener detach + `board:leave` emit — yalnız `joined` flag set'liyse [connect-then-join guard]); `index.ts` barrel. `useOptimisticBoardMutation` + `useOptimisticBoardListMutation` (Faz 4C) `onMutate`'te `addInFlightClientMutationId`, `onSettled`'da `removeInFlightClientMutationId` ile entegre (additive — public yüzey değişmedi; Faz 4D 14/14 testi yeşil kaldı). Board sayfası `page.tsx` `useBoardRealtime(boardId)` mount + disconnect amber banner (`strings.realtime.disconnected` + `role="status" aria-live="polite"`). `cacheKeys` referansı `useRef` ile sabitlenip effect deps `[boardId, queryClient, socket]`'e indirgendi (sonsuz reconnect döngüsü önleme). Dep: `socket.io-client ^4.8.1`. Vitest 31/31 — `in-flight-store.test.ts` (6), `event-handlers.test.ts` (14 — her tip + unknown + card.get cascade), `use-board-realtime.test.tsx` (11 — mount/unmount/listener-detach/echo/apply/gap/stale/no-baseline/reconnect/connect-then-join/connection-status). Toplam apps/web 386/386 PASS. Playwright e2e + load test 5D ([DEM-86](https://linear.app/demirkol/issue/DEM-86)).

---

### 8.1.11 Notification center (Faz 6 — [DEM-29](https://linear.app/demirkol/issue/DEM-29))

Faz 5C realtime altyapısının (§8.1.10) `user:{userId}` room'una yayınlanan `notification.created` event'lerini tüketen kullanıcı bildirim merkezi. App shell header'da bell ikonu + unread badge + Popover dropdown panel. Backend: `notifications.*` procedure'leri (Faz 6A — [`03-backend.md`](03-backend.md) "Faz 6 — notification & push procedure'leri"); domain kuralları → [`../domain/04-bildirim-kurallari.md`](../domain/04-bildirim-kurallari.md); processor → [`06-bildirim-altyapisi.md`](06-bildirim-altyapisi.md) "Notification processor (Faz 6)".

- **Modül konvansiyonu:**
  - `apps/web/src/lib/realtime/use-user-realtime.ts` (yeni) — Faz 5C `useBoardRealtime` pattern'iyle paralel: `user:{userId}` room zaten otomatik join (5A bağlantıda); listener `notification.created` event'ini dinler → `trpc.notifications.list` ve `trpc.notifications.unreadCount` cache invalidate (badge anlık güncellenir). Echo ayıklama gerekmez (`notification.created` server-initiated; client `clientMutationId`'si yok). App shell layout'unda `(app)/layout.tsx`'te bir kez mount edilir; tüm sayfalarda aktif.
  - `apps/web/src/app/(app)/_components/notification-bell.tsx` (yeni) — header'da bell:
    - `lucide-react` `Bell` icon + unread count badge (>0 ise sayı; `>9` için `9+`; Tailwind absolute positioning; `aria-label="{unread} okunmamış bildirim"`).
    - onClick → shadcn `Popover` (`NotificationCenterPanel`).
  - `apps/web/src/app/(app)/_components/notification-center.tsx` (yeni) — Popover içeriği:
    - Başlık: "Bildirimler" + "Tümünü okundu işaretle" buton (`trpc.notifications.markAllRead`).
    - Liste: `trpc.notifications.list.useInfiniteQuery()` (cursor pagination; scroll'la fetch next; veya manuel "Daha fazla" buton).
    - Her satır: `Avatar size-xs` (actor varsa) + `activitySummary` Türkçe başlık (DEM-53 çeviri katmanı yeniden kullanılır) + zaman ("2 dakika önce" — `date-fns` veya `Intl.RelativeTimeFormat`); unread satır = `bg-accent/30` + sol kenar mavi şerit.
    - onClick → `router.push(...)` (ilgili karta/board'a yönlendir) + `trpc.notifications.markRead({ id })` (optimistic; cache'ten unread set'inden çıkar).
    - Boş durum: shadcn `EmptyState` ("Henüz bildirim yok").
    - Yükleme: skeleton (3-4 satır).
- **Realtime entegrasyon:** `useUserRealtime` `notification.created` aldığında:
  1. `trpc.notifications.list` cache invalidate → arkadan refetch (yeni notification listede görünür).
  2. `trpc.notifications.unreadCount` cache invalidate → badge anlık artar.
  3. Toast yok (gürültü); badge artışı yeterli görsel sinyal.
- **Tıklayınca davranış:** notification satırına tıkla → `payload.linkTo` (`/workspaces/{wsId}/boards/{boardId}?card={cardId}` veya benzer) URL'i hesapla → `router.push` + mark-read. Modal açma (kart detay) URL'deki `?card=` param'ı yakalar (DEM-53 mevcut pattern).
- **Bildirim tipi → görsel ikon eşleme (`packages/ui/notification-type-icon.tsx` veya inline):**
  - `card.member_added` → `UserPlus` (mavi)
  - `comment.mentioned` → `AtSign` (sarı/önemli)
  - `comment.created` → `MessageSquare` (gri)
  - `due_reminder_*` → `Clock` (turuncu/kırmızı overdue için)
  - `board.member_invited` → `Mail` (mavi)
  - `card.archived` → `Archive` (gri)
  - vb. (DEM-93 implementation turunda kararlaştırılır)
- **i18n strings (`apps/web/src/lib/strings.ts`):** `strings.notifications.*`:
  - `bellAria` — bell ikonu aria-label
  - `title` — "Bildirimler"
  - `markAllRead` — "Tümünü okundu işaretle"
  - `empty` — "Henüz bildirim yok"
  - `unreadCountLabel` — "{n} okunmamış"
  - `loadMore` — "Daha fazla yükle"
  - Notification type başlık template'leri (`{actor} sana '{card}' kartını atadı` vb.) — DEM-53 `activitySummary` çeviri katmanı yeniden kullanılır.
- **Yetki:** notifications.list yalnız kendi userId'ye filtreli; `protectedProcedure` enforce eder. Notification center'da silinmiş karta link → karta gidince 404 → UI graceful fallback ("Bu kart artık mevcut değil").
- **Tercih ekranı:** **Faz 6'da yok.** Default tercihler (in-app her zaman + push opt-in + email opt-in) ile çalışır; tercih ekranı UI sonraki tur. Backend tablo + API hazır (`notification_preferences`).
- **Test (Faz 6D + 6E):**
  - Vitest RTL: `NotificationBell` (unread count render + Popover açılır), `NotificationCenter` (`notifications.list` render + markRead optimistic + markAllRead toplu + tıkla → router.push + mark-read), `useUserRealtime` (`notification.created` event → cache invalidate + badge artar).
  - Playwright e2e (Faz 6E — alice/bob fixture): atama bildirimi (bob bell badge 0→1 + tıkla → karta yönlendir + mark-read → badge 0), mention bildirimi, realtime push (panel açık iken anlık güncelleme).
- **Faz kapsamı:** Faz 6D ([DEM-93](https://linear.app/demirkol/issue/DEM-93)) implementasyon. Mobile notification UI Faz 7 ([DEM-30](https://linear.app/demirkol/issue/DEM-30)) — Expo `apps/mobile`.

> **Wired — DEM-93 (2026-05-14):** Web notification center `apps/web/src/app/(app)/_components/notification-bell.tsx` + `notification-center.tsx` + `notification-link.ts` + `notification-type-icon.tsx` altında bağlandı; `AppShell` header'daki placeholder gerçek bell ile değişti ve `useUserRealtime()` shell seviyesinde mount edildi. Realtime hook `apps/web/src/lib/realtime/use-user-realtime.ts` içinde `notification.created` event'i için `notifications.list` + `notifications.unreadCount` invalidate eder; `event-handlers.ts` bilinçli olarak değiştirilmedi. Copy ve summary katmanı `apps/web/src/lib/strings.ts` + `apps/web/src/lib/activity-summary.ts`, relative time `apps/web/src/lib/format.ts`, Popover primitive `packages/ui/src/components/popover.tsx`.

---

## 8.2 Mobil (`apps/mobile`)

Expo + Expo Router. **Henüz iskelet kurulmadı** — ileri faz; kullanıcı açıkça istemeden `apps/mobile` **oluşturulmaz**.

İleride desteklenecek: auth session · board listesi · board görüntüleme · card detail · card
create/update · notification center · push notification deep link · cache persistence (faydalı yerlerde).

Mobil teknoloji: Expo, Expo Router, React Native Reanimated, Gesture Handler, Expo Notifications,
tRPC client, TanStack Query.

Mobil drag-drop ilk implementasyonda **öncelikli değildir** — alternatif taşıma modeli daha
sağlıklı: kart üzerinde "liste değiştir" aksiyonu, kart detayında "move to" picker; gerekirse
ileride basit long-press reorder. Dikkat: push token yönetimi, foreground/background notification
davranışı, deep link ile kart açma, offline mutation queue, cache persistence, auth session refresh.
