---
title: '08 — Web ve Mobil'
description: 'Next.js web ve ileri faz Expo mobile teknik kuralları.'
aliases:
  - 'Web ve Mobil'
  - 'Frontend'
tags:
  - 'pusula'
  - 'architecture/frontend'
  - 'mobile'
type: 'architecture'
axis: 'architecture'
status: 'active'
parent: '[[docs/architecture/README|Tasarım / Teknik Mimari]]'
updated: 2026-05-19
---

# 08 — Web ve Mobil

> Eksen: **tasarım / teknik**. Board mekaniği (drag-drop/optimistic/realtime) ayrı: [`05-board-mekanigi.md`](05-board-mekanigi.md).

---

## 8.1 Web (`apps/web` → `@pusula/web`)

Next.js App Router. App `apps/web/src/app`; `@/*` → `apps/web/src/*`.

Sorumluluklar: board ekranı, drag-drop deneyimi, workspace & board yönetimi, notification center,
activity feed, search, settings, auth ekranları.

> **UI tasarım dili (token'lar, board/kolon/kart/modal anatomisi, ortak desenler, `packages/ui` bileşen spec'leri) → [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md)** (Faz 2.7). Bu dosya web tarafının teknik/işlevsel ihtiyaçlarını tutar; _görsel/anatomi_ kararları 13'te. Faz 2.7 uygulamasında §8.1.4–8.1.6'daki ekranlar 13'teki tasarım diline çekilir.
>
> **Dark/light tema desteği:** root layout'ta `next-themes` `ThemeProvider` (`attribute="class"`, `defaultTheme="light"`, `enableSystem={false}`, `themes=["light","dark"]`, `storageKey="pusula-theme"`); app-shell header sağ tarafında ve public auth kabuğu üst satırında `ThemeToggle` (Sun/Moon ikon swap, `Button variant=ghost size=icon`); persistence = localStorage. Tüm ekranlar (`(auth)/*`, `(app)/*`, board, board settings, card detail modal, filter bar, Tiptap prose) light + dark token cascade'inden besleniyor — yeni renk eklenmez; §13.7 [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md#137-tema-modu-lightdark) "önce belge"si + DEM-96 uygulaması. Mod seti **ikili** (`system` algılaması yok), default `light`.
>
> **Font boyutu kontrolü ([DEM-112](https://linear.app/demirkol/issue/DEM-112)):** app-shell header sağ tarafında `ThemeToggle` ile aynı yoğunlukta `FontSizeToggle` bulunur. Trigger `Button variant=ghost size=icon`; tıklanınca shadcn `DropdownMenu` açılır. Kullanıcı yazı ölçeğini %90-%120 aralığında %5 adımlarla küçültür/büyütür, `Reset` ile varsayılan %100'e döner. Varsayılan mevcut UI'dır; tercih root layout'taki client provider tarafından `<html>` font-size'ına uygulanır ve `localStorage` `pusula-font-scale` anahtarında saklanır. Metinler `strings.shell.fontSize.*` üzerinden gelir; server-side kullanıcı tercihi, OS ayarı ve auth-only ayrı kontrol kapsam dışıdır.
>
> **App-shell v2 — workspace + board switcher + user nav menu ([DEM-97](https://linear.app/demirkol/issue/DEM-97), Faz 2.7 follow-up #3):** app-shell header üç yatay grup taşır — **sol** (brand `LayoutGridIcon+"Pusula"` + `Separator` + `WorkspaceSwitcher` + `BoardSwitcher`), **sağ** (`NotificationBellPlaceholder` + `ThemeToggle` + `UserNavMenu` avatar dropdown). Switcher'lar shadcn `team-switcher` (sidebar-07 bloğu) desenini header'a port eder (yeni shadcn primitive yok — `Button`/`DropdownMenu`/`Tooltip` zaten var). `BoardTopBar`'ın **sol identity bloğu kaldırılır** (workspace `Link` rozet + "PANO" eyebrow + inline-rename başlık + favori `StarIcon`) — switcher trigger'ı board adını + workspace bağlamını zaten gösterir; çift bilgi olmasın. View switch + actions (`Davet/Search/Activity/⋮`) `BoardTopBar`'da yerinde kalır; inline-rename ⋮ menü "Yeniden adlandır" akışıyla açılır (mevcut `RenameBoardForm` `hideTrigger` prop'u zaten destekliyor). Avatar dropdown'da Hesap ayarları + Çıkış (mevcut header'daki ayrı link + buton birleşir). Bildirim placeholder `Faz 6D — DEM-93` ile gerçek `NotificationBell`'e dönüşür. Detay ve bileşen spec'leri → §13.8 [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md#138-app-shell-v2-workspace--board-switcher--user-nav-menu). Route → switcher state matrix:
>
> **Pano arka planına bağlı chrome (DEM-111):** board route'larında AppShell wrapper aktif pano `background` değerinden `board-bg-*` sınıfını alır. Header `bg-board-shell`, `BoardTopBar` `bg-board-topbar` kullanır; bu iki yüzey light modda seçilen renge yakın koyu tonlara, dark modda ise sırasıyla koyu-renk ve siyaha daha yakın tona bağlanır. `boards.background = null` artık seçili indigo/mor-mavi `board-bg-default` zemindir.
>
> | Route                               | WorkspaceSwitcher trigger                         | BoardSwitcher trigger               |
> | ----------------------------------- | ------------------------------------------------- | ----------------------------------- |
> | `/` (workspace list / onboarding)   | "Workspace seç" placeholder (tıklanır → dropdown) | disabled, tooltip "Workspace seçin" |
> | `/workspaces/[id]`                  | aktif workspace adı + rol                         | "Pano seç" placeholder              |
> | `/workspaces/[id]/boards/[boardId]` | aktif workspace adı + rol                         | aktif pano adı + renk noktası       |
> | `/account`                          | "Workspace seç" placeholder                       | disabled                            |
>
> Mobile (`md:` altı): `BoardSwitcher` ikon-only, `WorkspaceSwitcher` ikon-only; sağ grup (bildirim + tema + avatar) her zaman görünür. Faz 6D'de `NotificationBell` placeholder yerine gerçek bileşen geçer (§8.1.11).

Kullanım ilkeleri:

- Ana API Next içinde **değil**, `apps/api` içinde. Route Handler'lar yalnızca web-BFF veya özel web endpoint'leri için.
- Server Component'ler initial shell/data için kullanılabilir; board ekranı interaktif olduğundan client component ağırlıklıdır.

Board ekranı teknik ihtiyaçları: stable layout · horizontal scroll · kart virtualization ihtimali ·
keyboard accessibility · drag overlay · multi-list reorder · optimistic cache update · realtime reconciliation.

### UI: yalnızca shadcn/ui

- **Yalnızca shadcn/ui** + Tailwind CSS (v4) + lucide-react. MUI, Chakra UI, Ant Design, Mantine, Headless UI veya başka component library **yok**. Radix primitive'ler yalnızca shadcn/ui bileşenlerinin parçası olarak.
- Ürüne özel bileşenler shadcn/ui üzerine inşa edilir. Paylaşılan web bileşenleri `@pusula/ui`; design token'lar `@pusula/ui/theme.css`; shadcn ekleme: `components.json` `packages/ui`'da konfigüre.
- **i18n & hardcode:** UI bileşenleri hardcode metin **içermez**; entity-bağımsız ve yerelleştirme (i18n) standartlarına uygun. Metinler çeviri katmanından gelir, etiketler/format locale-aware olur.
- Accessibility: keyboard navigation, focus yönetimi, ARIA — özellikle board ekranı ve modal akışları.

### 8.1.1 Auth ekranları & oturum yönetimi (Faz 1)

Better Auth HTTP route'ları `apps/api` (`${API_URL}/api/auth/*`); web tarafı `apps/web/src/lib/auth-client.ts` (`better-auth/react`) ile bunları çağırır. Web ve API farklı origin'de (dev: `:3000` / `:3001`) — session cookie API origin'inde set edilir, tRPC ve auth istekleri `credentials: 'include'` ile gider; bu yüzden **oturum yönetimi client-side**'dır (Next.js RSC, API origin'inin cookie'sine erişemez — server-side session kontrolü bu fazda yok).

- **Public route'lar:** `app/(auth)/sign-in`, `app/(auth)/sign-up`. `(auth)/layout.tsx` zaten oturum açıksa post-auth redirect'i tek noktadan (`RedirectIfAuthenticated`) yapar. Formlar native `<form>` + `@pusula/domain` zod şemalarıyla client-side validasyon; `authClient.signUp.email(...)` / `signIn.email(...)`; hata mesajı inline. **Başarı yönlendirme önceliği** (DEM-126 — 2026-05-15): (1) `?redirect=<safe-path>` varsa `safeRedirectPath` ile yönlendir (open-redirect koruması: yalnız tek `/` ile başlayan in-app path; `//`, `/\`, scheme veya host içeren değer reddedilir → `/` fallback); (2) yoksa `trpc.auth.defaultLandingRoute` query'si (`protectedProcedure`) çağrılır — kullanıcının üye olduğu **en eski arşivlenmemiş workspace** + onun **en eski arşivlenmemiş panosu**na (`workspaces.createdAt ASC`, sonra `boards.createdAt ASC`; workspace `guest` ise yalnız explicit `board_members` rowu olan panolar) `/workspaces/[ws]/boards/[b]` route'una yönlendirilir; (3) query `null` döndürürse (0 workspace veya 0 erişilebilir non-archived pano) ya da hata verirse `/` fallback'ine düşülür (workspace+pano seçici landing — §8.1.3). Backend procedure sözleşmesi → [`03-backend.md`](03-backend.md) (`auth.*`). `/` route'u **kendi başına** authenticated user'ı yönlendirmez; landing'i kullanmaya devam etmek isteyen kullanıcı switcher veya direkt URL ile erişebilir.
- **Korumalı kabuk:** `app/(app)/layout.tsx` (client component) `authClient.useSession()` kullanır — `pending` → iskelet/spinner; oturum yok → `router.replace('/sign-in?redirect=…')`; oturum var → app shell (header: kullanıcı adı + çıkış). `app/(app)/page.tsx` = `(app)/` varış noktası — workspace sayısına göre dallanır (onboarding / workspace + pano seçimi), bkz. §8.1.3. Çıkış: `authClient.signOut()` → `/sign-in`.
- **Workspace oluşturma:** shadcn `Dialog` + ad input → `trpc.workspace.create` mutation (`clientMutationId` istemcide üretilir, ör. `crypto.randomUUID()`); başarıda `workspace.list` invalidate. Tam optimistic UI Faz 4'te.
- **Workspace daveti (Faz 1.3 web):** workspace kartında — kullanıcı `admin+` ise — "Davet et" → shadcn `Dialog`, e-posta input (rol varsayılan `member`; rol seçimi sonraki iterasyon) → `trpc.workspace.members.invite`; hata inline (`CONFLICT` → "zaten üye/davetli"). Workspace listesinde ayrıca "Bekleyen davetler" bölümü → `trpc.workspace.invitations.mine` (kullanıcının kendi e-postasına gelen `pending` davetler: workspace adı, rol, davet eden, son tarih) + "Kabul et" / "Reddet" → `invitations.accept` / `invitations.decline` (`clientMutationId` ile); kabul başarılı → `workspace.list` (+ `invitations.mine`) invalidate. Gönderilmiş davetleri yönetme (`invitations.list`/`revoke`) workspace üyeler ekranıyla birlikte (sonraki iterasyon). Backend sözleşmesi → [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Workspace davet akışı).
- **Şifre sıfırlama (Faz 1 ek iş — [DEM-68](https://linear.app/demirkol/issue/DEM-68)):** `(auth)/forgot-password` — e-posta input (`@pusula/domain` `emailSchema` ile istemcide doğrula) → ``authClient.requestPasswordReset({ email, redirectTo: `${window.location.origin}/reset-password` })`` (`redirectTo` **mutlak** — web app origin'i; Better Auth `redirectTo`'yu sunucu tarafında `baseURL` [`API_URL`] göre çözer, göreli yol API sunucusuna işaret ederdi; mutlak web URL'i `trustedOrigins`/`APP_URL` origin kontrolünden geçer — bkz. [`07-auth.md`](07-auth.md)) → "Bağlantı e-posta adresine gönderildi" durum mesajı (e-posta kayıtlı mı belli edilmez — kullanıcı listesi sızdırılmaz; başarı/başarısızlık aynı mesajı verir). `(auth)/reset-password` — `?token=` URL'den okunur (`useSearchParams` + `<Suspense>`); token yoksa "geçersiz/eksik bağlantı" + `/forgot-password`'a link; varsa yeni parola + tekrar input (`passwordSchema` ile doğrula) → `authClient.resetPassword({ newPassword, token })` → başarıda `/sign-in?reset=1`'e yönlendir ("parolan güncellendi" bildirimi sign-in'de bir kez gösterilir), Better Auth token-geçersiz/süresi-dolmuş hatasında açıklayıcı inline mesaj + "yeni bağlantı iste". `/reset-password` ve `/forgot-password` rotaları `Referrer-Policy: no-referrer` ile servis edilir (token query'de — Referer sızıntısına karşı; `apps/web/next.config.ts`). Sign-in ekranında **"Unuttum"** linki parola etiketiyle aynı satırda durur. Yalnız shadcn/ui + mevcut bileşenler; metinler `strings.auth.*` (yeni `forgotPassword.*` / `resetPassword.*` grupları). `(auth)/layout.tsx` zaten oturum açıksa bu sayfalardan da `/`'a yönlendirir. Backend (Better Auth `sendResetPassword` → Resend transactional) → [`07-auth.md`](07-auth.md) (Şifre sıfırlama akışı).
- **Signup e-posta doğrulama (Faz 8 — [DEM-72](https://linear.app/demirkol/issue/DEM-72)):** yumuşak politika; signup sonrası kullanıcı içeri alınır, fakat `(app)` shell `session.user.emailVerified === false` ise doğrulama banner'ı gösterir. Banner resend butonu `authClient.sendVerificationEmail({ email, callbackURL: `${window.location.origin}/verify-email` })` çağırır. Başarılı resend sonrası buton **60 saniye cooldown** ile pasif kalır (label "Tekrar gönder (Nsn)" geri sayımı gösterir); `localStorage` anahtarı `pusula:email-verify-cooldown:{email}` ile tutulur — sayfa yenilense bile cooldown korunur, başka bir oturumdan/cihazdan bağımsız (sunucu rate-limit'i Better Auth varsayılanına bırakılır, ayrı backend kuralı yok). Başarısız istek (`result.error`) cooldown başlatmaz, kullanıcı hemen tekrar deneyebilir. `/verify-email` `(auth)` route group dışında public callback ekranıdır; başarılı redirect'te başarı, `?error=` ile geçersiz/süresi dolmuş bağlantı durumu gösterir.
- **Form kütüphanesi:** Faz 1 auth formları basit (email/parola) — native form + zod yeterli; ayrı form kütüphanesi (react-hook-form vb.) eklenmez. Karmaşık form ihtiyacı doğarsa ayrı karar.
- **shadcn bileşenleri** (`@pusula/ui`): `Button` mevcut; bu işle eklenenler (`Input`, `Label`, `Card`, `Dialog`, …) — `pnpm dlx shadcn@latest add …` `packages/ui` içinden, sonra `src/index.ts`'den export, `apps/web` zaten `@source` ile `packages/ui/src`'i Tailwind taramasına dâhil ediyor.
- **Test:** Auth formları için React Testing Library (render + validasyon + `authClient` mock'lu submit). Playwright e2e (sign-up → workspace listesi → workspace oluştur → çıkış) ileri faz / Faz 8 sertleştirmeyle birlikte.

### 8.1.2 Workspace yönetim ekranı (Faz 1)

Backend'de hazır olan workspace yönetim procedure'lerinin (`workspace.update`/`archive`, `workspace.members.{list,updateRole,remove}`, `workspace.invitations.{list,revoke}`) UI'ı. Backend sözleşmesi → [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Workspace yetkilendirme + davet akışı), [`03-backend.md`](03-backend.md).

- **Route:** `app/(app)/workspaces/[id]/page.tsx` (client component) — workspace detay/ayarlar görünümü. `workspaceId` = path param; `trpc.workspace.get` ile shell verisi (ad, slug, rol, üye sayısı). `app/(app)/page.tsx` ana seçim yüzeyinde workspace satırına tıklamak yalnızca o workspace'i seçer; detay/ayarlar route'una geçiş satırdaki ayar ikonu üzerinden yapılır. Faz 2'de board listesi/board ekranı bu route'un altına gelir (`workspaces/[id]/boards/[id]`).
- **Rol-bazlı görünürlük (UI seviyesi; sunucu zaten enforce eder):** ayarlar (rename/slug) ve davet/rol/üye-çıkar aksiyonları yalnızca `owner`/`admin`'e gösterilir; arşivleme yalnızca `owner`'a; gönderilmiş davet listesi `member+`'e görünür ama "iptal et" yalnızca `admin+`'e. Herkes (owner hariç) "workspace'ten ayrıl" görür. `owner` rolü UI'da değiştirilemez/çıkarılamaz olarak işaretlenir.
- **Ayarlar:** ad + slug düzenleme formu (`@pusula/domain` `workspaceNameSchema`/`workspaceSlugSchema` ile client-side validasyon) → `trpc.workspace.update`; `CONFLICT` (slug çakışması) inline.
- **Tehlikeli bölge** (yalnızca `owner`): (1) **Arşivle** → `trpc.workspace.archive` (onaylı `Dialog`) → başarıda `router.replace('/')` + `workspace.list` invalidate. (2) **Kalıcı sil** (`workspace.delete` — geri dönüşsüz; backend DEM-24, UI DEM-40) → onaylı `Dialog` içinde workspace adını birebir yazma input'u (`confirmName`; eşleşmeden onay butonu disabled); `trpc.workspace.delete({ workspaceId, confirmName, clientMutationId })` → başarıda `router.replace('/')` + `workspace.list` invalidate; `BAD_REQUEST` (ad eşleşmiyor) / `FORBIDDEN` inline `Alert`. "Sil" "Arşivle"den ayrı ve daha vurgulu (destructive) gösterilir.
- **Üyeler:** `trpc.workspace.members.list` → satır başına ad/e-posta/rol; `admin+` ise non-owner satırlarda rol değiştir (shadcn `Select` — `assignableWorkspaceRoleSchema`: `admin`/`member`/`guest`) → `trpc.workspace.members.updateRole`, ve "çıkar" → `trpc.workspace.members.remove`. Kendi satırında (owner değilse) "workspace'ten ayrıl" → `members.remove` (kendi `userId`) → başarıda `router.replace('/')` + `workspace.list` invalidate. Mutation sonrası `members.list` invalidate.
- **Gönderilmiş davetler:** `trpc.workspace.invitations.list` → bekleyen davetler (e-posta, rol, davet eden, son tarih); `admin+` ise "iptal et" → `trpc.workspace.invitations.revoke` → `invitations.list` invalidate. "Üye davet et" dialog'u (§8.1.1'deki `InviteMemberDialog`, davet sonrası `invitations.list` invalidate eder) bu ekrana taşınır; workspace listesi kartından kaldırılır.
- **shadcn:** bu işle eklenenler: `Select` (`@radix-ui/react-select`) — `packages/ui`'a eklenip `src/index.ts`'den export edilir.
- **`clientMutationId`:** her mutation istemcide üretilir (`crypto.randomUUID()`). Tam optimistic UI Faz 4 (DEM-27); bu fazda mutation sonrası ilgili query'ler invalidate edilir.
- **Test:** sunum (presentational) bileşenleri için React Testing Library (render + rol-bazlı görünürlük + validasyon + callback).

### 8.1.3 Yeni kullanıcı onboarding'i & signup bootstrap (Faz 1)

Yeni kayıt olan kullanıcı boş bir ekrana düşmesin diye signup'ta otomatik bir default workspace + boş "İlk Pano" oluşturulur; login sonrası `(app)/` varış noktası kullanıcının workspace sayısına göre dallanır. Domain kuralı (best-effort default workspace, `workspace.list` boş olabilir) → [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) (invariant 11). **Anasayfa "Variant A — Rafine Orijinal" yapısındadır (DEM-192, 2026-05-17):** 0 workspace → onboarding boş-durumu; 1+ workspace → sol workspace rayı + sağ içerik (workspace özet başlığı + stat strip + zengin board kart grid'i). Ayrıntılı yerleşim/anatomi (sütun ölçüleri, board kartı, stat strip, favori yıldız) → [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) §13.11 "Anasayfa anatomisi"; "Board favorisi" domain kavramı → [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md).

- **Signup bootstrap (`apps/api`):** Better Auth instance'ı (`apps/api/src/auth.ts`) `databaseHooks.user.create.after` ile yeni kullanıcı için **tek transaction'da**: `workspaces` (sabit ad `"Çalışma Alanım"` — `@pusula/domain` `ONBOARDING_WORKSPACE_NAME`; benzersiz slug, suffix'li) + `workspace_members` (rol `owner`) + `activity_events`(`workspace.created`) + `boards` (`"İlk Pano"` — `ONBOARDING_BOARD_TITLE`; `background` = `ONBOARDING_BOARD_BACKGROUND` preset gradient'i) + `board_members` (creator `admin`) + `activity_events`(`board.created`, `board.background_changed`) **+ showcase pano şablonu** (kullanıcının ilk girişinde ürün yetkinliklerini "trailer" disiplininde sergiler): `labels` board-scope etiket paleti (`ONBOARDING_LABELS` — ad + renk seti) + 4 liste (`ONBOARDING_LISTS`; 2'si `color`/`icon`/`iconColor` ile özelleştirilmiş + 2'si **default** [renk/ikon `null`] olarak yan yana gelir; `position` fractional, `@pusula/domain/position`) + listelerin içinde dağılmış kartlar (`ONBOARDING_CARDS`; her kart `description`, opsiyonel `dueAt` offset'i, opsiyonel `completed`, çoklu `labels` referansı, opsiyonel `members` rol seti, opsiyonel `checklists`+`items`, opsiyonel `comments` listesi taşır; **`coverColor` yalnızca kapak rengini tanıtan tek bir kartta** — diğer kartlar kapak rengi olmadan gelir [özellik yalnız anlatıldığı yerde görselleştirilsin disiplini, başlıklar emoji'siz] — hepsi sabit deklarasyonda) + ilgili `list.created` / opsiyonel `list.color_changed` / opsiyonel `list.icon_changed` (yalnızca özelleştirilmiş listelerde) / `card.created` / opsiyonel `card.cover_changed` (yalnız tek kart) / `card.due_set` / `card.label_added` / `card.member_added` / `checklist.created` / `checklist.item_added` / `checklist.item_checked` / `comment.created` / `card.completed` activity'leri (actor = yeni kullanıcı; payload `onboarding: true` bayrağı taşır). Aynı tx içinde her board/list/card/comment/label için `search_documents` upsert edilir (`@pusula/db/search-indexer` `upsertSearchDocument`) — showcase içeriği ilk girişte aranabilir. `boards.version` `0` kalır ve realtime publish edilmez (kimse seyretmiyor). Şablon içeriği `@pusula/domain` sabitleri (i18n placeholder; kullanıcı-yüzlü → Türkçe). Bootstrap **best-effort**: hata loglanır, exception **yeniden fırlatılmaz** — signup başarılı sayılır, kullanıcı yine `(app)/`'a düşer (orada 0-workspace onboarding'ini görür). Mantık `apps/api/src/bootstrap.ts`'te; Better Auth tarafı → [`07-auth.md`](07-auth.md). Domain kuralı (best-effort default workspace + showcase pano şablonu) → [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) (invariant 11).
- **`(app)/page.tsx` varış yüzeyi:** `trpc.workspace.list` yüklenir; sonuca göre:
  - **`pending`** → kısa "yükleniyor" placeholder.
  - **`error`** → hata `Alert`'i + tekrar dene.
  - **0 workspace** → onboarding boş-durumu (`_components/onboarding-empty-state.tsx`: Pusula/workspace kavramını anlatan kart + "Workspace oluştur" CTA → `CreateWorkspaceDialog`). Bootstrap çalıştıysa bu nadirdir, ama kullanıcı son workspace'inden ayrılınca da bu duruma düşer. `PendingInvitations` bu durumda da gösterilir.
  - **1+ workspace** → "Variant A" iki-sütun düzeni: solda **workspace rayı** (`lg:w-80` — "WORKSPACES" başlığı + workspace satırları [palette avatar + ad + "N pano · M üye" + rol rozeti] + dashed "Yeni alan kur" CTA), sağda seçili workspace'in **içeriği** üç parça hâlinde — (a) workspace özet başlığı (büyük avatar + ad + rol + slug + meta satırı + "Davet et"/"Pano oluştur" aksiyonları), (b) **stat strip** (4 kutu: Açık görev / Bu hafta tamamlanan / Vadesi geçen / Bana atanan — `trpc.workspace.stats({ workspaceId })` aggregate query'sinden), (c) zengin **board kart grid'i** (`trpc.board.list({ workspaceId })` — kapak görseli + favori yıldız toggle + sayaçlar + üye avatar yığını + filtre sekmeleri + dashed "Yeni pano" CTA). İlk workspace varsayılan seçilidir; workspace satırına tıklamak ayar sayfasına gitmez, sadece sağdaki içeriği değiştirir. Workspace detay/ayar route'u yalnızca satırdaki ayar ikonu (`/workspaces/[id]`) üzerinden açılır. Board kartları `/workspaces/[id]/boards/[boardId]` rotasına gider; `member+` kullanıcılar için grid içinde `CreateBoardDialog` bulunur. Tam yerleşim/anatomi + token disiplini → [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) §13.11; board favorisi (yıldız toggle) domain kavramı → [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md).
- **Metinler:** onboarding metinleri `strings.onboarding` (+ gerekirse `strings.workspace.redirecting`); hardcode metin yok.
- **`clientMutationId`:** onboarding'deki workspace oluşturma `CreateWorkspaceDialog` üzerinden (istemcide üretilir); yeni mutation yok.
- **Test:** `onboarding-empty-state` için React Testing Library (render + CTA); `(app)/page.tsx` varış yüzeyi için RTL (0 workspace / 1+ workspace / pending / error; workspace seçimi; `useTRPC` / `useQuery` mock'lu). Backend bootstrap için ileride tRPC/integration testi opsiyonel (best-effort yol).

### 8.1.4 Board ekranı (Faz 2D)

Backend Faz 2A/2B/2C tamam (`board.{list,create,get,update,archive}`, `list.{create,update,archive}`, `card.{create,get,update,archive}`). Bu faz web tarafında **salt CRUD** board ekranını kurar — drag-drop **yok** (Faz 3 — [DEM-26](https://linear.app/demirkol/issue/DEM-26)), optimistic UI **zorunlu değil** (Faz 4 — [DEM-27](https://linear.app/demirkol/issue/DEM-27); bu fazda mutation → `await` → ilgili query invalidate → refetch). Backend sözleşmesi → [`03-backend.md`](03-backend.md) (Faz 2 — board / list / card procedure'leri), [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Board / List / Card procedure haritası); board ekranı veri akışı → [`05-board-mekanigi.md`](05-board-mekanigi.md) §5.0.

- **Board listesi:** `app/(app)/workspaces/[id]/page.tsx` üst bölümüne (ayarlar/üyeler kartlarından **önce**) "Panolar" bölümü — `trpc.board.list` ({ workspaceId }); kart başına board adı → `Link` `/workspaces/[id]/boards/[boardId]`; "Pano oluştur" → shadcn `Dialog` + ad input (`@pusula/domain` `boardTitleSchema` ile client-side validasyon) → `trpc.board.create` (`clientMutationId` istemcide) → `board.list` invalidate. Arşivli board'lar listede soluk/ayrı işaretli ve salt-okunur; board satırındaki `role` alanına göre aksiyonlar gizlenir/gösterilir (gerçek kapı server-side).
- **Board detay:** `app/(app)/workspaces/[id]/boards/[boardId]/page.tsx` (client component) — `trpc.board.get` ({ boardId }) tek seferde `{ board: {…, role}, lists: [...] (arşivli dahil, `position`sıralı), cards: [...] (yalnızca aktif,`position` sıralı) }` döndürür. Yatay kaydırılan kolon (liste) düzeni; her kolonda listenin kartları (`cards` `listId`'ye göre gruplanır). Kolon/kart ölçüleri stabil — hover/edit'te layout shift yok. Üstte board başlığı (admin ise inline yeniden adlandırma) + "geri" linki (`/workspaces/[id]`). Board yoksa/erişim yoksa `NOT_FOUND`/`FORBIDDEN` → `Alert` + geri linki.
- **List CRUD** (board `member+` ise göster — `board.role`): "Liste ekle" (kolon listesi sonuna inline form → `trpc.list.create` { boardId, title }) · "yeniden adlandır" (kolon başlığı inline edit → `trpc.list.update` { listId, title }) · "arşivle" (kolon menüsü → `trpc.list.archive` { listId, archived: true }) — her biri sonrası `trpc.board.get` invalidate.
- **Card CRUD** (board `member+`): "Kart ekle" (kolon altına inline form → `trpc.card.create` { listId, title }) · "düzenle" (kart tıkla → dialog: başlık/açıklama/`due_at` → `trpc.card.update`) · "arşivle" (kart menüsü/dialog → `trpc.card.archive` { cardId, archived: true }) — her biri sonrası `trpc.board.get` invalidate.
- **Yetki:** UI board rolüne göre aksiyonları gizler/gösterir (`viewer` salt-okunur); arşivli board/liste salt-okunur (server zaten reddeder, UI da aksiyonları kapatır). Mutation hatası inline `Alert`.
- **shadcn:** mevcut bileşenler büyük ölçüde yeterli (`Button`/`Card`/`Dialog`/`Input`/`Label`/`Alert`/`Badge`); gerekirse `DropdownMenu` (`@radix-ui/react-dropdown-menu`) ve/veya `Textarea` eklenir (`packages/ui` → `pnpm dlx shadcn add …`, `src/index.ts` export). Türkçe metinler `apps/web/src/lib/strings.ts` (`strings.board`); UI bileşenleri hardcode metin içermez.
- **`clientMutationId`:** her mutation istemcide üretilir (`crypto.randomUUID()`). Tam optimistic UI Faz 4 (DEM-27).
- **Test:** React Testing Library — board listesi (render + "Pano oluştur" akışı + boş durum), board detay (kolon/kart render + boş board), liste/kart oluşturma akışları (presentational form bileşenleri izole, callback + validasyon). Playwright e2e (board oluştur → liste/kart ekle) ileri faz (Faz 8 — [DEM-31](https://linear.app/demirkol/issue/DEM-31)).
- **Layout & scroll davranışı ([DEM-88](https://linear.app/demirkol/issue/DEM-88) — 2026-05-13):** Board ekranı viewport-bound — `AppShell` board route'ta `h-svh overflow-hidden` (`_components/app-shell.tsx`'te `fullBleed` regex'iyle koşullu; diğer rotalar `min-h-svh` eski davranışı korur). Sayfa (body/page) scroll **YOK**. Flex zinciri her column parent'ında `min-h-0` taşır (Tailwind/CSS'te flex item default'u `min-height: auto` — bu override edilmezse overflow zinciri kırılır, child parent yüksekliğini aşar). `BoardColumns` strip `flex items-start gap-3 overflow-x-auto overflow-y-hidden pusula-scrollbar` (yatay ince scrollbar; dikey strip yapmaz — `items-start` ile kolonlar top-aligned ve içeriği kadar yüksek). `ListColumn` 3-segment: kolon `flex max-h-full flex-col` (içeriği kadar; viewport sınırına dayanınca sınırlanır — `h-full` değil, aksi halde boş kolonlar viewport-tall görünür) · header `shrink-0` · cards area `flex min-h-0 flex-col overflow-y-auto pusula-scrollbar` (içerik kadar yer kapla, sınıra dayanınca kart listesi içinde scroll — `flex-1` yok çünkü içerik az olsa cards area boş alanı doldurup kolonu şişirirdi) · footer (Kart ekle) `shrink-0`. Custom scrollbar utility `.pusula-scrollbar` (`packages/ui/src/styles/theme.css` `@layer utilities` — 6px thin, transparent track, soft OKLCH thumb; `--scrollbar-thumb` + `--scrollbar-thumb-hover` token'lar light + dark) — strip yatay scrollbar'a da uygulanır. Tasarım dili spec'i → [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) §13.2 (kolon anatomisi + scrollbar token).

### 8.1.5 Kart detay görünümü (Faz 2.5D)

Board ekranında (§8.1.4 Board ekranı) bir karta tıklanınca açılan kart detay görünümü — Trello kart modali. Backend Faz 2.5A/2.5B (`comment.*`, `checklist.*`/`checklist.item.*`, `card.members.*`, `card.labels.*`, `label.*`) tamam olmalı. Backend sözleşmesi → [`03-backend.md`](03-backend.md) (Faz 2.5 procedure'leri), [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Board / Card içerik procedure haritası — Faz 2.5).

- **Sunum: modal + URL query param.** Kart detayı shadcn `Dialog` ile board ekranının **üstünde** açılır (board arkada kalır — Trello hissi). Açık kart `?card=<cardId>` query param'ıyla URL'e yansır (paylaşılabilir/derin link; sayfa yenilenince modal açık gelir). Next.js intercepting/parallel route kullanılmaz — `useSearchParams` + `router.push`/`replace` (shallow) ile yönetilir; modal kapanınca param kaldırılır. Param geçersiz kart id'siyse modal açılmaz (sessiz) ya da kısa "kart bulunamadı" gösterir.
- **Veri:** `trpc.card.get` ({ cardId }) — kart + kullanıcının kart ilişkileri (`card_members`); ayrıca `trpc.comment.list` ({ cardId }), checklist'ler (kart payload'ında veya ayrı query — `card.get` zaten kartı döndürüyor; checklist/etiket/üye için ek alanlar eklenir ya da ayrı query'lerle çekilir; implementasyonda netleşir), board etiketleri (`board.get`'e `labels` eklenir ya da `label.list`), kart activity feed (ayrı `activity.list` ({ cardId }) — board/kart query'sini şişirme; **G2** boşluk-tara notu burada). Permission `card.get` ile (board `viewer+`) — `viewer` salt-okunur.
- **İçerik (modal):** başlık (board `member+` ise inline edit → `card.update`) · açıklama (inline edit, boş→`null`/`''` normalize → `card.update`) · due-date picker (`<input type=date>` veya küçük takvim → `card.update` `dueAt` set/clear) · üyeler — `assignee`/`watcher` listesi + ekle (board üyeleri arasından seç → `card.members.add`) / çıkar (`card.members.remove`); `viewer` yalnızca "kendini watcher yap / bırak" görür · etiketler — kartın etiketleri + board etiket listesinden ata/kaldır (`card.labels.add`/`remove`) · checklist'ler — checklist ekle/yeniden adlandır/sil (`checklist.*`), item ekle/işaretle/düzenle/sil/sırala (`checklist.item.*`), her checklist için ilerleme (X/Y) · yorumlar — listele (`comment.list`, eskiden yeniye) + ekle (`comment.create`, düz metin) + düzenle/sil (yazan veya board `admin` — `comment.update`/`delete`; silinmiş yorum "silindi" placeholder'ı) · kart activity feed (kronolojik geçmiş — okunabilir özet satırları) · "arşivle" (zaten board ekranında var; modalda da olabilir → `card.archive`).
- **Yetki & arşiv:** UI board rolüne göre aksiyonları gizler (`viewer` salt-okunur; "kendini watcher yap" hariç); arşivli board/liste/kart salt-okunur (server zaten reddeder, UI da kapatır). Mutation hatası inline `Alert`.
- **Reconcile:** Optimistic UI **bu fazda zorunlu değil** (Faz 4 — [DEM-27](https://linear.app/demirkol/issue/DEM-27)) — her mutation → `await` → ilgili query'leri (`card.get`/`comment.list`/`board.get`/checklist/activity) invalidate → refetch. `clientMutationId` her mutation'da istemcide üretilir (`crypto.randomUUID()`).
- **shadcn:** mevcutlar büyük ölçüde yeterli (`Dialog`/`Button`/`Input`/`Textarea`/`Label`/`Card`/`Alert`/`Badge`/`Select`); gerekirse `Checkbox` (checklist item), `Popover` (etiket/üye seçici), `Tabs` (yorum/activity sekmeleri) eklenir (`packages/ui` → `pnpm dlx shadcn add …`, `src/index.ts` export — yalnızca shadcn/ui + Tailwind + lucide-react). Türkçe metinler `apps/web/src/lib/strings.ts` (`strings.card`); UI bileşenleri hardcode metin içermez.
- **Test:** React Testing Library — modal aç/kapa (+ `?card` param senkronu), açıklama/due inline edit, üye/etiket ekle-kaldır, checklist item toggle, yorum ekle/sil, activity feed render; presentational form/list bileşenleri izole (callback + validasyon, tRPC mock'lu). Playwright e2e ileri faz (Faz 8).
- **Faz 2.7C ([DEM-64](https://linear.app/demirkol/issue/DEM-64)) + 2.7C-2 ([DEM-74](https://linear.app/demirkol/issue/DEM-74)) — yapısal yeniden kurulum + tasarım uyumu:** bu modal Faz 2.7'de tasarım diline çekildi (bağlayıcı spec → [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) §13.3 + §13.5; ortak bileşenler §13.4). Yapı: shadcn `Dialog` içinde **iki-kolon** (`grid md:grid-cols-[1fr_360px]`; modal `w-[min(960px,92vw)]` + `sm:max-w-none` — shadcn `DialogContent` base'inin `sm:max-w-lg`'ini geçersiz kılmak için ŞART; sol kolon `min-w-0 overflow-y-auto px-5 py-4 space-y-5`), **kapak-renkli başlık çubuğu** (breadcrumb pano/liste + bell/link/⋮/×; kapak rengi seçiliyse `bg-palet-{ad} text-palet-{ad}-foreground`, yoksa `bg-background border-b` — DEM-67 backend'i `cards.coverColor`, DEM-74'te wire), **sol kolon** = sticky başlık + `CardCompleteToggle` (DEM-66 `card.complete`/`card.uncomplete` ile wire — tamamlanmış → `bg-success` tik + başlık `line-through text-muted-foreground`) + meta chip satırı (üye/due/etiket + kapak-rengi → 12-renk `--palet-*` picker → `card.update({coverColor})`) + AÇIKLAMA (`RichTextEditor` — Tiptap, §13.5) + KONTROL LİSTESİ (`SectionHeader` aksiyon-slotu ikon-only `ghost` + `Tooltip` — inline metin değil; `Progress` + atanan `Avatar`), **sağ panel** ≈360px = `Tabs` sekme strip (Yorumlar/**Aktivite**/Ekler/Tümü — DEM-74'te "İşlemler"→"Aktivite") + yorum composer (`Avatar` + mini `RichTextEditor` + B/I/`PaperclipIcon` disabled placeholder + Gönder) + aktivite feed + `EmptyState`; mobilde (`md:` altı) tek kolon (sağ panel alta düşer). Kart üzerinde (`card-item.tsx`): kapak görseli yoksa `coverColor` varsa üst şerit `-mx-2 -mt-2 mb-1.5 h-3 rounded-t-md bg-palet-{ad}`; `CardCompleteToggle` hover'da görünür (tamamlanmışsa hep), tamamlanmış → başlık `line-through`. **Backend sözleşmesi değişmez** — `card.update` / `comment.*` / `card.complete` / `card.uncomplete` procedure imzaları aynı (DEM-64/66/67'de eklendi, DEM-74 yalnız UI-wire); `description`/`body` artık **Tiptap JSON** olarak saklanır ama `text` kolonu değişmez, migration yok, mevcut plain-text içerikler render/edit anında düz paragrafa parse edilir (parse-time fallback; §13.5 + [`02-teknoloji-kararlari.md`](02-teknoloji-kararlari.md) Karar kaydı 2026-05-12). Optimistic UI yine yok (Faz 4); `clientMutationId` istemcide.

### 8.1.6 Board üye + etiket yönetimi UI (Faz 2.5E — [DEM-54](https://linear.app/demirkol/issue/DEM-54))

Board ayarları/detay ekranına üye/rol yönetimi + etiket yönetimi + board ekranında etiket filtresi. Backend Faz 2.5B (`label.*`) + Faz 2.5C (`board.members.*`, `board.invitations.*`) tamam. Backend sözleşmesi → [`03-backend.md`](03-backend.md) (Faz 2.5 procedure'leri), [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md).

- **Konum:** board detay sayfasında (§8.1.4 Board ekranı), üst bar aksiyon alanında `Paylaş` + `Etiketler` + `Üyeler` + `Pano ayarları` girişleri bulunur. `Paylaş` board linkini panoya kopyalar. `Etiketler` (`TagsIcon` ikon-butonu, `BoardLabelsDropdown`) tek panelde board etiket paletini taşır. `Üyeler` (`UsersIcon`) shadcn `DropdownMenu` içinde `Tabs` ile **"Üyeler" + "Davetler" + "Talepler"** üyelik-bağlamı sekmelerini taşır; `Pano ayarları` (`Settings2Icon`) aynı desende **"Arka plan" + "Pano işlemleri"** sekmelerini taşır (DEM-154 ayrımı — üyelik bağlamı ve etiket paleti ayar bağlamından ayrı yüzeylere alındı). `Etiketler` ve `Üyeler` butonları tüm rollere mount edilir (palet/üye listesi herkese açık); etiket düzenleme + `Davetler`/`Talepler` sekme içerikleri admin'e gate'lidir. Bekleyen erişim talebi varsa `Üyeler` butonu üstünde + "Talepler" sekme tetikçisinde kırmızı sayı rozeti (yalnız admin, `board.accessRequests.list` sayısı, `9+` taşması). Eski `Davet et / paylaş` birleşik butonu ve ayrı `⋮` board menüsü kaldırılır; rename/archive/restore gibi board lifecycle aksiyonları tek yüzey olarak "Pano işlemleri" altında kalır. Mutasyon kontrolleri yalnız board `admin`'e açıktır (gerçek kapı backend; UI rol-bazlı gizler — §8.1.2 modeli). Satır içi destructive onaylar mevcut shadcn `Dialog` pattern'ini korur.
- **Board aktivite drawer** (`board-activity-drawer.tsx`): üst bardaki activity ikonu sağ drawer açar; `trpc.board.activity.list` cursor feed'i ile board geçmişini actor + okunabilir özet satırlarıyla gösterir. Salt-okunur (`viewer+`) yüzeydir, `board.get` payload'ını şişirmez.
- **Üyeler** (`board-settings/board-members-section.tsx` + sunum `board-member-row.tsx`): `trpc.board.members.list` → satır başına ad + e-posta (DEM-157 — adlar çakışabildiği için kimlik netleştirme; workspace `MemberRow` ile aynı desen) + rol; inherited (workspace owner/admin) satırlar "workspace yöneticisi — düzenlenemez" işaretli (rol/çıkar yok). `admin` ise: rol değiştir (shadcn `Select` — `boardRoleSchema`: `admin`/`member`/`viewer`; tek explicit `admin` ve inherited satır kilitli) → `board.members.updateRole`; çıkar → `board.members.remove` (kendi satırında "board'dan ayrıl" → başarıda `/workspaces/[id]`'e yönlendir + `board.list`/`workspace.list` invalidate); üye ekle (e-posta + rol — `add-board-member-form.tsx`) → `board.members.add`, sonuç `kind` discriminated union: `added` / `added_as_guest` / `invited` → inline durum mesajı; `CONFLICT` → "zaten üye / zaten davetli". Mutation sonrası `board.members.list` + `board.get` (+ ekleme sonrası `board.invitations.list`) invalidate.
- **Gönderilmiş board davetleri** (`board-settings/board-sent-invitations.tsx`): `trpc.board.invitations.list` → bekleyen davetler (e-posta, rol, davet eden, son tarih); `admin` ise "iptal et" → `board.invitations.revoke` → `invitations.list` invalidate. Kullanıcının kendi board davetleri (`board.invitations.mine`) workspace davetleriyle aynı yerde — `(app)/page.tsx` "Bekleyen davetler" bölümü (§8.1.1'deki `PendingInvitations`) genişletildi: "Workspace davetleri" + "Board davetleri" alt grupları; board satırı: board adı + workspace adı + rol + davet eden + son tarih; "Kabul et"/"Reddet" → `board.invitations.accept`/`decline` (`clientMutationId` ile); kabul sonrası `workspace.list` + `board.invitations.mine` + `workspace.invitations.mine` invalidate.
- **Board erişim talepleri** (`board-settings/board-access-requests-section.tsx`): `trpc.board.accessRequests.list` → board linkinden talep gönderen kullanıcılar (ad/e-posta, mesaj). `admin` satır başına yalnızca board rolünü (`member`/`viewer`) seçer; `Onayla` → `board.accessRequests.approve` (backend tek transaction'da gerekirse workspace `guest` + board membership oluşturur) ve `accessRequests.list` + `board.members.list` + `board.get` invalidate eder. `Reddet` → `board.accessRequests.reject` ve yalnızca talep listesini yeniler. DEM-154 ile yeni bir talep oluşunca board admin'lerine `board_access_requested` bildirimi düşer (bildirim zilinde satır); domain kuralı → [`../domain/04-bildirim-kurallari.md`](../domain/04-bildirim-kurallari.md) "DEM-154".
- **Etiketler** (`board-settings/board-labels-dropdown.tsx` → `board-labels-section.tsx` + sunum `board-label-row.tsx` / `create-board-label-form.tsx`): üst bardaki `Etiketler` ikon-butonu dropdown'unda `trpc.label.list` → satır başına renk swatch + ad (boşsa "(adsız)"); buton tüm rollere açık, düzenleme yetkisi `canEdit` ile gate'li (board `admin` + board aktif — yetkisiz kullanıcı paleti görür ama mutasyon kontrolleri pasif): etiket ekle (renk seçici — `@pusula/domain` `LABEL_COLORS` paleti + opsiyonel ad) → `label.create`; satır içi düzenle (renk + ad) → `label.update`; sil (onaylı — kartlardan da kalkar) → `label.delete`. `CONFLICT` (aynı renk+ad) inline. Mutation sonrası `label.list` + `board.get` (kart rozetleri) invalidate. Renk→Tailwind swatch eşlemesi paylaşılan `_components/label-colors.ts`'te (`LABEL_SWATCH`; eskiden `card-detail/` altındaydı, yukarı taşındı — kart detayı + board ayarları + board ekranı ortak kullanır).
- **Etiket filtresi (board ekranı)** (`board-filter-bar.tsx` + saf `board-filter.ts`): board kolonlarının üstünde bir filtre çubuğu — etiket çipleri (çoklu seçim; bir kart **en az bir** seçili etikete sahipse gösterilir — client-side filtre, `board.get` verisi üzerinde). Etiket paleti `board.get`'in döndürdüğü `cards[].labels`'tan türetilir (board ekranı `label.list` çağırmaz). Filtre durumu local state (URL param değil — sade tutuldu). Kart rozetleri: `card-item.tsx` artık `card.labels`'ı küçük renk çipleri olarak gösterir.
- **Arşivli öğeler (G7 + DEM-71):** filtre menüsü yalnız etiket filtrelerini taşır; "Arşivli listeleri göster" toggle'ı (+ "N arşivli liste" sayacı) top bar'daki `Arşivli öğeler` dropdown'ına taşınır. Bu toggle dropdown içeriğini değil pano kolon görünümünü kontrol eder: board ekranı varsayılan olarak arşivli listeleri **gizler**, toggle açıkken arşivli listeler soluk ve salt-okunur gösterilir. Aynı dropdown'daki "Arşivli kartları göster" toggle'ı da pano seviyesinde state taşır; dropdown'un kendi amacı arşiv görünümü olduğu için arşivli liste ve kart bölümleri dropdown açıkken her zaman görünür. Arşivli kart satırları `trpc.card.listArchived({ boardId })` ile ayrı query'den gelir. Liste satırı `list.archive({ archived:false })` ile geri yüklenir. Kart aktif listedeyse `card.archive({ archived:false })`; kart arşivli listedeyse kullanıcı aktif hedef liste seçer, UI önce `card.moveToList({ toListId })`, sonra `card.archive({ archived:false })` çağırır. `board.get` aktif-kart-only kalır; arşiv görünümü ayrı query ile açılır.
- **`board.get` additive alanı:** board ekranı etiket filtresi + kart rozetleri için `board.get`'in `cards` projection'ına her kart için `labels: { labelId, name, color }[]` eklendi (`card_labels ⋈ labels`, board genelinde tek sorgu, server'da Map ile gruplanır) — mevcut shape additive, eski testler kırılmaz; bkz. [`03-backend.md`](03-backend.md) (`board.get`).
- **shadcn:** yalnızca mevcut bileşenler — `DropdownMenu`/`Tabs`/`Dialog`/`Select`/`Button`/`Input`/`Label`/`Badge`/`Alert` (yeni paket eklenmedi; renk seçici/arşiv toggle için `Popover`/`Switch` yerine native `button`/`input` + mevcut bileşenler — §8.1.5'teki gibi). Türkçe metinler `strings.board.settings` / `strings.board.filter` / `strings.invitations` (board grupları); hardcode metin yok. `clientMutationId` her collaborative mutation'da istemcide (`crypto.randomUUID()`). Optimistic UI yok (Faz 4) — mutation → `await` → ilgili query invalidate → refetch; hata inline `Alert`.
- **Test:** React Testing Library — board üye satırı (rol-bazlı görünürlük + inherited/last-admin işareti + rol değiştir/çıkar callback), board üye ekle formu (e-posta/rol validasyon + submit), board etiket satırı (düzenle/sil callback + no-op save), board etiket oluştur formu (renk/ad + conflict), board filtre çubuğu (çip seçim/temizle + arşiv toggle), board filtre saf fonksiyonları (`board-filter.ts` — OR semantiği + arşivli liste filtresi/sayacı), kart rozetleri. Backend için `board.test.ts`'e `board.get` `cards[].labels` testi eklendi. Playwright e2e ileri faz (Faz 8).

### 8.1.7 Profil / hesap ayarları ekranı (Faz 1)

Kullanıcının kendi hesabını yönetebileceği ekran — `bosluk-tara` (G5) ile açıldı ([DEM-55](https://linear.app/demirkol/issue/DEM-55)). Bu ekran **tRPC kullanmaz**; doğrudan Better Auth client (`apps/web/src/lib/auth-client.ts`) uçlarına gider — sözleşme → [`07-auth.md`](07-auth.md) (Profil & hesap yönetimi); şemalar `@pusula/domain` (`updateProfileInput` / `changePasswordInput` / `deleteAccountInput`). Hesap silme domain kuralı → [`../domain/02-yetkilendirme-kurallari.md`](../domain/02-yetkilendirme-kurallari.md) (Hesap (User) — öz-yönetim), [`../domain/01-urun-modeli.md`](../domain/01-urun-modeli.md) (invariant 14).

- **Route:** `app/(app)/account/page.tsx` (client component) — `authClient.useSession()` ile mevcut kullanıcı (oturum kabuğu `(app)/layout.tsx` zaten korur). App-shell header'ına "Hesap" linki (`/account`) eklenir (kullanıcı adının yanında).
- **Profil:** ad input (`@pusula/domain` `userNameSchema` ile client-side validasyon) + avatar URL input (boş → kaldır; URL formatı doğrulanır) → `authClient.updateUser({ name, image: image || null })`; başarıda `authClient.useSession().refetch()` (header güncellensin) + başarı mesajı. Avatar **basit URL** (yükleme yok — Faz 8 attachment'a bağlanabilir; karar 2026-05-12).
- **Parola değiştir:** mevcut parola + yeni parola (+ yeni parola tekrar — client-side eşleşme kontrolü) → `authClient.changePassword({ currentPassword, newPassword, revokeOtherSessions: true })`; hatalar inline (`currentPassword` yanlış → "Mevcut parola hatalı"); başarıda alanlar temizlenir + başarı mesajı.
- **Tehlikeli bölge — hesabı sil:** onaylı `Dialog` içinde parola input'u (re-auth) → `authClient.deleteUser({ password })`; başarıda `/sign-in`'e dön. Kullanıcı bir workspace'in `owner`'ıysa sunucu (`beforeDelete` hook) `BAD_REQUEST` döndürür → inline `Alert` ("Önce sahibi olduğun çalışma alanlarını silmen/devretmen gerekiyor" + workspace listesine link). UI bunu önceden de işaret edebilir (kullanıcının `owner` olduğu workspace sayısı `trpc.workspace.list`'in `role` alanından okunabilir) ama gerçek kapı sunucudadır.
- **shadcn:** mevcutlar yeterli (`Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardContent`/`Button`/`Input`/`Label`/`Alert`/`Dialog`); yeni bileşen yok. Türkçe metinler `apps/web/src/lib/strings.ts` (`strings.account`); UI bileşenleri hardcode metin içermez. `clientMutationId` yok (Better Auth uçları — tRPC collaborative mutation değil).
- **Test:** React Testing Library — presentational form bileşenleri (`ProfileForm` / `ChangePasswordForm` / `DeleteAccountSection`): render + validasyon + callback (auth-client mock'lu). `canDeleteOwnAccount` domain kuralı için `@pusula/domain` Vitest. Playwright e2e ileri faz (Faz 8).

### 8.1.8 Board ekranı drag-drop (Faz 3B — [DEM-43](https://linear.app/demirkol/issue/DEM-43))

Board ekranına (§8.1.4 Board ekranı / §8.1.5 kart detayı; tasarım dili [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) §13.2) sürükle-bırak: **kolon (liste) reorder · kart reorder (liste içi) · kart cross-list taşıma**. Yalnızca **Atlassian Pragmatic Drag and Drop** (`@atlaskit/pragmatic-drag-and-drop`; dnd-kit vb. **yok** — proje kararı). Backend sözleşmesi `list.move` + `card.move` (Faz 3A — [DEM-42](https://linear.app/demirkol/issue/DEM-42)) → [`03-backend.md`](03-backend.md); mekanik / server akışı + zorunlu UI ilkeleri → [`05-board-mekanigi.md`](05-board-mekanigi.md) §5.1; sıralamanın iş anlamı (komşu pozisyon, eşzamanlı `CONFLICT`, compaction) → [`../domain/03-siralama-kurallari.md`](../domain/03-siralama-kurallari.md).

- **Bağımlılık & kapsam:** Faz 3A ([DEM-42](https://linear.app/demirkol/issue/DEM-42) — `list.move`/`card.move`) ve Faz 2D ([DEM-37](https://linear.app/demirkol/issue/DEM-37) — board ekranı) hazır olmalı. Tam optimistic cache modeli **Faz 4** ([DEM-27](https://linear.app/demirkol/issue/DEM-27)); bu fazda drag UX akıcıdır ama kalıcılaştırma `trpc.board.get` cache'i lokal güncelle → mutation → `onSettled` invalidate + refetch (server'ın kesin `position`'ıyla reconcile) düzeyinde tutulur. Realtime echo (Faz 5 — [DEM-28](https://linear.app/demirkol/issue/DEM-28)) ve compaction worker (Faz 3C — [DEM-44](https://linear.app/demirkol/issue/DEM-44)) ayrı işler; bu UI yalnızca `clientMutationId` üretir/gönderir (ileri-uyum, henüz tüketici yok).
- **Drag sırasında:** backend mutation **atılmaz**. React state yalnız local güncellenir — sürüklenen kartın/kolonun "hayalet" gösterimi + hedef konumda placeholder; kart/kolon ölçüleri stabil (layout shift yok). Pragmatic DnD `draggable` (kart/kolon) + `dropTargetForElements` (kolon gövdesi = kart hedefi; board şeridi = kolon hedefi) + `monitorForElements` ile yönetilir; `closestEdge` ile kart için "üst/alt", kolon için "sol/sağ" edge'i belirlenir. Otomatik kenar-kaydırma (auto-scroll) için Pragmatic DnD'nin scroll paketi kullanılır.
- **Custom drag preview — kart için body-portal pattern** (DEM-87): Pragmatic DnD'nin `setCustomNativeDragPreview` HTML5 drag-image bitmap yolu kart için terk edildi (alpha kanalı, soft-shadow keskinleşmesi, `rotate` ile transparent köşe sızıntısı bug'ları). Yerine: `disableNativeDragPreview` (1×1 transparent gif → browser preview görünmez) + `useBoardDnd` hook'unun lazy `createCardDragOverlayController()` factory'si — `position: fixed; pointer-events: none; z-index: 9999` body element'ı oluşturur, `createRoot` + `flushSync` ile `CardDragPreview` React komponentini canlı tree'de mount eder, `monitorForElements.onDrag` callback'inde DOM `style.transform = translate(...)` ile cursor pozisyonuna pinler (her frame React state yok → board re-render yok). `onDrop` ve hook unmount cleanup'ta portal kapatılır. Sürüklenen kartın orijinali "**rüya modu**"na geçer (eski Pusula `dnd-kit DragOverlay` deneyiminden esinlenildi): outer `<article>` `border-dashed border-primary/60 bg-primary/5` placeholder olur, içerik wrapper `invisible` (yer korunur, layout shift yok). Kolon preview'i hâlâ DOM clone (`renderLiftedPreview` + `setCustomNativeDragPreview` — kolon büyük + yatay tilt soft, bug pratikte hissedilmiyor). Tasarım dili → [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) §13.2.
- **`onDrop` (drag bitişi):** hedef konuma göre `before`/`after` komşular belirlenir; client `@pusula/domain/position` `positionBetween(before?.position, after?.position)` ile `newPosition` hesaplar (server doğrular veya yeniden hesaplar). Konum değişmediyse (aynı yere bırakıldı) mutation atılmaz. **Tek** mutation çalışır:
  - kart → `trpc.card.move({ cardId, fromListId, toListId, beforeCardId?, afterCardId?, newPosition?, clientMutationId })` — `toListId === fromListId` ise liste-içi reorder, farklıysa cross-list (yalnız **aynı board**; başka board'a taşıma ileri faz `card.moveToList`, Faz 3E — [DEM-69](https://linear.app/demirkol/issue/DEM-69)).
  - kolon → `trpc.list.move({ listId, beforeListId?, afterListId?, newPosition?, clientMutationId })`.
- **Optimistic + rollback:** `onDrop` anında cache snapshot alınır + `queryClient.setQueryData(trpc.board.get …)` ile kart/liste yeni sıraya çekilir (kartın `listId`+`position` veya listenin `position` güncellenir, ilgili liste içi sıra yeniden sıralanır), sonra mutation gönderilir. `onSettled` → `trpc.board.get` invalidate. `onError` → snapshot cache'e **geri yazılır** + düşük gürültülü hata (`strings.board.dnd.error`). `CONFLICT` (kart artık `fromListId`'de değil — eşzamanlı taşıma; bkz. `03-siralama-kurallari.md`) → rollback + `trpc.board.get` refetch + "kart başkası tarafından taşındı, görünüm yenilendi" bilgisi (kart sessizce kaybolmaz).
- **Yetki & arşiv:** drag yalnızca board `member+` için etkin (`board.role`; `viewer` salt-okunur → `draggable` mount edilmez); arşivli liste **drop hedefi olamaz** (UI dnd hedefini kapatır + server zaten reddeder). Mutation hatası inline.
- **Erişilebilirlik / alternatif:** sürükle-bırak yanında klavye-ve-buton tabanlı taşıma — kart ⋮ menüsünde "Taşı" (liste + konum picker → `card.move`), kolon ⋮ menüsünde "Sola/Sağa taşı" (→ `list.move`); klavye ve mobil kullanıcıları için (mobil drag-drop öncelikli değil — §8.2). Drag handle'lar ARIA etiketli; sürükleme durumu için `aria-live` duyurusu (best-effort).
- **shadcn / metinler:** yeni component library yok — mevcut board bileşenleri (`card-item.tsx`, kolon bileşenleri) Pragmatic DnD ile sarılır; ⋮ menüleri mevcut `DropdownMenu` ile. Türkçe metinler `apps/web/src/lib/strings.ts` (`strings.board.dnd.*` — "taşı" / "sola taşı" / hata-çakışma mesajları); UI bileşenleri hardcode metin içermez. `clientMutationId` her move'da istemcide (`crypto.randomUUID()`).
- **Test:** React Testing Library — komşu/edge → `before`/`after` seçimi ve `positionBetween` sarmalayıcısı (saf fonksiyon, izole), cache optimistic update + rollback (mock `queryClient`). Pragmatic DnD etkileşiminin uçtan uca doğrulaması Playwright'ta (Faz 3D — [DEM-45](https://linear.app/demirkol/issue/DEM-45)): liste reorder · kart reorder (aynı liste) · cross-list move · `card.move` başarısızlık → rollback · (ops.) `CONFLICT`/eşzamanlı kullanıcı. Geniş e2e suite → Faz 8 ([DEM-31](https://linear.app/demirkol/issue/DEM-31)).

---

### 8.1.9 Optimistic UI (Faz 4 — [DEM-27](https://linear.app/demirkol/issue/DEM-27))

Faz 3B drag-drop (§8.1.8) `card.move`/`list.move` için optimistic akışı kurdu; Faz 4 bunu **tüm board/list/card collaborative mutation'larına** genelleştirir, ortak cache modelini + helper'ları çıkarır. Cache modeli + query key konvansiyonu + lifecycle protokolü + `clientMutationId` semantiği + `CONFLICT` davranışı → [`05-board-mekanigi.md`](05-board-mekanigi.md) §5.2.

- **Modül konvansiyonu:** `apps/web/src/lib/board-cache/` — tüm cache erişimi tek modülden geçer:
  - `keys.ts` — query key factory (`boardKey(id)` / `boardsKey(workspaceId)` / `cardKey(id)`); component'larda literal array yok.
  - `types.ts` — `BoardCache` / `ListCache` / `CardCache` tipleri (tRPC `board.get` / `card.get` output'undan türetilmiş, tek kaynak `@pusula/api`).
  - `primitives.ts` — pure cache update fonksiyonları (`updateCardInCache` / `moveCardInCache` / `moveListInCache` / `addCardToCache` / `removeCardFromCache` / `archiveCardInCache` / `archiveListInCache` / `addListToCache` / `updateListInCache` / `updateBoardInCache`); queryClient ile değil cache snapshot ile çağrılabilir (4D Vitest pure-function testleriyle [DEM-81](https://linear.app/demirkol/issue/DEM-81) doğrulanır).
  - `mutations.ts` — `useOptimisticBoardMutation` higher-order hook: lifecycle iskeletini standartlaştırır (`onMutate` snapshot + apply + rollback context; `onError` rollback + toast; `onSettled` invalidate). Component'lar mutation-spesifik `apply` callback'i geçerek kendi mutation'ını kurar; `clientMutationId` (`crypto.randomUUID()`) hook tarafından transparan eklenir (component her seferinde elden eklemez).
- **Optimistic kapsam (apps/web ekranları):**
  - **§8.1.4 board ekranı** (DEM-37/63/74) — `card.create` / `card.update` / `card.archive` / `card.complete` / `card.uncomplete` (tamamla toggle § 8.1.5 modal + kart üzeri inline; 2.7C-2 / DEM-74 wire) / `list.create` / `list.update` / `list.archive`.
  - **§8.1.8 drag-drop** (DEM-43) — `card.move` / `list.move` (Faz 3B'de zaten optimistic; 4C `useOptimisticBoardMutation`'a göç ettirir, kullanıcıya görünen davranış aynı; sadece kod ortak hook'a taşınır).
  - **§8.1.5 kart detay modalı** (DEM-53/64/74) — `card.update` (description/title/due/cover) + `card.complete` / `uncomplete` + `card.archive`.
  - **Board listesi (`workspaces/[id]`)** — `board.create` / `board.update` / `board.archive`.
  - Kart ⋮ menüsü "Listeye taşı" / "Kopyala" (Faz 3E backend `card.moveToList` / `card.copy` UI follow-up) — Faz 4'te optimistic kurulur.
- **Kapsam dışı (Faz 5/6'da optimistic'lenecek):** `comment.*` · `checklist.*` · `card.label.*` · `board.members.*` · `card.members.*` — realtime echo + activity feed ile birlikte ele alınır (Faz 5 — [DEM-28](https://linear.app/demirkol/issue/DEM-28); Faz 6 — [DEM-29](https://linear.app/demirkol/issue/DEM-29)). Faz 4'te bunlar mevcut `await mutate(...)` + `queryClient.invalidate(...)` pattern'ini korur (network beklemesi var ama hata yolu basit, optimistic kompleksiyonu yok).
- **UX davranışı:**
  - **Optimistic uygulanmış mutation hata aldıysa** (`CONFLICT` dışı): rollback (cache eski hale) + toast (`strings.board.optimistic.error` — "Değişiklik kaydedilemedi. Tekrar dener misin?") + opsiyonel "Tekrar dene" CTA. Toast `@pusula/ui` (`sonner` wrapper); destructive variant. **Otomatik retry yok** (yan etki tekrarı riski).
  - **`CONFLICT`:** rollback + `board.get` refetch + bilgi toast (`strings.board.conflict.refreshed` — "Liste başka bir yerde güncellendi, yeniden yüklendi.") — neutral variant.
  - **Loading state:** optimistic uygulandığı için spinner / disabled state **göstermez**; yalnızca mutation gerçekten yavaşsa (>1s) form/butonu pasif yapma davranışı mevcut pattern'lerden değişmez. Drag-drop'ta drag-handle blokajı yok (Faz 3B davranışı korunur).
- **Türkçe metinler:** yeni toast metinleri `apps/web/src/lib/strings.ts` altında `strings.board.optimistic.*` ve `strings.board.conflict.*`; UI bileşenleri hardcode metin içermez ([`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md)).
- **Test (Faz 4D — [DEM-81](https://linear.app/demirkol/issue/DEM-81)):** Vitest + Testing Library + msw/tRPC mock — failure modları: network hata (rollback), `CONFLICT` (refetch + toast), race (eşzamanlı `card.move`), rollback doğrulaması (cache snapshot eski state), `clientMutationId` injection (UUID v4 regex). Mutation başına en az 1 senaryo: `card.move` / `card.create` / `card.archive` / `list.move` / `board.update`. Cache primitives'in pure-function birim testleri 4B'de ([DEM-79](https://linear.app/demirkol/issue/DEM-79)). E2E (Playwright) Faz 8'e ([DEM-31](https://linear.app/demirkol/issue/DEM-31)), realtime echo ayıklama testleri Faz 5'e ([DEM-28](https://linear.app/demirkol/issue/DEM-28)) bırakılır.
- **Wired (Faz 4B — [DEM-79](https://linear.app/demirkol/issue/DEM-79), 2026-05-13):** `apps/web/src/lib/board-cache/` modülü kuruldu — `keys.ts` `useBoardCacheKeys()` hook + `BoardCacheKeys` arayüzü (`board(id)`/`boards(workspaceId)`/`card(id)` tRPC `queryFilter` wrapper'ları; literal key array yok); `types.ts` `BoardCache`/`ListCache`/`CardCache`/`BoardSummary`/`CardDetailCache` `RouterOutputs` türevli (tek kaynak `@pusula/api`); `primitives.ts` 10 pure transform `applyCardMove`/`applyListMove`/`applyCardPatch`/`applyCardAdd`/`applyCardRemove`/`applyCardArchive`/`applyListAdd`/`applyListPatch`/`applyListArchive`/`applyBoardPatch` (yukarıdaki `updateCardInCache`/... isimleri tasarım metniydi; implementasyon `apply*` prefixiyle landed — `queryClient` almazlar, snapshot üzerinde pure çalışırlar; same-list+same-position no-op ref-stable); `mutations.ts` `useOptimisticBoardMutation` higher-order hook iskeleti (snapshot+cancel+apply / rollback + `CONFLICT` branch [data.code → invalidate + `onConflict`; aksi `onMutationError`] / `board.get` + opsiyonel `card.get` invalidate) + `withClientMutationId` UUID v4 helper (4C tüm collaborative mutation'larda zorunlu üretim). Faz 3B `board-dnd-cache.ts` + `board-dnd-cache.test.ts` **silindi** (göç); `use-board-dnd.ts` artık `applyCardMove`/`applyListMove` + `cacheKeys.board(boardId)` üzerinden — davranış aynı. Mevcut bileşenlerin literal `trpc.*.queryFilter(...)` çağrılarının `cacheKeys.*` factory'sine göçü **bu commit'in kapsamı dışında** — Faz 4C optimistic mutation wiring ile paralel tab çakışmasını önlemek için ileri turlara bırakıldı. Şu an `apps/web` içinde 13+ literal `queryFilter` çağrısı hâlâ mevcut; ilgili refactor Faz 8 hardening turunda yapılır (Faz 4 review fix turu 2026-05-15'te drift doğrulandı). Vitest 26 pure-function birim testi PASS. Optimistic WIRE 4C ([DEM-80](https://linear.app/demirkol/issue/DEM-80)), failure testleri 4D ([DEM-81](https://linear.app/demirkol/issue/DEM-81)).

---

### 8.1.10 Realtime board sync (Faz 5 — [DEM-28](https://linear.app/demirkol/issue/DEM-28))

Faz 4 optimistic UI (§8.1.9) tek-kullanıcı UX'i çözdü; Faz 5 **çoklu-kullanıcı sync**'i ekler: Alice kartı taşır → Bob `<500ms` içinde canlı görür (worker queue + outbox sayesinde `<1s` dayanıklı). Backend: outbox + worker + Socket.IO server → [`03-backend.md`](03-backend.md) "Faz 5" + [`06-bildirim-altyapisi.md`](06-bildirim-altyapisi.md) "Realtime event yayın katmanı"; tablo şeması → [`04-veri-katmani.md`](04-veri-katmani.md) (`realtime_events`); event envelope + reconciliation mekaniği → [`05-board-mekanigi.md`](05-board-mekanigi.md) §5.3.

- **Modül konvansiyonu (`apps/web/src/lib/realtime/`):**
  - `client.ts` — `socket.io-client` tek `io()` instance; `API_URL`'a bağlanır; `withCredentials: true` (session cookie); `transports: ['websocket']` (Faz 5 başlangıcında long-polling fallback yok — Redis adapter sticky session sorununu azaltır); auto-reconnect (varsayılan); `disconnect()` cleanup.
  - `useBoardRealtime(boardId)` — board sayfasında mount edilen React hook:
    - Mount: socket.connect (henüz değilse) → `socket.emit('board:join', { boardId })` → server `resolveBoardAccess` kontrolü + join ack → event listener register (`card.*`, `list.*`, `board.*`)
    - Unmount: `socket.emit('board:leave', { boardId })` + listener detach
  - `event-handlers.ts` — event türüne göre `apps/web/src/lib/board-cache/primitives.ts` (Faz 4B / [DEM-79](https://linear.app/demirkol/issue/DEM-79)) çağrısı: `card.moved` → `moveCardInCache` · `card.created` → `addCardToCache` · `card.updated` → `updateCardInCache` · `card.archived` → `archiveCardInCache` · `card.completed`/`uncompleted` → `updateCardInCache({completedAt})` · `list.*` → ilgili primitive · `board.updated`/`archived` → `updateBoardInCache`.
  - `in-flight-store.ts` — `useOptimisticBoardMutation` (Faz 4C / [DEM-80](https://linear.app/demirkol/issue/DEM-80)) ile entegre: aktif mutation'ların `clientMutationId` set'i (mutation `onMutate`'te add, `onSettled`'da delete). Realtime event listener bu set'ten gelen `clientMutationId`'leri **skip** eder (echo ayıklama).
- **Echo ayıklama (kritik):** Alice kart taşır → optimistic update cache'i hemen güncelliyor → mutation server'a gidiyor → tx commit + outbox insert → worker publish → Alice'in socket'i de event'i alıyor. **Eğer echo skip yapılmazsa** Alice'in cache'i ikinci kez güncellenir (idempotent ama gereksiz; race condition'da yanlış sonuç). `if (event.clientMutationId && inFlightSet.has(event.clientMutationId)) return;` zorunlu.
- **`seq` gap tespiti:** Board cache'de `lastAppliedSeq` (board'un `version` aynası). Gelen event:
  - `event.seq === lastAppliedSeq + 1` → uygula, `lastAppliedSeq = event.seq`.
  - `event.seq > lastAppliedSeq + 1` → **gap** (kaçırılan event'ler) → `boardKey` invalidate + `board.get` refetch → `lastAppliedSeq` server response'un `version`'ına eşitlenir.
  - `event.seq <= lastAppliedSeq` → stale/duplicate → skip.
- **Reconnect resync:** Socket reconnect event'inde board sayfasındaki `boardKey` invalidate + refetch (kaçırılan event'ler için tek refetch yeterli; outbox replay client'a değil server-side cache miss'e karşı).
- **Yetki & arşiv:** `viewer` rolü realtime event'leri görür (cache update olur, UI ona göre render eder), ama `useOptimisticBoardMutation` zaten mutation atamıyor; arşivli board'a join talebi server tarafında reddedilir (`apps/web` sayfa zaten arşivli board'ı yönlendirir veya salt-okunur gösterir).
- **UX davranışı:**
  - Realtime event uygulandığında **toast yok** (alice'in eylemi bob için "doğal" akış — gürültü çıkarmaz). İstisna: kart bob'un açık olduğu modalda değişirse subtle bilgi banner'ı (sonraki tur).
  - Çevrimdışı: socket disconnect olursa subtle "Bağlantı koptu, tekrar bağlanılıyor..." banner'ı (`strings.realtime.disconnected`); reconnect olduğunda banner kaybolur. Disconnect süresince mutation'lar optimistic çalışmaya devam eder ama server'a gitmez (4C `useOptimisticBoardMutation` zaten retry'lı değil — kullanıcı manuel "tekrar dene"). Sonraki tur: çevrimdışı kuyruk (Faz 8).
- **Türkçe metinler:** `apps/web/src/lib/strings.ts` `strings.realtime.*` (`disconnected` / `reconnecting` / banner mesajları).
- **Test (Faz 5D — [DEM-86](https://linear.app/demirkol/issue/DEM-86)):**
  - Vitest birim: event handler'ların cache primitives doğru çağırması (her event tipi için); echo ayıklama (in-flight set'ten gelen event skip); `seq` gap → refetch tetiklenir; `seq` stale → skip; reconnect → `boardKey` invalidate; `useBoardRealtime` mount/unmount cleanup.
  - Playwright e2e iki kullanıcı (`alice` + `bob` fixture; ortak board): card move sync · list create sync · card archive sync · reconnect resync · echo ayıklama doğruluğu. Faz 3D ([DEM-45](https://linear.app/demirkol/issue/DEM-45)) Playwright harness'ı üstüne.
- **Wired (Faz 5C — [DEM-85](https://linear.app/demirkol/issue/DEM-85), 2026-05-13):** `apps/web/src/lib/realtime/` modülü kuruldu: `client.ts` (`socket.io-client` singleton `getRealtimeSocket()` — `withCredentials: true` + `transports: ['websocket']` + `autoConnect: false`; `REALTIME_EVENT_CHANNEL = 'realtime:event'` apps/api `emit.ts` ile birebir aynı tek-channel envelope ile); `in-flight-store.ts` (modül-scope `Set<string>` — `add/has/remove/clear`); `event-handlers.ts` `dispatchRealtimeEvent(qc, filters, envelope)` (12 event tipi → `board-cache/primitives` çağrısı; `card.completed`/`uncompleted` superjson `Date` dönüşümü; `card.updated` `card.get` cache cascade; unknown event tipi → `console.warn` + skip — forward compat); `use-board-realtime.ts` `useBoardRealtime(boardId)` (mount: singleton connect + `board:join` emit + `realtime:event`/`connect`/`disconnect` listener'ları register; her envelope için **echo skip → seq gating [seq+1 apply + cache.board.version bump / seq>+1 invalidate / seq≤version stale drop / no-baseline invalidate] → reconnect handler invalidate+rejoin**; unmount: listener detach + `board:leave` emit — yalnız `joined` flag set'liyse [connect-then-join guard]); `index.ts` barrel. `useOptimisticBoardMutation` + `useOptimisticBoardListMutation` (Faz 4C) `onMutate`'te `addInFlightClientMutationId`, `onSettled`'da `removeInFlightClientMutationId` ile entegre (additive — public yüzey değişmedi; Faz 4D 14/14 testi yeşil kaldı). Board sayfası `page.tsx` `useBoardRealtime(boardId)` mount + disconnect amber banner (`strings.realtime.disconnected` + `role="status" aria-live="polite"`). `cacheKeys` referansı `useRef` ile sabitlenip effect deps `[boardId, queryClient, socket]`'e indirgendi (sonsuz reconnect döngüsü önleme). Dep: `socket.io-client ^4.8.1`. Vitest 31/31 — `in-flight-store.test.ts` (6), `event-handlers.test.ts` (14 — her tip + unknown + card.get cascade), `use-board-realtime.test.tsx` (11 — mount/unmount/listener-detach/echo/apply/gap/stale/no-baseline/reconnect/connect-then-join/connection-status). Toplam apps/web 386/386 PASS. Playwright e2e + load test 5D ([DEM-86](https://linear.app/demirkol/issue/DEM-86)).

---

### 8.1.11 Notification center (Faz 6 — [DEM-29](https://linear.app/demirkol/issue/DEM-29))

Faz 5C realtime altyapısının (§8.1.10) `user:{userId}` room'una yayınlanan `notification.created` event'lerini tüketen kullanıcı bildirim merkezi. App shell header'da bell ikonu + unread badge + Popover dropdown panel. Backend: `notifications.*` procedure'leri (Faz 6A — [`03-backend.md`](03-backend.md) "Faz 6 — notification & push procedure'leri"); domain kuralları → [`../domain/04-bildirim-kurallari.md`](../domain/04-bildirim-kurallari.md); processor → [`06-bildirim-altyapisi.md`](06-bildirim-altyapisi.md) "Notification processor (Faz 6)".

- **Modül konvansiyonu:**
  - `apps/web/src/lib/realtime/use-user-realtime.ts` (yeni) — Faz 5C `useBoardRealtime` pattern'iyle paralel: `user:{userId}` room zaten otomatik join (5A bağlantıda); listener `notification.created` event'ini dinler → `trpc.notifications.list` ve `trpc.notifications.unreadCount` cache invalidate (badge anlık güncellenir). Echo ayıklama gerekmez (`notification.created` server-initiated; client `clientMutationId`'si yok). App shell layout'unda `(app)/layout.tsx`'te bir kez mount edilir; tüm sayfalarda aktif.
  - `apps/web/src/app/(app)/_components/notification-bell.tsx` (yeni) — header'da bell:
    - `lucide-react` `Bell` icon + unread count badge (>0 ise sayı; `>9` için `9+`; Tailwind absolute positioning; `aria-label="{unread} okunmamış bildirim"`).
    - onClick → shadcn `Popover` (`NotificationCenterPanel`).
  - `apps/web/src/app/(app)/_components/notification-center.tsx` (yeni) — Popover içeriği:
    - Başlık: "Bildirimler" + "Tümünü okundu işaretle" buton (`trpc.notifications.markAllRead`).
    - Liste: `trpc.notifications.list.useInfiniteQuery()` (cursor pagination; scroll'la fetch next; veya manuel "Daha fazla" buton).
    - Her satır: `Avatar size-xs` (actor varsa) + `activitySummary` Türkçe başlık (DEM-53 çeviri katmanı yeniden kullanılır) + zaman ("2 dakika önce" — `date-fns` veya `Intl.RelativeTimeFormat`); unread satır = `bg-accent/30` + sol kenar mavi şerit.
    - onClick → `router.push(...)` (ilgili karta/board'a yönlendir) + `trpc.notifications.markRead({ id })` (optimistic; cache'ten unread set'inden çıkar).
    - Boş durum: shadcn `EmptyState` ("Henüz bildirim yok").
    - Yükleme: skeleton (3-4 satır).
- **Realtime entegrasyon:** `useUserRealtime` `notification.created` aldığında:
  1. `trpc.notifications.list` cache invalidate → arkadan refetch (yeni notification listede görünür).
  2. `trpc.notifications.unreadCount` cache invalidate → badge anlık artar.
  3. Toast yok (gürültü); badge artışı yeterli görsel sinyal.
- **Tıklayınca davranış:** notification satırına tıkla → `payload.linkTo` (`/workspaces/{wsId}/boards/{boardId}?card={cardId}` veya benzer) URL'i hesapla → `router.push` + mark-read. Modal açma (kart detay) URL'deki `?card=` param'ı yakalar (DEM-53 mevcut pattern).
- **Bildirim tipi → görsel ikon eşleme (`packages/ui/notification-type-icon.tsx` veya inline):**
  - `card.member_added` → `UserPlus` (mavi)
  - `comment.mentioned` → `AtSign` (sarı/önemli)
  - `comment.created` → `MessageSquare` (gri)
  - `due_reminder_*` → `Clock` (turuncu/kırmızı overdue için)
  - `board.member_invited` → `Mail` (mavi)
  - `card.archived` → `Archive` (gri)
  - vb. (DEM-93 implementation turunda kararlaştırılır)
- **i18n strings (`apps/web/src/lib/strings.ts`):** `strings.notifications.*`:
  - `bellAria` — bell ikonu aria-label
  - `title` — "Bildirimler"
  - `markAllRead` — "Tümünü okundu işaretle"
  - `empty` — "Henüz bildirim yok"
  - `unreadCountLabel` — "{n} okunmamış"
  - `loadMore` — "Daha fazla yükle"
  - Notification type başlık template'leri (`{actor} sana '{card}' kartını atadı` vb.) — DEM-53 `activitySummary` çeviri katmanı yeniden kullanılır.
- **Yetki:** notifications.list yalnız kendi userId'ye filtreli; `protectedProcedure` enforce eder. Notification center'da silinmiş karta link → karta gidince 404 → UI graceful fallback ("Bu kart artık mevcut değil").
- **Tercih ekranı:** **Faz 6'da yok.** Default tercihler (in-app her zaman + push opt-in + email opt-in) ile çalışır; tercih ekranı UI sonraki tur. Backend tablo + API hazır (`notification_preferences`).
- **Test (Faz 6D + 6E):**
  - Vitest RTL: `NotificationBell` (unread count render + Popover açılır), `NotificationCenter` (`notifications.list` render + markRead optimistic + markAllRead toplu + tıkla → router.push + mark-read), `useUserRealtime` (`notification.created` event → cache invalidate + badge artar).
  - Playwright e2e (Faz 6E — alice/bob fixture): atama bildirimi (bob bell badge 0→1 + tıkla → karta yönlendir + mark-read → badge 0), mention bildirimi, realtime push (panel açık iken anlık güncelleme).
- **Faz kapsamı:** Faz 6D ([DEM-93](https://linear.app/demirkol/issue/DEM-93)) implementasyon. Mobile notification UI Faz 7 ([DEM-30](https://linear.app/demirkol/issue/DEM-30)) — Expo `apps/mobile`.

> **Wired — DEM-93 (2026-05-14):** Web notification center `apps/web/src/app/(app)/_components/notification-bell.tsx` + `notification-center.tsx` + `notification-link.ts` + `notification-type-icon.tsx` altında bağlandı; `AppShell` header'daki placeholder gerçek bell ile değişti ve `useUserRealtime()` shell seviyesinde mount edildi. Realtime hook `apps/web/src/lib/realtime/use-user-realtime.ts` içinde `notification.created` event'i için `notifications.list` + `notifications.unreadCount` invalidate eder; `event-handlers.ts` bilinçli olarak değiştirilmedi. Copy ve summary katmanı `apps/web/src/lib/strings.ts` + `apps/web/src/lib/activity-summary.ts`, relative time `apps/web/src/lib/format.ts`, Popover primitive `packages/ui/src/components/popover.tsx`.

---

### 8.1.12 Arama UI (Faz 6.5 — [DEM-56](https://linear.app/demirkol/issue/DEM-56))

Faz 6.5 web yüzeyi iki parçadan oluşur: app-shell global arama ve board ekranı içi arama/filtresi. Backend sözleşmesi → [`03-backend.md`](03-backend.md) "Faz 6.5 — Search procedure'leri"; domain kuralları → [`../domain/06-arama-kapsami.md`](../domain/06-arama-kapsami.md); FTS altyapısı → [`09-depolama-ve-arama.md`](09-depolama-ve-arama.md) §9.2.

- **Global entrypoint:** AppShell header'da search trigger. App-shell v2'de sol grup workspace/board switcher, sağ grup notification/theme/user taşıdığı için arama trigger'ı orta hatta veya `BoardTopBar` search aksiyonuyla çakışmayacak şekilde header içinde tek, sabit genişlikli command/search button olur. Kısa yol: `Cmd/Ctrl+K` ve `Ctrl+Space`. UI metinleri `strings.search.*`; hardcode metin yok.
- **Arama yüzeyi:** shadcn `Command` primitive'i yoksa mevcut `Dialog`/`Popover` + `Input` ile başla; yeni primitive gerekiyorsa `packages/ui` içinden shadcn standardıyla eklenir. İlk sürüm önerisi `Dialog`: klavye ve sonuç gezinimi daha net, mobilde tam ekran davranışına daha yakın.
- **Query davranışı:** UI 2 karakterden önce `search.query` çağırmaz. 250-300ms debounce; query değişince eski istek sonucu yeni query'yi ezmez (TanStack Query key query string + scope içerir). Loading skeleton, empty state ve error state ayrı.
- **Scope:** Global aramada `workspaceId`/`boardId` verilmez veya aktif workspace varsa workspace scope verilir; API permission filtreli sonuç döndürür. Board içi aramada `boardId` zorunlu ve sonuçlar aktif board ile sınırlıdır. `includeArchived` ilk UI'da kapalıdır; arşivli öğeler dahil et toggle'ı ileri iterasyonda veya board içi aramada eklenebilir.
- **Sonuç listesi:** Entity tipine göre ikon + başlık + snippet + context satırı:
  - Board: workspace adı + board başlığı.
  - List: board adı + liste adı.
  - Card: board/list bağlamı + kart başlığı.
  - Comment: kart başlığı + yorum snippet'i.
  - Label: board adı + etiket adı/renk swatch.
- **Navigasyon:** Sonuç satırı tıklanınca `targetUrl` kullanılır. Card/comment sonucu `?card=<cardId>` ile kart modalını açar; board/list/label sonucu board sayfasına gider. URL hesaplaması backend response'ta deterministiktir; UI route bilgisiyle yeniden icat etmez.
- **Board içi arama/filtresi:** Board topbar'daki mevcut `Search` aksiyonu gerçek aramaya dönüşür. Board scope query ile server-side sonuç alınır; kart/list görünürlüğü local filtreyle değil, sonuç listesi/drawer ile gösterilir. Board ekranındaki mevcut label filter ayrı kalır; search query onu bozmaz.
- **A11y:** Dialog açıldığında input focus alır; `Escape` kapatır; ok tuşları sonuçlar arasında gezer; sonuç satırları `role="option"` veya uygun button/link semantiği taşır. Empty/loading durumları `role="status"` ile duyurulur.
- **Test (Faz 6.5D + 6.5E):** RTL: keyboard shortcut açar, 2 karakter altı API çağırmaz, debounce sonrası sonuç render eder, empty/error state, sonuç tıklayınca `router.push(targetUrl)`. Playwright: iki workspace/board fixture ile permission kapsamı, kart sonucundan `?card=` modal açılışı, board içi arama. DEM-108 ile `e2e/search.spec.ts` bu üç tarayıcı yolunu sabit seed üzerinden doğrular.

> **Klavye kısayolları — DEM-73 (2026-05-15):** Küçük shortcut katmanı `apps/web/src/lib/shortcuts` altında normalize edilir ve aktif input/contenteditable hedeflerinde çalışmaz. Global arama `Cmd/Ctrl+K` ve `Ctrl+Space` ile aynı dialog'u açar. Board ekranı kart modalı açık değilken `/` ile board-scoped arama, `N` ile ilk aktif listeye kart ekleme, `Shift+N` veya `L` ile liste ekleme ve `?` ile kısayol yardım dialog'unu destekler. Kart modalı kendi scope'unda `E` başlık düzenleme, `C` tamamlandı toggle, `D` arşiv, `M` üye menüsü, `T` son tarih menüsü, `A` ek yükleme ve `?` yardım davranışlarını taşır. Aktif kart/liste seçimi ve ok tuşlarıyla board navigasyonu ayrı follow-up kapsamıdır.

> **Kapsam dışı:** Mobil arama UI (Faz 7+), Meilisearch instant-search, gelişmiş facet paneli, fuzzy typo tolerance, attachment içerik araması.

### 8.1.14 Kart eki — "Ekler" sekmesi (Faz 11D — [DEM-150](https://linear.app/demirkol/issue/DEM-150) — ✅ wired)

> **Wired (DEM-150, 2026-05-15):** Bu bölüm implement edildi. `@pusula/ui` `Dropzone` + `AttachmentTile` + `AttachmentPreviewDialog` bileşenleri; `card-detail-attachments.tsx` (gerçek konum: `apps/web/src/app/(app)/workspaces/[id]/boards/[boardId]/_components/card-detail/`); `card-meta-row.tsx` paperclip chip; `card-detail-cover-color.tsx` "Kapak görseli" sekmesi; realtime `attachment.added/removed` handler'ı; `strings.attachment.*`. `uploadWithProgress` saf helper `apps/web/src/lib/upload-with-progress.ts`'e ayrıldı (iptal edilebilir XHR). Doğrulama: `@pusula/web` 715 test + typecheck + lint yeşil; `@pusula/ui` typecheck + lint yeşil.

Kart detay modalı sağ panelinde mevcut `Tabs` (Yorumlar / Aktivite / Ekler / Tümü — `13-ui-tasarim-dili.md` §13.3) "Ekler" sekmesi Faz 2.7C-2 (DEM-74) sonrası **placeholder**'dı; Faz 11D bu sekmeye gerçek attachment yönetimini bağlar. UI tasarım dili → [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) §13.10; iş kuralları → [`../domain/07-ek-kurallari.md`](../domain/07-ek-kurallari.md); akış → [`09-depolama-ve-arama.md`](09-depolama-ve-arama.md) §9.1.

- **Yer:** `apps/web/src/app/(app)/_components/card-detail/card-detail-attachments.tsx` (yeni) — `CardModalSidebar` Tabs'in "Ekler" panelinde mount edilir; "Tümü" sekmesinde de ek satırları kronolojik karışıma girer (yorum + activity + ek). Sekme başlığı sayaç `attachment.list` count'unu yansıtır (`Tabs trigger` "Ekler N").
- **Üst alan — Dropzone / yükle:** `@pusula/ui` yeni `Dropzone` bileşeni (§13.10 — `<input type="file" hidden>` + drag-drop event handler'ları + keyboard `Enter`/`Space` aktivasyon). Drag-over `border-2 border-dashed border-primary/40` highlight. Tıklayınca native file picker; `accept` attribute `ATTACHMENT_MIME_TYPES` listesinden. Yetki check: board admin/member → dropzone aktif; viewer → "Yalnızca okuyabilirsiniz" tooltip ile disabled. Tek dosya / tek upload (çoklu eşzamanlı V1 dışı).
- **Upload akışı (two-phase) — UI state:** kullanıcı dosya seçer → opsiyonel açıklama input alanı (Textarea, placeholder "Açıklama (opsiyonel, ≤500 karakter)", `text-xs text-muted-foreground` "x/500" sayaç) → "Yükle" butonu (primary, disabled while uploading) → (1) `trpc.attachment.initiate` mutation (`useMutation`; `cardId/fileName/mimeType/size/description?`); response `{ attachmentId, upload: { url, headers } }`. (2) `fetch(upload.url, { method: 'PUT', body: file, headers: upload.headers })` — `XMLHttpRequest` onprogress ile `@pusula/ui` `Progress` bar (`bg-primary` fill). (3) Başarılıysa `trpc.attachment.commit` mutation (`attachmentId`, `clientMutationId`); response `{ attachment }` → optimistic `attachment.list` cache append (eksiksiz `useOptimisticBoardMutation` paterni değil — daha basit `queryClient.setQueryData` + invalidate). (4) Başarısızsa rollback (form sıfırlanır), Sonner toast `strings.attachment.error.uploadFailed`. Açıklama uygulanamadıysa (commit `BAD_REQUEST 500/500+ char`) form'da inline hata.
- **Liste — `AttachmentTile`:** `attachment.list` sonucundan kronolojik (yeni üstte) `flex flex-col gap-2`. Her tile (`@pusula/ui` `AttachmentTile`, §13.10): sol 56×56 thumbnail/ikon (image → mini önizleme `<img>` `object-cover rounded-md`; pdf → `FileTextIcon` text-destructive bg-destructive/10 rounded-md; office → mimeType'a göre `FileTextIcon` mavi/yeşil/turuncu — docx/xlsx/pptx) → orta: `fileName` (truncate), `text-xs text-muted-foreground` boyut + uploader name + tarih + (varsa) açıklama (italik) → sağ aksiyon ikonları (`size-7 ghost`): "Önizle/Aç" (`EyeIcon`), "İndir" (`DownloadIcon`), "Düzenle açıklama" (`Edit3Icon`; uploader veya admin), "Kapak yap/Kapağı kaldır" (`ImageIcon`; image kind + uploader/member+), `MoreHorizontalIcon` DropdownMenu (Sil — uploader veya admin; viewer item disabled). Tile boş listede `EmptyState` "Henüz ek yok."
- **Önizleme dialog'u — `AttachmentPreviewDialog`:** `@pusula/ui` `Dialog` (max-w-4xl) — image kind: `<img>` `max-h-[80vh] object-contain` + zoom toggle (`+`/`-` butonları, `scale-150` ↔ `scale-100`); pdf kind: `<iframe src={presignedGetUrl} sandbox="allow-same-origin" className="h-[80vh] w-full">`; office kind: dialog'a girilmez — "İndir" doğrudan tetiklenir. URL `trpc.attachment.getDownloadUrl({ attachmentId })` ile lazy alınır (TTL 10 dk; dialog kapanınca state temizlenir).
- **Açıklama inline edit:** "Düzenle" butonuna tıklanınca tile genişler; Textarea + "Kaydet" + "İptal" → `trpc.attachment.update({ attachmentId, description, clientMutationId })`. Boş değer → `description = null` (silme).
- **Silme:** DropdownMenu → "Sil" → `AlertDialog` onay ("Bu eki kalıcı olarak sil?" + dosya adı) → `trpc.attachment.delete({ attachmentId, clientMutationId })` → cache invalidate + Sonner toast. Storage temizliği worker'da; UI satırı hemen kaybolur.
- **Realtime:** `useBoardRealtime` hook `attachment.added`/`attachment.removed` event'lerini dinler → `attachment.list` query invalidate (`{ cardId: event.payload.cardId }`). Başka bir kullanıcının eklediği/sildiği attachment listede canlı görünür. Echo ayıklama: client kendi `clientMutationId`'sini görürse skip.
- **Board kart yüzü meta chip:** `card-meta-row.tsx` (Faz 2.7B'de "Faz 8" notuyla bırakılmıştı) — eğer `attachmentCount > 0` ise `PaperclipIcon` + sayı (`MetaChip` paterni; `text-[10px]` `gap-1`). `trpc.board.get` response'unda her kart için `attachmentCount` alanı eklenir (Faz 11B/10D additive — toplu sorgu, N+1 yok).
- **Cover-image picker (DEM-110 entegrasyon):** Kart detay modalında mevcut "Kapak rengi" picker'ı (`13-ui-tasarim-dili.md` §13.3) Faz 11D'de "Kapak görseli" sekmesiyle genişler: 12-renk + (`attachment.list` filtreli image satırları) image swatch'ları. Image seçilince `card.update({ coverImageAttachmentId })` çağrılır; "Kapağı kaldır" → `coverImageAttachmentId: null` + `coverColor: null`. Hangi attachment kapak ise `AttachmentTile` "Kapak" rozeti taşır.
- **i18n — `apps/web/src/lib/strings.ts`:** `strings.attachment.*` (entity-bağımsız Türkçe metinler — hardcode yok): `tabs.attachments` ("Ekler"), `dropzone.idle`/`active`/`error`, `progress.label(n)`, `description.placeholder`, `description.counter(used,max)`, `actions.download`/`preview`/`edit`/`delete`/`makeCover`/`removeCover`, `confirmDelete.title`/`description`, `error.uploadFailed`/`commitFailed`/`tooLarge`/`mimeRejected`, `empty.title`/`description`, `cover.badge` ("Kapak").
- **A11y:** Dropzone `role="button"` + keyboard activate; `aria-label` "Dosya yükle"; upload progress `role="progressbar"` `aria-valuenow`; tile aksiyonları `Tooltip` ile etiket; image preview dialog'ta `role="dialog"` + `aria-label={fileName}`; pdf iframe `title={fileName}`.
- **Test:** RTL — dropzone keyboard activate + dosya seç → initiate → upload → commit happy path (mock fetch + mock trpc); MIME reject + size reject branch'ları; açıklama inline edit; silme onay flow'u; cover picker image seçimi `card.update` çağırır. Playwright e2e (10E): admin upload + indir + sil; viewer upload disabled + indir OK; uploader silebilir + non-uploader/non-admin silemez; iki kullanıcı realtime "Ekler N" rozeti senkron.

> **Kapsam dışı (Faz 11D):** Drag-drop kart üstüne dosya bırakma (sadece dropzone), çoklu eşzamanlı upload, image thumbnail server-side generation, EXIF temizleme, antivirus tarama, mobil "Ekler" sekmesi (Faz 7+), misafir attachment görüntüleme (Faz 9 paylaşım linkinde gizli — faz planı satır 187 uyumu).

---

## 8.2 Mobil (`apps/mobile`)

Expo + Expo Router tabanlı mobil uygulama. **Faz 7 ([DEM-30](https://linear.app/demirkol/issue/DEM-30)) kapsamında inşa edilir** — Faz 7 öncesi `apps/mobile` **oluşturulmaz**. Faz 7 tek epic altında 16 alt işe bölündü (`faz-bol 7`, 2026-05-16); alt iş listesi ve bağımlılık zinciri → [`../process/02-mvp-faz-plani.md`](../process/02-mvp-faz-plani.md) "Faz 7 alt işleri". Bu bölüm 7.0 önce-belge ([DEM-176](https://linear.app/demirkol/issue/DEM-176)) ile sabitlenen mobil mimari kararlarını taşır.

### Hedef ve kapsam

Mobil MVP **store yayını dahil** (App Store + Google Play). Kapsam: auth + oturum · workspace/board listesi · board görüntüleme · kart detay (tam etkileşim) · kart create/update + "move to list" · arama · kart eki · notification center + bildirim ayarları · push + deep link. Web'in Faz 6.5 (arama), Faz 10 (bildirim ayarları), Faz 11 (kart eki) ve workspace/board üye yönetimi & davet kabul yetenekleri mobil MVP'ye **dahildir**.

### Faz 7.0 mimari kararları (2026-05-17)

| Konu              | Karar                                            | Not                                                                                                                                                                                          |
| ----------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| UI yaklaşımı      | **NativeWind** (Tailwind for RN)                 | Web Tailwind v4 ile aynı zihniyet; [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) design token'ları (renk/spacing/radius) RN tarafına taşınır. `@pusula/ui` shadcn web bileşenleri mobilde **kullanılmaz** — mobil kendi bileşenlerini NativeWind ile kurar. |
| Tiptap içerik     | **Zengin render + düz-metin düzenleme**          | Kart açıklaması/yorum Tiptap JSON saklanır; mobilde JSON'u React Native bileşen ağacına render eden bir katman gerekir (7F). Düzenleme/yeni yorum düz metin — tam mobil rich editör yok (7G).  |
| Realtime          | **Pull-to-refresh + push** (mobil Socket.IO yok) | Board'a giriş/yenilemede fetch; değişiklikleri push bildirimi haber verir. Mobilde socket background/reconnect/pil maliyeti taşımaz. Web Socket.IO katmanı değişmez.                            |
| Offline           | **Cache persistence — okuma offline**            | TanStack Query persist; offline'da son görülen board/kart görünür. Mutation offline kuyruğu **yok** — değişiklik bağlantı gerektirir. Bağlantı yoksa "bağlantı yok" banner'ı (7M).             |
| Test              | **Maestro (e2e) + Vitest birim**                 | Maestro YAML tabanlı e2e (kritik akışlar); domain/component birim testleri Vitest + RN Testing Library (7N).                                                                                  |
| Tema              | **Dark/light**                                   | Web 2.7 tema desteğiyle simetrik; `13-ui-tasarim-dili.md` token'ları üzerinden.                                                                                                               |
| Hata izleme       | **Sentry mobil** (ayrı DSN)                      | web/api/worker Sentry kurulumuyla simetrik; crash reporting (7A).                                                                                                                             |
| Dağıtım           | **EAS Build + EAS Update (OTA)**                 | Production build iOS + Android; OTA güncelleme store review'a takılmadan hızlı düzeltme sağlar (7O).                                                                                           |

### Mobil teknoloji yığını

Expo · Expo Router · NativeWind · React Native Reanimated · Gesture Handler · Expo Notifications · `expo-secure-store` · tRPC client (paylaşılan `packages/api` sözleşmesi) · TanStack Query (+ persist) · Sentry · Maestro. `EXPO_PUBLIC_*` env Zod ile doğrulanır (`apps/mobile/src/env.ts`).

### Drag-drop yerine "move to list"

Mobil drag-drop **kapsam dışı** — kart taşıma kart üzerinde/detayında "move to list" picker ile yapılır (7H). Collaborative mutation disiplini web ile aynı: optimistic UI, rollback, `clientMutationId`. Gerekirse ileride basit long-press reorder ayrı iş.

### Alt iş grupları

4 mantıksal grup: **Temel** (7A-7E: scaffold, auth, navigasyon, üye yönetimi, board görüntüleme) · **Kart** (7F-7J: kart detay, etkileşim, create/move, arama, ek) · **Bildirim** (7K-7L: notification center + ayarlar, push + deep link) · **Yayın** (7M-7O: realtime/offline, test, store yayını). Push dilimi (7K/7L) Faz 6 bildirim altyapısına bağlıdır — Faz 6 kapanana dek `Blocked`.

### Dikkat noktaları

Push token yönetimi · foreground/background notification davranışı · deep link + universal links (associated domains / intent filters) · auth session refresh + `expo-secure-store` · cache persistence + bağlantı durumu banner'ı. Auth tarafı → [`07-auth.md`](07-auth.md) (Mobil oturum); push tarafı → [`06-bildirim-altyapisi.md`](06-bildirim-altyapisi.md) (Push kanalı).

### Faz 7A — `apps/mobile` iskeleti (Wired)

Faz 7A ([DEM-177](https://linear.app/demirkol/issue/DEM-177)) `apps/mobile`'ı oluşturdu — iskelet + altyapı; auth (7B), navigasyon (7C) ve ekranlar ayrı alt işlerde gelir. Kurulan katmanlar:

- **Expo SDK 54 + Expo Router 6** scaffold, TypeScript strict (`@pusula/config/tsconfig` tabanlı). Turborepo pipeline'a `build` (`expo export` — JS bundle) · `lint` · `typecheck` · `test` task'larıyla bağlandı; native iOS/Android build EAS'te.
- **NativeWind 4** + `tailwind.config.js`; [`13-ui-tasarim-dili.md`](13-ui-tasarim-dili.md) renk/spacing/radius token'ları `src/theme/tokens.ts` + Tailwind teması olarak RN tarafına taşındı (light/dark). `@pusula/ui` shadcn web bileşenleri mobilde kullanılmaz.
- **Paylaşılan tRPC client** — `@pusula/api` `AppRouter` sözleşmesi; `@trpc/client` + `@trpc/tanstack-react-query` + `superjson`. TanStack Query + `@tanstack/react-query-persist-client` & AsyncStorage ile okuma-offline cache persistence (7.0 kararı).
- **`EXPO_PUBLIC_*` env** — `src/env.ts` Zod doğrulaması (`EXPO_PUBLIC_API_URL` zorunlu varsayılanlı, `EXPO_PUBLIC_SENTRY_DSN` opsiyonel). Dev'de gerçek API host'u `src/lib/api-url.ts` tarafından Expo Metro `hostUri`'sinden türetilir — cihaz Metro'ya hangi adresten bağlandıysa API isteği de oraya gider; emulator (`10.0.2.2` fallback) ve fiziksel cihaz tek `.env` değeriyle çalışır, ağ değişiminde elle düzenleme gerekmez. Üretim build'inde (`__DEV__` false) `EXPO_PUBLIC_API_URL` (https) doğrudan kullanılır.
- **EAS** — `eas.json` development / preview / production build profilleri (`app.config.ts` dinamik Expo yapılandırması).
- **Sentry mobil** — `@sentry/react-native`, ayrı DSN (`EXPO_PUBLIC_SENTRY_DSN`); DSN boşsa `Sentry.init` no-op (web/api/worker simetrisi).
- **Branding** — app ikonu / adaptive icon / splash + uygulama içi marka token'ları (`apps/mobile/assets`); UI metinleri `src/lib/strings.ts` katmanında (iskelet — hardcode metin yok).

### Faz 7B — Auth: Better Auth Expo + session (Wired)

Faz 7B ([DEM-178](https://linear.app/demirkol/issue/DEM-178)) mobil kimlik doğrulama + oturum yönetimini kurdu. Yalnız auth + korumalı kabuk iskeleti — navigasyon ağacı / workspace-board listesi 7C'nin işi. Auth altyapısı → [`07-auth.md`](07-auth.md) (Mobil oturum).

- **Better Auth Expo client** — `apps/mobile/src/lib/auth-client.ts`: `createAuthClient` + `@better-auth/expo` `expoClient` plugin (`scheme: 'pusula'`, `storage: expo-secure-store`). Oturum cookie'si SecureStore'da; `authClient.getCookie()` tRPC `httpBatchLink`'ine `Cookie` başlığı olur (`src/trpc/provider.tsx`). Server tarafı `apps/api/src/auth.ts`'e `expo()` plugin + `pusula://` trustedOrigin eklendi (Expo entegrasyonu zorunlu kılar).
- **Route grupları** — Expo Router: `app/(auth)/*` (public — sign-in / sign-up / forgot-password / reset-password) ve `app/(app)/*` (korumalı). `(app)/_layout.tsx` oturum yoksa `(auth)/sign-in`'e, `(auth)/_layout.tsx` oturum varsa `(app)`'e `<Redirect>` eder — web `(app)`/`(auth)` layout simetrisi. Oturum çözülürken tam ekran `LoadingScreen` — web ile aynı "compass" Lottie animasyonunu kullanan `AppSpinner` (`src/components/app-spinner.tsx`, `lottie-react-native`); mobil yükleniyor göstergeleri RN `ActivityIndicator` yerine bu bileşeni kullanır (yalnız ikon / `showLabel` ile ikon + metin varyasyonları).
- **Ekranlar** — sign-in / sign-up / sign-out, forgot-password, reset-password. Formlar `@pusula/domain` zod şemalarıyla (`signInInput` / `signUpInput` / `forgotPasswordInput` / `resetPasswordInput`) client-side doğrulanır — web ile aynı sözleşme. NativeWind bileşenleri (`Button`, `TextField`, `FormMessage`) — `@pusula/ui` shadcn kullanılmaz. Metinler `strings.auth.*`.
- **Signup bootstrap** — yeni kullanıcının default workspace + "İlk Pano"sı sunucu tarafında `databaseHooks.user.create.after` ile oluşur; mobil signup ayrı bootstrap kodu yazmaz, yalnız `authClient.signUp.email` ile tetikler.
- **Şifre sıfırlama** — `forgot-password` `requestPasswordReset({ email, redirectTo })` çağırır (`redirectTo` = `expo-linking` derin bağlantısı); e-postadaki link mobil `reset-password` ekranını `?token=` ile açar. E-posta var/yok ayırt edilmez (web simetrisi).
- **Kapsam dışı** — workspace/board listesi + navigasyon ağacı (7C), push deep-link (7L/7M), evrensel bağlantı (universal links) hardening.

### Faz 7C — Navigasyon + workspace/board listesi (Wired)

Faz 7C ([DEM-179](https://linear.app/demirkol/issue/DEM-179)) mobil navigasyon kabuğunu + workspace/board listeleme ekranlarını kurdu.

- **App-shell = alt tab bar (4 sekme)** — korumalı `app/(app)/_layout.tsx` Expo Router `<Tabs>`: **Panolar / Arama / Bildirimler / Hesap** (kullanıcı kararı, 2026-05-17). 7C'de Panolar + Hesap dolu; Arama (7I) ve Bildirimler (7K) "yakında" placeholder ekranıyla gelir — app-shell baştan tam. Oturum kapısı (`<Redirect>`) `(app)/_layout`'ta korunur.
- **Panolar sekmesi** = `app/(app)/(boards)/` route grubu, kendi `<Stack>`'i: `index` (workspace listesi) → `workspaces/[id]` (o workspace'in board listesi) drill-down. Web'in iki-sütun anasayfası mobilde dikey drill-down olur.
- **Workspace listesi** — `trpc.workspace.list` (üye olunan workspace'ler + `boardCount`/`memberCount`/rol). 0 workspace → **onboarding empty state** (web §8.1.3 simetrisi — Pusula/workspace kavramını anlatan bilgilendirme; signup bootstrap sayesinde nadir). Workspace/board **oluşturma** mutation'ları 7C kapsamı dışı (sonraki iş).
- **Board listesi** — `trpc.board.list({ workspaceId })` (board + kapak/sayaç/üye/favori meta). 7C'de board satırına dokunmak "yakında" bilgilendirmesi gösterir; gerçek board ekranı (kolon/kart) + navigasyon bağlantısı Faz 7E.
- **Hesap sekmesi** — 7B'nin geçici giriş-sonrası ekranı (kullanıcı bilgisi + çıkış) buraya taşındı.
- **İkonlar** — `@expo/vector-icons` `Feather` (web `lucide-react` ile görsel dil tutarlı; Expo'yla gelir, ek dep yok); `Icon` sarmalayıcı bileşeninden geçer.

### Faz 7E — Board görüntüleme, read-only (Wired)

Faz 7E ([DEM-181](https://linear.app/demirkol/issue/DEM-181)) mobil board ekranını kurdu — salt-okunur.

- **Board ekranı** — `app/(app)/(boards)/boards/[boardId].tsx`; `trpc.board.get({ boardId })` tek seferde board + listeler + kartları döndürür. Listeler yatay `ScrollView` kolonları; her kolon kendi kartlarını dikey listeler (`position` sıralı, web `board.get` sözleşmesiyle aynı). Aktif listeler gösterilir.
- **Kart yüzü** (`CardFace`) — başlık + etiket chip'leri (renkli) + meta satırı: due tarihi (gecikmişse vurgulu), üye baş harfleri, checklist `✓ tamam/toplam`, yorum ve ek sayısı chip'leri. Tamamlanan kart işaretli. Salt-okunur — kart detayına dokunma Faz 7F.
- **Navigasyon** — 7C'de "yakında" Alert gösteren board satırı (`(boards)/workspaces/[id]`) artık board ekranına `router.push` eder.
- **Kapsam dışı** — mobil drag-drop (kart taşıma "move to list" picker ile, 7H); kart detayı (7F); etiket filtresi (7.0'da "opsiyonel" — ertelendi, 7E-2'de eklendi); kapak görseli render'ı (presigned URL — ileri iş).

#### Faz 7E-2 — Board etiket filtresi ([DEM-200](https://linear.app/demirkol/issue/DEM-200)) (Wired)

7E board ekranını salt-okunur kurdu ama etiket filtresini 7.0 kararıyla "opsiyonel" bırakıp erteledi; web board ekranında çoklu etiket filtresi (DEM-54) zaten var. 7E-2 bunun mobil karşılığını ekler. Yeni backend/procedure yok — `board.get` Faz 2.7'den (DEM-63) beri her kart için `labels: { labelId, name, color }[]` taşır (§7E "additive alanı"), filtre tamamen istemci tarafıdır.

- **Filtre giriş noktası** — board header'ına (arama + üye ikonlarının yanına) üçüncü bir `filter` ikonu eklenir. Etiket seçiliyken ikon `primary` tonuna döner ve seçili etiket sayısını taşıyan küçük bir rozet gösterir — filtrenin etkin olduğu board'a bakınca görülür.
- **`LabelFilterSheet`** (`src/components/label-filter-sheet.tsx`) — ikona dokununca mevcut `Sheet` (RN `Modal` bottom sheet) ile açılır; board etiketleri (`label.list`) çoklu-seçilebilir renkli chip'ler olarak listelenir, seçili chip işaretli. "Tümünü temizle" aksiyonu seçimi sıfırlar. Sheet yalnız açıkken mount edilir → `label.list` sorgusu kapalı board'da çalışmaz.
- **İstemci tarafı filtre, OR semantiği** — `src/lib/board-filter.ts` saf helper'ları (`cardPassesLabelFilter` / `filterCardsByLabels`) web `board-filter.ts` etiket helper'larının mobil karşılığıdır: hiç etiket seçili değilse her kart geçer; aksi halde kart seçili etiketlerden **en az birini** taşıyorsa geçer. Saf modül — birim test edilir (`__tests__/board-filter.test.ts`).
- **Kolon davranışı** — filtre kartları daraltır, listeleri değil: tüm kolonlar render edilmeye devam eder; bir kolonun tüm kartları elenirse mevcut "Kart yok" boş durumu görünür. "Liste ekle" kolonu filtreden etkilenmez.
- **Geçici state** — seçili etiketler board ekranında `useState` ile tutulur; ekrandan ayrılınca sıfırlanır (web istemci-tarafı filtre simetrisi — kalıcı değil). Due-date / üye / arşivli-liste filtreleri kapsam dışı (web filter bar'ının yalnız etiket parçası — DEM-200 hedefi).

### Faz 7F — Kart detay görüntüleme + Tiptap zengin render (Wired)

Faz 7F ([DEM-182](https://linear.app/demirkol/issue/DEM-182)) mobil kart detay ekranını kurdu — salt-okunur. 7E'de dokunulamaz olan board kartı artık kart detayını açar.

- **Kart detay ekranı** — `app/(app)/(boards)/cards/[cardId].tsx`; tek ekranda altı sorgu paralel: `card.get` (başlık, açıklama Tiptap JSON, due, completed) + `card.labels.list` + `card.members.list` + `checklist.list` + `comment.list` + `card.activity.list`.
- **Tiptap JSON render katmanı** (`TiptapRender`) — 7.0 kararı: kart açıklaması ve yorum gövdeleri Tiptap JSON saklanır; bu bileşen JSON ağacını RN bileşenlerine çevirir (paragraf, başlık, madde/numaralı liste, blockquote, kod, `bold`/`italic`/`strike`/`code` mark'ları, `mention`, `hardBreak`). Açıklama + her yorum gövdesi biçimli gösterilir.
- **Bölümler** — checklist (başlık + ilerleme + maddeler, tamamlanan işaretli), etiket, due tarihi, üyeler, **yorum listesi** (yazar + Tiptap gövde + zaman), **aktivite** okuma görünümü (`activity-summary` ile tip→Türkçe etiket). Yorum yazarı adı `card.activity.list` aktör adlarından çözümlenir (`comment.list` yalnız `authorId` döndürür).
- **Kapsam dışı** — düzenleme / yorum yazma / checklist toggle (read-only — sonraki fazlar); tam mobil rich editör yok (7.0: zengin render + düz-metin düzenleme, düzenleme ileride).

### Faz 7G — Kart detay tam etkileşim (Wired)

Faz 7G ([DEM-183](https://linear.app/demirkol/issue/DEM-183)) 7F'in salt-okunur kart detayını düzenleme aksiyonlarıyla genişletti. Mevcut tRPC procedure'leri tüketilir — yeni backend procedure yok.

- **Bölüm editörleri** — `src/components/card-detail/` altında: `description-editor` (açıklama düz-metin düzenleme), `labels-editor` (etiket ekle/çıkar), `due-date-editor` (son tarih ayarla/temizle), `members-editor` (kart üyesi ekle/çıkar), interaktif `checklist-section` (kontrol listesi oluştur/sil [onaylı] + madde işaretle/ekle/sil — listesi olmayan kart düzenleyene doğrudan "kontrol listesi ekle" girişi gösterir; DEM-198), `comment-composer` (yorum yazma). Her biri ilgili bölümün `DetailSection`'ı içinde render edilir.
- **Collaborative mutation disiplini** — tüm yazmalar optimistic UI: `onMutate` ilgili sorgu cache'ini (`card.get` / `card.labels.list` / `card.members.list` / `checklist.list` / `comment.list`) anında yamar, `onError` snapshot'tan geri alır, `onSettled` invalidate eder (+ `card.activity.list`). Her mutation `clientMutationId` taşır (`expo-crypto` UUID). Tüketilen procedure'ler: `card.update` (açıklama/due), `card.labels.add/remove`, `card.members.add/remove`, `checklist.create/delete`, `checklist.item.toggle/create/delete`, `comment.create`/`comment.update`/`comment.delete`.
- **Düz-metin Tiptap yazımı** (7.0 kararı — tam mobil rich editör yok) — `src/lib/tiptap.ts` `serializeTiptapDoc` (düz metin → Tiptap `doc` JSON string, satır başına paragraf) + `tiptapToPlainText` (saklanan değer → editör tohum metni). Açıklama düzenleme web `isSameRichText` simetrisiyle anlamca değişiklik yoksa mutation atmaz. Yeni yorum gövdesi de Tiptap JSON'a serialize edilir.
- **Son tarih hazır-ayarları** — "Bugün / Yarın / Hafta sonu / Gelecek hafta" hazır-ayar çipleri (saat 18:00) + "Son tarihi kaldır". 7G'de tam takvim seçici yoktu; belirli bir tarih seçimi 7G-3'te eklendi (DEM-197).
- **Düzenleme yetkisi** — editörler yalnız board `member+` için aktif; çağıranın board rolü `board.members.list`'ten çözümlenir (web kart modalı simetrisi — çözülene dek `viewer` varsayılır, salt-okunur). `board.members.list` ayrıca kart üyesi aday havuzudur.
- **Kapsam dışı** — `@`-mention besteleme; tam mobil rich editör (7.0 kararı). _(7G'de kapsam dışı olan kontrol listesi oluştur/sil DEM-198 ile, yorum düzenle/sil DEM-199 ile eklendi — alt başlıklara bkz.)_

#### Faz 7G-2 — Kart detay meta çubuğu (bottom sheet rafinasyonu, [DEM-194](https://linear.app/demirkol/issue/DEM-194)) (Wired)

7G'nin etiket / son tarih / üye düzenleyicileri kart detayında üç ayrı tam-genişlik `DetailSection`'dı; her birinin satır-içi "ekle" toggle'ı açılınca ekranı uzatıyordu. 7G-2 bunları web kart modalı `CardModalMetaChips` (§13.3) deseninin mobil karşılığına geçirir — yeni backend/procedure yok, mevcut tRPC sözleşmesi tüketilmeye devam eder; 7G'nin collaborative mutation disiplini (optimistic + rollback + `clientMutationId`) aynen korunur.

- **Meta çubuğu** (`src/components/card-detail/meta-bar.tsx` — `CardMetaBar`) — kart başlığının altında kompakt, sarmalanan (`flex-wrap`) chip satırı. Dört chip kartın durumunu **özet** gösterir: **Üyeler** (avatar yığını + sayı), **Son tarih** (kısa tarih; gecikmişse `destructive` ton), **Etiketler** (renk noktaları + sayı), **Liste** (kartın bulunduğu listenin adı). Boş alan placeholder metniyle ("Üye", "Son tarih", "Etiket") yine dokunulabilir.
- **Bottom sheet düzenleme** — bir chip'e dokununca ilgili düzenleme mevcut `Sheet` bileşeniyle (Modal tabanlı bottom sheet — §8.2 7H'de tanımlı) alttan açılır; kullanıcı kart detayından ayrılmaz. 7G editörleri sheet gövdesine dönüştürüldü: `labels-sheet` / `due-date-sheet` / `members-sheet` (`DetailSection` sarmalayıcısı + "ekle" toggle'ı kaldırıldı — sheet zaten düzenleme yüzeyi, picker doğrudan görünür). Liste chip'i mevcut `MoveToListSheet`'i açar — ayrı "Listeyi değiştir" butonu kaldırıldı; kartın hangi listede olduğu artık sürekli görünür. Sheet gövdeleri yalnız sheet açıkken mount edilir (board etiket/üye aday sorguları gereksiz çalışmaz).
- **Yeni native bağımlılık yok** — `react-native-reanimated` / `@gorhom/bottom-sheet` eklenmez; 7H'den beri kullanılan `Sheet` (RN `Modal` + NativeWind) yeterli.
- **Salt-okunur** — `canEdit=false` (board `viewer`) durumunda chip'ler yine dokunulabilir ve sheet'ler salt-okunur açılır (üye/etiket listesi görünür, mutation kontrolleri yok); liste chip'i viewer için dokunulamaz (yalnız ad gösterir). Açıklama / kontrol listeleri / ekler / yorumlar / aktivite bölümleri 7G'deki gibi `DetailSection` olarak kalır (içerik, metadata değil).

#### Faz 7G-3 — Son tarih tam takvim seçici ([DEM-197](https://linear.app/demirkol/issue/DEM-197)) (Wired)

7G/7G-2 son tarih sheet'i yalnız hazır-ayar (Bugün / Yarın / Hafta sonu / Gelecek hafta) sunuyordu; belirli bir tarih (örn. üç hafta sonrası) seçilemiyordu. 7G-3 hazır-ayar çiplerinin yanına gerçek bir tarih seçimi ekler. Yeni backend/procedure yok — `card.update` (`dueAt`) tüketilmeye devam eder; 7G'nin collaborative mutation disiplini korunur.

- **Bağımlılıksız ay-grid takvim** (`src/components/card-detail/month-calendar.tsx` — `MonthCalendar`) — Pazartesi-başlangıçlı tek ay ızgarası; başlıkta ‹ ay-yıl › navigasyonu, bugün ve seçili gün vurgulu. Yeni native bağımlılık **yok** (`@react-native-community/datetimepicker` eklenmez) — 7G-2 §"Yeni native bağımlılık yok" / 7K presedanı; Expo Go uyumlu, NativeWind ile design token'lara hizalı. Türkçe ay/gün adları `format-date.ts` desenindeki gibi elle çevrilir (deterministik, Hermes/Intl bağımsız).
- **Saat varsayılanı** — takvimden seçilen tarih de hazır-ayarlar gibi `DUE_PRESET_HOUR` (18:00) saatine sabitlenir; mobilde saat seçimi kapsam dışı.
- **Mutation disiplini** — takvim seçimi de mevcut `card.update` optimistic akışını (snapshot + rollback + `clientMutationId`) kullanır; hazır-ayar yolundan ayrı bir kod yolu yoktur.
- **Salt-okunur** — `canEdit=false` (board `viewer`) durumunda takvim render edilmez (hazır-ayar çipleri ve "kaldır" gibi yalnız `canEdit` ise mount edilir).

#### Faz 7G-4 — Yorum düzenleme + silme ([DEM-199](https://linear.app/demirkol/issue/DEM-199)) (Wired)

7G yorum **yazmayı** ekledi ama düzenleme/silme kapsam dışıydı (web kart modalında ikisi de var). 7G-4 `comment-list.tsx`'i salt-okunur listeden satır-içi düzenleme + onaylı silmeye genişletir. Yeni backend/procedure yok — mevcut `comment.update` / `comment.delete` tüketilir; 7G'nin collaborative mutation disiplini korunur.

- **Yetki** — kullanıcı kendi yorumunu (`authorId === currentUserId`) düzenler + siler; board `admin` başkasının yorumunu da düzenler/siler (web `card-detail-comments.tsx` simetrisi; backend her iki procedure'de yine doğrular). Düzenle/Sil aksiyonları yalnız board `member+` (`canEdit`) ve yorum silinmemişse görünür.
- **Düz-metin düzenleme** — satır-içi `TextArea`; tohum metni saklanan gövdeden `tiptapToPlainText` ile çıkarılır, kaydetme `serializeTiptapDoc` ile Tiptap JSON'a serialize eder (7G deseni — tam mobil rich editör yok). Anlamca değişiklik yoksa mutation atılmaz (web `isSameRichText` simetrisi); boş gövdeyle kaydetme engellenir.
- **Silme** — `Alert.alert` onayı (ek silme deseniyle simetrik); soft-delete sonrası satır mevcut `deletedComment` ("Bu yorum silindi.") yer tutucusunu gösterir, aksiyonları gizlenir.
- **Mutation disiplini** — `comment.update` / `comment.delete` optimistic: `onMutate` `comment.list` cache'ini yamar (düzenleme `body` + `editedAt`, silme `deletedAt` + boş `body`), `onError` snapshot'tan geri alır, `onSettled` `comment.list` + `card.activity.list` invalidate eder; her mutation `clientMutationId` taşır.

#### Faz 7G-5 — Kartı tamamla / geri al toggle ([DEM-195](https://linear.app/demirkol/issue/DEM-195)) (Wired)

7G kart detayı yalnız "Tamamlandı" rozetini gösteriyordu — dokunulamazdı; `card.complete`/`card.uncomplete` mobilde hiç çağrılmıyordu (kullanıcı mobilde kartı bitiremiyordu). 7G-5 kart detay başlık satırına web `CardCompleteToggle` simetrisinde bir tamamla/geri al toggle'ı ekler. Yeni backend/procedure yok — mevcut `card.complete` / `card.uncomplete` tüketilir.

- **Toggle** (`src/components/card-detail/complete-toggle.tsx` — `CardCompleteToggle`) — başlık satırında, başlık metninin solunda: tamamlanmış kart dolu yeşil `check-circle`, tamamlanmamış kart `circle` ana hattı; tamamlanınca başlık üstü çizili (`line-through text-muted-foreground`) + mevcut "Tamamlandı" rozeti. `canEdit=false` (board `viewer`) durumunda dokunulamaz ikon.
- **Mutation disiplini** — `useCardMutations`'a `toggleComplete` eklendi: mevcut duruma göre `card.complete` ya da `card.uncomplete` çağırır, `card.get` cache'i iyimser yamanır (`completed`), `onError` snapshot'tan geri alır, `onSettled` `card.get` + `board.get` invalidate eder; `clientMutationId` taşır. `board.get` cache'i burada *yamanmaz* — kart yüzünün `completedBy`/`completedAt` alanları çağıranın kimliğini gerektirir; `onSettled` board invalidate'i kart yüzünü arka planda tazeler.

### Faz 7D — Üye yönetimi + davet kabul (Wired)

Faz 7D ([DEM-180](https://linear.app/demirkol/issue/DEM-180)) mobil üye listesi ekranlarını + davet akışını kurdu. Mobilin **ilk mutation'lı** fazı — önceki 7A–7F salt-okunurdu.

- **Üye listesi ekranları** — `app/(app)/(boards)/workspace-members/[id].tsx` (`trpc.workspace.members.list`) ve `app/(app)/(boards)/board-members/[boardId].tsx` (`trpc.board.members.list`). Üyeler avatar + ad + rol rozetiyle salt görüntülenir; board listesinde workspace owner/admin'den devralınan admin'ler ikinci "Devralındı" rozetiyle işaretlenir. Çağıranın rolü ayrı sorgu yerine üye listesi içinden kendi `userId`'si eşlenerek bulunur.
- **Giriş noktaları** — board listesi (`workspaces/[id].tsx`) ve board ekranı (`boards/[boardId].tsx`) header'larına `users` ikonlu `headerRight` butonu; ilgili üye ekranına `router.push` eder.
- **Üye davet etme** — `admin+` workspace rolü / `admin` board rolü ise üye ekranında satır-içi açılan davet formu (`MemberInviteForm` — e-posta `TextField` + rol çip seçici `RoleSelect` + gönder). Workspace daveti `workspace.members.invite`, board daveti `board.members.add`. Başarıda form kapanır, üye listesi invalidate edilir, bilgi mesajı gösterilir.
- **Aldığım bekleyen davetler** — `(boards)/index.tsx` (workspace listesi) üstüne `PendingInvitations` bölümü: `workspace.invitations.mine` + `board.invitations.mine` tek listede birleşir, her satırda kabul/reddet butonu (`*.invitations.accept` / `decline`). Davet yoksa bölüm tamamen gizli. Kabul sonrası workspace listesi de invalidate edilir (yeni workspace görünür).
- **Mutation disiplini** — tüm davet/kabul/reddet mutation'ları opsiyonel `clientMutationId` taşır; her çağrıda `expo-crypto` `randomUUID()` ile yeni UUID üretilir (`src/lib/client-mutation-id.ts`). Hata → `Alert` / `FormMessage`; pending sırasında butonlar disabled.
- **Kapsam dışı** — rol değiştirme, üye çıkarma, gönderilen davet iptali (revoke). Yalnız liste + davet-al + davet-et.

### Faz 7H — Kart create/update + "move to list" (Wired)

Faz 7H ([DEM-184](https://linear.app/demirkol/issue/DEM-184)) mobilin board ekranını ilk kez **yazılabilir** yaptı — 7E/7F salt-okunurdu. Mevcut tRPC procedure'leri tüketildi; yeni backend yok.

- **Yetki** — board ekranı `board.get` `board.role`'ü taşır; düzenleme yüzeyleri (`canEditBoard` helper'ı — board `admin`/`member`) yalnız `member+` için mount edilir. `viewer` board'u salt görür (7E davranışı korunur). Backend her procedure'de yine kendi kontrolünü yapar.
- **Kart oluşturma** — her kolonun altında satır-içi "Kart ekle" composer'ı (`InlineComposer`); `card.create` ile listenin **sonuna** eklenir. Kart create yüzeyi board kolonudur (docs §8.2 kapsamı; kart detayından ayrı oluşturma giriş noktası yok).
- **Liste oluşturma** — board şeridinin sonundaki "Liste ekle" kolonu (`ListAddColumn`); `list.create`.
- **Liste yeniden adlandırma / arşivleme** — kolon başlığındaki ⋮ butonu bottom sheet (`Sheet`) açar: "Yeniden adlandır" (inline composer → `list.update`) ve "Arşivle" (`list.archive`). Arşivlenen liste board görünümünden düşer.
- **"Move to list" picker** — mobil drag-drop yok: board kartına **uzun basma** veya kart detayındaki "Listeyi taşı" aksiyonu `MoveToListSheet`'i açar (board'un aktif listeleri; kartın mevcut listesi işaretli + pasif). Seçim `card.moveToList` ile hedef listenin **sonuna** taşır. Liste-içi sıra seçimi (reorder) kapsam dışı.
- **Kart başlık düzenleme** — kart detay ekranında başlık dokunulabilir; `InlineComposer` ile `card.update({ title })`. 7H "temel alan" = başlık; açıklama/due/etiket/üye/checklist düzenleme 7G'nin işi.
- **Collaborative mutation disiplini** — tüm mutation'lar opsiyonel `clientMutationId` taşır (`expo-crypto` UUID). TanStack Query optimistic akış: `onMutate` ilgili sorguları iptal + snapshot + iyimser uygula → `onError` snapshot'a geri sar → `onSettled` `board.get`/`card.get` invalidate. Optimistic cache dönüşümleri saf fonksiyonlardır (`lib/board-cache.ts`) ve birim test edilir; mutation'lar `useBoardMutations` hook'unda toplanır.
- **Pozisyon** — tamsayı `order` yok; `@pusula/domain` `positionBetween`/`firstPosition` ile composer ve taşıma iyimser olarak listenin/kolonun sonuna ekler (server kesin `position`'ı `onSettled` refetch'inde reconcile eder).
- **Kapsam dışı** — liste-içi/listeler-arası kart reorder (sıra seçimi — ileride long-press reorder ayrı iş), liste reorder, liste rengi/ikonu, açıklama/due/etiket/üye/checklist düzenleme (7G), gerçek-zamanlı eş-zamanlılık (7.0: pull-to-refresh + push).

### Faz 7I — Arama, global + board içi (Wired)

Faz 7I ([DEM-185](https://linear.app/demirkol/issue/DEM-185)) mobil arama deneyimini kurdu — 7C'de "yakında" placeholder olan "Arama" sekmesi gerçek ekranla değişti. Faz 6.5 `search.query` procedure'ü tüketildi; yeni backend yok, permission filtresi server-side.

- **İki yüzey, tek gövde** — global arama (alt tab "Arama" sekmesi, `app/(app)/search.tsx`) ve board içi arama (`app/(app)/(boards)/board-search/[boardId].tsx`, board ekranı header'ındaki arama butonundan açılır) ortak `SearchView` bileşenini paylaşır. `boardId` prop'u verilirse arama o board ile sınırlanır; verilmezse global (tüm erişilebilir workspace/board).
- **Sorgu davranışı** — 2 karakterden önce API çağrılmaz (domain `06-arama-kapsami.md`); 275 ms debounce (web arama diyaloğuyla hizalı); `keepPreviousData` ile sorgu değişirken önceki sonuçlar ekranda kalır. Giriş 200 karakterle sınırlı (`searchQueryInput` şeması).
- **Sonuç sunumu** — sonuçlar entity tipine göre gruplanır (pano → liste → kart → yorum → ek → etiket; web §8.1.12 sırası); her satır entity ikonu + başlık + düz-metin snippet + bağlam (global'de workspace/board adı, board içinde yalnız bağlı kart). Boş / yükleniyor / hata / "en az 2 karakter" durumları ayrı `EmptyState`.
- **Derin link navigasyonu** — sonucun `targetUrl` alanı (web rota deseni) mobilde **kullanılmaz**; hedef `entityType` + `boardId`/`cardId`/`workspaceId`'den türetilir (`lib/search-target.ts` — saf modül, birim test edilir): kart/yorum/ek → kart detayı, board/liste/etiket → board ekranı. Board içi aramada bulunulan board'un kendisine işaret eden sonuç stack'e ikinci kez itilmez — `router.back()` ile mevcut board'a dönülür.
- **Kapsam dışı** — sonuç sayfalama (`search.query` `cursor` döndürür; mobil MVP ilk 25 sonucu gösterir, load-more yok), arşivli kayıt toggle'ı (`includeArchived`), facet/filtre paneli, klavye kısayolları (mobilde anlamsız).

### Faz 7J — Kart eki: görüntüleme + kamera/galeri yükleme (Wired)

Faz 7J ([DEM-186](https://linear.app/demirkol/issue/DEM-186)) kart detayına "Ekler" bölümünü ekledi — web §8.1.14 "Ekler" sekmesinin mobil karşılığı. Faz 11 `attachment.*` tRPC procedure'leri tüketildi; yeni backend yok. İş kuralları → [`../domain/07-ek-kurallari.md`](../domain/07-ek-kurallari.md); two-phase akış → [`09-depolama-ve-arama.md`](09-depolama-ve-arama.md) §9.1.

- **"Ekler" bölümü** — `src/components/card-detail/attachments-section.tsx`, kart detay ekranında checklist ile yorumlar arasında bir `DetailSection`. `attachment.list` committed ekleri (yeni üstte) listeler; her satır `attachment-tile.tsx` — kind ikonu (resim/PDF/Office) + dosya adı + meta (boyut · yükleyen · tarih) + varsa açıklama + "Kapak" rozeti. Liste tüm rollere açık (viewer dahil); boş listede bilgilendirme metni.
- **İki-fazlı yükleme** — `src/lib/use-attachment-upload.ts` hook'u: kamera / galeri / dosya seçiciden alınan dosya `attachment.initiate` (draft satır + presigned PUT URL) → `expo-file-system` `uploadAsync` ile doğrudan MinIO'ya `PUT` → `attachment.commit`. Yetkili boyut `getInfoAsync`'tan okunur (presigned `content-length` imzasıyla eşleşmeli — picker `fileSize` ipucu kullanılmaz). Yükleme anlık değil: süresince bekleyen tile gösterilir, `commit` sonrası `attachment.list` invalidate edilir. `initiate`/`commit` `clientMutationId` taşır.
- **İstemci doğrulaması** — `src/lib/attachment-format.ts` (saf modül, birim test edilir): MIME allowlist (8 tip) + 50 MiB boyut sınırı backend `attachment.initiate` Zod'u ile aynı; geçersiz dosya `initiate` çağrılmadan net hatayla reddedilir (presigned URL israfı yok). Picker `mimeType` vermezse dosya uzantısından türetilir.
- **Önizleme** — resim eki tam-ekran lightbox modalı (`attachment-image-viewer.tsx`; presigned GET URL `staleTime: 0` ile her açılışta taze). PDF/Office için tarayıcı/iframe önizleme yok — `attachment.getDownloadUrl` → `expo-file-system` indir → `expo-sharing` native paylaşım sayfası ("indir/paylaş" deseni).
- **Silme** — uploader veya board admin; `attachment.delete` optimistic (`attachment.list` cache'inden anında düşer) + rollback + onay `Alert`'i. `clientMutationId` taşır.
- **İzin akışı** — kamera/galeri erişimi `expo-image-picker` `get*/request*PermissionsAsync` ile istenir; kalıcı redde "Ayarları aç" yönlendirmesi (`Linking.openSettings`). iOS izin metinleri `app.config.ts` `expo-image-picker` plugin'iyle (`NSCameraUsageDescription` / `NSPhotoLibraryUsageDescription`).
- **Yeni native bağımlılık** — `expo-image-picker` · `expo-document-picker` · `expo-file-system` · `expo-sharing` (SDK 54 uyumlu sürümler). MinIO'ya erişim S3-uyumlu presigned PUT/GET üzerinden — MinIO'ya özel API'ye bağlanmaz.
- **Board kart yüzü** — `card-face.tsx` paperclip + sayı chip'i 7E'den beri `board.get` `attachmentCount` (committed-only) ile besleniyor — 7J ek bir değişiklik gerektirmedi.
- **Kapsam dışı (7J)** — ek açıklaması (yüklemede girme + `attachment.update` inline düzenleme), kapak görseli seçme (`card.update({ coverImageAttachmentId })`) + board kart yüzü kapak render'ı, yükleme ilerleme yüzdesi — bunlar **Faz 7P** ([DEM-193](https://linear.app/demirkol/issue/DEM-193)) parite takip işine alındı. Çoklu eşzamanlı upload ürün geneli V1-dışı ([`../domain/07-ek-kurallari.md`](../domain/07-ek-kurallari.md)) — 7P kapsamında da değil.

### Faz 7P — Kart eki: açıklama + kapak görseli + yükleme yüzdesi (Wired)

Faz 7P ([DEM-193](https://linear.app/demirkol/issue/DEM-193)) 7J'nin "Kapsam dışı" bıraktığı üç parite açığını kapattı — web §8.1.14 ile tam denklik. Faz 11 `attachment.*` + mevcut `card.update` tRPC procedure'leri tüketildi; **yeni backend yok**. İş kuralları → [`../domain/07-ek-kurallari.md`](../domain/07-ek-kurallari.md).

- **Ek açıklaması** — yükleme öncesi opsiyonel açıklama: dosya seçilip doğrulandıktan sonra `use-attachment-upload.ts` dosyayı doğrudan yüklemez, `pendingFile` olarak bekletir; `attachments-section.tsx` bir açıklama Sheet'i açar (`Textarea` + `n/500` sayaç + "Yükle"). Açıklama `attachment.initiate({ description })` ile gönderilir. Yükleme sonrası satır-içi düzenleme: `attachment-tile.tsx` kebab menüsünden "Açıklamayı düzenle" → tile satır-içi `Textarea`'ya genişler → `attachment.update({ attachmentId, description, clientMutationId })`. Boş değer → açıklama silme (`description = null`). Domain `attachmentDescriptionSchema` (≤500 karakter, plain text — Tiptap yok). Yetki: uploader veya board admin (UI yansıtır, backend her procedure'de doğrular). Optimistic + rollback (`attachment.list` cache yamanır).
- **Kapak görseli** — `attachment-tile.tsx` kebab menüsünde resim kind ek için "Kapak yap" / "Kapağı kaldır" → `card.update({ coverImageAttachmentId })` / `{ coverImageAttachmentId: null }`. `attachment.list` (`isCover` bayrağı) + `board.get` (kart yüzü kapak şeridi) birlikte iyimser güncellenir (`board-cache.ts` `setCardCoverImageInCache`), hata olursa ikisi de geri sarılır. Yetki: board member+ (yalnız image kind ek kapak olabilir — backend de zorlar).
- **Board kart yüzü kapak şeridi** (7E'nin ertelediği render) — `card-face.tsx` kartın üstüne kenara dayalı bir kapak görseli şeridi ekler. `board.get` kart sözleşmesi kapak için yalnız `{ attachmentId, fileName, mimeType, size }` döndürür — hazır URL yok; yeni `card-cover-image.tsx` bileşeni presigned GET URL'i `attachment.getDownloadUrl` ile kart başına tembel çeker (web §8.1.14 `CardCoverImage` ile aynı desen; `board.get`'e kapak URL'i eklemek backend sözleşme değişikliği — kapsam dışı). TanStack Query `attachmentId`'ye göre cache yapar; URL gelene kadar `bg-muted` blok yer tutar.
- **Yükleme yüzde göstergesi** — `use-attachment-upload.ts` `FileSystem.uploadAsync` yerine `FileSystem.createUploadTask(url, uri, options, progressCallback)` kullanır; callback `totalBytesSent / totalBytesExpectedToSend` verir (`attachment-format.ts` saf `uploadPercent` helper'ı 0–100'e çevirir). Hook `uploadingName` yanında `uploadProgress` (0–100 | `null`) döndürür; `attachments-section.tsx` bekleyen tile'da gerçek ilerleme barı (`accessibilityRole="progressbar"`) gösterir — initiate fazında spinner (`null`), PUT fazında bar, commit fazında bar %100'de.
- **Ek tile aksiyonları** — 7J'nin satır-içi ikon dizisi (önizle/indir/sil) mobilde dar satıra beş ikon sığdırmamak için **kebab menüsü**ne (`more-vertical` → Sheet) toplandı: önizle (resim) + indir satır-içi kalır; açıklamayı düzenle, kapak yap/kaldır, sil kebab Sheet'inde gruplanır (web §8.1.14 `MoreHorizontal` deseniyle hizalı).
- **i18n** — yeni metinler `apps/mobile/src/lib/strings.ts` `attachments.*` (hardcode yok): `uploadingProgress`, `actionMore`, `descriptionSheetTitle`, `descriptionPlaceholder`, `descriptionUploadAction`, `actionEditDescription`, `descriptionEditPlaceholder`, `descriptionEditError`, `actionsSheetTitle`, `actionMakeCover`, `actionRemoveCover`, `coverError`.
- **Kapsam dışı (7P)** — çoklu eşzamanlı upload (ürün geneli V1-dışı — [`../domain/07-ek-kurallari.md`](../domain/07-ek-kurallari.md)), `board.get`'e kapak presigned URL'i ekleme (backend sözleşme değişikliği), kart detayında kapak görseli render'ı (yalnız board kart yüzü). Mobilde realtime ek senkronu yok — 7M kararı (pull-to-refresh + push).

### Faz 7M — Realtime + offline/cache persistence (Wired)

Faz 7M ([DEM-189](https://linear.app/demirkol/issue/DEM-189)) 7.0'ın **pull-to-refresh + push** realtime kararı ile **cache persistence / okuma offline** kararını mobile uyguladı. Mobilde Socket.IO client veya offline mutation kuyruğu **yok** — 7.0 kararı sabit. Yeni native bağımlılık eklenmedi: `expo-network` ve TanStack Query persist (`@tanstack/react-query-persist-client` + AsyncStorage) 7A'da kuruluydu.

- **Pull-to-refresh** — board ekranı kolon `FlatList`'lerine ve kart detay ekranının `ScrollView`'una `RefreshControl`. Board ekranının dış scroll'u **yataydır**; `RefreshControl` yatay scroll'da çalışmadığından yenileme jesti dikey kolon listelerine konur — herhangi bir kolonu aşağı çekmek `board.get`'i yeniden çeker (`BoardColumn` `refreshing`/`onRefresh` prop'ları). Kart detayında aşağı çekmek kartın sekiz sorgusunu (`card.get` + `card.labels/members.list` + `checklist.list` + `comment.list` + `card.activity.list` + `board.members.list` + `board.get`) birlikte tazeler. Workspace ve board listesi ekranları pull-to-refresh'i zaten 7C'de taşıyordu — 7M board + kart ekranlarını tamamlar.
- **Bağlantı durumu banner** — `ConnectionBanner` app-shell üstünde (`app/(app)/_layout.tsx` `<Tabs>`'in üstü); cihaz çevrimdışıyken "Bağlantı yok" kırmızı şeridi belirir (status bar alanı da boyanır), çevrimiçiyken hiç yer kaplamaz. Ağ durumu `expo-network` `useNetworkState()`'ten okunur (`useNetworkStatus` hook'u); saf türetme `lib/network-status.ts` `isOfflineState` — yalnız **kesin** `false` çevrimdışı sayılır, `undefined`/`null` (durum bilinmiyor) çevrimiçi varsayılır; ilk render'da banner yanıp sönmez. Wi-Fi'ye bağlı ama internet erişilemiyorsa (`isInternetReachable === false`) de çevrimdışı sayılır. `network-status.ts` saf modüldür — birim test edilir.
- **TanStack Query `onlineManager` ↔ `expo-network`** — `lib/online-manager.ts` `configureOnlineManager()` kök layout'ta (`app/_layout.tsx`) bir kez çağrılır. React Native'de `navigator.onLine` yok; bu wiring olmadan Query her zaman "çevrimiçi" sanar ve çevrimdışı sorgular boşuna retry'lar. Bağlandıktan sonra çevrimdışıyken sorgular **pause** edilir, bağlantı dönünce otomatik resume + refetch olur.
- **Offline = okuma offline (cache persistence)** — `PersistQueryClientProvider` + AsyncStorage (`gcTime` 24 saat) 7A'da kuruluydu; 7M `onlineManager` wiring'iyle tamamlanır: çevrimdışıyken cache persistence son görülen board/kart'ı gösterir, banner verinin neden tazelenmediğini açıklar. **Mutation offline kuyruğu yok** — değişiklik bağlantı gerektirir (7.0 kararı).
- **Kapsam dışı** — Socket.IO mobil client, offline mutation kuyruğu (7.0 kararı), push teslimi + deep link (7L); boş board ekranının pull-to-refresh'i (kolon `FlatList`'i yokken `RefreshControl` taşıyacak dikey liste de yok — board içeriklenince gelir; `EmptyState` salt bilgilendirme).

### DEM-233 — Mobil board liste görünümü (Wired)

[DEM-233](https://linear.app/demirkol/issue/DEM-233) board ekranına kanban kolon görünümünün yanına ikinci bir görünüm modu — listelere göre gruplu dikey **liste görünümü** — ekledi. Veri katmanı değişmedi: tek `trpc.board.get` query aynen tüketilir; yeni backend / procedure / şema yok.

- **İki görünüm modu** — `kanban` (mevcut yatay `ScrollView` kolonları, 7E/7H) ve `list` (yeni `BoardListView`). Board header'ındaki mevcut filtre/arama/üye/⋮ butonlarının yanında **ikon-only mini segmented control** (`BoardViewToggle` — `columns` / `list` Feather ikonu) ile geçilir; dikey alan eklenmez, görünür metin yok (a11y etiketleri `strings.board.view.*`). Kullanıcı tam-genişlik bar versiyonunu 2026-05-20'de eledi — header'a sığan mini segment seçildi.
- **Liste görünümü** — `SectionList`: her aktif liste bir section (başlık = liste ikonu/rengi şeridi + ad + kart sayısı + `member+` için ⋮); kartlar section içinde **sade tek-satır** `CardListRow` (yeni — başlık + max 3 etiket renk noktası + sağda yalnız mevcut olan meta: due-date · checklist · ek/yorum sayıları · max 2 üye avatarı). Kapak görseli/şeridi ve etiket bantları **yoktur** — liste görünümü tarayıp-bulma yüzeyidir, kapak yüzü değil (kullanıcı kararı 2026-05-20 "sade tek satır"). Kanban tarafı `CardRow` + `CardFace` kullanmaya devam eder (`card-row.tsx` `board-column.tsx`'ten çıkarıldı — etkileşim deseni paylaşılır, satır görseli ayrı). Kanban davranışlarının dikey karşılığı korunur: etiket filtresi, pull-to-refresh (`SectionList` `RefreshControl`), satır-içi "Kart ekle" (section footer) + "Liste ekle" (liste footer), kart uzun-basma → "move to list".
- **Görünüm tercihi** — `src/lib/board-view-preference.ts` saf get/set helper (`theme-preference.ts` deseni; `AsyncStorage` anahtarı `pusula:board-view-mode`, değer `kanban|list`, birim test edilir) + `useBoardViewMode` hook'u. Tercih **global ve kalıcı** — tek tercih tüm board'lar için geçerli, uygulama yeniden açılışında korunur (board-başına tercih bilinçli olarak elendi — sadelik).
- **Kart taşıma** — değişmedi: long-press → `MoveToListSheet`. Mobil drag-drop yine yok (7H kararı); liste görünümünde de liste-içi reorder kapsam dışı.
- **i18n** — yeni metinler `apps/mobile/src/lib/strings.ts` `board.view.*` (segment etiketleri + erişilebilirlik; hardcode yok).
- **Kapsam dışı** — section katlama/collapse (kullanıcı kararı: düz gruplu), board-başına tercih, liste reorder, web tarafı board görünüm modları (ayrı iş).

### Faz 7K — Notification center + bildirim ayarları + push token kaydı (Wired)

Faz 7K ([DEM-187](https://linear.app/demirkol/issue/DEM-187)) mobil bildirim dilimini kurdu — 7C'de "yakında" placeholder olan "Bildirimler" sekmesi gerçek bildirim merkeziyle değişti, bildirim ayarları ekranı eklendi ve Expo push token kaydı bağlandı. Faz 6A/6B + Faz 10B-F `notifications.*` / `push.tokens.*` tRPC procedure'leri tüketildi; yeni backend yok. İş kuralları → [`../domain/04-bildirim-kurallari.md`](../domain/04-bildirim-kurallari.md); push/quiet-hours altyapısı → [`06-bildirim-altyapisi.md`](06-bildirim-altyapisi.md).

- **Bildirim merkezi** — `app/(app)/(notifications)/index.tsx`, "Bildirimler" sekmesinin kök ekranı. `notifications.list` ilk sayfasını (`limit: 25`, `keepPreviousData`) tek `useQuery` ile çeker — load-more yok (7I arama "ilk 25" deseni gibi sade). Bildirimler tarih gruplarıyla (Bugün/Dün/Bu hafta/Daha eski) listelenir; her satır (`src/components/notifications/notification-row.tsx`) tip ikonu + aktör/özet metni + göreli zaman + okunmamış noktası. Boş/yükleniyor/hata state'leri `EmptyState`; pull-to-refresh `RefreshControl` (7M deseni).
- **Sekme rozeti** — `notifications.unreadCount` `(app)/_layout.tsx` `AppShell`'de çekilir; `<Tabs.Screen name="(notifications)">` `tabBarBadge` ile okunmamış sayısı rozeti çizilir (0 → rozet yok). `markRead`/`markAllRead` mutation'ları bu sorguyu invalidate eder.
- **Okundu işaretleme** — header'da "Tümünü okundu işaretle" (`markAllRead`) + satıra dokununca `markRead` (idempotent). `src/lib/use-notification-mutations.ts` optimistic + rollback; saf cache dönüşümleri `src/lib/notification-cache.ts` (`markNotificationRead` / `markAllNotificationsRead` / `decrementUnreadCount` / `resetUnreadCount` — birim test edilir).
- **Derin link** — `src/lib/notification-target.ts` saf helper (web `notification-link.ts`'in mobil karşılığı; birim test edilir): bildirim payload'ından (`cardId`/`boardId`/`workspaceId`) Expo Router hedefini deterministik türetir — kart detayı / board ekranı / workspace ekranı. Web rota deseni taşıyan `payload.linkTo` mobilde kullanılmaz. Satıra dokununca hedefe yönlendirir.
- **Sistem (aktörsüz) bildirimler** — `due_*` tipleri için satır aktör adı basmaz, "Sistem" rozeti gösterir (web `notification-center.tsx` `SYSTEM_NOTIFICATION_TYPES` kümesi). Tip→Feather ikon + aktör-prefixsiz özet metin `src/lib/notification-display.ts`'te (web `notification-type-icon.tsx` + `activity-summary.ts` karşılığı; birim test edilir).
- **Bildirim ayarları ekranı** — `app/(app)/(notifications)/notification-settings.tsx`, "Bildirimler" sekmesinin kendi `<Stack>`'inde (`(notifications)/_layout.tsx`) pushed route — app-shell 4 sekme sabit kaldı, yeni sekme açılmadı. "Bildirimler" sekmesi tek-ekran dosyasından `(notifications)` route grubuna dönüştürüldü ki ayarlar ekranı bildirim sekmesinin stack'inde itilsin. Giriş noktası: bildirim merkezi header'ındaki dişli butonu. Bölümler (web Faz 10 paritesi, NativeWind'e uyarlı): **Genel kanallar** (in-app sabit + email/push `Toggle` + sustur seviyesi `RoleSelect` çipleri); **Bildirim tipleri** (tip × kanal referans matrisi — `notification-type-matrix.tsx`, web `notifications-type-matrix.tsx`'in fiilî davranışıyla aynı: tip-bazlı kanal kaydı backend'de yok, matris **salt-okunur** referans tablodur, fazladan toggle uydurulmadı); **Kapsam ayarları** (workspace/board/card override satırları — sustur seviyesi düzenleme + sil; mobil MVP override **ekleme** içermez — web `scope-add-dialog` workspace/board ağacı gerektirir, mevcut override'lar düzenlenir/silinir); **Sessiz saatler** (global pencere — `Toggle` + `HH:MM` metin girişi, yeni native dep eklemeden — `@react-native-community/datetimepicker` yok; doğrulama `src/lib/quiet-hours.ts` saf helper); **Cihazlar** (`push.tokens.list` aktif cihazlar — salt-okunur; cihaz başına iptal logout'la). Tüm yazmalar optimistic + rollback + `clientMutationId` (`src/lib/use-notification-preferences.ts`).
- **Push token kaydı** — `src/lib/use-push-token-registration.ts` hook'u `(app)/_layout.tsx` `AppShell`'de oturum hazır olunca **bir kez** koşar: `expo-notifications` `getPermissionsAsync` → izin yoksa sade OS dialog'u (`requestPermissionsAsync`) → izin verilirse `getExpoPushTokenAsync({ projectId })` → `push.tokens.register({ token, platform, deviceName })`. İzin reddedilirse oturum başına tekrar sorulmaz. Kaydedilen token `src/lib/push-token-store.ts` modül-belleğinde tutulur; `account.tsx` logout akışı `authClient.signOut()` **öncesi** `push.tokens.revoke({ token })` çağırır (signOut sonrası istek kimliksiz giderdi). Saf parçalar `src/lib/push-device.ts` (`pushPlatform` / `deviceLabel` — birim test edilir).
- **Yeni native bağımlılık** — `expo-notifications` (`~0.32.17`, SDK 54 uyumlu) + `app.config.ts` `expo-notifications` plugin'i. EAS `projectId` `getExpoPushTokenAsync` için `Constants.easConfig`/`expoConfig.extra.eas`'ten okunur (henüz `app.config.ts`'te boş — `eas init` ile dolar).
- **Kapsam dışı (7K)** — push **teslimi** (foreground/background notification handler, push'a tıklayınca deep link açma, cilalı priming pre-prompt) **Faz 7L** kapsamı — dokunulmadı. Bildirim merkezi load-more sayfalama, scope override **ekleme** yüzeyi, cihaz başına `revokeById`, sessiz saat zaman dilimi seçici, e-posta digest modu (Faz 10G) mobil MVP-dışı. Mobilde Socket.IO yok (7.0 kararı) — bildirim merkezi pull-to-refresh + sekme rozeti `unreadCount` invalidate ile tazelenir.

### Faz 7L — Push teslimi + deep link + universal links (Wired)

Faz 7L ([DEM-188](https://linear.app/demirkol/issue/DEM-188)) 7K'nın kurduğu push token kaydının üzerine **bildirim teslimini** ekledi: gelen push'un foreground/background gösterimi, bildirime dokununca içeriğe yönlendirme, cilalı izin priming ve universal link yapılandırması. Faz 6 push backend'i ([DEM-29](https://linear.app/demirkol/issue/DEM-29)) hazır — mobil yeni backend tüketmez; worker push payload'ı `data: { type, cardId?, boardId? }` taşır ([`06-bildirim-altyapisi.md`](06-bildirim-altyapisi.md) "Push kanalı").

- **Foreground/background notification davranışı** — `expo-notifications` `setNotificationHandler` kök layout'ta (`app/_layout.tsx`, `configureOnlineManager` yanında) bir kez kurulur: uygulama **açıkken** gelen push da banner + bildirim listesinde gösterilir (`shouldShowBanner`/`shouldShowList` true — SDK 54 alan adları; ses/badge kapalı). Uygulama arka planda/kapalıyken bildirimi OS gösterir; handler yalnız foreground sunumunu etkiler.
- **Push'a dokunma → deep link** — `src/lib/use-notification-deep-link.ts` hook'u `AppShell`'de (oturum garantili) iki yolu dinler: cold-start `getLastNotificationResponseAsync()` (uygulama kapalıyken bildirime dokunulup açıldıysa — bir kez işlenir) ve sıcak `addNotificationResponseReceivedListener` (uygulama açık/arka plandayken dokunma). İkisi de bildirimin `request.content.data`'sını çözer.
- **Deep link hedef türetme** — push `data` (`{ type, cardId?, boardId? }`) mevcut `src/lib/notification-target.ts` saf helper'ına `{ workspaceId: null, boardId, cardId, payload: data }` olarak verilir → kart detayı / board ekranı Expo Router hedefi. Worker payload'ı web rota desenli `linkTo` taşısa da mobilde kullanılmaz (7K deseni). Hedef türetilirse `router.navigate` edilir; türetilemezse navigasyon yapılmaz (uygulama yalnız açılır).
- **Universal links — URL → mobil rota** — `src/lib/deep-link.ts` saf helper'ı (birim test edilir) bir gelen URL'yi (`https://pusulaportal.com/...` universal link **veya** `pusula://...` özel şema) Expo Router hedefine çevirir: `/workspaces/{id}/boards/{boardId}?card={cardId}` → kart detayı, `/workspaces/{id}/boards/{boardId}` → board ekranı, `/workspaces/{id}` → workspace ekranı, eşleşmeyen yol → `null` (uygulama açılır, navigasyon yapılmaz). Web rota desenleri mobil Expo Router rota ağacıyla **eşleşmediği** için `expo-linking` `getInitialURL()` (cold) + `addEventListener('url', …)` (warm) ile yakalanıp helper'la çevrilir ve `router.navigate` edilir; özel şema mobil rotalarını Expo Router kendi linking'iyle çözmeye devam eder.
- **Universal links / associated domains yapılandırması** — `app.config.ts`: `ios.associatedDomains = ['applinks:pusulaportal.com']` + `android.intentFilters` (`VIEW` eylemi + `BROWSABLE`/`DEFAULT` kategorileri, `scheme: https`, `host: pusulaportal.com`, `autoVerify: true`). **Kullanıcı kararı (2026-05-18, AskUserQuestion):** kapsam = **tüm `pusulaportal.com` yolları** (yola göre kısıtlama yok) — uygulama kuruluysa her web linki uygulamayı açar, kurulu değilse tarayıcıda kalır.
- **`apple-app-site-association` + `assetlinks.json` Faz 7O'ya ertelendi** — universal link domain doğrulaması bu iki "well-known" dosyasının `pusulaportal.com` kökünden sunulmasını ister; içerikleri **Apple Team ID** ve **Android imza SHA-256** gerektirir, bu değerler `eas init` / EAS production build sonrası kesinleşir. **Kullanıcı kararı (2026-05-18, AskUserQuestion):** dosyalar **Faz 7O**'da (EAS build turu) gerçek değerlerle `apps/web` `.well-known` altına eklenir; 7L yalnız `app.config.ts` tarafını yazar.
- **İzin priming (pre-prompt)** — 7K'da push izni login sonrası **doğrudan** OS dialog'uyla isteniyordu; 7L bunu cilalı bir ön-istem ekranıyla değiştirir. `usePushTokenRegistration` refactor edildi: izin `undetermined` + `canAskAgain` ise OS dialog'unu doğrudan açmaz, bir priming `Sheet`'i (`PushPermissionPrimer`) açar — bildirimin ne işe yaradığını (atama / yorum / mention / son tarih) anlatır + "İzin ver" / "Şimdi değil". "İzin ver" → OS `requestPermissionsAsync` → izin verilirse token kaydı (7K akışı). "Şimdi değil" → OS dialog'u hiç açılmaz (oturum başına bir kez). İzin **zaten verilmişse** priming atlanır, doğrudan token kaydı koşar; `denied` (tekrar sorulamaz) ise sessizce geçilir.
- **i18n** — yeni metinler `src/lib/strings.ts` `push.*` (hardcode yok): priming başlık/açıklama + "İzin ver" / "Şimdi değil".
- **Gerçek cihaz sınırı / follow-up** — universal link uçtan uca doğrulaması (associated domains + well-known dosyaları + cihazda kurulu uygulama) ve gerçek push teslimi **gerçek cihaz + domain doğrulaması** gerektirir; Expo Go ve `expo export` ile tam test edilemez (Expo Go SDK 53+ push token vermez). Uçtan uca doğrulama **Faz 7O** (EAS build) turunda yapılır — 7L'de follow-up olarak izlenir.
- **Kapsam dışı** — bildirim eylem butonları (action buttons), rich/kategori bildirimleri, uygulama ikonu badge sayısı senkronu, Android bildirim kanalı özelleştirmesi; well-known doğrulama dosyaları (7O).

### Mobil tipografi — Poppins (2026-05-18)

Proje geneli tek-tip font kararının ([`02-teknoloji-kararlari.md`](02-teknoloji-kararlari.md) Karar kaydı 2026-05-18) `apps/mobile` ayağı. Daha önce mobil OS sistem fontunu kullanıyordu; artık tüm görünür metin **Poppins**.

- **Yükleme** — `@expo-google-fonts/poppins` + `expo-font` `useFonts` ile dört ağırlık (`Poppins_400Regular` / `500Medium` / `600SemiBold` / `700Bold`) kök `app/_layout.tsx`'te yüklenir. `expo-splash-screen` `preventAutoHideAsync` ile fontlar hazır olana kadar splash tutulur, yüklenince `hideAsync` — sistem-fontu "flash"ı yok. Yükleme hata verirse uygulama yine de açılır (fallback sistem fontu). `expo-font` SDK 54 ile hizalı pin (`~14.0.11`).
- **Ağırlık mekanizması** — Poppins değişken (variable) font **değildir**; her ağırlık ayrı bir aileyle gelir. React Native'de `fontWeight` non-variable bir fontta aileyi seçmez (fake-bold üretir), bu yüzden NativeWind ağırlık sınıfları (`font-medium` / `font-semibold` / `font-bold`) **kullanılmaz**. Bunun yerine ortak `Text` sarmalayıcı bileşeni (`src/components/text.tsx`) bir `weight` prop'u (`regular` / `medium` / `semibold` / `bold`) alır ve `src/theme/fonts.ts`'teki ağırlık→aile eşlemesiyle doğru Poppins ailesini `style.fontFamily` olarak uygular. Tüm ekran/bileşenlerde RN `Text` yerine bu sarmalayıcı kullanılır → sınıfsız (regular) metinler dahil hiçbir metin sistem fontunda kalmaz.
- **`Text` olmayan metin yüzeyleri** — expo-router `Link`, `TextInput`, native `Stack` `headerTitleStyle` ve `Tabs` `tabBarLabelStyle` `Text` bileşeni değildir; bunlara Poppins `fontFamily` `style`/option ile açıkça bağlanır (`fontFamilyForWeight` / `defaultFontFamily` helper'ları).

### Faz 7N — Mobil test stratejisi & e2e (Wired, 2026-05-18)

Faz 7.0 test kararının ([`02-teknoloji-kararlari.md`](02-teknoloji-kararlari.md) Karar kaydı 2026-05-17 + 2026-05-18) `apps/mobile` ayağı. Mobil uygulama **üç test katmanı** taşır; saf domain/helper katmanı coverage'ı **%96** (CLAUDE.md/safety %80+ hedefi karşılandı).

- **Birim testleri — Vitest `unit` projesi (`node` ortamı)** — `src/lib/*` saf domain/helper modülleri (`board-cache`, `notification-*`, `attachment-format`, `deep-link`, `search-target`, `quiet-hours`, `activity-summary`, `label-color`, `client-mutation-id`, `tiptap` vb.) `src/**/*.test.ts` olarak `node` ortamında koşar. Native bağımlılık taşımaz — CI'da deterministik ve hızlı.
- **Component testleri — Vitest `component` projesi (`happy-dom`)** — presentational bileşenler (`src/components/**/*.test.tsx`): render + prop görünürlüğü + callback + erişilebilirlik rolü. **Araç sapması:** Faz 7.0 "RN Testing Library" (`@testing-library/react-native`) öngörmüştü; ancak RNTL React 19 + RN 0.81 + Vitest bileşiminde çalışmadı (RN `index.js` Flow syntax'ı Vitest transform'undan geçmez; RNTL CJS matcher'ları Vite alias'ını atlar). Yerine **`react-native-web` + `@testing-library/react`** (DOM sorguları) kullanıldı — RNW uygulamanın zaten kullandığı web render katmanı, `accessibilityRole`/`accessibilityLabel` → ARIA `role`/`aria-label` olarak doğrulanabilir; `jest-expo`'ya geçilmedi. Sınır: NativeWind style hesabı test edilmez (yalnız davranış + erişilebilirlik). Native Expo modülleri `vitest.setup.tsx`'te mock'lanır.
- `unit` + `component` projeleri `vitest.config.ts` `projects` ile tek `pnpm test` altında birleşir.
- **E2E — Maestro** — `apps/mobile/.maestro/` altında YAML flow'ları kritik kullanıcı akışlarını kapsar: **auth** (sign-up / sign-in / sign-out) · **board görüntüleme** · **kart create + "move to list"** · **bildirim merkezi**. **Gerçek koşum cihaz/emülatör + EAS dev build gerektirir** — Expo Go / `expo export` ile çalıştırılamaz; flow'lar bu fazda yazıldı, uçtan uca koşum + CI bağlama **Faz 7O** turunda yürütülür (7L push doğrulamasıyla aynı follow-up penceresi). Bazı seçiciler `strings.ts` metnine bağlı kırılgan — 7O'da `testID` eklenmesi `.maestro/README.md`'de izlenir.
- **Coverage** — `@vitest/coverage-v8`; saf domain/helper katmanı %96 statement / %95 branch. Hook'lar (`use-*`) ve native-bağımlı modüller (`api-url`, `auth-client`, `online-manager`, `*-registration` vb.) `node` birim coverage'ı dışında — component/integration katmanına aittir.

### Mobil kart arşivleme — kart detay aksiyonu ([DEM-196](https://linear.app/demirkol/issue/DEM-196)) (Wired, 2026-05-18)

[DEM-196](https://linear.app/demirkol/issue/DEM-196) kart detayına onaylı arşivleme aksiyonu ekledi — `card.archive` mobilde ilk kez tüketilir; web kart context menüsündeki "Arşivle" ([DEM-101](https://linear.app/demirkol/issue/DEM-101)) paritesi. DEM-194 boşluk taramasının işlevsel boşluk bulgusu; mevcut tRPC procedure tüketilir, **yeni backend yok**.

- **Aksiyon yüzeyi — başlık yanı ⋮ menüsü** (kullanıcı kararı 2026-05-18) — kart detay `Stack.Screen` header'ına sağda ⋮ butonu; dokununca mevcut `Sheet` bottom sheet "Kart işlemleri" başlığıyla açılır, içinde destructive "Kartı arşivle" satırı (`card-detail/card-actions-sheet.tsx`). Web kart modalı `CardModalHeader` ⋮ menüsüyle simetrik; ileride taşı/kopyala/geri yükle aynı menüye eklenir. ⋮ butonu yalnız board `member+` (`canEdit`) ve kart arşivli değilken çizilir.
- **Onay + navigasyon** — arşivleme `Alert` ile onaylanır (board'dan kart düşüren işlem); onaylanınca optimistic mutation tetiklenir ve board ekranına geri navigasyon (`router.back()`) yapılır — arşivlenen kart board görünümünde görünmez.
- **Optimistic akış** — `useCardMutations` `archive` mutation'ı: `onMutate` `card.get` (`archivedAt` set) + `board.get` (yeni `removeCardFromCache` ile kart cache'ten çıkar — board görünümünden anında düşer) cache'lerini iyimser günceller + snapshot; `onError` ikisini geri sarar + `Alert`; `onSettled` invalidate. `clientMutationId` taşır. `removeCardFromCache` saf fonksiyon olarak `board-cache.ts`'e eklendi (birim test edilir). Mutation callback'leri kart ekranı `router.back()` ile unmount olsa da çalışır (TanStack Query mutation'ı observer'dan bağımsız tamamlanır).
- **i18n** — yeni metinler `strings.ts` `cardDetail.*` (hardcode yok): `cardActionsTitle`, `archiveAction`, `archiveConfirmTitle`, `archiveConfirmBody`, `archiveConfirmAction`, `archiveError`.
- **Kapsam dışı** — arşivli kartı geri yükleme (restore — kart detayına arşivli kartla yalnız arama / derin link ulaşır, ayrı iş), kart silme, taşı/kopyala menü öğeleri.

### Mobil kart kapak rengi — meta çubuğu chip'i ([DEM-201](https://linear.app/demirkol/issue/DEM-201)) (Wired, 2026-05-18)

[DEM-201](https://linear.app/demirkol/issue/DEM-201) kart kapak rengi seçicisini mobile getirdi — web kart modalındaki 12-renk kapak rengi picker'ının (`13-ui-tasarim-dili.md` §13.3 — `card.update({ coverColor })`) mobil karşılığı. DEM-194 boşluk taramasının kozmetik web-parity bulgusu (düşük öncelik); mevcut tRPC procedure tüketilir, **yeni backend yok**.

- **Kapak chip'i — `CardMetaBar`** — 7G-2 meta çubuğuna beşinci bir chip ("Kapak", `image` ikonu) eklenir; chip kapak rengi seçiliyse o rengin noktasını + renk adını, seçili değilse soluk "Kapak" placeholder'ını gösterir. Dokununca mevcut `Sheet` ile `CoverColorSheetBody` (`card-detail/cover-color-sheet.tsx`) açılır: 12 renk swatch ızgarası (`@pusula/domain` `CARD_COVER_COLORS`) + seçili renk varken "Rengi kaldır". `canEdit=false` (board `viewer`) ise chip salt-gösterim (dokunulamaz).
- **Kapak rengi paleti** — `src/lib/cover-color.ts` `coverColorHex` haritası 12 rengi hex'e çevirir: `theme/tokens.ts` `paletteColors` (11 etiket rengi) + `siyah`. `paletteColors` etiket paletidir ve 11'de kalır (`scaffold.test.ts` sözleşmesi); kapak paletinin 12. rengi `siyah` ayrı eklenir (web `--palet-siyah` oklch değerinin tema-bağımsız yaklaşık karşılığı). Renk adları `strings.ts` `cardDetail.coverColorNames` (i18n — hardcode yok).
- **Kart yüzü şeridi** (issue'da "isteğe bağlı" — yapıldı) — `card-face.tsx` kapak görseli yoksa `coverColor` varsa kartın üstüne kenara dayalı ince bir renk şeridi çizer (web `card-item` kapak şeridi paritesi; kapak görseli önceliklidir).
- **Collaborative mutation disiplini** — kapak rengi seçimi `card.update` (`coverColor`) optimistic akışını kullanır: `onMutate` `card.get` **ve** `board.get` cache'lerini iyimser günceller (kart detayı + board kart yüzü şeridi birlikte; `board-cache.ts` `setCardCoverColorInCache` saf helper'ı), `onError` ikisini snapshot'tan geri sarar, `onSettled` `card.get` + `card.activity.list` invalidate eder. Her mutation `clientMutationId` taşır. Aynı değer yeniden seçilirse mutation atılmaz (web `card-item` `coverColor !== color` simetrisi).
- **Kapsam dışı** — kapak görseli seçimi (Faz 7P'de yapıldı — `coverImageAttachmentId`); kart detayında kapak rengi şeridi render'ı (yalnız board kart yüzü + meta chip); board arka plan rengi (DEM-202).

### Merkezi "Ekle" butonu + Hızlı Notlar + oluşturma menüsü ([DEM-203](https://linear.app/demirkol/issue/DEM-203))

[DEM-203](https://linear.app/demirkol/issue/DEM-203) mobil alt tab bar'ına merkezi bir aksiyon butonu, yeni bir **Hızlı Not** domain entity'si ve dört yeni oluşturma akışı getirir. Bu issue yalnız mobil UI + backend kapsar — web tarafı ayrı issue. Bu bölüm DEM-203 önce-belge adımında (kullanıcı onayı 2026-05-18) sabitlenir; somut wiring sonraki turda.

- **Merkezi "Ekle" butonu — sekme değil aksiyon** — alt tab bar'ın **ortasına yükseltilmiş** (FAB tarzı, `primary` renkli, büyük "+") bir aksiyon butonu eklenir. Bu **gezinme sekmesi değildir** — 7C'de sabitlenen "app-shell = 4 sekme" kararı (**Panolar / Arama / Bildirimler / Hesap**) **korunur**; merkezi buton beşinci sekme yaratmaz, navigasyon ağacına girmez, yalnız aksiyon tetikler. Görsel olarak tab bar düzlemi üzerinde yükseltilmiş (`Tabs` `tabBarButton` veya tab bar üzerine absolute konumlu özel bileşen) durur.
- **Dokunma → Hızlı Notlar ekranı** — butona kısa dokunma "Hızlı Notlar" ekranını açar (aşağıya bkz.). _(DEM-230 ile güncellendi: kullanıcı anasayfadayken kısa dokunma ekran açmaz, anasayfa hızlı-not dock'una yazılı taslağı kaydeder — bkz. "Faz 7 sonrası — anasayfa hızlı-not dock'u".)_
- **Uzun basış → oluşturma menüsü** — butona uzun basma mevcut `Sheet` (RN `Modal` tabanlı bottom sheet — §8.2 7H'de tanımlı) ile bir oluşturma menüsü açar: **Kart oluştur · Liste oluştur · Pano oluştur · Workspace oluştur**. Her satır ilgili oluşturma akışını başlatır. Yeni native bağımlılık eklenmez — mevcut `Sheet` yeterli (7G-2/7G-3/7E-2 deseni sürer).

**Hızlı Not — yeni domain entity.** Hızlı Not kişiye özel ve **global**'dir — herhangi bir workspace/board/list/kart bağlamından bağımsızdır; yalnız sahibi erişir. Hafif bir "yakalama" yüzeyidir: activity, notification veya realtime event **üretmez** (kişisel, collaborative değil). Domain modeli + invariant'ları `docs/domain/` ekseninde tanımlanır (bu architecture dosyası yalnız mobil UI ayağını taşır). Bir Hızlı Not "panoya taşı" aksiyonuyla karta dönüştürülebilir: hedef konum seçilir, kart oluşturulur ve Hızlı Not silinir (not → kart tek yönlü dönüşüm).

**Yeni mobil ekran/akışlar:**

1. **Hızlı Notlar ekranı** — merkezi butona dokununca açılır; sahibinin notlarını listeler. Hızlı-ekleme girişi (satır-içi metin → yeni not), her notta düzenle/sil, pull-to-refresh (7M deseni). Notlar global olduğundan workspace/board seçimi yok.
2. **Not → kart dönüşümü akışı** — bir Hızlı Not'tan "panoya taşı": `LocationPicker` ile hedef workspace→pano→liste seçilir, kart oluşturulur, kaynak not silinir.
3. **Kart oluştur akışı** — `LocationPicker` ile workspace→pano→liste seçimi + kart başlığı; açıklama/son tarih/etiket gibi opsiyonel alanlar **katlanmış** (varsayılan gizli, isteyen açar). Oluşturma sonrası yeni kartın detay ekranına gidilir.
4. **Liste oluştur akışı** — `LocationPicker` ile workspace→pano seçimi + liste adı; `list.create` ile panonun sonuna eklenir.
5. **Pano oluştur ekranı** — mobilde **yeni** (7C'de "kapsam dışı" bırakılmıştı): workspace seçimi + pano adı; `board.create` tüketilir. Bu issue mobilde ilk kez `board.create` çağırır.
6. **Workspace oluştur ekranı** — mobilde **yeni** (7C'de "kapsam dışı"): workspace adı; `workspace.create` tüketilir. Mobilde ilk kez `workspace.create` çağrılır.

**`LocationPicker` ortak bileşeni.** Kademeli workspace→pano→liste seçici; kart oluştur, liste oluştur ve not→kart dönüşümü akışlarında yeniden kullanılır (her akış ihtiyaç duyduğu derinliğe kadar — liste oluştur pano düzeyinde durur). Tek bir bileşende toplanması seçim UX'ini ve veri çekimini (`workspace.list` → `board.list` → `list.list` zinciri) tekrarsız kılar. `MoveToListSheet`'ten farkı: `MoveToListSheet` tek board içinde liste seçer; `LocationPicker` workspace düzeyinden başlayan tam kademeli seçicidir.

**Backend.** Hızlı Not yeni entity olduğundan yeni tRPC procedure'leri + Drizzle şeması gerektirir (önce-belge: domain ekseni + `04-veri-katmani.md`). Kart/liste/pano/workspace oluşturma mevcut `card.create` / `list.create` / `board.create` / `workspace.create` procedure'lerini tüketir — bunlar için yeni backend yok; mobilde `board.create` ve `workspace.create` ilk kez kullanılır.

### Kart detay — bölüm yüzeyleri + hiyerarşi rafinasyonu ([DEM-204](https://linear.app/demirkol/issue/DEM-204))

[DEM-204](https://linear.app/demirkol/issue/DEM-204) mobil kart detay ekranının (`app/(app)/(boards)/cards/[cardId].tsx`) görsel hiyerarşisini rafine eder. 7F–7G ekranı tek `ScrollView` içinde `gap-6` ile düz akan bloklardan oluşuyordu; bölümler yalnız minik `text-xs uppercase` `DetailSection` başlığıyla ayrılıyordu — görsel kapsayıcı (yüzey/kart/ayraç) yok, "çok fazla veri, hiyerarşi yok" geri bildirimi. DEM-194–201 mobil rafinasyon batch'inin devamı; mevcut tRPC sözleşmesi tüketilir, **yeni backend / şema yok** — salt görsel + etkileşim rafinasyonu.

- **Görsel sistem — iki yüzey tipi** (kullanıcı kararı, AskUserQuestion 2026-05-18: "kart yüzeyleri"; sekmeli düzen ve yalnız-ayraç reddedildi) — sayfa zemini `bg-background` → `bg-muted`; içerik bu zemin üzerine oturan `bg-card` yüzeylerde toplanır. *Header card*: tamamlandı rozeti + tamamla/geri al toggle + başlık + meta chip satırı (`CardMetaBar`). *Section card*: `bg-card rounded-xl border border-border` kapsayıcı, başlık satırı ikon + başlık + sağda opsiyonel **özet rozeti**.
- **Bölüm dizilimi** — header card altında ayrı section kartları: Açıklama · Kontrol listeleri (başlıkta `tamam/toplam` ilerleme rozeti) · Ekler (adet rozeti) · Yorumlar (adet rozeti) · Aktivite. Hiyerarşi: birincil (başlık/açıklama/kontrol listeleri) → ikincil (ekler/yorumlar) → üçüncül (aktivite).
- **Bölüm yüzeyi (`DetailSection`)** — `card-detail/section.tsx` `DetailSection` kart yüzeyine (`bg-card rounded-xl border`) genişletilir; sağda opsiyonel adet/ilerleme rozeti slotu (`trailing`) alır. Mevcut çağıranlar (`description-editor`, `attachments-section`, `[cardId].tsx`) yeni görünümü kendiliğinden devralır. `collapsing nav` eşik ölçümü (7G-3 `handleTitleLayout`) header card'a taşınır.
- **Composer'lar butona saklı** (kullanıcı kararı 2026-05-18) — `checklist-section.tsx` "Kontrol listesi ekle" / "Madde ekle" hep-açık satır-içi `TextField` + buton composer'ları varsayılan gizli; yerlerine kompakt "+ ekle" tetikleyicisi, dokununca açıl/kapan composer'ı açılır (7H board kolon "Kart ekle" / `attachments-section.tsx` "Ek ekle" simetrisi — Ekler bölümü zaten bu desende). Boş giriş kutuları + pasif butonlar ekranı doldurmaz.
- **Aktivite — son N + genişlet** — `activity-list.tsx` varsayılan son birkaç olayı gösterir, "Tüm aktiviteyi gör" tetikleyicisiyle tümü açılır; aktivite üçüncül içerik olduğundan sonsuz scroll'u keser.
- **Meta çubuğu** — chip satırı `flex-wrap` kalır (kullanıcı kararı 2026-05-18; yatay scroll reddedildi — "Liste" gibi chip'ler ekran dışına kaçmasın), header card içinde kapsanır.
- **Kapsam dışı** — backend/tRPC/şema değişikliği; sekmeli düzen; yeni renk token'ı; bottom sheet düzenleme akışlarının içeriği (7G-2 deseni korunur).

### Hesap sayfası yeniden tasarımı + kullanıcı-seçimli tema ([DEM-207](https://linear.app/demirkol/issue/DEM-207) + [DEM-208](https://linear.app/demirkol/issue/DEM-208))

Mobil Hesap sekmesi (`app/(app)/account.tsx`) ortalanmış avatar + ad + e-posta + tek "Çıkış" butonundan ibaretti — ayar yoktu. Bu iş onu gruplu bir ayar ekranına dönüştürür ve mobile kullanıcı-seçimli tema getirir. Yeni backend / tRPC / şema **yok** — profil/şifre doğrudan Better Auth client uçları, tema tercihi cihaz-yerel. Kararlar `AskUserQuestion` ile sabitlendi (2026-05-18).

- **Kullanıcı-seçimli tema (DEM-207)** — model **Açık / Koyu / Sistem** (varsayılan Sistem). React Native `Appearance.setColorScheme()` ile uygulanır: `system` → `Appearance.setColorScheme(null)` (cihaza bırak), aksi halde zorla. Tek noktadan hem ~45 dosyadaki `themeFor(useColorScheme())` çağrılarını hem NativeWind className temasını sürer — `tailwind.config.js` `darkMode: 'media'` + `global.css` `@media` korunur, codemod yok. Tercih `AsyncStorage`'da (`pusula:theme-preference`); `theme/theme-preference.ts` saf get/set helper, `theme/theme-provider.tsx` kök context açılışta yükleyip uygular + aktif tercih/setter sağlar. `app/_layout.tsx` `ThemeProvider` ile sarmalanır.
- **Route yapısı** — `account.tsx` → `(account)` route grubu (kendi `Stack`'i; `(boards)`/`(notifications)` deseni): `(account)/index.tsx` (ekran) + `(account)/profile-edit.tsx` + `(account)/change-password.tsx` + `(account)/_layout.tsx`. `(app)/_layout.tsx` Tabs `name="account"` → `name="(account)"`. Sub-ekranlar native header + DEM-206 geri-butonu-gizli desenini izler.
- **Gruplu ayar listesi** — yeni `components/settings/{settings-group,settings-row}.tsx`: `SettingsGroup` başlıklı `bg-card` kart, satırlar arası hairline ayraç; `SettingsRow` ikon + etiket + opsiyonel değer/chevron, dokunulabilir. Hesap ekranı bölümleri: **Profil** (avatar + ad + e-posta, "Düzenle" → `profile-edit`) · **Görünüm** (Açık/Koyu/Sistem seçici — DEM-207'yi tüketir) · **Bildirimler** ("Bildirim ayarları" → mevcut `notification-settings`'e `router.push`) · **Hesap & güvenlik** ("Şifre değiştir" → `change-password`) · **Hakkında** (uygulama sürümü — `expo-constants`) · **Çıkış** (mevcut `signOut` akışı; push token revoke korunur).
- **Profil düzenleme** — `profile-edit` ekranı ad alanı + kaydet; `authClient.updateUser({ name })`. Avatar yükleme + e-posta değiştirme kapsam dışı.
- **Şifre değiştir** — `change-password` ekranı mevcut şifre + yeni şifre + onay; `authClient.changePassword({ currentPassword, newPassword, revokeOtherSessions: true })`.
- **Kapsam dışı** — yeni backend/tRPC/şema, sunucu-tarafı tema tercihi (`users.theme`), avatar yükleme, e-posta değiştirme, oturum/cihaz yönetimi ekranı.

### Web Hızlı Notlar paneli — panel + notu karta sürükleme ([DEM-205](https://linear.app/demirkol/issue/DEM-205))

[DEM-205](https://linear.app/demirkol/issue/DEM-205), [DEM-203](https://linear.app/demirkol/issue/DEM-203) ile gelen **Hızlı Not** domain entity'sinin web ayağıdır. Trello "Gelen Kutusu" mantığında, pano ekranında açılıp kapanan bir panel + bir notu pano listesine sürükleyerek karta dönüştürme. Yeni domain entity / DB tablosu yok — mevcut `quickNote` tRPC router'ı tüketilir, yalnız `convertToCard` konum parametreleriyle genişler. Kararlar kullanıcı tarafından `AskUserQuestion` ile sabitlendi (2026-05-18).

- **Panel kapsamı — yalnız pano ekranı** (kullanıcı kararı) — panel yalnız board detay route'unda (`/workspaces/[id]/boards/[boardId]`) görünür; karta sürükleme zaten aktif bir pano gerektirir. App-shell'e veya diğer route'lara girmez.
- **`QuickNotesPanel`** — pano ekranının **tüm yüzeyinin soluna** yerleşen, içeriği iten (üstüne binmeyen) daraltılabilir panel. Trello "Gelen Kutusu" referansı: panel pano başlık çubuğu (`BoardTopBar`) **dâhil** tüm pano yüzeyinin solunda, ayrı bir parça olarak durur — yalnız app-shell header'ı sabit/tam-genişlik kalır; pano başlık çubuğu panel açıkken sağa kayar. Panel pano yüzeyinin bir parçası gibi okunur: başlık şeridi `BoardTopBar` ile aynı renk (`bg-board-topbar`) ve aynı yükseklikte (`min-h-14`), gövdesi pano arka planını taşır (`boardBackgroundClass`). Not kartları + not ekleme composer'ı `bg-card` yüzeylerdir. Panel ile pano iki ayrı yüzeydir; aralarında app-shell header rengiyle (`bg-board-shell`) dolu dikey bir ayraç şerit vardır — pano sayfası kökü `bg-board-shell` + flex `gap`, iki yüzey de kendi `boardBackgroundClass`'ını taşır (Trello "Gelen Kutusu" deseni; kullanıcı kararı 2026-05-19). Panel gövdesine `p-5`, pano kolon alanına da `p-5` iç boşluk verilir. Üstte not ekleme alanı (`Textarea` — `quickNoteContentSchema`), altında notlar yeniden-eskiye listelenir; her satır satır-içi düzenleme + **onaylı silme** (mobil parite — `Dialog` tabanlı `ConfirmDeleteDialog`) sunar. Açık/kapalı durumu `localStorage`'da saklanır (varsayılan kapalı). Hızlı Not kişiseldir — rol gate'i yok; CRUD mutation'ları `quickNote.list` cache'i üzerinde optimistic + rollback (`useQuickNoteMutations` web hook'u — mobil hook'un web karşılığı).
- **Aç/kapat butonu — `BoardTopBar`** (kullanıcı kararı) — üst bara "Hızlı Notlar" ikonlu (`inbox`) chrome buton; `aria-pressed` ile durum bildirimi.
- **Notu karta sürükleme** — bir not satırı Pragmatic DnD `draggable`'ıdır (`type: 'quick-note'`). Board'un global `monitorForElements`'i quick-note sürüklemesini de işler: kart / `list-cards` drop hedeflerine bırakılınca `quickNote.convertToCard` tetiklenir. Konum **tam bırakılan yere** çözülür (kullanıcı kararı) — `planQuickNoteConvert` saf helper'ı komşu kartlardan `beforeCardId`/`afterCardId`/`newPosition` üretir (`planCardMove` simetrisi; kaynak kart olmadığından self-drop / from-list yok). Kart sürükleme deseninin (placeholder, overlay, `clientMutationId`) board-mekaniği kuralları aynen geçerli; backend `convertToCard` konumu yine server-side doğrular.
- **Dönüşüm sonrası — panoda kal** (kullanıcı kararı) — convert mutation notu `quickNote.list` cache'inden optimistic düşürür; `onSettled` `board.get`'i invalidate eder (yeni kart listede belirir). Navigasyon yok, kart detayı açılmaz (mobil `convertToCard`'dan farkı — mobil oluşan kartın detayına gider). `card.created` realtime echo'su kendi `clientMutationId`'mizle atlanır.
- **Kapsam dışı** — panelin diğer route'larda görünmesi; notu sürüklemeden karta çevirme (web'de "panoya taşı" picker'ı yok — sürükleme tek yol); board favorileri / arama gibi başka panel içerikleri.

### Faz 7 sonrası mobil rafinasyon — board / üye / hesap ([DEM-209](https://linear.app/demirkol/issue/DEM-209)–[DEM-212](https://linear.app/demirkol/issue/DEM-212)) (Wired, 2026-05-18)

`bosluk-tara` (2026-05-18) ürün boşluğu taramasından çıkan dört bağımsız web-parity issue'su. Hepsi mevcut tRPC sözleşmesini / Better Auth client uçlarını tüketir — **yeni backend / şema yok**.

- **[DEM-209](https://linear.app/demirkol/issue/DEM-209)** — mobil board kolonunda **liste rengi + ikonu** render. `board.get` lists `color`/`icon`/`iconColor` taşıyordu ama mobilde gösterilmiyordu (web DEM-98 paritesi). `board-column.tsx` kolon üstüne ince renk şeridi (`card-face` DEM-201 kapak şeridi deseni) + başlıkta liste ikonu çizer; renk/ikon `null` ise nötr görünüm korunur. Yeni `lib/list-icon.ts` — `LIST_ICONS` → Feather `IconName` köprüsü (`entity-icon.ts` deseni; iki ikon seti ayrı — `LIST_ICONS ≠ ENTITY_ICONS`) + token daraltıcılar + hex çözücüler.
- **[DEM-210](https://linear.app/demirkol/issue/DEM-210)** — mobil **üye yönetimi tamamlama**. Faz 7D (DEM-180) üye listesi + davetini kurdu ama rol değiştirme / üye çıkarma / gönderilen davet iptali kapsam dışıydı. Üye satırına `admin+` için ⋮ aksiyon yüzeyi (`MemberActionSheet` — rol seçici + üyeyi çıkar) + "Gönderilen davetler" listesi (`SentInvitationRow` — onaylı iptal). `use-member-mutations.ts` workspace + board için optimistic + rollback + `clientMutationId` hook'ları. Devralınan-admin / workspace `owner` / öz-satır satırlarında aksiyon gizli (permission yine server-side).
- **[DEM-211](https://linear.app/demirkol/issue/DEM-211)** — mobil **board aksiyonları**. Faz 7H (DEM-184) liste yeniden adlandır/arşivlemeyi getirdi ama board-seviyesi aksiyon yoktu. Board ekranı header'ına ⋮ menüsü (`BoardActionsSheet` — `ListActionsSheet` simetriği): yeniden adlandır (`InlineComposer` → `board.update`) + arşivle (`Alert` onaylı → `board.archive` + board listesine geri navigasyon). ⋮ yalnız board `admin` + board arşivli değilken. Nav başlığı tek kaynaktan (`board.get` cache — rename optimistic yamalar).
- **[DEM-212](https://linear.app/demirkol/issue/DEM-212)** — mobil **hesap tamamlama**. DEM-208 profil + şifreyi getirdi ama avatar yükleme + hesap silme yoktu. Profil düzenleme ekranına avatar bölümü: kamera/galeri seçici → `user.initiateAvatarUpload` (DEM-160) presigned PUT → MinIO → `updateUser({ image })` (`use-avatar-upload.ts` tek-fazlı yükleme hook'u; PUT yalnız `content-type` gönderir — `use-attachment-upload` deseni). "Hesap & güvenlik" grubuna "Hesabı sil" → yeni `(account)/delete-account.tsx` (parola re-auth — `deleteAccountInput` + `Alert` onay → `authClient.deleteUser`; son workspace owner server'da engellenir — `canDeleteOwnAccount`). Silme öncesi push token revoke (signOut simetrisi).

### Faz 7 sonrası — Çalışma Alanları kart tasarımı ([DEM-214](https://linear.app/demirkol/issue/DEM-214)) (Wired, 2026-05-19)

"Panolar" sekmesi kökü (`(boards)/index.tsx`) çalışma alanlarını ortak `ListRow` ile sıkışık yatay satır olarak listeliyordu; genelde az sayıda çalışma alanı olduğundan ekran seyrek duruyordu. Yeni `WorkspaceCard` (`src/components/workspace-card.tsx`) **iki sütunlu grid**'de sade, ikon tabanlı kart çizer: üstte entity ikonu (`EntityAvatar`, 44px) + **rol rozeti** (`RoleBadge` — `workspaceRoleLabel` ile çağıranın bu çalışma alanındaki rolü: Sahip / Misafir / Yönetici / Üye **net görünür**), altında ad (2 satıra kadar) ve ikonlu pano/üye sayaçları. `(boards)/index.tsx` workspace'leri ikişerli satırlara böler (tek kalan satırın solunda, boş yarı görünmez `flex-1` spacer). Chevron yok — tüm kart dokunulabilir. `ListRow` board listesinde değişmeden kalır. Yeni backend / tRPC / şema yok (`workspace.list` zaten `role` döndürüyor). Tasarım birkaç tur kullanıcı geri bildirimiyle olgunlaştı; bir ara Unsplash foto-kapak yönü denendi (DEM-220) fakat **daha sade, ikon tabanlı yapı** tercih edilip geri alındı.

### Faz 7 sonrası — workspace/board ikonları liste yüzeylerinde ([DEM-216](https://linear.app/demirkol/issue/DEM-216)) (Wired, 2026-05-19)

Workspace ve board entity'leri seçilebilir bir ikon taşır (`workspaces.icon` / `boards.icon`; `entity-icon-picker` ile seçilir) ama mobil `EntityAvatar` yalnız addan baş-harf + renk çiziyordu — ikon hiçbir yerde görünmüyordu. `EntityAvatar`'a opsiyonel `icon` prop'u eklendi: öncelik `image` (profil görseli) → `icon` (entity ikonu, renkli zemin + beyaz glyph) → baş harf. `entity-icon.ts` `featherForEntityName` güvenli helper'ı DB `text` kolonundan gelen serbest `string`'i `ENTITY_ICONS` ile doğrular, tanınmazsa `DEFAULT_WORKSPACE_ICON` fallback'ine düşer. Çalışma Alanları (`WorkspaceCard`) ve board listesi (`workspaces/[id].tsx` `ListRow` leading) ikonu gösterir. `workspace.list` + `board.list` zaten `icon` döndürdüğünden yeni backend / tRPC / şema yok. Kapsam dışı (follow-up): bekleyen davetler (`invitations.mine` çıktısına `icon` eklenmesi gerekir) ve native header başlıkları.

### Mobil görsel yükleme UX — lazy load + Pusula spinner ([DEM-217](https://linear.app/demirkol/issue/DEM-217)) (Wired, 2026-05-19)

Mobilde kart kapak görselleri ve avatarlar yüklenirken bulundukları ekranın render'ını geciktirmesin, görsel inene kadar bir gösterge yer tutsun diye ortak `RemoteImage` bileşeni eklendi (`src/components/remote-image.tsx`). Görsel inene kadar Pusula spinner ya da verilen placeholder gösterir, `onLoad`'da RN `Animated` opacity ile yumuşakça belirir (fade-in), `onError`'da spinner kalkar (sonsuz dönmez). **Yeni native bağımlılık yok** (`Animated` RN yerleşiği). Saf istemci UI işi — yeni tRPC / Drizzle şema / teknoloji kararı yok.

- **`CardCoverImage`** — düz `bg-muted` yer tutucu yerine `RemoteImage` (spinner + fade-in). Yeni `variant` prop'u: `card` (board kart yüzü ince şerit) / `detail` (kart detay kapağı, kendi yuvarlatılmış kartı).
- **Kart detay kapağı** — 7P'nin "Kapsam dışı" bıraktığı kart detay kapak görseli eklendi (web kart modalı kapak paritesi): `card.get` zaten `coverImage` döndürdüğünden header card'ın üstünde `<CardCoverImage variant="detail">` render edilir; backend değişikliği yok.
- **`EntityAvatar`** — `image` verildiğinde `RemoteImage` ile yüklenir; baş-harf/ikon avatarı **placeholder** olur (foto inene kadar boş kare yerine baş harf görünür, foto fade-in ile belirir; yükleme hata verirse placeholder kalıcı kalır). Üye/yorum/aktivite avatarları ve profil düzenleme avatarı tek bileşenden kapsanır.
- **`AttachmentImageViewer`** (resim lightbox) — ortak `RemoteImage`'a taşındı; davranış aynı (URL + resim inerken büyük beyaz spinner), yerel `imageLoading` state'i kaldırıldı.

Doğrulama: `@pusula/mobile` typecheck + lint + test (435/435; yeni `remote-image.test.tsx` + güncellenen `entity-avatar.test.tsx`) + `expo export` build GREEN.

### Faz 7 sonrası — kart detayında kapak rengi ([DEM-218](https://linear.app/demirkol/issue/DEM-218)) (Wired, 2026-05-19)

DEM-217 kart detayına kapak **görselini** ekledi ama kapak **rengini** kapsamadı — DEM-201 "Kapsam dışı: kart detayında kapak rengi şeridi" bıraktığı boşluk. DEM-218 bu boşluğu kapatır: kart detay ekranında (`app/(app)/(boards)/cards/[cardId].tsx`) kart kapak görseli **yoksa** ama `card.coverColor` **varsa**, header card'ın üstünde — kapak görseliyle aynı yerde — standalone bir renk kartı (`h-14 w-full rounded-xl`, `coverColorHex`) çizilir. Görsel önceliklidir (web kart modalı `CardModalHeader` + board `card-face` kapak şeridi paritesi). `card.get` zaten `coverColor` döndürdüğünden ve `asCoverColor`/`coverColorHex` (`lib/cover-color.ts`, DEM-201) palet çözümleyicileri hazır olduğundan **yeni backend / tRPC / şema yok** — salt görsel wiring. Kapak rengi düzenleme yine `CardMetaBar` "Kapak" chip'i + `CoverColorSheetBody` üzerinden (DEM-201).

### Faz 7 sonrası — mobil realtime tutarlılığı: foreground push → refetch ([DEM-219](https://linear.app/demirkol/issue/DEM-219))

Faz 7M/7L sonrası `kontrol` realtime denetiminin (2026-05-19) bulgusu. Mobil realtime kararı sabittir — **Socket.IO yok, pull-to-refresh + push** (7.0 kararı korunur; bu iş **socket eklemez**). Denetimde iki tutarlılık boşluğu çıktı, DEM-219 ikisini kapatır:

1. **Foreground push → refetch (asıl iş).** Faz 7L push teslimini kurdu ama foreground'da gelen push yalnız banner gösteriyordu (`setNotificationHandler`) — açık board/kart ekranı ve sekme bildirim rozeti **kendiliğinden tazelenmiyordu**; kullanıcı pull-to-refresh yapana dek eski veriyi görüyordu. Yeni `useForegroundNotificationRefresh` hook'u `AppShell`'de (`app/(app)/_layout.tsx`, oturum garantili — `useNotificationDeepLink` deseni) mount edilir; `expo-notifications` `addNotificationReceivedListener` ile **foreground'da gelen** bildirimi yakalar (dokunma değil — o `use-notification-deep-link.ts`'in işi, `addNotificationResponseReceivedListener`). Bildirimin `request.content.data`'sı (`{ type, cardId?, boardId? }`) ilgili tRPC sorgularını **invalidate** eder:
   - **Her zaman** — `notifications.list` + `notifications.unreadCount` (bildirim merkezi + sekme rozeti canlı güncellenir).
   - `boardId` varsa — `board.get({ boardId })`.
   - `cardId` varsa — kart-keyed detay sorguları (`card.get` + `card.labels.list` + `card.members.list` + `comment.list` + `checklist.list` + `card.activity.list`, `cardId`'ye göre filtreli — atama bildirimi üye listesini, yorum/mention yorum+aktiviteyi, son tarih `card.get`'i etkiler).
   Saf parça: `src/lib/notification-refresh.ts` (`notificationRefreshScope(payload)` → `{ boardId, cardId }` — `notification-target.ts` ayrıştırma idiomu; birim test edilir). Echo gürültüsü yok: invalidate edilen sorgu aktif değilse TanStack Query yalnız stale işaretler, refetch bir sonraki mount'a kalır; toast/banner **yok** (web "realtime event uygulandığında toast yok" simetrisi). Bu, açık board ekranını "live collaborative" yapmaz — yalnız push tetiklendiğinde tazeler; tam canlı senkron mobilde hâlâ kapsam dışı (7.0 kararı).
2. **Arama ekranı pull-to-refresh.** §7M pull-to-refresh tüm liste ekranlarına `RefreshControl` koymuştu; arama ekranı (`src/components/search/search-view.tsx`) atlanmıştı — diğer ekranlarla tutarsız. Sonuç `ScrollView`'una `RefreshControl` eklenir (`search.query` sorgusunu yeniden çeker).

**Yeni backend / tRPC / şema / native bağımlılık yok** — `expo-notifications` 7K'da kuruluydu, saf istemci wiring. Önce-belge: bu alt bölüm.

### Faz 7 sonrası — mobil kontrol listesi madde aksiyonları ([DEM-221](https://linear.app/demirkol/issue/DEM-221))

Mobil kart detayında kontrol listesi maddeleri (`card-detail/checklist-section.tsx`) her satırda **checkbox + metin + küçük `x` (sil)** butonu yan yana taşıyordu. Küçük bir alanda birçok `x` butonu birbirine yakın — tek elle kullanımda yanlış dokunma riski yüksek (özellikle zararsız "tamamla" ile yıkıcı "sil" yan yana); satır yüksekliği de mobil dokunma hedefi standardının (min 48dp) altındaydı. DEM-221 satır içi `x`'i kaldırıp olgun task uygulamaları (Apple Reminders / Todoist / Trello) desenine geçirir. Karar `AskUserQuestion` ile sabitlendi (2026-05-19). Yeni backend / tRPC / şema yok — mevcut `checklist.item.toggle` / `.update` / `.delete` tüketilir.

- **Madde anatomisi (3 aksiyon)** — bir kontrol listesi maddesi satırı min **48dp** yüksekliğinde, içerik dikey ortalı; satırlar arası ayraç çizgisi yok (her satır kendi 48dp bloğu). Üç aksiyon:
  - **Checkbox'a dokun** — maddeyi tamamla / geri al. Checkbox görsel ikon ~22px ama dokunma bölgesi satır yüksekliği boyunca ~48dp genişlikte (`square` / `check-square`, tamamlıysa metin üstü çizili).
  - **Metne dokun** — satır-içi (inline) yeniden adlandırma: metin yerinde bir `TextInput`'a dönüşür, klavye açılır; gönderim (`onSubmitEditing`) ya da odak kaybında (`onBlur`) kaydedilir. Boş ya da değişmemiş içerik mutation atmaz (saf karar helper'ı `lib/checklist-item-edit.ts` `resolveChecklistItemRename`). `checklist.item.update` tüketilir.
  - **Sola kaydır** — kırmızı "Sil" aksiyon yüzeyi açılır; dokununca onaysız siler (silme tek dokunuş uzağında değil — kaydırma + dokunma iki adım yeterli sürtünme). Sağa kaydırma / ikinci jest yönü **eklenmez** (yeniden adlandırma nadir aksiyon; tek yönlü kaydırma daha sağlam, öğrenilmesi kolay).
- **Kaydırma mekaniği — yeni native bağımlılık yok** — kaydırma RN yerleşik `Animated` + `PanResponder` ile yazılır (DEM-217 `RemoteImage` / 7G-2 §"Yeni native bağımlılık yok" presedanı; `react-native-gesture-handler` / `reanimated` eklenmez). Ortak `src/components/swipe-row.tsx` bileşeni: bir satırı saran, sola kaydırınca arkadan kırmızı sil aksiyonunu açan yatay kaydırma yüzeyi. `PanResponder` jesti **yalnız yatay hareket** baskınken sahiplenir (`|dx| > |dy|` + eşik) → dikey `ScrollView` scroll'u ve pull-to-refresh bozulmaz. Açık satıra içerikten dokununca satır kapanır.
- **Bileşen ayrımı** — `card-detail/checklist-item-row.tsx` (yeni) tek maddenin satırını çizer: `SwipeRow` + checkbox + metin/inline-input; `checklist-section.tsx` listeyi/composer'ları ve mutation'ları (artık `checklist.item.update` dahil) yönetir. Salt-okunur (`canEdit=false`) ya da optimistic (henüz sunucuda olmayan) satırda kaydırma + yeniden adlandırma devre dışı.
- **Dokunma hedefi** — `SectionAddTrigger` ("Madde ekle" / "Kontrol listesi ekle" / "Ek ekle") dikey dokunma payı artırıldı (paylaşılan bileşen — tüm çağıranlar devralır); kontrol listesi başlığındaki sil butonuna `hitSlop` eklendi.

**Yeni backend / tRPC / şema / native bağımlılık yok** — saf istemci UI + etkileşim rafinasyonu (DEM-224 ile aynı desen mobil geneline yayıldı). Önce-belge: bu alt bölüm.

### Faz 7 sonrası — mobil dokunma hedefi + satır aksiyonu tutarlılığı ([DEM-224](https://linear.app/demirkol/issue/DEM-224))

DEM-221 deseninin (`kontrol` / scout pattern-propagation taraması, 2026-05-19) mobil genelinde yayılması. Aynı sorun — küçük alanda yıkıcı buton + 48dp altı dokunma hedefi — kart detayında ve birkaç sheet'te tekrarlıyordu. Yeni backend / tRPC / şema / native bağımlılık yok; mevcut mutation'lar tüketilir, collaborative mutation disiplini korunur.

- **Yorum satırı (`card-detail/comment-list.tsx`) — DEM-221 simetrisi** (kullanıcı kararı, AskUserQuestion 2026-05-19: "tam simetri"). Yorumun satır-altı küçük "Düzenle / Sil" buton ikilisi (ikon `size=13`, dokunma hedefi ~24dp) kaldırıldı. Yerine: **yorum gövdesine dokun → satır-içi düzenleme** (kontrol listesi maddesiyle aynı; gövde en az 44dp dokunma yüksekliğinde bir `Pressable`'a sarılır) ve **sola kaydır → "Sil"** (`SwipeRow`). Silme yine `Alert` ile onaylanır (yorum kontrol listesi maddesinden değerli — checklist maddesi onaysız silinir, yorum onaylı). Düzenleme yetkisi yorumlar için **değişkendir** (kendi yorumu ya da board `admin`); yetkisi olmayan / silinmiş / optimistic yorumda kaydırma ve gövdeye dokunma etkisizdir — kabul edilen bir ödünleşim (kullanıcı kararı). `editing` / `busy` sırasında kaydırma kilitlenir.
- **Sheet içi `x` butonları** — `members-sheet.tsx` (kart üyesi çıkar) ve `labels-sheet.tsx` (etiket çıkar) satır/chip sonundaki çıplak `x`. Sheet içinde yatay kaydırma sheet jestiyle çakışacağından **SwipeRow uygulanmaz**; bunun yerine dokunma hedefi ~48dp'ye büyütülür (`hitSlop` + sabit dokunma kutusu). Üye çıkarma artık `Alert` ile onaylanır (önceden `x`'e dokunma anında siliyordu — yanlış dokunma riski). Etiket çıkarma onaysız kalır (kolay geri eklenir).
- **Dokunma hedefi taraması** — yıkıcı/aksiyon butonlarının 48dp standardına hizalanması: kart detay "kaldır" linkleri (`due-date-sheet` "Son tarihi kaldır", `cover-color-sheet` "Rengi kaldır") `min-h-11`; `attachment-tile` ikon butonları `h-8`→`h-10` (+`hitSlop`); `quick-note-row` aksiyon/dönüştür butonları `h-9`→`h-11`; kontrol listesi başlık sil butonu satırı `min-h-11`; üye sheet aday satırı `min-h-12`.

**Yeni backend / tRPC / şema / native bağımlılık yok** — `SwipeRow` (DEM-221) yeniden kullanılır; saf istemci UI + etkileşim rafinasyonu. Önce-belge: bu alt bölüm.

### Faz 7 sonrası — anasayfa hızlı-not dock'u ([DEM-230](https://linear.app/demirkol/issue/DEM-230))

DEM-203 Hızlı Notlar'ı merkezi "Ekle" butonuna dokununca açılan ayrı bir ekran olarak kurmuştu. DEM-230 hızlı-not yakalamayı **anasayfaya** (Panolar sekmesi kökü = `app/(app)/(boards)/index.tsx`, çalışma alanı listesi) sabit bir "dock" olarak getirir — kullanıcı not eklemek için ekran değiştirmez. Mevcut Hızlı Notlar altyapısı (`useQuickNoteMutations`, `quickNote.list`) yeniden kullanılır; karar `AskUserQuestion` + birkaç tur cihaz geri bildirimiyle sabitlendi (2026-05-19). Yeni backend / tRPC / şema / native bağımlılık yok — saf istemci UI.

Dock yalnız **not yazma yüzeyidir** — butonsuz, iç içe kartsız, **çok satırlı** metin alanı; eklenen notlar dock'ta gösterilmez (notlar tam Hızlı Notlar ekranındadır). Kaydetme **alt tab bar'daki merkezi "+" butonuyla** yapılır: anasayfadayken "+" ekran açmaz, dock'a yazılı taslağı kaydeder.

- **Bileşen ayrımı** — `src/lib/quick-note-draft.tsx` paylaşılan taslak context'i (`QuickNoteDraftProvider` / `useQuickNoteDraft`): `draft` metni, `submit` (taslağı `createNote`'a verir + temizler) ve `active` (dock odakta mı). `src/components/quick-note-dock.tsx` veri katmanı (context + `useFocusEffect`); `src/components/quick-note-dock-view.tsx` saf sunum katmanı (trpc / native bağımsız → birim test edilebilir; `card-face` deseni). Provider `<Tabs>`'i sarar (`app/(app)/_layout.tsx`) — hem tab bar butonu hem ekranlar erişir.
- **Dock anatomisi** — anasayfanın altına `absolute` konumlanmış, **tam satırı kaplayan**, opak (`bg-card`) + üst kenarlıklı panel; içinde **tek bir düz `TextInput`** (buton yok, ayrı kart yok). `TextInput` **çok satırlı** (`multiline`) — uzun not tek satıra sıkışmaz, içerik büyüdükçe panel yükselir (`MAX_INPUT_HEIGHT`'te durur, sonrası kayar) ve `onLayout` yeniden ölçülür. Çok satırlı olduğundan klavye "enter" tuşu satır atlar; kaydetme yalnız "+" butonuyla. Tam genişlik `KeyboardAvoidingView`'a açıkça `style={{ position:'absolute', left:0, right:0, bottom:0 }}` ile verilir — NativeWind `inset-x-0` `KeyboardAvoidingView`'da güvenilir uygulanmıyor (dock içeriğe göre büzülüp yarım genişlikte kalıyordu).
- **Kaydetme — merkezi "+" butonu (DEM-203 `CreateTabButton`) yeniden tanımlanır.** Dock ekranda odaktayken (`useFocusEffect` → `QuickNoteDraftProvider.active = true`) "+" butonuna **tek dokunma**, Hızlı Notlar ekranını açmayı baypas edip dock taslağını kaydeder (`submit`; taslak boşsa no-op). Dock odakta değilken (başka sekme / board / yüklenme-hata durumu) "+" eski işini yapar — `router.push` ile Hızlı Notlar ekranı. **Uzun basış** her ekranda oluşturma menüsünü açar (değişmedi).
- **İçeriğin altından kayma** — anasayfa kaydırılan içeriği (workspace `FlatList` / boş durum `ScrollView`) dock'un **altından** kayar: dock yüksekliği `onLayout` ile ölçülür (aynı-değer guard'ı), kaydırılan içeriğin `contentContainerStyle.paddingBottom`'una verilir → son workspace satırı dock'un arkasında gizli kalmaz. Dock opak olduğundan içerik görsel olarak ardına geçer.
- **Klavye** — dock `KeyboardAvoidingView` ile sarılır (iOS `behavior="padding"`, Android'de pencere yeniden boyutlanır) → input'a odaklanınca dock klavyenin üstüne çıkar. Dock alt tab bar'ın hemen üstüne oturduğundan alt güvenli alanı (iOS home indicator) tab bar zaten karşılar — ek inset gerekmez.
- **Görünürlük** — dock çalışma alanı listesi yüklendiğinde **ve** 0 workspace boş durumunda görünür; yükleniyor / hata ekranlarında çizilmez. Hızlı not workspace'ten bağımsız kişisel bir entity olduğundan (DEM-203) boş durumda da anlamlıdır.
- **Geri bildirim** — `submit` taslağı temizler; `createNote` optimistic akışı notu `quickNote.list`'e işler (tam Hızlı Notlar ekranında görünür). Taslak ekranlar arası gezinmede korunur (context provider `<Tabs>` üstünde).

**Yeni backend / tRPC / şema / native bağımlılık yok** — DEM-203 altyapısı yeniden kullanılır; saf istemci UI. Önce-belge: bu alt bölüm.

### Faz 7 sonrası — Hızlı Notlar satır aksiyonları + `SwipeRow` çok-aksiyon ([DEM-231](https://linear.app/demirkol/issue/DEM-231))

Hızlı Notlar ekranındaki not satırı (`quick-note-row.tsx`) üç görünür buton taşıyordu (Düzenle / Sil / büyük "Panoya taşı"). DEM-221/224'ün kart detayı için çözdüğü "satır-içi buton kalabalığı" sorununun aynısı. DEM-231 bu satırı da kaydırmalı aksiyona taşır. Karar `AskUserQuestion` ile sabitlendi (2026-05-19): satır **sola kaydırılınca** arkadan üç aksiyon açılır (app genelindeki kaydırma yönü kuralıyla — DEM-221 checklist, DEM-224 yorum — tutarlı). Yeni backend / tRPC / şema / native bağımlılık yok.

- **`SwipeRow` çok-aksiyonlu hale getirildi** — DEM-221'in tek-`onDelete`'lik `SwipeRow`'u `actions: SwipeAction[]` alır (`SwipeAction = { key, label, accessibilityLabel, icon, variant, onPress }`; `variant` → `destructive` kırmızı / `primary`). Açılan panel genişliği aksiyon sayısıyla ölçeklenir (`actions.length × ACTION_WIDTH`). Mevcut tek-aksiyonlu çağıranlar (`card-detail/checklist-item-row.tsx`, `card-detail/comment-list.tsx`) tek-elemanlı `actions` dizisine taşındı — kaydırma davranışı, jest mekaniği ve yıkıcı sil aynı kaldı.
- **`quick-note-row.tsx` yeniden tasarımı** — satır-içi `RowAction` ikili (düzenle/sil) + büyük "Panoya taşı" butonu kaldırıldı. Satır artık `SwipeRow` ile sarılır; sola kaydırınca üç aksiyon açılır: **Düzenle** (`primary`, satır-içi `InlineComposer`) · **Taşı** (`primary`, not→kart picker'ı) · **Sil** (`destructive`, `Alert` onaylı). Düzenleme yine satır-içi composer'la; geçici (`tmp-`) id'li notta kaydırma kapalı, satır düz çizilir. `QuickNoteRow` prop'ları (`note`/`onUpdate`/`onDelete`/`onConvert`) değişmedi — `quick-notes.tsx` etkilenmez.

**Yeni backend / tRPC / şema / native bağımlılık yok** — `SwipeRow` (DEM-221/228 — `reanimated`/`gesture-handler`) yeniden kullanılır; saf istemci UI + etkileşim rafinasyonu. Önce-belge: bu alt bölüm.

### Faz 7 sonrası — mobil performans: görsel cache + UI-thread animasyon ([DEM-228](https://linear.app/demirkol/issue/DEM-228)) (Wired, 2026-05-19)

DEM-217 (`RemoteImage`) ve DEM-221 (`SwipeRow`) "yeni native bağımlılık yok" presedanıyla yazılmıştı; bu pratik iki performans sorunu üretiyordu: (1) kapak görselleri ve avatarlar disk cache olmadığı için her görüntülemede ağdan yeniden iniyordu; (2) kaydırma jesti RN `PanResponder` + JS `Animated.Value` ile JS-thread'inde koşuyordu — yoğun listede jank riski. DEM-228 üç native bağımlılığı (kullanıcı onayıyla, 2026-05-19) devreye alır — bunlardan `react-native-reanimated` + `react-native-gesture-handler` zaten §8.2 "Mobil teknoloji yığını" listesinde planlıydı.

- **`expo-image`** — `RemoteImage` (`src/components/remote-image.tsx`) artık RN `Animated.Image` + elle yazılmış `useState` fade yerine `expo-image` `<Image>` kullanır: `cachePolicy="memory-disk"` (disk + bellek cache), `transition` (native fade), `recyclingKey` (liste geri dönüşümünde doğru görsel), `allowDownscaling` (container boyutuna göre downsampling). Public API (`uri` / `resizeMode` / `placeholder` / `spinnerSize` / `spinnerColor` / `className` / `style`) ve davranış (URL beklerken spinner/placeholder, hata halinde placeholder kalıcı) korundu — çağıranlar (`CardCoverImage`, `EntityAvatar`, `AttachmentImageViewer`) değişmedi.
- **`react-native-reanimated` + `react-native-gesture-handler`** — `SwipeRow` (`src/components/swipe-row.tsx`) `PanResponder` + `Animated` yerine `Gesture.Pan()` + `useSharedValue`/`useAnimatedStyle` ile yeniden yazıldı: sürükleme tamamen UI-thread'inde. Davranış korundu — yalnız yatay hareket sahiplenilir (`activeOffsetX` / `failOffsetY` ile dikey `ScrollView` scroll + pull-to-refresh bozulmaz), sola kaydır = sil aksiyonu, hızlı fling desteği, settle animasyonu; silme yine `Alert` onaylı (yorumda). Kart detay (`app/(app)/(boards)/cards/[cardId].tsx`) collapsing-header scroll handler'ı `useAnimatedScrollHandler` + `Animated.ScrollView`'a geçti; eşik geçişinde `runOnJS` ile `setCollapsed`.
- **Wiring** — `babel.config.js`'e worklet plugin'i **elle eklenmedi**; `babel-preset-expo` SDK 54, `react-native-worklets` paketi kuruluyken `react-native-worklets/plugin`'i (reanimated 4'ün doğru worklet plugin'i) **otomatik** ekler — `babel.config.js`'e yalnız bunu açıklayan bir yorum eklendi (elle eklemek çift kayıt hatası verir). Kök layout (`app/(app)/_layout.tsx`) `GestureHandlerRootView` ile sarıldı.
- **Test** — `vitest.setup.tsx` üç modülü mock'lar: `expo-image`, `react-native-reanimated` resmi mock'u, `react-native-gesture-handler` hafif stub. `SwipeRow` / `RemoteImage` / kart detay testleri yeni implementasyona göre güncellendi.

**Native modül — dev-client rebuild gerekir** (`expo-image`, `react-native-reanimated`, `react-native-gesture-handler` native kod içerir; Expo Go yerine custom dev-client / `eas build`). JS bundle (`expo export`) GREEN. Yeni backend / tRPC / şema yok — saf istemci performans işi. Önce-belge: bu alt bölüm + `02-teknoloji-kararlari.md` 2026-05-19 karar kaydı.

### Faz 7 sonrası — iOS hızlı yakalama uzantıları: Share Extension + App Intents ([DEM-232](https://linear.app/demirkol/issue/DEM-232))

Hızlı Notlar (`quickNote`) bugün yalnız uygulama içinden yakalanır. iOS, uygulama dışından yakalama için iki yüzey sunar; ikisi de **native** (Swift/SwiftUI — React Native değil) ama tek bir native altyapıyı paylaşır. Karar `AskUserQuestion` ile sabitlendi (2026-05-19): **ikisi birlikte** + **anlık senkron**. Bu alt bölüm DEM-232 önce-belge adımıdır; implementasyon Faz 7 mobil MVP kapsamı **dışında** (Faz 7 sonrası iş).

- **Neden uzantı, neden widget değil** — iOS widget'ı WidgetKit/SwiftUI'dir, JS çalıştıramaz ve yalnız timeline ile yenilenir; asıl maliyet widget yüzeyi değil senkrondur. Share Extension + App Intents hızlı yakalamanın çoğunu daha düşük maliyetle, veri sahipliği Pusula'da kalarak verir. Dış not uygulamasına (Apple Notes / Google Keep) bağlanma alternatifi elendi — çoğu tüketici not uygulamasının public API'si yok, olanlar da source-of-truth = PostgreSQL ilkesini bozuyordu.

- **Share Extension** — iOS paylaş menüsüne "Pusula'ya Not Ekle" girişi ekler. Kullanıcı başka bir uygulamada (Safari, Notlar, Mesajlar) metin/URL seçip paylaşır; küçük bir SwiftUI form sayfası açılır, not kaydedilir — ana uygulama tam açılmadan. *Tepkisel* yakalama: ortada zaten bir içerik vardır. Metin + URL alır (resim kapsam dışı — hızlı notlar metin odaklı).

- **App Intents / App Shortcuts** — "hızlı not ekle" eylemini sistem geneline açar: ana ekran kısayolu, **kilit ekranı**, **Action Button** (iPhone 15 Pro+), Siri (sesli), Spotlight. *Öngörülü* yakalama: ortada kaynak içerik yoktur, kullanıcı sıfırdan bir düşünceyi yakalar. Action Button → konuş/yaz → kaydedildi, uygulama hiç açılmadan.

- **Native altyapı** — iki uzantı `@bacons/apple-targets` config plugin'i ile target olarak eklenir; **CNG (Continuous Native Generation) korunur**, EAS Build derler — Xcode'da elle target açma yok. Ana app + iki uzantı ortak bir **App Group** entitlement'ı ve **paylaşımlı keychain access group** paylaşır. Mobil teknoloji yığını listesine (§8.2) eklenir; ilk planda yoktu, bu karar onu genişletir. Dev-client rebuild gerekir (native kod).

- **Senkron modeli — anlık (ana yol) + App Group bekleyen-kuyruğu (emniyet ağı):**
  - Uzantı/intent, paylaşımlı keychain'deki Better Auth session token ile **doğrudan `quickNote.create` tRPC procedure'ünü** HTTP üzerinden çağırır. Yeni endpoint açılmaz — tek API sözleşmesi tRPC kalır (kök CLAUDE.md kural #2). %95 durumda not anında senkronlanır, web ve diğer cihazda hemen görünür.
  - `401` (token süresi dolmuş) **veya** ağ yok → not App Group'ta "bekleyen" olarak yazılır. Ana RN uygulaması bir sonraki **foreground**'da bu kuyruğu boşaltır — `apps/mobile/src/lib/use-foreground-notification-refresh.ts` deseniyle akraba bir hook. Token yenileme **hep RN tarafında**, Better Auth'un kendi akışında kalır; native Swift tarafına refresh mantığı taşınmaz.
  - `quickNote.create` çağrısı `clientMutationId` taşır: anlık yol ile kuyruk boşaltma arasında bir not iki kez gönderilebileceği için idempotency gerekir. Mevcut `createQuickNoteInput` (`@pusula/domain`) bunu taşımıyor — **additive** alan eklenir; `quickNote.create` ([`packages/api/src/routers/quick-note.ts`](../../packages/api/src/routers/quick-note.ts)) tek-satır insert, realtime/outbox üretmez, additive değişiklik küçüktür.

- **Token paylaşımı** — RN tarafı login/logout'ta Better Auth session token'ını paylaşımlı keychain'e yazar/temizler; uzantı bu token'ı `Authorization` ile gönderir. Token `expo-secure-store` dışında ayrıca App Group erişimli keychain group'una konur.

- **Kapsam dışı** — Android paritesi (`ACTION_SEND` paylaşım + App Shortcuts) ayrı takip işi. Interaktif iOS 17+ widget UI'ı ileride App Intents üzerine eklenebilir; bu işte yok.

**Yeni native yüzey + küçük additive backend (`clientMutationId`).** Önce-belge: bu alt bölüm + `02-teknoloji-kararlari.md` 2026-05-19 karar kaydı.
