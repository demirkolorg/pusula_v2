# apps/web — copy to `apps/web/.env.local` (Next.js reads env files from the app dir).
# Defaults in code already use http://localhost:3001, so this is optional in dev.

# Public base URL of the API server (must be reachable from the browser).
NEXT_PUBLIC_API_URL=http://localhost:3001
