# Expo
.expo/
dist/
web-build/
# NOT: expo-env.d.ts repoda tutulur — turbo `typecheck` (`tsc --noEmit`) Expo
# çalıştırmadan koştuğu için `expo/types` referansı CI'da hazır olmalı.

# Native projeleri (CNG — prebuild ile üretilir, repoda tutulmaz)
/ios
/android

# Metro
.metro-health-check*

# Misc
*.log
.DS_Store
.turbo

# Secrets — asla commit etme
# Google Play submit service account key (eas.json submit.production.android)
google-play-service-account.json
# Firebase / FCM V1 service account (push credential — EAS'e yüklenir)
google-services.json
*-service-account*.json
# İndirilen Google Cloud service account key'leri (isim proje-id prefix'li iner)
gen-lang-client-*.json

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli