{
  "//": "Root tsconfig — IDE convenience only. Each package/app extends @pusula/config presets.",
  "extends": "./packages/config/tsconfig/base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": [],
  "exclude": ["node_modules", "**/dist", "**/.next"]
}
