{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Pusula — Node app (api / worker)",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "types": ["node"],
    "noEmit": true
  }
}
